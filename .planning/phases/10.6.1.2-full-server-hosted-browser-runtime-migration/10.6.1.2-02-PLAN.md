---
phase: 10.6.1.2-dedicated-windows-browser-block-deployment
plan: 02
type: execute
wave: 2
depends_on: [10.6.1.2-01-SUMMARY.md]
files_modified:
  - infra/windows-block/
  - docs/remote-relay-server.md
  - docs/windows-browser-block.md
  - .planning/
autonomous: true
requirements: [WINBLK-02, WINBLK-03]
user_setup:
  - Optional Windows Server desktop access if deployment rehearsal reaches manual login
---

<objective>
Wire the current Linux relay/API contract cleanly to a deployable Windows Server block and make the handoff contract explicit.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10.6.1.2-full-server-hosted-browser-runtime-migration/10.6.1.2-CONTEXT.md
@.planning/phases/10.6.1.2-full-server-hosted-browser-runtime-migration/10.6.1.2-RESEARCH.md
@docs/remote-relay-server.md
@docs/windows-browser-block.md
@infra/windows-block/start-reverse-tunnels.ps1
@infra/windows-block/register-browser-block-tasks.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize the Windows block handoff contract</name>
  <files>docs/windows-browser-block.md, docs/remote-relay-server.md, infra/windows-block/</files>
  <read_first>docs/windows-browser-block.md, docs/remote-relay-server.md, infra/windows-block/start-reverse-tunnels.ps1</read_first>
  <action>Make the deployment handoff explicit: which local ports the Windows block owns, which remote forwarded ports Linux expects, how host-controller is reached, and how the public API remains unchanged while the browser runtime moves from this laptop to a Windows Server host.</action>
  <verify>rg -n "14021|14027|4040|host-controller|remote relay|Windows Server" docs/windows-browser-block.md docs/remote-relay-server.md infra/windows-block</verify>
  <acceptance_criteria>
    - Windows block and Linux relay responsibilities are documented without ambiguity
    - the public API contract remains unchanged for callers
    - reverse tunnel mapping is explicit and reusable on a new Windows Server host
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Prepare Windows Server deployment handoff assets</name>
  <files>infra/windows-block/, docs/windows-browser-block.md</files>
  <read_first>infra/windows-block/register-browser-block-tasks.ps1, infra/windows-block/start-browser-block.ps1, docs/windows-browser-block.md</read_first>
  <action>Prepare the concrete handoff assets a Windows Server admin would need: startup scripts, logon-task registration, expected software, and the operator note that browser workers are on-demand interactive-session processes rather than Windows services.</action>
  <verify>rg -n "AtLogOn|interactive|Windows Server|ssh.exe|reverse tunnel" infra/windows-block docs/windows-browser-block.md</verify>
  <acceptance_criteria>
    - deployment handoff steps exist for a Windows Server admin
    - interactive-session requirement is documented clearly
    - no tracked file stores passwords, bearer tokens, or private keys
  </acceptance_criteria>
</task>

</tasks>

<success_criteria>

- Windows block deployment handoff is explicit
- Linux relay/API contract stays stable while the Windows runtime host changes

</success_criteria>

---
phase: 10.6.1.2-dedicated-windows-browser-block-deployment
verified: 2026-03-30T16:45:00+03:00
status: complete
score: 4/4 requirements satisfied
---

# Phase 10.6.1.2 Verification

## Result

`Phase 10.6.1.2` is **complete**.

In plain words:

- the browser runtime is now packaged as a separate Windows browser block
- Linux remains the public relay/API host
- Windows Server startup and handoff assets are ready
- and the latest direct seven-worker proof on the current machine is `7/7 usable`

## Fresh Direct Worker Matrix

Latest full direct proof:

- `dad` -> `dad-windows-block-ok`
- `wife` -> `wife-windows-block-ok`
- `shared-1` -> `shared-1-windows-block-ok`
- `shared-2` -> `shared-2-windows-block-ok`
- `shared-3` -> `shared-3-windows-block-ok`
- `shared-4` -> `shared-4-windows-block-ok`
- `shared-5` -> `shared-5-windows-block-ok`

Artifacts:

- [10.6.1.2-CURRENT-WINDOWS-BLOCK-MATRIX.md](d:/OpenAi/.planning/phases/10.6.1.2-full-server-hosted-browser-runtime-migration/10.6.1.2-CURRENT-WINDOWS-BLOCK-MATRIX.md)
- [10.6.1.2-CURRENT-WINDOWS-BLOCK-MATRIX.json](d:/OpenAi/.planning/phases/10.6.1.2-full-server-hosted-browser-runtime-migration/10.6.1.2-CURRENT-WINDOWS-BLOCK-MATRIX.json)

After the run, local host-controller health showed all seven workers stopped again:

- `agentListening=false`
- `browserListening=false`

## Requirement Coverage

| Requirement | Status | Notes |
|-------------|--------|-------|
| `WINBLK-01` | x VERIFIED | the seven-worker runtime is now packaged as a separate Windows browser block with dedicated deployment assets |
| `WINBLK-02` | x VERIFIED | Windows logon startup assets exist for `host-controller` and reverse tunnels without tracked secrets |
| `WINBLK-03` | x VERIFIED | Linux relay/API and Windows browser runtime responsibilities are explicit and stable |
| `WINBLK-04` | x VERIFIED | the phase ends with a fresh explicit seven-worker matrix rather than assumptions |

## Supporting Checks

- PowerShell parser check passed for:
  - `infra/windows-block/start-browser-block.ps1`
  - `infra/windows-block/start-reverse-tunnels.ps1`
  - `infra/windows-block/stop-reverse-tunnels.ps1`
  - `infra/windows-block/register-browser-block-tasks.ps1`
- `cmd /c npm.cmd test --prefix services/host-controller` -> `21/21` passed

## Boundary Truth

This phase completes the packaging and deployment-prep work for the dedicated Windows browser block.

It does **not** claim that:

- a dedicated Windows Server cutover is already done
- the current operator PC is already out of the runtime path
- the Linux host runs browser workers locally

## Recommended Next Step

Plan rollout smoke confidence on top of the packaged Windows browser block:

```text
$gsd-plan-phase 11
```

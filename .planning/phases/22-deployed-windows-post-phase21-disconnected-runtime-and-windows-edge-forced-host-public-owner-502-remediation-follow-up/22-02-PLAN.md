---
phase: 22-deployed-windows-post-phase21-disconnected-runtime-and-windows-edge-forced-host-public-owner-502-remediation-follow-up
plan: 02
type: execute
wave: 2
depends_on: [22-01-PLAN.md]
files_modified:
  - services/control-api/src/
  - services/control-api/dist/
  - services/control-api/test/
autonomous: true
requirements: [WPFU-03]
user_setup: []
---

<objective>
Expose the latest Phase 22 post-Phase-21 follow-up result in the real `control-api` runtime so operators can compare the deployed-host artifact against the same file-backed surface they already trust.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-deployed-windows-post-phase21-disconnected-runtime-and-windows-edge-forced-host-public-owner-502-remediation-follow-up/22-RESEARCH.md
@.planning/phases/22-deployed-windows-post-phase21-disconnected-runtime-and-windows-edge-forced-host-public-owner-502-remediation-follow-up/22-VALIDATION.md
@services/control-api/src/config.ts
@services/control-api/src/server.ts
@services/control-api/src/routes/internal-admin-page.ts
@services/control-api/src/routes/read-latest-state.ts
@services/control-api/test/internal-admin-page.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a file-backed latest post-Phase-21 follow-up route to control-api and regenerate dist runtime files</name>
  <files>services/control-api/src/config.ts, services/control-api/src/server.ts, services/control-api/src/routes/internal-post-phase21-disconnected-runtime-followup.ts, services/control-api/dist/, services/control-api/test/</files>
  <read_first>services/control-api/src/config.ts, services/control-api/src/server.ts, services/control-api/src/routes/read-latest-state.ts, services/control-api/src/routes/internal-post-parity-disconnected-runtime-remediation.ts, services/control-api/test/internal-post-parity-disconnected-runtime-remediation.test.ts</read_first>
  <action>Add a new config field `postPhase21DisconnectedRuntimeFollowupStatePath` sourced from `POST_PHASE21_DISCONNECTED_RUNTIME_FOLLOWUP_STATE_PATH` with default repo path `infra/data/post-phase21-disconnected-runtime-followup/latest.json`. Create `services/control-api/src/routes/internal-post-phase21-disconnected-runtime-followup.ts` with `GET /internal/post-phase21-disconnected-runtime-followup/latest` that returns `{ latest: null }` when the file is missing and returns the parsed latest follow-up payload when it exists. Reuse `read-latest-state.ts` so BOM-prefixed `latest.json` still parses cleanly. Register the route behind the internal admin guard in `server.ts`, add tests that prove both the empty state and the populated state, and regenerate the runtime files the deployed host actually starts by running `npm.cmd --prefix services/control-api run build` so `services/control-api/dist/config.js`, `services/control-api/dist/server.js`, and `services/control-api/dist/routes/internal-post-phase21-disconnected-runtime-followup.js` contain the same route/config contract.</action>
  <verify>rg -n "POST_PHASE21_DISCONNECTED_RUNTIME_FOLLOWUP_STATE_PATH|postPhase21DisconnectedRuntimeFollowupStatePath|/internal/post-phase21-disconnected-runtime-followup/latest|latest: null|preRemediationReadyCount|postRemediationReadyCount|dominantRuntimeBlocker|firstFailingHop|loopbackStatus|forcedHostStatus|publicOwnerStatus|verdict" services/control-api/src services/control-api/dist services/control-api/test</verify>
  <acceptance_criteria>
    - `services/control-api/src/routes/internal-post-phase21-disconnected-runtime-followup.ts` exists
    - `config.ts` supports `POST_PHASE21_DISCONNECTED_RUNTIME_FOLLOWUP_STATE_PATH`
    - `server.ts` registers `/internal/post-phase21-disconnected-runtime-followup/latest`
    - tests prove the route returns `{ latest: null }` when no follow-up result exists
    - the route reuses BOM-safe latest-state parsing
    - `services/control-api/dist/config.js`, `services/control-api/dist/server.js`, and `services/control-api/dist/routes/internal-post-phase21-disconnected-runtime-followup.js` contain the new route/config contract after build
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Render the latest post-Phase-21 follow-up result on the internal admin page</name>
  <files>services/control-api/src/routes/internal-admin-page.ts, services/control-api/dist/routes/internal-admin-page.js, services/control-api/test/internal-admin-page.test.ts</files>
  <read_first>services/control-api/src/routes/internal-admin-page.ts, services/control-api/test/internal-admin-page.test.ts, services/control-api/src/routes/internal-post-phase21-disconnected-runtime-followup.ts</read_first>
  <action>Add a dedicated `Latest post-phase21 disconnected runtime follow-up` section to `/internal/admin`. Make the page fetch `/internal/post-phase21-disconnected-runtime-followup/latest`, render an empty state `No post-phase21 disconnected runtime follow-up captured yet.` when there is no file, and render the latest timestamp, pre-remediation ready count, post-remediation ready count, dominant runtime blocker, first failing hop, loopback status, forced-host status, public-owner status, compatibility version, and final verdict when the file exists. Keep the existing worker summary and current latest-result sections intact. Regenerate `services/control-api/dist/routes/internal-admin-page.js` through the build output so the deployed host serves the same admin page from `dist`.</action>
  <verify>rg -n "Latest post-phase21 disconnected runtime follow-up|No post-phase21 disconnected runtime follow-up captured yet.|/internal/post-phase21-disconnected-runtime-followup/latest|pre-remediation ready count|post-remediation ready count|dominant runtime blocker|first failing hop|loopback status|forced-host status|public-owner status|Compatibility|verdict" services/control-api/src/routes/internal-admin-page.ts services/control-api/dist/routes/internal-admin-page.js services/control-api/test/internal-admin-page.test.ts</verify>
  <acceptance_criteria>
    - the admin page contains the text `Latest post-phase21 disconnected runtime follow-up`
    - the admin page fetches `/internal/post-phase21-disconnected-runtime-followup/latest`
    - the admin page has an explicit empty state `No post-phase21 disconnected runtime follow-up captured yet.`
    - the populated state renders pre-remediation ready count, post-remediation ready count, dominant runtime blocker, first failing hop, loopback status, forced-host status, public-owner status, compatibility version, and verdict
    - the existing worker summary and current latest-result sections remain present
  </acceptance_criteria>
</task>

</tasks>

<success_criteria>

- the latest post-Phase-21 follow-up result is visible from the same runtime files the deployed host actually serves
- no operator has to reconstruct the Phase 22 follow-up truth from mixed terminal output and older remediation notes

</success_criteria>

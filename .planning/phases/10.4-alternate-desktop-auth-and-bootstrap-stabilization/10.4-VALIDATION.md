---
phase: 10.4
slug: alternate-desktop-auth-and-bootstrap-stabilization
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-28
---

# Phase 10.4 - Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `node --test` and `vitest` |
| **Config file** | `services/host-controller/package.json`, `services/control-api/package.json`, `workers/agent/package.json` |
| **Quick run command** | `cmd /c npm.cmd test --prefix services/host-controller && cmd /c npm.cmd test --prefix services/control-api && cmd /c npm.cmd test --prefix workers/agent` |
| **Full suite command** | `cmd /c npm.cmd test --prefix services/host-controller && cmd /c npm.cmd test --prefix services/control-api && cmd /c npm.cmd run build --prefix services/control-api && cmd /c npm.cmd test --prefix workers/agent && cmd /c npm.cmd run build --prefix workers/agent` |
| **Estimated runtime** | ~180 seconds |

---

## Sampling Rate

- **After every task commit:** run the smallest affected package test command
- **After every plan wave:** run the full suite command
- **Before `$gsd-verify-work`:** full suite must be green
- **Max feedback latency:** 180 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 10.4-01-* | 01 | 1 | ADST-03 | unit/integration | `cmd /c npm.cmd test --prefix services/host-controller && cmd /c npm.cmd test --prefix services/control-api` | partial | pending |
| 10.4-02-* | 02 | 2 | ADST-01, ADST-02 | unit + live probe | `cmd /c npm.cmd test --prefix workers/agent && powershell -ExecutionPolicy Bypass -File .\\infra\\host-worker\\test-alternate-desktop-runtime.ps1 -WorkerId wife -ReturnJson` | partial | pending |
| 10.4-03-* | 03 | 3 | ADST-01, ADST-02, ADST-03 | docs + live proof | `powershell -ExecutionPolicy Bypass -File .\\infra\\host-worker\\test-alternate-desktop-runtime.ps1 -WorkerId dad -ReturnJson` | yes | pending |

*Status: pending / green / red / flaky*

---

## Wave 0 Requirements

- [ ] Add route coverage for post-transition validation and runtime-state reporting in `services/control-api`
- [ ] Add worker-agent tests for step-level bootstrap evidence and current `Temporary Chat` confirmation handling
- [ ] Ensure the alternate-desktop proof path distinguishes auth failure, bootstrap drift, and assignment timeout in machine-readable output

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Confirm `dad` can be reauthenticated visibly and returned to `alternate_desktop` without leaving a routine browser on the main desktop | ADST-01 | Requires real ChatGPT auth and real Windows desktop behavior | 1. Start visible reauth for `dad`. 2. Finish login manually. 3. Press `Complete login -> non-visible runtime`. 4. Confirm `/internal/workers` shows non-visible runtime and no routine browser remains on the main desktop. |
| Confirm `wife` reaches usable `Temporary Chat` flow on `alternate_desktop` | ADST-02 | Current failure is live UI drift, not something unit tests can prove alone | 1. Run the canonical alternate-desktop probe for `wife`. 2. Confirm `temporary_confirmation_not_found` is gone. 3. Verify proof output reaches a usable bootstrap/relay result. |
| Confirm Phase 11 can resume only after at least one worker records `phase11Ready=true` | ADST-03 | This is a milestone gate, not only a code assertion | 1. Review proof output and runtime decision/update artifacts. 2. Verify roadmap/state next step changes only after a real passing worker exists. |

---

## Validation Sign-Off

- [ ] All tasks have automated verify or explicit manual gate coverage
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all missing references
- [ ] No watch-mode flags
- [ ] Feedback latency < 180s for automated checks
- [ ] `nyquist_compliant: true` set in frontmatter once plan-specific task rows are finalized

**Approval:** pending

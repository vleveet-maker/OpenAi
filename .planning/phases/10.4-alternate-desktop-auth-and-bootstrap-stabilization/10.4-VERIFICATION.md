---
phase: 10.4-alternate-desktop-auth-and-bootstrap-stabilization
verified: 2026-03-29T06:00:00+03:00
status: partial
score: 1/3 phase truths verified
---

# Phase 10.4: Alternate Desktop Auth And Bootstrap Stabilization Verification Report

Phase goal: stabilize alternate-desktop auth and bootstrap enough to produce at least one `phase11Ready=true` worker, or block Phase 11 with explicit evidence.

Verified: 2026-03-29T06:00:00+03:00
Status: partial

## Goal Achievement

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | At least one worker can recover or confirm valid ChatGPT auth in `alternate_desktop` after visible auth and return to routine non-visible operation. | ~ PARTIAL | `dad` now classifies cleanly as `bootstrap_auth_required` at `auth_check`, but no worker has passed auth recovery into rollout-ready operation. |
| 2 | At least one worker can pass `Temporary Chat`, preferred-model selection, composer unlock, and record `phase11Ready=true`. | x NOT VERIFIED | Live proofs on `wife` and `shared-1` now fail at `bootstrap_navigation_failed` / `navigation`; no worker recorded `phase11Ready=true`. |
| 3 | Operator and control-plane evidence distinguish auth loss, bootstrap drift, and other proof tails clearly enough to decide whether Phase 11 can proceed. | x VERIFIED | `infra/host-worker/test-alternate-desktop-runtime.ps1`, direct worker `/health`, `.planning/phases/10.4-alternate-desktop-auth-and-bootstrap-stabilization/10.4-UNBLOCK-DECISION.md`, updated docs and roadmap/state. |

Score: 1/3 truths verified

## Automated Checks

| Command | Status |
|---------|--------|
| `cmd /c npm.cmd test --prefix workers/agent` | x PASSED |
| `cmd /c npm.cmd test --prefix services/control-api` | x PASSED |
| `cmd /c npm.cmd run build --prefix workers/agent` | x PASSED |
| `cmd /c npm.cmd run build --prefix services/control-api` | x PASSED |

## Live Checks

| Probe | Result |
|-------|--------|
| `Invoke-RestMethod http://127.0.0.1:4022/health` after manual restart | x VERIFIED -> new worker health now surfaces `lastBootstrap*` fields, proving the updated runtime is live |
| `powershell -ExecutionPolicy Bypass -File .\infra\host-worker\test-alternate-desktop-runtime.ps1 -WorkerId wife -TimeoutSeconds 180 -ReturnJson` | x VERIFIED -> `proofFailureClass=bootstrap_failed`, `bootstrapFailureCode=bootstrap_navigation_failed`, `bootstrapStep=navigation`, `phase11Ready=false` |
| `powershell -ExecutionPolicy Bypass -File .\infra\host-worker\test-alternate-desktop-runtime.ps1 -WorkerId dad -TimeoutSeconds 180 -ReturnJson` | x VERIFIED -> `proofFailureClass=auth_required`, `bootstrapFailureCode=bootstrap_auth_required`, `bootstrapStep=auth_check`, `phase11Ready=false` |
| `powershell -ExecutionPolicy Bypass -File .\infra\host-worker\test-alternate-desktop-runtime.ps1 -WorkerId shared-1 -TimeoutSeconds 180 -ReturnJson` | x VERIFIED -> `proofFailureClass=bootstrap_failed`, `bootstrapFailureCode=bootstrap_navigation_failed`, `bootstrapStep=navigation`, `phase11Ready=false` |

## Requirements Coverage

| Requirement | Status |
|-------------|--------|
| ADST-01 | ~ PARTIAL - auth failure is now isolated cleanly on `dad`, but no worker has yet returned to routine validated operation |
| ADST-02 | x NOT VERIFIED - no worker produced `phase11Ready=true` |
| ADST-03 | x VERIFIED - proof output and operator evidence now separate auth loss from bootstrap failure |

## Residual Risk

- `wife` and `shared-1` now fail earlier at navigation, so the next follow-up must investigate alternate-desktop navigation/bootstrap rescue before Phase 11 can resume.
- `dad` still needs manual auth recovery in the selected runtime path.
- Internal admin code now exposes better bootstrap diagnostics, but the running Docker `control-api` process may still need a recycle on the operator host before the live UI reflects the newest fields.

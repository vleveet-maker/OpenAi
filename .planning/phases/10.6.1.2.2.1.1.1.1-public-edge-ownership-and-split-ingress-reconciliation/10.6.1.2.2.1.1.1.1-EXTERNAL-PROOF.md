# 10.6.1.2.2.1.1.1.1 External Proof

- Phase: `10.6.1.2.2.1.1.1.1`
- Date: `2026-03-31`
- Status: `complete`
- Final blocker class: `split_ingress_owner_conflict`

## Short Report

The outside proof for this phase is complete.

It did not end with a successful public chat.

It ended with one exact blocker.

Simple explanation:

- outside clients hit the wrong public owner first
- so the remaining blocker is now fully outside the browser/runtime layer

## Outside-Client Results

### HTTP

- `http://77.66.186.75/healthz` -> `404 Not Found`
- `http://77.66.186.75/v1/models` -> `404 Not Found`
- responder:
  - `Server: nginx/1.18.0 (Ubuntu)`

### HTTPS

- `https://77.66.186.75/healthz` -> `404 Not Found`
- responder:
  - `Server: nginx/1.18.0 (Ubuntu)`

This phase therefore no longer treats TLS as the primary ambiguity.

The stronger truth is:

- both public HTTP and public HTTPS currently terminate on Ubuntu `nginx`, not on the preserved Windows `Caddy` edge

### Worker-backed canary chat

Public canary chat status:

- `not_run`

Reason:

- `healthz` and `v1/models` already prove that the public owner path is wrong before chat is reached

## Corroborating Evidence

From Ubuntu over SSH:

- local relay edge:
  - `http://127.0.0.1/healthz` -> `200 ok`
- LAN Windows edge:
  - `http://192.168.88.250/healthz` -> `308` from `Server: Caddy`
- public IP:
  - still not routed to the Windows edge

That corroborates the same split-ingress truth from three angles:

1. public outside-client path
2. Ubuntu local relay path
3. Windows LAN-side `Caddy` path

## Final Outside Proof Verdict

External proof verdict:

- `hold_preserve_accounts`

Exact blocker:

- `split_ingress_owner_conflict`

Meaning:

- the public contract is still owned by the wrong layer
- the preserved worker/runtime pool is not the current blocker
- `Phase 11` cannot resume honestly until the public owner is deliberately unified

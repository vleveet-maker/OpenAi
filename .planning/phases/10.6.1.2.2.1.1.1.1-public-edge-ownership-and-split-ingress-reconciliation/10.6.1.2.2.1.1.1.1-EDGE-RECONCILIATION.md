# 10.6.1.2.2.1.1.1.1 Edge Reconciliation

- Phase: `10.6.1.2.2.1.1.1.1`
- Date: `2026-03-31`
- Status: `complete`
- Result: `exact blocker recorded preserve-first`

## Short Report

This phase did not perform a live cutover.

That was intentional.

Simple explanation:

- we now know exactly who owns public `77.66.186.75`
- that owner is not the preserved Windows `Caddy` host
- so the correct preserve-first reconciliation step is to stop at truth instead of pushing another risky edge change on the Windows side

## Deliberate Owner Path Decision

The project now has one deliberate reconciliation target:

- the public owner of `77.66.186.75` must be unified before any honest rollout can resume

That unification can happen only in one of these two preserve-first ways:

1. Ubuntu remains the public owner and deliberately reverse-proxies the public API contract onward to the preserved Windows edge
2. Ubuntu relinquishes public ownership of `77.66.186.75` for the relay/API contract so that public traffic lands on the intended Windows edge instead

What this phase deliberately did **not** do:

- it did not rewrite Ubuntu `nginx` blindly
- it did not rewrite Windows `Caddy` under the false assumption that Windows already owns the public IP
- it did not broaden worker impact beyond the canary scope

## Why Live Reconciliation Stopped Here

The ownership audit proved three facts together:

1. public `77.66.186.75` is currently claimed by Ubuntu `nginx`
2. Windows `Caddy` is only the LAN-side owner on `192.168.88.250`
3. Linux relay worker-forward ports are not currently active on `127.0.0.1:14021..14029`

That means there is no safe “small tweak” inside the preserved Windows environment that would honestly reconcile the public contract by itself.

The remaining work belongs to the public-owner layer.

## Canary Safety Decision

Canary `shared-6` public proof is **not safe to treat as phase-closing proof** against the current public path.

Reason:

- public `healthz` and `v1/models` already fail on the wrong owner before the worker-backed chat path is reached

So the correct preserve-first action is:

- classify the blocker at the public-owner layer
- stop before touching the preserved worker pool broadly

## Privacy And Safety Guarantees Preserved

- raw `4040` remains private
- worker-agent ports remain private
- no broad 9-worker restart happened
- no relogin happened
- no profile cleanup happened

## Exact Reconciliation Blocker

The exact blocker is:

- `split_ingress_owner_conflict`

Meaning:

- public `77.66.186.75` is currently owned by Ubuntu `nginx`
- the intended Windows public API edge exists, but only on the LAN-side host path
- public routing and ownership still need one deliberate owner decision outside the browser/runtime layer

# Phase 10.6.1.2.2.1.1.1.1 Validation

## Validation Focus

This phase is valid only if it removes the ambiguity about who owns public `77.66.186.75` without damaging the preserved Windows Server browser block.

It is invalid if it:

- mass-starts or mass-restarts the 9 workers
- treats Windows local success as proof of public ownership
- changes routing or public edge ownership without recording rollback
- claims outside chat proof while `healthz` and `v1/models` still land on the wrong responder
- opens `4040` or worker-agent ports publicly

## Required Evidence

- one ownership audit artifact that names the public responder chain end-to-end
- one preserve-first reconciliation artifact that records the chosen owner path or exact blocker
- one canary-only proof artifact on `shared-6`
- one final verification artifact with verdict:
  - `safe_to_promote`
  - `safe_but_hold`
  - `hold_preserve_accounts`

## What Must Be True Before Closeout

- the project now knows whether public `77.66.186.75` belongs to Ubuntu, Windows, or a deliberate proxy chain between them
- one deliberate public owner path is either proven or exactly blocked
- `shared-6` remains the only required worker-backed proof path unless the phase ends before chat proof because ownership is still wrong earlier
- the 9 accounts remain preserved
- `Phase 11` is unblocked or held on explicit public-edge truth, not guesswork

## Recommended Checks

- audit the live public responder for `healthz` and `v1/models`
- confirm listener and config ownership on both Ubuntu and Windows sides
- confirm MikroTik/NAT or upstream ingress intent where needed
- perform one bounded preserve-first reconciliation to a single owner path
- run canary `shared-6` proof only after ownership is coherent
- rollback immediately if the deliberate public owner path regresses

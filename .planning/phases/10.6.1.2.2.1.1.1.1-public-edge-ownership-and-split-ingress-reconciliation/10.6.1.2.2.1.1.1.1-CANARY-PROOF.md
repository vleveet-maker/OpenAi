# 10.6.1.2.2.1.1.1.1 Canary Proof

- Phase: `10.6.1.2.2.1.1.1.1`
- Date: `2026-03-31`
- Canary worker: `shared-6`
- Status: `blocked before public canary chat`

## Short Report

The public-owner canary was not run as a worker-backed chat proof in this phase.

Simple explanation:

- the public edge is already wrong before chat
- `healthz` and `v1/models` land on the wrong responder
- so a worker-backed canary chat would not be honest proof of the reconciled public owner

## What Was Verified

### Public owner pre-check

Outside-client probes showed:

- `http://77.66.186.75/healthz` -> `404`
- `http://77.66.186.75/v1/models` -> `404`
- responder:
  - `nginx/1.18.0 (Ubuntu)`

That means the public owner path is still wrong before the canary worker is even relevant.

### Windows canary safety context

Previously proven preserve-first context still stands:

- `shared-6` was already the chosen canary for the Windows edge
- the preserved Windows-side canary passed in the earlier direct host phase
- this phase did not touch or relogin `shared-6`

### Private-surface truth

- `4040` remained private
- worker-agent ports remained private
- no broad worker exposure was introduced as a workaround

## Final Canopy Verdict

Public canary result:

- `not_run`

Exact blocker:

- `split_ingress_owner_conflict`

Reason:

- the current public responder is still Ubuntu-side and fails before the canary chat path can honestly be evaluated

# 10.6.1.2.2.1.1.1.1 Verification

- Phase: `10.6.1.2.2.1.1.1.1`
- Date: `2026-03-31`
- Status: `complete`
- Final verdict: `hold_preserve_accounts`

## Short Report

This phase succeeded.

Not because the public edge was fixed.

Because the public-edge ambiguity is now fully removed.

Simple explanation:

- the preserved Windows `Caddy` edge is still healthy on the LAN-side host
- the Linux relay is still healthy locally on Ubuntu
- but public `77.66.186.75` is currently owned by the Ubuntu-side public responder
- therefore the exact blocker is now one ownership conflict outside the browser/runtime layer

The project should stop treating this as a TLS mystery or a worker/runtime issue.

## What Landed

Phase artifacts:

- [10.6.1.2.2.1.1.1.1-OWNERSHIP-AUDIT.md](d:/OpenAi/.planning/phases/10.6.1.2.2.1.1.1.1-public-edge-ownership-and-split-ingress-reconciliation/10.6.1.2.2.1.1.1.1-OWNERSHIP-AUDIT.md)
- [10.6.1.2.2.1.1.1.1-EDGE-RECONCILIATION.md](d:/OpenAi/.planning/phases/10.6.1.2.2.1.1.1.1-public-edge-ownership-and-split-ingress-reconciliation/10.6.1.2.2.1.1.1.1-EDGE-RECONCILIATION.md)
- [10.6.1.2.2.1.1.1.1-CANARY-PROOF.md](d:/OpenAi/.planning/phases/10.6.1.2.2.1.1.1.1-public-edge-ownership-and-split-ingress-reconciliation/10.6.1.2.2.1.1.1.1-CANARY-PROOF.md)
- [10.6.1.2.2.1.1.1.1-EXTERNAL-PROOF.md](d:/OpenAi/.planning/phases/10.6.1.2.2.1.1.1.1-public-edge-ownership-and-split-ingress-reconciliation/10.6.1.2.2.1.1.1.1-EXTERNAL-PROOF.md)

## Requirement Verdict

- `PEDGE-01` complete
- `PEDGE-02` complete
- `PEDGE-03` complete as blocker-classified canary truth
- `PEDGE-04` complete

## Exact Public-Edge Truth

Current real public owner:

- Ubuntu-side `nginx`

Current preserved Windows edge:

- `Caddy` on `192.168.88.250`
- reachable on the LAN-side path
- not the public owner of `77.66.186.75`

Current Linux relay:

- healthy locally on `127.0.0.1:4010` behind Ubuntu `nginx`
- not currently the public owner for the observed outside-client contract either

## Exact Blocker

The final blocker is:

- `split_ingress_owner_conflict`

This is more precise than the previous phase's `public_edge_only`.

Why:

1. public `77.66.186.75` is explicitly claimed by an Ubuntu-side public responder
2. the preserved Windows `Caddy` edge is still alive, but only on the LAN-side host path
3. the public contract is therefore split between layers and has not been deliberately unified

## Preserve-First Result

- 9 accounts remained preserved
- no profile deletion/reset
- no cookie or local-storage clearing
- no mass restart of the 9-worker pool
- canary worker `shared-6` was not widened into a broader rollout
- raw `4040` and worker-agent ports remained private

## Impact On Phase 11

`Phase 11` still cannot resume.

It remains blocked, but now by one exact upstream/public-edge blocker instead of a vague edge ambiguity.

What must happen next:

- either the Ubuntu public owner must deliberately proxy the public API contract onward
- or public ownership of `77.66.186.75` must be reassigned away from the Ubuntu responder to the intended Windows edge

## Final Decision

Decision:

- `hold_preserve_accounts`

Rationale:

- the phase successfully reduced the problem to one exact blocker
- that blocker lives at the public-edge ownership layer
- changing browser/runtime behavior further would not address the real issue

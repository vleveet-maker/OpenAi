# 10.6.1.2.2 Plan 02 Summary

- The Windows-side public API now has a real canary-safe path instead of only a final-port path.
- `run-public-api.ps1` now serializes a single allowlisted worker correctly for Windows PowerShell.
- `probe-public-api.ps1` can now start one canary worker through `host-controller`, wait for readiness, probe the API, and stop that worker again.
- A local shadow API proof succeeded on `127.0.0.1:4012` with `shared-5`, returning `probe-ok` without touching the rest of the pool.

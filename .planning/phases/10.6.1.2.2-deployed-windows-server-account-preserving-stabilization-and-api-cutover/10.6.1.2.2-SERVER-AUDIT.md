# 10.6.1.2.2 Server Audit

- Phase: `10.6.1.2.2`
- Date: 2026-03-30
- Audit status: `freeze_point_captured`
- Audit path: `77.66.186.75:2222 -> 192.168.88.250`

## Short Report

The deployed Windows Server already contains a real 9-account browser block and must be treated as preserve-first.

The audit was intentionally non-destructive:

- no profile deletion
- no cookie clearing
- no mass restarts
- no forced relogin

## Deployment Truth

Observed target:

- Windows Server host: `192.168.88.250`

Observed control-plane:

- `host-controller`: reachable on `http://192.168.88.250:4040`
- local proxy reported by host-controller: `http://127.0.0.1:7897`

Observed public edge:

- `tcp/80`: open
- `tcp/443`: open
- `tcp/3389`: open
- `tcp/4040`: open
- `tcp/4010`: closed
- `tcp/5985`: closed
- `tcp/5986`: closed

Observed HTTP behavior on the deployed host:

- `http://192.168.88.250/healthz` -> `308 Permanent Redirect`
- `http://192.168.88.250/v1/models` -> `308 Permanent Redirect`
- redirect target is `https://192.168.88.250/...`
- HTTPS on that host is currently not usable for our API path

## Deployed Worker Inventory

The deployed server currently exposes these workers through `host-controller`:

- `dad`
- `wife`
- `shared-1`
- `shared-2`
- `shared-3`
- `shared-4`
- `shared-5`
- `shared-6`
- `shared-7`

Deployed profile-path pattern confirmed by returned runtime metadata:

- `D:\OpenAi\infra\data\host-profiles\<workerId>`

## Freeze-Point Runtime Truth

At the audit freeze point, all nine workers were present but stopped:

- `agentListening = false`
- `browserListening = false`
- `runtimeMode = null`
- `runtimeClass = null`

This is the safest starting state for preserve-first uplift because the host already contains live accounts, but none of them were actively running during the audit.

## Provenance Reconciliation

What was reconciled successfully:

- the deployed server clearly runs the 9-slot Windows browser block shape
- the naming and port pattern match the expected deployed Git-based shape
- `shared-6` and `shared-7` exist on the deployed host, proving this is not the older 7-slot package

What is still not directly pinned:

- no Windows-side shell/WinRM/SSH path was available to read the exact deployed git commit hash
- therefore provenance is reconciled by runtime surface and inventory truth, not by commit hash from the target host

## Startup Surface Truth

Confirmed directly:

- `host-controller` is already running
- the Windows block proxy is already running

Not directly audited from the target host:

- scheduled-task definitions
- Windows Service definitions
- exact startup command line for the currently deployed API edge

Those items remain operational debt, but they are not required to conclude the preserve-first verdict for this phase.

## Preserve-First Backup Contract

The backup/rollback contract for this deployed server is now:

1. capture metadata freeze point first
2. only perform cold profile backup when workers are stopped
3. restore only from an explicit operator-confirmed rollback
4. never use profile deletion or forced mass relogin as a normal uplift step

Repo assets that enforce this contract:

- [backup-browser-block-state.ps1](d:/OpenAi/infra/windows-block/backup-browser-block-state.ps1)
- [restore-browser-block-state.ps1](d:/OpenAi/infra/windows-block/restore-browser-block-state.ps1)

## Audit Verdict

- The deployed Windows Server is real and valuable.
- It already contains 9 live browser profiles.
- It is suitable for staged preserve-first uplift.
- It is **not** safe to treat as a disposable redeploy target.

# 10.6.1.2.2 Worker Matrix

- Date: 2026-03-30
- Scope: deployed Windows Server at `192.168.88.250`
- Matrix type: `preserve_first_runtime_and_cutover_truth`

This is **not** a blind full-pool relay matrix. It is the truthful preserve-first matrix for the already-deployed 9-account server after freeze-point audit plus staged canary/subset handling.

| Worker | Present On Server | State At End | Staged Evidence | Deployed API Chat Proof | Current Classification |
| --- | --- | --- | --- | --- | --- |
| `dad` | yes | stopped | untouched during this phase | not run | `preserve_hold` |
| `wife` | yes | stopped | untouched during this phase | not run | `preserve_hold` |
| `shared-1` | yes | stopped | untouched during this phase | not run | `preserve_hold` |
| `shared-2` | yes | stopped | untouched during this phase | not run | `preserve_hold` |
| `shared-3` | yes | stopped | untouched during this phase | not run | `preserve_hold` |
| `shared-4` | yes | stopped | untouched during this phase | not run | `preserve_hold` |
| `shared-5` | yes | stopped | subset `start -> ready -> stop` passed | not run on deployed host | `subset_validated` |
| `shared-6` | yes | stopped | canary `start -> ready -> stop` passed | not run on deployed host | `canary_validated` |
| `shared-7` | yes | stopped | subset `start -> ready -> stop` passed | not run on deployed host | `subset_validated` |

## Why This Matrix Stops Here

The phase goal was to protect nine live accounts first, not to force a premature public cutover.

The full deployed-host relay/API proof was intentionally **not** escalated to the entire pool because:

- the Windows-side public API is not yet listening on `:4010`
- the current `80/443` edge on `192.168.88.250` is not yet our trusted relay boundary
- forcing a broad rollout before that edge is reconciled would add profile risk without giving trustworthy API proof

## Matrix Verdict

- deployed inventory truth: `9/9 confirmed`
- canary-safe start/stop path: `passed`
- subset-safe start/stop path: `passed`
- full deployed-host API cutover: `hold_preserve_accounts`

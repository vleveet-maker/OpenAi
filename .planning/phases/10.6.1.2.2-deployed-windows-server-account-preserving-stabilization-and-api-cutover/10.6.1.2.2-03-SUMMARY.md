# 10.6.1.2.2 Plan 03 Summary

- The deployed Windows Server now has staged evidence, not guesswork: freeze-point audit, one-worker canary, and two-worker subset start/stop proof.
- The phase ends with a preserve-first verdict of `hold_preserve_accounts`.
- Phase 11 remains blocked because the deployed Windows Server public API is still not promotable:
  - direct `:4010` is closed
  - `80/443` currently hit `Caddy`
  - `http://192.168.88.250/...` redirects to `https://192.168.88.250/...`
  - HTTPS on that host currently fails before our API can be trusted

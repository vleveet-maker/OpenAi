---
phase: 10.5.1.1.1-compact-visible-runtime-rollout-and-crash-restore-popup-hardening
verified: 2026-03-29T13:45:00+03:00
status: complete
score: 3/3 phase truths verified
---

# Phase 10.5.1.1.1 Verification Report

Phase goal: formalize compact visible as the current rollout fallback, harden `Restore pages` popup handling, and end with fresh proof strong enough to decide whether `Phase 11` can resume.

Verified: 2026-03-29T13:45:00+03:00
Status: complete

## Goal Achievement

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Compact visible is now a first-class operator/runtime path with truthful control-plane wording. | x VERIFIED | Host-controller defaults, control-api pool snapshots, internal admin copy, and worker runtime labels now all treat compact visible as the routine fallback baseline. |
| 2 | Popup-safe browser lifecycle suppresses `Restore pages` without wiping durable login state. | x VERIFIED | Launcher and stop scripts use graceful close, restore-artifact cleanup, and anti-popup flags instead of deleting durable profiles or cookies. |
| 3 | Fresh worker-by-worker proof shows whether compact visible gives Phase 11 a real baseline. | x VERIFIED | `.planning/phases/.../10.5.1.1.1-PROOF.json` records six successful live runs with `Temporary Chat`, `GPT-5.4 Thinking`, and `smoke-ok` replies. |

Score: 3/3 truths verified

## Live Compact-Visible Proof

Proof artifact:

- `.planning/phases/10.5.1.1.1-compact-visible-runtime-rollout-and-crash-restore-popup-hardening/10.5.1.1.1-PROOF.json`

Worker-by-worker result:

| Worker | Runtime Class | Conversation | Model | Relay | Assistant Reply | Verdict |
|--------|---------------|--------------|-------|-------|-----------------|---------|
| `dad` | `host_visible_compact` | `temporary` | `GPT-5.4 Thinking` | `complete` | `smoke-ok` | usable |
| `wife` | `host_visible_compact` | `temporary` | `GPT-5.4 Thinking` | `complete` | `smoke-ok` | usable |
| `shared-1` | `host_visible_compact` | `temporary` | `GPT-5.4 Thinking` | `complete` | `smoke-ok` | usable |
| `shared-2` | `host_visible_compact` | `temporary` | `GPT-5.4 Thinking` | `complete` | `smoke-ok` | usable |
| `shared-3` | `host_visible_compact` | `temporary` | `GPT-5.4 Thinking` | `complete` | `smoke-ok` | usable |
| `shared-4` | `host_visible_compact` | `temporary` | `GPT-5.4 Thinking` | `complete` | `smoke-ok` | usable |

Notes:

- The proof ran one worker at a time.
- Windows opened only as needed in compact corner mode.
- All worker windows were closed again after the script finished.

## Automated Checks

| Command | Status |
|---------|--------|
| `cmd /c npm.cmd test --prefix services/host-controller` | x PASSED |
| `cmd /c npm.cmd test --prefix services/control-api` | x PASSED |
| `cmd /c npm.cmd run build --prefix services/control-api` | x PASSED |
| `cmd /c npm.cmd test --prefix workers/agent` | x PASSED |
| `cmd /c npm.cmd run build --prefix workers/agent` | x PASSED |
| `powershell -Command "[void][scriptblock]::Create((Get-Content -Raw 'infra/host-worker/test-host-worker-relay.ps1'))"` | x PASSED |
| `powershell -Command "[void][scriptblock]::Create((Get-Content -Raw 'infra/host-worker/start-host-native-worker.ps1'))"` | x PASSED |
| `powershell -Command "[void][scriptblock]::Create((Get-Content -Raw 'infra/host-worker/start-alternate-desktop-browser.ps1'))"` | x PASSED |
| `powershell -Command "[void][scriptblock]::Create((Get-Content -Raw 'infra/host-worker/stop-host-native-worker.ps1'))"` | x PASSED |
| `docker compose -f infra/docker-compose.yml -f infra/docker-compose.host-native.yml config` | x PASSED |

## Requirements Coverage

| Requirement | Status |
|-------------|--------|
| STAB-01 | x VERIFIED - current ChatGPT relay succeeded on `dad`, `wife`, and `shared-1` during the compact-visible proof |
| CVRT-01 | x VERIFIED - compact visible is now the formal baseline in control-plane truth |
| CVRT-02 | x VERIFIED - popup hardening is explicit and profile-safe |
| CVRT-03 | x VERIFIED - fresh proof produced six current usable workers |

## Residual Risk

- Alternate desktop remains a separate experimental runtime and is no longer the rollout gate.
- Compact-visible proof is fresh and strong, but longer-term routine use still belongs in Phase 11 rollout-smoke confidence rather than being assumed forever from one phase.
- Recreating `control-api` may require reloading `edge` so Nginx picks up the new upstream container IP.

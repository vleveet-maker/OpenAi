---
phase: 10.5.1.1.1-compact-visible-runtime-rollout-and-crash-restore-popup-hardening
plan: 03
subsystem: compact-visible-proof-and-decision
tags: [proof, relay, temporary-chat, docs, runtime-decision]
provides:
  - canonical compact-visible proof script
  - fresh worker-by-worker proof artifact
  - Phase 11 unblock decision
key-files:
  modified:
    - infra/host-worker/test-host-worker-relay.ps1
    - docs/host-native-worker.md
    - docs/internal-worker-ops.md
    - .planning/phases/10.5.1.1.1-compact-visible-runtime-rollout-and-crash-restore-popup-hardening/10.5.1.1.1-PROOF.json
requirements-completed: [CVRT-03]
completed: 2026-03-29
---

# Phase 10.5.1.1.1 Plan 03: Summary

Compact-visible proof is now current, reusable, and strong enough to unblock rollout-smoke planning.

## Accomplishments

- Reworked `test-host-worker-relay.ps1` so it starts only the requested worker in `CompactCorner`, proves `Temporary Chat -> GPT-5.4 Thinking -> relay`, and then closes the worker again by default.
- Added richer proof output: `workerId`, `runtimeClass`, `conversationMode`, `modelLabel`, bootstrap result, relay result, and exact assistant reply text.
- Rewrote the operator docs around compact visible as the real rollout baseline and documented `Restore pages` recovery in plain language.
- Produced a fresh proof artifact at `.planning/phases/.../10.5.1.1.1-PROOF.json`.

## Live Result

- `dad`: usable
- `wife`: usable
- `shared-1`: usable
- `shared-2`: usable
- `shared-3`: usable
- `shared-4`: usable

All six passed the same path:

`CompactCorner -> Temporary Chat -> GPT-5.4 Thinking -> relay -> smoke-ok`

## Verification

- `cmd /c npm.cmd test --prefix services/host-controller`
- `cmd /c npm.cmd test --prefix services/control-api`
- `cmd /c npm.cmd run build --prefix services/control-api`
- `cmd /c npm.cmd test --prefix workers/agent`
- `cmd /c npm.cmd run build --prefix workers/agent`
- live compact-visible proof across the official six-worker set

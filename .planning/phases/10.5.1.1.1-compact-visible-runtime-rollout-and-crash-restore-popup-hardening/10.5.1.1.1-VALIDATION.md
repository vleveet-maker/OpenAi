---
phase: 10.5.1.1.1
slug: compact-visible-runtime-rollout-and-crash-restore-popup-hardening
status: complete
nyquist_compliant: true
wave_0_complete: true
created: 2026-03-29
---

# Phase 10.5.1.1.1 - Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `node --test`, `vitest`, and PowerShell parser/live scripts |
| **Config file** | `services/host-controller/package.json`, `services/control-api/package.json`, `workers/agent/package.json` |
| **Quick run command** | `cmd /c npm.cmd test --prefix services/host-controller && cmd /c npm.cmd test --prefix services/control-api && cmd /c npm.cmd test --prefix workers/agent` |
| **Full suite command** | `cmd /c npm.cmd test --prefix services/host-controller && cmd /c npm.cmd test --prefix services/control-api && cmd /c npm.cmd run build --prefix services/control-api && cmd /c npm.cmd test --prefix workers/agent && cmd /c npm.cmd run build --prefix workers/agent && docker compose -f infra/docker-compose.yml -f infra/docker-compose.host-native.yml config` |
| **Estimated runtime** | ~360 seconds |

---

## Sampling Rate

- **After every task commit:** run the smallest affected package test command
- **After every plan wave:** run the full suite command plus any touched PowerShell parser checks
- **Before `$gsd-verify-work`:** full suite must be green
- **Max feedback latency:** 360 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 10.5.1.1.1-01-* | 01 | 1 | CVRT-01 | host-controller + control-api | `cmd /c npm.cmd test --prefix services/host-controller && cmd /c npm.cmd test --prefix services/control-api` | yes | green |
| 10.5.1.1.1-02-* | 02 | 2 | CVRT-02 | PowerShell + runtime regression | `powershell -Command "[void][scriptblock]::Create((Get-Content -Raw 'infra/host-worker/start-host-native-worker.ps1'))"` | yes | green |
| 10.5.1.1.1-03-* | 03 | 3 | live proof + docs | `powershell -ExecutionPolicy Bypass -File .\\infra\\host-worker\\test-host-worker-relay.ps1 -WorkerId wife -TimeoutSeconds 180` | yes | green |

*Status: pending / green / red / flaky*

---

## Wave 0 Requirements

- [x] Add or update host-controller tests for expanded worker inventory and compact-visible start semantics
- [x] Add or update control-api tests for compact-visible admin/runtime truth
- [x] Keep PowerShell parser checks explicit for launcher/stop-script changes

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Confirm compact visible windows stay small and corner-positioned | CVRT-01 | Real desktop placement cannot be fully asserted from unit tests | 1. Start one worker in compact visible mode. 2. Confirm window is small and placed in the configured corner slot. 3. Close it again after proof. |
| Confirm no `Restore pages` popup blocks ChatGPT after stop/start | CVRT-02 | Requires real Edge lifecycle behavior on the host | 1. Stop the worker through the new path. 2. Restart it in compact visible mode. 3. Confirm ChatGPT is visible instead of the restore popup. |
| Confirm the worker-by-worker compact-visible proof is current | CVRT-03 | Requires real ChatGPT UI, live login state, and live relay | 1. Run the compact-visible proof on the official worker set. 2. Record bootstrap + relay result for each named worker. 3. Use that artifact to unblock or keep blocking Phase 11. |

---

## Validation Sign-Off

- [x] All tasks have automated verify or explicit manual gate coverage
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all missing references
- [x] No watch-mode flags
- [x] Feedback latency < 360s for automated checks
- [x] `nyquist_compliant: true` set in frontmatter once plan-specific task rows are finalized

**Approval:** complete

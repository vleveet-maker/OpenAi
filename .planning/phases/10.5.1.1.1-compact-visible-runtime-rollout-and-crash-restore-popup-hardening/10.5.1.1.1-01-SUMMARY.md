---
phase: 10.5.1.1.1-compact-visible-runtime-rollout-and-crash-restore-popup-hardening
plan: 01
subsystem: compact-visible-control-plane
tags: [host-controller, control-api, admin, compact-visible]
provides:
  - compact visible as the routine host-pool baseline
  - official six-worker host inventory in runtime truth
  - admin wording aligned to compact visible
key-files:
  modified:
    - services/host-controller/src/config.mjs
    - services/host-controller/src/host-controller.mjs
    - services/control-api/src/workers/host-controller-client.ts
    - services/control-api/src/workers/host-pool-service.ts
    - services/control-api/src/workers/worker-registry.ts
    - services/control-api/src/routes/internal-host-pool.ts
    - services/control-api/src/routes/internal-admin-page.ts
requirements-completed: [CVRT-01]
completed: 2026-03-29
---

# Phase 10.5.1.1.1 Plan 01: Summary

Compact visible is now the declared routine fallback runtime, not an accidental side path.

## Accomplishments

- Changed host-controller defaults to `runtimeMode=visible_auth` with `browserWindowMode=CompactCorner`.
- Promoted `host_visible_compact` into routine worker truth inside control-api and host-pool snapshots.
- Extended the official host inventory to include `shared-2`, `shared-3`, and `shared-4`.
- Updated internal admin wording so `Start pool` means compact visible baseline, while alternate desktop is now an explicit probe path.

## Verification

- `cmd /c npm.cmd test --prefix services/host-controller`
- `cmd /c npm.cmd test --prefix services/control-api`

## Residual Note

- Stability-gate counters still belong to the old alternate-desktop path; compact-visible rollout truth now comes from the fresh proof artifact in this phase.

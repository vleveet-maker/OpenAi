---
phase: 15-deployed-windows-disconnected-baseline-and-forced-host-edge-remediation
plan: 02
type: execute
wave: 2
depends_on: [15-01-PLAN.md]
files_modified:
  - services/control-api/src/
  - services/control-api/test/
autonomous: true
requirements: [WREM-01, WREM-03]
user_setup: []
---

<objective>
Expose the latest disconnected-baseline remediation result in an internal operator surface so the repair attempt is visible without terminal archaeology.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-deployed-windows-disconnected-baseline-and-forced-host-edge-remediation/15-RESEARCH.md
@.planning/phases/15-deployed-windows-disconnected-baseline-and-forced-host-edge-remediation/15-VALIDATION.md
@services/control-api/src/config.ts
@services/control-api/src/server.ts
@services/control-api/src/routes/internal-admin-page.ts
@services/control-api/src/routes/internal-zero-ready-root-cause.ts
@services/control-api/test/internal-admin-page.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a file-backed latest disconnected-baseline remediation route to control-api</name>
  <files>services/control-api/src/config.ts, services/control-api/src/server.ts, services/control-api/src/routes/internal-disconnected-baseline-remediation.ts, services/control-api/test/</files>
  <read_first>services/control-api/src/config.ts, services/control-api/src/server.ts, services/control-api/src/routes/internal-zero-ready-root-cause.ts, services/control-api/test/internal-zero-ready-root-cause.test.ts</read_first>
  <action>Add a new config field `disconnectedBaselineRemediationStatePath` sourced from `DISCONNECTED_BASELINE_REMEDIATION_STATE_PATH` with default repo path `infra/data/disconnected-baseline-remediation/latest.json`. Create `services/control-api/src/routes/internal-disconnected-baseline-remediation.ts` with `GET /internal/disconnected-baseline-remediation/latest` that returns `{ latest: null }` when the file is missing and returns the parsed latest remediation payload when it exists. Register the route behind the internal admin guard in `server.ts`, and add tests that prove both the empty state and the populated state, including the exact fields `recoveredReadyCount`, `dominantRemainingBlockerClass`, `forcedHostHopStatus`, `publicOwnerHopStatus`, and `verdict`.</action>
  <verify>rg -n "DISCONNECTED_BASELINE_REMEDIATION_STATE_PATH|disconnectedBaselineRemediationStatePath|/internal/disconnected-baseline-remediation/latest|latest: null|recoveredReadyCount|dominantRemainingBlockerClass|forcedHostHopStatus|publicOwnerHopStatus|verdict" services/control-api/src services/control-api/test</verify>
  <acceptance_criteria>
    - `services/control-api/src/routes/internal-disconnected-baseline-remediation.ts` exists
    - `config.ts` supports `DISCONNECTED_BASELINE_REMEDIATION_STATE_PATH`
    - `server.ts` registers `/internal/disconnected-baseline-remediation/latest`
    - tests prove the route returns `{ latest: null }` when no remediation result exists
    - tests prove the populated payload includes `recoveredReadyCount`, `dominantRemainingBlockerClass`, `forcedHostHopStatus`, `publicOwnerHopStatus`, and `verdict`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Render the latest disconnected-baseline remediation on the internal admin page</name>
  <files>services/control-api/src/routes/internal-admin-page.ts, services/control-api/test/internal-admin-page.test.ts</files>
  <read_first>services/control-api/src/routes/internal-admin-page.ts, services/control-api/test/internal-admin-page.test.ts, services/control-api/src/routes/internal-disconnected-baseline-remediation.ts</read_first>
  <action>Add a dedicated `Latest disconnected baseline remediation` section to `/internal/admin`. Make the page fetch `/internal/disconnected-baseline-remediation/latest`, render an empty state `No disconnected baseline remediation captured yet.` when there is no file, and render the latest timestamp, recovered ready count, dominant remaining blocker class, forced-Host hop status, public-owner hop status, and final verdict when the file exists. Keep the existing worker summary, latest readiness recovery, latest rollout smoke, latest post-recovery regression, and latest zero-ready root-cause sections intact.</action>
  <verify>rg -n "Latest disconnected baseline remediation|No disconnected baseline remediation captured yet.|/internal/disconnected-baseline-remediation/latest|recovered ready count|dominant remaining blocker class|forced-Host hop|public-owner hop|verdict" services/control-api/src/routes/internal-admin-page.ts services/control-api/test/internal-admin-page.test.ts</verify>
  <acceptance_criteria>
    - the admin page contains the text `Latest disconnected baseline remediation`
    - the admin page fetches `/internal/disconnected-baseline-remediation/latest`
    - the admin page has an explicit empty state `No disconnected baseline remediation captured yet.`
    - the populated state renders recovered ready count, dominant remaining blocker class, forced-Host hop status, public-owner hop status, and verdict
    - the existing worker summary, latest readiness recovery, latest rollout smoke, latest post-recovery regression, and latest zero-ready root-cause sections remain present
  </acceptance_criteria>
</task>

</tasks>

<success_criteria>

- the latest disconnected-baseline remediation result is visible from an internal operator surface
- no operator has to infer the current repair state from scattered root-cause and smoke artifacts

</success_criteria>

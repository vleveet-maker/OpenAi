---
phase: 15-deployed-windows-disconnected-baseline-and-forced-host-edge-remediation
plan: 01
type: execute
wave: 1
depends_on: [14-VERIFICATION.md]
files_modified:
  - .planning/phases/15-deployed-windows-disconnected-baseline-and-forced-host-edge-remediation/
  - docs/windows-browser-block.md
  - docs/windows-public-api-block.md
  - docs/internal-worker-ops.md
  - infra/windows-block/
  - infra/data/disconnected-baseline-remediation/
autonomous: true
requirements: [WREM-01, WREM-02]
user_setup: []
---

<objective>
Create the canonical preserve-first remediation harness for the disconnected baseline plus forced-Host edge branch and document the exact repo-to-host sync contract.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-deployed-windows-disconnected-baseline-and-forced-host-edge-remediation/15-RESEARCH.md
@.planning/phases/14-deployed-windows-zero-ready-post-recovery-baseline-and-public-canary-502-root-cause-investigation/14-VERIFICATION.md
@docs/windows-browser-block.md
@docs/windows-public-api-block.md
@docs/internal-worker-ops.md
@infra/windows-block/recover-browser-block-readiness.ps1
@infra/windows-block/investigate-zero-ready-root-cause.ps1
@infra/windows-block/probe-public-api.ps1
@infra/windows-block/test-rollout-smoke.ps1
@infra/data/zero-ready-root-cause/latest.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a preserve-first remediation harness and durable remediation artifact contract</name>
  <files>infra/windows-block/remediate-disconnected-baseline-and-forced-host-edge.ps1, infra/data/disconnected-baseline-remediation/, .planning/phases/15-deployed-windows-disconnected-baseline-and-forced-host-edge-remediation/</files>
  <read_first>infra/windows-block/recover-browser-block-readiness.ps1, infra/windows-block/investigate-zero-ready-root-cause.ps1, infra/windows-block/probe-public-api.ps1, infra/windows-block/test-rollout-smoke.ps1, .planning/phases/14-deployed-windows-zero-ready-post-recovery-baseline-and-public-canary-502-root-cause-investigation/14-VERIFICATION.md</read_first>
  <action>Create `infra/windows-block/remediate-disconnected-baseline-and-forced-host-edge.ps1` as the canonical Phase 15 wrapper. Give it concrete defaults `SessionBaseUrl=http://127.0.0.1:8080`, `PublicApiBaseUrl=http://77.66.186.75`, `InternalBaseUrl=http://127.0.0.1:8081`, `HostControllerBaseUrl=http://127.0.0.1:4040`, `WindowsEdgeBaseUrl=http://127.0.0.1`, `PublicHostHeader=77.66.186.75`, `CanaryWorkerId=shared-6`, `RecoveryWaitSeconds=15`, and `EdgeSettleSeconds=10`. Make it capture exact stage snapshots named `before_remediation`, `after_recovery`, `after_windows_edge_forced_host`, `after_public_owner_probe`, and `after_canary_stop` from host-controller `/health` plus `/internal/host-pool`, `/internal/workers`, and `/internal/observability/summary`. Make it derive per-worker before/after blocker classes for the disconnected baseline, reuse the existing readiness-recovery plus hop-aware public probes, and write `.planning/phases/15-deployed-windows-disconnected-baseline-and-forced-host-edge-remediation/15-REMEDIATION-SUMMARY.json`, `.planning/phases/15-deployed-windows-disconnected-baseline-and-forced-host-edge-remediation/15-REMEDIATION-SUMMARY.md`, and `infra/data/disconnected-baseline-remediation/latest.json`. Make the remediation artifact expose the exact fields `recoveredReadyCount`, `dominantRemainingBlockerClass`, `forcedHostHopStatus`, `publicOwnerHopStatus`, and `verdict`, where `verdict` is exactly `remediated_ready_for_smoke` or `hold_rollout`.</action>
  <verify>rg -n "before_remediation|after_recovery|after_windows_edge_forced_host|after_public_owner_probe|after_canary_stop|recoveredReadyCount|dominantRemainingBlockerClass|forcedHostHopStatus|publicOwnerHopStatus|remediated_ready_for_smoke|hold_rollout|disconnected-baseline-remediation/latest.json|15-REMEDIATION-SUMMARY" infra/windows-block/remediate-disconnected-baseline-and-forced-host-edge.ps1</verify>
  <acceptance_criteria>
    - `infra/windows-block/remediate-disconnected-baseline-and-forced-host-edge.ps1` exists
    - the wrapper contains the exact stage names `before_remediation`, `after_recovery`, `after_windows_edge_forced_host`, `after_public_owner_probe`, and `after_canary_stop`
    - the wrapper queries host-controller `/health` and the exact internal routes `/internal/host-pool`, `/internal/workers`, and `/internal/observability/summary`
    - the wrapper writes both phase artifacts and `infra/data/disconnected-baseline-remediation/latest.json`
    - the wrapper exposes the exact fields `recoveredReadyCount`, `dominantRemainingBlockerClass`, `forcedHostHopStatus`, `publicOwnerHopStatus`, and `verdict`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Document the exact Phase 15 repo-to-host sync list and live remediation command</name>
  <files>docs/windows-browser-block.md, docs/windows-public-api-block.md, docs/internal-worker-ops.md</files>
  <read_first>docs/windows-browser-block.md, docs/windows-public-api-block.md, docs/internal-worker-ops.md, infra/windows-block/remediate-disconnected-baseline-and-forced-host-edge.ps1, .planning/phases/15-deployed-windows-disconnected-baseline-and-forced-host-edge-remediation/15-RESEARCH.md</read_first>
  <action>Update `docs/windows-browser-block.md`, `docs/windows-public-api-block.md`, and `docs/internal-worker-ops.md` with one exact Phase 15 repo-to-host sync checklist and one exact live remediation command. The sync list must explicitly include `infra/windows-block/remediate-disconnected-baseline-and-forced-host-edge.ps1`, `infra/windows-block/recover-browser-block-readiness.ps1`, `infra/windows-block/probe-public-api.ps1`, and `infra/windows-block/test-rollout-smoke.ps1`. The live command must use `shared-6`, `RecoveryWaitSeconds=15`, `EdgeSettleSeconds=10`, `PublicHostHeader=77.66.186.75`, and the exact output paths `15-REMEDIATION-SUMMARY.json` plus `15-REMEDIATION-SUMMARY.md`. Repeat the preserve-first ban on profile deletion, cookie clearing, local-storage clearing, blind full-pool restart, and mass relogin.</action>
  <verify>rg -n "remediate-disconnected-baseline-and-forced-host-edge.ps1|recover-browser-block-readiness.ps1|probe-public-api.ps1|test-rollout-smoke.ps1|shared-6|RecoveryWaitSeconds|EdgeSettleSeconds|77.66.186.75|15-REMEDIATION-SUMMARY|profile deletion|mass relogin" docs/windows-browser-block.md docs/windows-public-api-block.md docs/internal-worker-ops.md</verify>
  <acceptance_criteria>
    - all three docs mention `remediate-disconnected-baseline-and-forced-host-edge.ps1`
    - all three docs include the exact Phase 15 sync list with the four script paths
    - all three docs restate the exact output artifact names `15-REMEDIATION-SUMMARY.json` and `15-REMEDIATION-SUMMARY.md`
    - all three docs repeat the preserve-first ban on destructive reset behavior
    - the documented command includes `shared-6`, `RecoveryWaitSeconds=15`, `EdgeSettleSeconds=10`, and `PublicHostHeader=77.66.186.75`
  </acceptance_criteria>
</task>

</tasks>

<success_criteria>

- one canonical remediation harness exists for the deployed Windows browser-block host
- the remediation run can now be repeated with one exact sync list and one exact live command instead of ad-hoc operator memory

</success_criteria>

---
phase: 10.6.1-remote-relay-server-verification-and-public-api-readiness
verified: 2026-03-29T21:25:00+03:00
status: complete_through_followup
---

# Phase 10.6.1 Server Verification

## Operator Verification Path

Known repeatable check:

```powershell
powershell -ExecutionPolicy Bypass -File .\infra\remote\verify-remote-relay.ps1 -Password <ssh-password>
```

This is the official rerunnable verification path for:

- service truth
- ingress truth
- autostart truth
- topology truth

## Service Truth

Verified on `77.66.186.75` over `ssh -p 2222 mi50@77.66.186.75`.

| Check | Result |
|------|--------|
| `systemctl is-enabled owmcgp-remote-relay` | `enabled` |
| `systemctl is-active owmcgp-remote-relay` | `active` |
| relay bind | `127.0.0.1:4010` |
| topology hint | `separate_host_runtime_via_reverse_tunnel` |
| server-local relay health | `200 OK` |

## Edge Truth

| Check | Result |
|------|--------|
| `nginx` installed | yes |
| `systemctl is-enabled nginx` | `enabled` |
| `systemctl is-active nginx` | `active` |
| `nginx -t` | syntax OK |
| `curl http://127.0.0.1/healthz` | `200 ok` |
| `curl http://127.0.0.1:8080/healthz` | `200 ok` |

The deliberate edge exists and fronts `/api/relay/*` to the raw relay on `127.0.0.1:4010`.

## Public Ingress Truth At Plan Time

| Path | Result |
|------|--------|
| operator machine -> `http://77.66.186.75:4010/api/relay/health` | timeout |
| operator machine -> `http://77.66.186.75/api/relay/health` | timeout |
| server self-call -> `http://77.66.186.75/healthz` | upstream `404`, not our nginx edge |
| server self-call -> `http://77.66.186.75:8080/healthz` | connection refused |

Additional host facts:

- `ufw` inactive
- `iptables` default policy `ACCEPT`

## Topology Truth

Current verified topology:

1. Linux relay service under `systemd`
2. raw relay only on `127.0.0.1:4010`
3. deliberate `nginx` edge on the Linux host
4. compact-visible Windows workers on the separate operator host
5. reverse SSH tunnels provide server-local worker ports

## Verdict

Server truth is explicit and repeatable.

This server-verification artifact remains useful as the point-in-time baseline taken before the router-side fix.

That upstream ingress blocker was later resolved in `Phase 10.6.1.1`, where outside-client proof reached the deliberate relay edge successfully.

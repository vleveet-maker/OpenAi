---
phase: 10.6.1-remote-relay-server-verification-and-public-api-readiness
plan: 03
type: execute
wave: 3
depends_on: [10.6.1-01-PLAN.md, 10.6.1-02-PLAN.md]
files_modified:
  - docs/remote-relay-server.md
  - infra/remote/
  - services/control-api/src/routes/public-remote-relay.ts
autonomous: true
requirements: [EXTAPI-03, EXTAPI-04]
user_setup: []
---

<objective>
Put one deliberate external ingress path in front of the relay and document a public-ready API boundary that other clients can consume safely.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@docs/remote-relay-server.md
@services/control-api/src/routes/public-remote-relay.ts
@infra/remote/deploy-remote-relay.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define one deliberate public ingress path</name>
  <files>infra/remote/, docs/remote-relay-server.md</files>
  <read_first>docs/remote-relay-server.md, .planning/phases/10.6.1-remote-relay-server-verification-and-public-api-readiness/10.6.1-RESEARCH.md</read_first>
  <action>Add one explicit public ingress layer in front of the relay service, such as `nginx` or an audited equivalent, so outside callers use a deliberate front door instead of the raw `:4010` process socket. Keep the raw relay service behind that edge.</action>
  <verify>there is a concrete ingress config artifact and docs explain how it fronts the relay</verify>
  <acceptance_criteria>
    - one deliberate public ingress path exists
    - the raw relay process is no longer treated as the public contract by itself
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Harden and document the outside-client API contract</name>
  <files>docs/remote-relay-server.md, services/control-api/src/routes/public-remote-relay.ts</files>
  <read_first>services/control-api/src/routes/public-remote-relay.ts, docs/remote-relay-server.md</read_first>
  <action>Keep the public relay contract narrow and explicit for outside clients: auth header, health, fresh dialog, continuing dialog, and end-dialog. Update docs with example requests, example responses, error behavior, and a clear note that internal admin or worker routes are not part of the public API.</action>
  <verify>docs/remote-relay-server.md contains public API examples and auth truth</verify>
  <acceptance_criteria>
    - outside-client examples exist for fresh and continuing dialog calls
    - auth expectations are explicit
    - docs state clearly what is not public
  </acceptance_criteria>
</task>

<task type="manual">
  <name>Task 3: Run external ingress smoke and record the final public-readiness verdict</name>
  <files>docs/remote-relay-server.md, .planning/phases/10.6.1-remote-relay-server-verification-and-public-api-readiness/10.6.1-SERVER-VERIFICATION.md</files>
  <read_first>docs/remote-relay-server.md, .planning/phases/10.6.1-remote-relay-server-verification-and-public-api-readiness/10.6.1-SERVER-VERIFICATION.md</read_first>
  <action>Call the service through the intended external ingress path and record whether it is genuinely usable by outside clients. If it still fails, record a precise blocker instead of falling back to the raw socket as if the problem were solved.</action>
  <verify>the final verdict says whether other clients can use the intended public path today</verify>
  <acceptance_criteria>
    - external ingress was actually tested
    - the result is explicit: usable or blocked
    - the blocker, if any, is precise
  </acceptance_criteria>
</task>

</tasks>

<success_criteria>

- the project has one deliberate public relay boundary for outside clients
- public API truth is explicit instead of being inferred from an internal port

</success_criteria>

---
phase: 10.6.1-remote-relay-server-verification-and-public-api-readiness
verified: 2026-03-29T19:35:00+03:00
status: partial
---

# Phase 10.6.1 Worker Matrix

All official workers were audited through the real remote topology:

- Linux `owmcgp-remote-relay`
- server-local relay path
- reverse SSH tunnel
- compact-visible Windows worker

## Latest Matrix

| Worker | Relay Path | Classification | Failure Code / Result | Conversation Mode | Model | Notes |
|--------|------------|----------------|-----------------------|-------------------|-------|-------|
| `dad` | server-local relay + reverse tunnel | `usable` | `dad-remote-ok` | `temporary` | `GPT-5.4 Thinking` | latest rerun passed twice in a row; the earlier relay failure now looks intermittent rather than durable |
| `wife` | server-local relay + reverse tunnel | `usable` | `wife-remote-ok` | `temporary` | `GPT-5.4 Thinking` | remote path proved end-to-end |
| `shared-1` | server-local relay + reverse tunnel | `usable` | `shared-1-remote-ok` | `temporary` | `GPT-5.4 Thinking` | remote path proved end-to-end |
| `shared-2` | server-local relay + reverse tunnel | `usable` | `shared-2-remote-ok` | `temporary` | `GPT-5.4 Thinking` | remote path proved end-to-end |
| `shared-3` | server-local relay + reverse tunnel | `usable` | `shared-3-remote-ok` | `temporary` | `GPT-5.4 Thinking` | remote path proved end-to-end |
| `shared-4` | server-local relay + reverse tunnel | `usable` | `shared-4-remote-ok` | `temporary` | `GPT-5.4 Thinking` | remote path proved end-to-end |

## Summary

- `usable`: `6`
- `relay_failed`: `0`
- `auth_required`: `0`
- `bootstrap_failed`: `0`

## Operational Note

Each worker was opened only as needed for the remote relay smoke and then closed again afterward. The matrix reflects the real remote architecture, not a local-only browser check.

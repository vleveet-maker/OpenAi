---
phase: 10.6.1-remote-relay-server-verification-and-public-api-readiness
plan: 01
subsystem: remote-server-verification
tags: [server, systemd, nginx, verification, topology]
provides:
  - repeatable remote server verification path
  - explicit service truth versus ingress truth
  - dedicated server verification artifact
key-files:
  created:
    - .planning/phases/10.6.1-remote-relay-server-verification-and-public-api-readiness/10.6.1-SERVER-VERIFICATION.md
  modified:
    - docs/remote-relay-server.md
    - infra/remote/deploy-remote-relay.ps1
    - infra/remote/verify-remote-relay.ps1
    - infra/remote/deploy-public-ingress.ps1
requirements-progressed: [EXTAPI-01]
completed: 2026-03-29
---

# Phase 10.6.1 Plan 01: Summary

The remote relay server now has one known verification path instead of ad-hoc SSH debugging.

## Accomplishments

- Added `infra/remote/verify-remote-relay.ps1` as the operator verification path.
- Separated service truth from ingress truth and topology truth.
- Recorded the current server shape in `10.6.1-SERVER-VERIFICATION.md`.
- Updated server docs so the relay is described as local-only behind a deliberate `nginx` edge.

## Verification

- live SSH verification on `77.66.186.75`
- `systemctl is-enabled owmcgp-remote-relay`
- `systemctl is-active owmcgp-remote-relay`
- `curl http://127.0.0.1:4010/api/relay/health` on the server
- `curl http://127.0.0.1/healthz` on the server
- `curl http://127.0.0.1:8080/healthz` on the server

## Residual Note

At the time this plan finished, public ingress was still blocked upstream even though server-local relay and `nginx` were healthy. That ingress gap was later resolved in `Phase 10.6.1.1`.

---
phase: 10.6.1-remote-relay-server-verification-and-public-api-readiness
verified: 2026-03-29T21:25:00+03:00
status: complete_through_followup
score: 4/4 requirements satisfied through Phase 10.6.1.1
---

# Phase 10.6.1 Verification

## Result

`Phase 10.6.1` is **complete through follow-up evidence**.

In plain words:

- the server module is really running
- the real remote worker matrix is now explicit
- the public API contract is documented and deliberately fronted
- and the last public-ingress blocker was resolved by `Phase 10.6.1.1`

## Truth Verification

| Truth | Status | Evidence |
|------|--------|----------|
| Server truth is explicit and repeatable | x VERIFIED | `10.6.1-SERVER-VERIFICATION.md`, `infra/remote/verify-remote-relay.ps1`, server-local relay and nginx checks |
| Worker/session truth is explicit through the real remote topology | x VERIFIED | `10.6.1-WORKER-MATRIX.md`, worker-pinned remote smoke on all six official workers |
| Public API truth is explicit | x VERIFIED | deliberate nginx edge exists, docs are explicit, and outside-client reachability now passes through the MikroTik NAT follow-up |

## Requirement Coverage

| Requirement | Status | Notes |
|-------------|--------|-------|
| `EXTAPI-01` | x VERIFIED | repeatable server verification path exists |
| `EXTAPI-02` | x VERIFIED | all six official workers were audited through the real remote topology |
| `EXTAPI-03` | x VERIFIED | resolved by `Phase 10.6.1.1`, which hardened MikroTik NAT and proved outside-client ingress to the deliberate edge |
| `EXTAPI-04` | x VERIFIED | public API auth and route contract are explicit and documented |

## Current Worker Verdict

- usable: `6/6`
- relay_failed: `0/6` in the latest rerun
- note: `dad` previously produced one intermittent `relay_failed`, then passed two consecutive reruns

## Current Public Ingress Verdict

The relay is now public-ready through the deliberate edge:

- server-local `nginx` works
- server-local relay works
- outside clients now reach `http://77.66.186.75`
- invalid bearer on `/api/relay/health` now reaches the relay boundary and returns `403`

Raw `:4010` remains intentionally non-public.

## Recommended Next Step

`Phase 11` can now be planned on top of the real remote architecture.

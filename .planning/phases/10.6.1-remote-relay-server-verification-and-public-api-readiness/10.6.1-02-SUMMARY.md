---
phase: 10.6.1-remote-relay-server-verification-and-public-api-readiness
plan: 02
subsystem: remote-worker-audit
tags: [remote-relay, workers, matrix, reverse-tunnel]
provides:
  - worker-by-worker remote relay matrix
  - pinned worker remote relay path
  - explicit 6/6 usable audit truth after rerun
key-files:
  created:
    - .planning/phases/10.6.1-remote-relay-server-verification-and-public-api-readiness/10.6.1-WORKER-MATRIX.md
  modified:
    - services/control-api/src/remote/remote-relay-service.ts
    - services/control-api/test/public-remote-relay.test.ts
    - infra/remote/test-remote-relay-worker.ps1
    - docs/remote-relay-server.md
requirements-progressed: [EXTAPI-02]
completed: 2026-03-29
---

# Phase 10.6.1 Plan 02: Summary

The project now has a real remote worker matrix instead of assuming local worker health equals remote usability.

## Accomplishments

- Added optional fresh-dialog `workerId` pinning to the remote relay service for operator audits.
- Added route coverage for pinned workers and unknown-worker rejection.
- Added `infra/remote/test-remote-relay-worker.ps1` for one-worker-at-a-time remote smoke.
- Audited all six official workers through the real remote topology and recorded the results in `10.6.1-WORKER-MATRIX.md`.

## Verification

- `cmd /c npm.cmd test --prefix services/control-api`
- `cmd /c npm.cmd run build --prefix services/control-api`
- live worker-by-worker remote smoke on:
  - `dad`
  - `wife`
  - `shared-1`
  - `shared-2`
  - `shared-3`
  - `shared-4`

## Residual Note

- The latest rerun is `6/6 usable`, but `dad` did previously produce one `relay_failed` result. Treat that as intermittent runtime debt until repeated monitoring says otherwise.

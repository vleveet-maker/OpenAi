---
phase: 13-deployed-windows-post-recovery-smoke-regression-investigation-and-stabilization
plan: 02
type: execute
wave: 2
depends_on: [13-01-PLAN.md]
files_modified:
  - services/control-api/src/
  - services/control-api/test/
autonomous: true
requirements: [WREG-01, WREG-03]
user_setup: []
---

<objective>
Expose the latest post-recovery smoke-regression result in an internal operator surface so the stabilization truth is not trapped in raw terminal output.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-deployed-windows-post-recovery-smoke-regression-investigation-and-stabilization/13-RESEARCH.md
@.planning/phases/13-deployed-windows-post-recovery-smoke-regression-investigation-and-stabilization/13-VALIDATION.md
@services/control-api/src/config.ts
@services/control-api/src/server.ts
@services/control-api/src/routes/internal-admin-page.ts
@services/control-api/src/routes/internal-readiness-recovery.ts
@services/control-api/src/routes/internal-rollout-smoke.ts
@services/control-api/test/internal-admin-page.test.ts
@services/control-api/test/internal-readiness-recovery.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a file-backed latest post-recovery regression route to control-api</name>
  <files>services/control-api/src/config.ts, services/control-api/src/server.ts, services/control-api/src/routes/internal-post-recovery-regression.ts, services/control-api/test/</files>
  <read_first>services/control-api/src/config.ts, services/control-api/src/server.ts, services/control-api/src/routes/internal-readiness-recovery.ts, services/control-api/src/routes/internal-rollout-smoke.ts, services/control-api/test/internal-readiness-recovery.test.ts</read_first>
  <action>Add a new config field `postRecoveryRegressionStatePath` sourced from `POST_RECOVERY_REGRESSION_STATE_PATH` with default repo path `infra/data/post-recovery-regression/latest.json`. Create `services/control-api/src/routes/internal-post-recovery-regression.ts` with `GET /internal/post-recovery-regression/latest` that returns `{ latest: null }` when the file is missing and returns the parsed latest regression payload when it exists. Register the route behind the internal admin guard in `server.ts`, and add tests that prove both the empty state and the populated state, including the exact fields `scriptCompatibilityVersion`, `stageSnapshots`, `firstRegressionStage`, and `verdict`.</action>
  <verify>rg -n "POST_RECOVERY_REGRESSION_STATE_PATH|postRecoveryRegressionStatePath|/internal/post-recovery-regression/latest|latest: null|scriptCompatibilityVersion|firstRegressionStage" services/control-api/src services/control-api/test</verify>
  <acceptance_criteria>
    - `services/control-api/src/routes/internal-post-recovery-regression.ts` exists
    - `config.ts` supports `POST_RECOVERY_REGRESSION_STATE_PATH`
    - `server.ts` registers `/internal/post-recovery-regression/latest`
    - tests prove the route returns `{ latest: null }` when no regression result exists
    - tests prove the populated payload includes `scriptCompatibilityVersion`, `stageSnapshots`, `firstRegressionStage`, and `verdict`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Render the latest post-recovery smoke regression on the internal admin page</name>
  <files>services/control-api/src/routes/internal-admin-page.ts, services/control-api/test/internal-admin-page.test.ts</files>
  <read_first>services/control-api/src/routes/internal-admin-page.ts, services/control-api/test/internal-admin-page.test.ts, services/control-api/src/routes/internal-post-recovery-regression.ts</read_first>
  <action>Add a dedicated `Latest post-recovery smoke regression` section to `/internal/admin`. Make the page fetch `/internal/post-recovery-regression/latest`, render an empty state `No post-recovery smoke regression captured yet.` when there is no file, and render the latest timestamp, `scriptCompatibilityVersion`, recovered ready count, smoke ready count, `firstRegressionStage`, final `verdict`, stage list, and `shared-6` canary outcome when the file exists. Keep the existing worker summary, latest readiness recovery, and latest rollout-smoke sections intact.</action>
  <verify>rg -n "Latest post-recovery smoke regression|No post-recovery smoke regression captured yet|/internal/post-recovery-regression/latest|firstRegressionStage|scriptCompatibilityVersion|shared-6" services/control-api/src/routes/internal-admin-page.ts services/control-api/test/internal-admin-page.test.ts</verify>
  <acceptance_criteria>
    - the admin page contains the text `Latest post-recovery smoke regression`
    - the admin page fetches `/internal/post-recovery-regression/latest`
    - the admin page has an explicit empty state `No post-recovery smoke regression captured yet.`
    - the populated state renders `scriptCompatibilityVersion`, `firstRegressionStage`, and `verdict`
    - the existing worker summary, latest readiness recovery, and latest rollout-smoke sections remain present
  </acceptance_criteria>
</task>

</tasks>

<success_criteria>

- the latest regression/stabilization result is visible from an internal operator surface
- no operator has to rely on raw terminal output to understand the latest post-recovery regression truth

</success_criteria>

---
phase: 26-deployed-ubuntu-sync-recovery-reverse-tunnel-task-retention-and-external-authenticated-smoke-restoration
plan: 01
type: execute
wave: 1
depends_on: [25-VERIFICATION.md]
files_modified:
  - .planning/phases/26-deployed-ubuntu-sync-recovery-reverse-tunnel-task-retention-and-external-authenticated-smoke-restoration/
  - docs/windows-browser-block.md
  - docs/windows-public-api-block.md
  - docs/remote-relay-server.md
  - docs/internal-worker-ops.md
  - infra/windows-block/
  - infra/data/post-phase25-external-restoration/
  - services/control-api/test/edge-config.test.ts
autonomous: true
requirements: [UTSR-01, UTSR-02]
user_setup: []
---

<objective>
Backport the remaining live operational fixes from the Phase 25 run, add the canonical Phase 26 restoration wrapper, and prepare a GitHub-first deployed-host handoff that no longer depends on local-only archives or Windows-local bearer-token assumptions.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-deployed-windows-post-phase24-live-fix-smoke-wrapper-backport-and-persistent-disconnected-runtime-forced-host-public-owner-503-remediation/25-RESEARCH.md
@.planning/phases/25-deployed-windows-post-phase24-live-fix-smoke-wrapper-backport-and-persistent-disconnected-runtime-forced-host-public-owner-503-remediation/25-VERIFICATION.md
@.planning/phases/26-deployed-ubuntu-sync-recovery-reverse-tunnel-task-retention-and-external-authenticated-smoke-restoration/26-RESEARCH.md
@docs/windows-browser-block.md
@docs/windows-public-api-block.md
@docs/remote-relay-server.md
@docs/internal-worker-ops.md
@infra/windows-block/start-reverse-tunnels.ps1
@infra/windows-block/register-browser-block-tasks.ps1
@infra/windows-block/recover-reverse-tunnels-and-external-api-readiness.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backport the remaining live operational fixes and make the repo copy stable on Windows again</name>
  <files>infra/windows-block/recover-reverse-tunnels-and-external-api-readiness.ps1, services/control-api/test/edge-config.test.ts, docs/windows-browser-block.md, docs/windows-public-api-block.md, docs/remote-relay-server.md, docs/internal-worker-ops.md</files>
  <read_first>infra/windows-block/recover-reverse-tunnels-and-external-api-readiness.ps1, services/control-api/test/edge-config.test.ts, .planning/phases/25-deployed-windows-post-phase24-live-fix-smoke-wrapper-backport-and-persistent-disconnected-runtime-forced-host-public-owner-503-remediation/25-VERIFICATION.md, .planning/phases/26-deployed-ubuntu-sync-recovery-reverse-tunnel-task-retention-and-external-authenticated-smoke-restoration/26-RESEARCH.md</read_first>
  <action>Backport the live Phase 25 fixes that were still missing from tracked repo state. In `infra/windows-block/recover-reverse-tunnels-and-external-api-readiness.ps1`, make `NextRunTime` and `LastRunTime` null-safe so missing scheduled-task timestamps do not throw on the deployed host. In `services/control-api/test/edge-config.test.ts`, normalize CRLF-sensitive expectations so the same test passes on Windows checkouts. Update the operator docs to state the live truth explicitly: the canonical public path is `Ubuntu nginx -> 127.0.0.1:4010`, reverse tunnels are the critical dependency for external chat, Windows `Caddy` is not the active public edge, and cross-host handoff is GitHub-first. Stamp the deployment-critical chain with `phase26-ubuntu-sync-recovery-v1`.</action>
  <verify>rg -n "phase26-ubuntu-sync-recovery-v1|127.0.0.1:4010|reverse tunnel|GitHub-first|NextRunTime|LastRunTime|CRLF|Windows Caddy" infra/windows-block/recover-reverse-tunnels-and-external-api-readiness.ps1 services/control-api/test/edge-config.test.ts docs</verify>
  <acceptance_criteria>
    - `recover-reverse-tunnels-and-external-api-readiness.ps1` no longer assumes scheduled-task times are always present
    - `edge-config.test.ts` is resilient to CRLF in Windows runs
    - the docs state `Ubuntu nginx -> 127.0.0.1:4010` as the canonical public path
    - the docs state reverse tunnels as rollout-critical and Windows `Caddy` as inactive in the live public path
    - the deployment-critical chain contains `phase26-ubuntu-sync-recovery-v1`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add the canonical Phase 26 wrapper and GitHub-first deployed-host prompt</name>
  <files>infra/windows-block/recover-ubuntu-sync-and-external-auth-smoke.ps1, infra/data/post-phase25-external-restoration/, .planning/phases/26-deployed-ubuntu-sync-recovery-reverse-tunnel-task-retention-and-external-authenticated-smoke-restoration/26-DEPLOYED-HOST-AGENT-TASK.md</files>
  <read_first>infra/windows-block/start-reverse-tunnels.ps1, infra/windows-block/register-browser-block-tasks.ps1, infra/windows-block/recover-reverse-tunnels-and-external-api-readiness.ps1, .planning/phases/26-deployed-ubuntu-sync-recovery-reverse-tunnel-task-retention-and-external-authenticated-smoke-restoration/26-RESEARCH.md</read_first>
  <action>Create `infra/windows-block/recover-ubuntu-sync-and-external-auth-smoke.ps1` as the canonical Phase 26 wrapper. It must accept concrete parameters for `-RemoteHost`, `-RemotePort`, `-RemoteUser`, `-SshKeyPath`, `-PublicBaseUrl`, `-BearerToken`, `-BearerTokenEnvVar`, `-InternalBaseUrl`, `-HostControllerBaseUrl`, `-OutputJsonPath`, and `-OutputMarkdownPath`. The wrapper must record Ubuntu SSH reachability, Ubuntu `nginx -T` upstream truth, Ubuntu local `127.0.0.1:4010` truth, reverse-tunnel task state, Ubuntu listener ports `14021..14027` and `14040`, ready-worker truth, authenticated public smoke for `/healthz`, `/v1/models`, and `/v1/chat/completions` when a bearer token is available, and a final verdict constrained to exactly `externally_ready` or `hold_rollout`. It must write `26-EXTERNAL-RESTORATION-SUMMARY.json`, `26-EXTERNAL-RESTORATION-SUMMARY.md`, and `infra/data/post-phase25-external-restoration/latest.json`. Also create `26-DEPLOYED-HOST-AGENT-TASK.md` that points only to GitHub branch `windows-browser-block-api-20260331`, tells Ubuntu and Windows to `git fetch/checkout/pull`, and explicitly says the final authenticated smoke may run from whichever host actually has the bearer token while still targeting `77.66.186.75`.</action>
  <verify>rg -n "phase26-ubuntu-sync-recovery-v1|externally_ready|hold_rollout|BearerToken|BearerTokenEnvVar|ubuntuSshReachable|canonicalPublicUpstream|reverseTunnelTaskStatus|ubuntuTunnelListenerStatus|externalChatStatus|post-phase25-external-restoration|windows-browser-block-api-20260331" infra/windows-block/recover-ubuntu-sync-and-external-auth-smoke.ps1 .planning/phases/26-deployed-ubuntu-sync-recovery-reverse-tunnel-task-retention-and-external-authenticated-smoke-restoration/26-DEPLOYED-HOST-AGENT-TASK.md</verify>
  <acceptance_criteria>
    - `infra/windows-block/recover-ubuntu-sync-and-external-auth-smoke.ps1` exists
    - the wrapper records Ubuntu SSH truth, upstream truth, listener truth, ready-worker truth, and external smoke truth
    - the wrapper writes both Phase 26 artifacts and `infra/data/post-phase25-external-restoration/latest.json`
    - the wrapper verdict is constrained to `externally_ready` or `hold_rollout`
    - `26-DEPLOYED-HOST-AGENT-TASK.md` exists and uses GitHub branch `windows-browser-block-api-20260331` as the only source of truth
    - the prompt explicitly says authenticated smoke can run from the host that actually has the bearer token
  </acceptance_criteria>
</task>

</tasks>

<success_criteria>

- the repo now carries the last Phase 25 hotfixes that were still only known from the deployed host
- operators have one canonical Phase 26 wrapper instead of another ad-hoc cross-host command chain
- the next deployed-host handoff is GitHub-first and no longer assumes Windows-local bearer-token availability

</success_criteria>

---
phase: 26-deployed-ubuntu-sync-recovery-reverse-tunnel-task-retention-and-external-authenticated-smoke-restoration
plan: 03
type: execute
wave: 3
depends_on: [26-01-PLAN.md, 26-02-PLAN.md]
files_modified:
  - .planning/phases/26-deployed-ubuntu-sync-recovery-reverse-tunnel-task-retention-and-external-authenticated-smoke-restoration/
  - infra/data/post-phase25-external-restoration/
autonomous: false
requirements: [UTSR-01, UTSR-02, UTSR-03, UTSR-04]
user_setup:
  - SSH access to Ubuntu public-owner host `77.66.186.75`
  - Admin access to the deployed Windows browser-block host `192.168.88.250`
  - A valid bearer token available on at least one execution host or operator terminal
---

<objective>
Sync the GitHub-backed Phase 26 state onto Ubuntu and Windows, prove Ubuntu topology plus reverse-tunnel retention from tracked assets, rerun authenticated external smoke, and end with one final verdict `externally_ready` or `hold_rollout`.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-deployed-ubuntu-sync-recovery-reverse-tunnel-task-retention-and-external-authenticated-smoke-restoration/26-RESEARCH.md
@.planning/phases/26-deployed-ubuntu-sync-recovery-reverse-tunnel-task-retention-and-external-authenticated-smoke-restoration/26-VALIDATION.md
@.planning/phases/26-deployed-ubuntu-sync-recovery-reverse-tunnel-task-retention-and-external-authenticated-smoke-restoration/26-01-PLAN.md
@.planning/phases/26-deployed-ubuntu-sync-recovery-reverse-tunnel-task-retention-and-external-authenticated-smoke-restoration/26-02-PLAN.md
@.planning/phases/26-deployed-ubuntu-sync-recovery-reverse-tunnel-task-retention-and-external-authenticated-smoke-restoration/26-DEPLOYED-HOST-AGENT-TASK.md
@infra/windows-block/recover-ubuntu-sync-and-external-auth-smoke.ps1
@infra/windows-block/recover-reverse-tunnels-and-external-api-readiness.ps1
@infra/windows-block/start-reverse-tunnels.ps1
@infra/windows-block/register-browser-block-tasks.ps1
@infra/remote/nginx/owmcgp-remote-relay.conf
@services/control-api/src/routes/internal-post-phase25-external-restoration.ts
@services/control-api/src/routes/internal-admin-page.ts
</context>

<tasks>

<task type="manual">
  <name>Task 1: Sync both hosts from GitHub and confirm repo-backed Ubuntu plus Windows runtime truth</name>
  <files>.planning/phases/26-deployed-ubuntu-sync-recovery-reverse-tunnel-task-retention-and-external-authenticated-smoke-restoration/26-EXTERNAL-RESTORATION-SUMMARY.json, .planning/phases/26-deployed-ubuntu-sync-recovery-reverse-tunnel-task-retention-and-external-authenticated-smoke-restoration/26-EXTERNAL-RESTORATION-SUMMARY.md, infra/data/post-phase25-external-restoration/latest.json</files>
  <read_first>.planning/phases/26-deployed-ubuntu-sync-recovery-reverse-tunnel-task-retention-and-external-authenticated-smoke-restoration/26-01-PLAN.md, .planning/phases/26-deployed-ubuntu-sync-recovery-reverse-tunnel-task-retention-and-external-authenticated-smoke-restoration/26-DEPLOYED-HOST-AGENT-TASK.md, infra/windows-block/recover-ubuntu-sync-and-external-auth-smoke.ps1, infra/remote/nginx/owmcgp-remote-relay.conf</read_first>
  <action>On Windows `D:\OpenAi`, run `git fetch origin`, `git checkout windows-browser-block-api-20260331`, and `git pull --ff-only origin windows-browser-block-api-20260331`; then run parser checks for `start-reverse-tunnels.ps1`, `register-browser-block-tasks.ps1`, `recover-reverse-tunnels-and-external-api-readiness.ps1`, and `recover-ubuntu-sync-and-external-auth-smoke.ps1`, run `npm.cmd --prefix services/control-api test -- edge-config.test.ts internal-admin-page.test.ts internal-post-phase25-external-restoration.test.ts`, run the full `npm.cmd --prefix services/control-api test`, run `npm.cmd --prefix services/control-api run build`, and restart `control-api` on `127.0.0.1:8081`. On the Ubuntu live repo checkout, run the same branch sync from GitHub, `sudo nginx -t`, `curl -i http://127.0.0.1:4010/healthz`, `curl -i -H "Authorization: Bearer <TOKEN>" http://127.0.0.1:4010/v1/models` when the token is available there, and `sudo nginx -T | egrep -n 'server_name|listen|proxy_pass|4010|192\\.168\\.88\\.250'` to confirm `proxy_pass http://127.0.0.1:4010` is still the only live upstream. Record which host actually has the bearer token if Ubuntu does not.</action>
  <verify>Both hosts are on branch `windows-browser-block-api-20260331`, Windows tests and build are green, Ubuntu `nginx -T` shows only `127.0.0.1:4010`, and the exact token-bearing execution host is known before the final smoke run.</verify>
  <acceptance_criteria>
    - Windows and Ubuntu both sync from GitHub branch `windows-browser-block-api-20260331`
    - Windows parser checks pass for the four runtime-critical PowerShell scripts
    - Windows targeted tests, full `control-api` suite, and build all pass
    - Ubuntu `nginx -T` confirms `proxy_pass http://127.0.0.1:4010` and no stale `192.168.88.250` upstream
    - Ubuntu local `127.0.0.1:4010/healthz` returns `200`
    - the execution host for authenticated smoke is explicit before Task 2 begins
  </acceptance_criteria>
</task>

<task type="manual">
  <name>Task 2: Prove reverse-tunnel retention, verify the operator surface, rerun authenticated public smoke, and write the final verdict</name>
  <files>.planning/phases/26-deployed-ubuntu-sync-recovery-reverse-tunnel-task-retention-and-external-authenticated-smoke-restoration/26-VERIFICATION.md</files>
  <read_first>.planning/phases/26-deployed-ubuntu-sync-recovery-reverse-tunnel-task-retention-and-external-authenticated-smoke-restoration/26-EXTERNAL-RESTORATION-SUMMARY.md, services/control-api/src/routes/internal-post-phase25-external-restoration.ts, services/control-api/src/routes/internal-admin-page.ts, infra/windows-block/recover-ubuntu-sync-and-external-auth-smoke.ps1</read_first>
  <action>Run `infra/windows-block/recover-ubuntu-sync-and-external-auth-smoke.ps1` from the host that can both reach the public path and provide a valid bearer token, using `-RemoteHost 77.66.186.75`, `-RemotePort 2222`, `-RemoteUser mi50`, `-PublicBaseUrl http://77.66.186.75`, `-InternalBaseUrl http://127.0.0.1:8081`, and `-HostControllerBaseUrl http://127.0.0.1:4040`, plus the Phase 26 output paths. Confirm the reverse-tunnel task stays `Running` after a settle window, confirm Ubuntu listeners `14021..14027` and `14040`, confirm `/internal/post-phase25-external-restoration/latest` or `/internal/admin` matches the written artifact, and rerun authenticated external smoke for `/healthz`, `/v1/models`, and `/v1/chat/completions` using model `owmcgp-browser`. If the GitHub-backed run stays green without new post-pull hotfixes, record `externally_ready`. If any post-pull fix is still needed, the task falls back to `Ready`, listeners disappear, or the authenticated smoke fails, write `hold_rollout` with the exact blocker in `26-VERIFICATION.md`.</action>
  <verify>The live artifact, route/admin surface, reverse-tunnel retention, and final external smoke all agree on one verdict and one blocker story.</verify>
  <acceptance_criteria>
    - `26-EXTERNAL-RESTORATION-SUMMARY.json` and `26-EXTERNAL-RESTORATION-SUMMARY.md` exist
    - `infra/data/post-phase25-external-restoration/latest.json` exists
    - the reverse-tunnel task is explicitly observed as `Running` or the exact retention failure is captured
    - Ubuntu listeners `14021..14027` and `14040` are explicitly observed or the exact missing ports are recorded
    - `/internal/post-phase25-external-restoration/latest` or `/internal/admin` matches the artifact
    - authenticated external smoke is rerun for `/healthz`, `/v1/models`, and `/v1/chat/completions` with model `owmcgp-browser`
    - `.planning/phases/26-deployed-ubuntu-sync-recovery-reverse-tunnel-task-retention-and-external-authenticated-smoke-restoration/26-VERIFICATION.md` exists
    - the final verdict is exactly `externally_ready` or `hold_rollout`
  </acceptance_criteria>
</task>

</tasks>

<success_criteria>

- the canonical Ubuntu public path is re-verified from repo-backed state on the real hosts
- reverse-tunnel retention is proven or reduced to one exact operational blocker
- the final public API claim is backed by authenticated external smoke on tracked assets rather than one-off live fixes

</success_criteria>

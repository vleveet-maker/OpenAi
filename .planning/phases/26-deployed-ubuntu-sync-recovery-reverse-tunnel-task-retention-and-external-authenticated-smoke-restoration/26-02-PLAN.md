---
phase: 26-deployed-ubuntu-sync-recovery-reverse-tunnel-task-retention-and-external-authenticated-smoke-restoration
plan: 02
type: execute
wave: 2
depends_on: [26-01-PLAN.md]
files_modified:
  - services/control-api/src/
  - services/control-api/dist/
  - services/control-api/test/
autonomous: true
requirements: [UTSR-03]
user_setup: []
---

<objective>
Expose the latest Phase 26 restoration result in the real `control-api` runtime so operators can confirm Ubuntu sync, reverse-tunnel retention, and external smoke truth before making the final public-readiness claim.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-deployed-ubuntu-sync-recovery-reverse-tunnel-task-retention-and-external-authenticated-smoke-restoration/26-RESEARCH.md
@.planning/phases/26-deployed-ubuntu-sync-recovery-reverse-tunnel-task-retention-and-external-authenticated-smoke-restoration/26-VALIDATION.md
@services/control-api/src/config.ts
@services/control-api/src/server.ts
@services/control-api/src/routes/internal-admin-page.ts
@services/control-api/src/routes/read-latest-state.ts
@services/control-api/src/routes/internal-post-phase24-external-api-readiness.ts
@services/control-api/test/internal-admin-page.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a file-backed latest post-Phase-25 external-restoration route and rebuild the served runtime files</name>
  <files>services/control-api/src/config.ts, services/control-api/src/server.ts, services/control-api/src/routes/internal-post-phase25-external-restoration.ts, services/control-api/dist/, services/control-api/test/</files>
  <read_first>services/control-api/src/config.ts, services/control-api/src/server.ts, services/control-api/src/routes/read-latest-state.ts, services/control-api/src/routes/internal-post-phase24-external-api-readiness.ts</read_first>
  <action>Add a new config field `postPhase25ExternalRestorationStatePath` sourced from `POST_PHASE25_EXTERNAL_RESTORATION_STATE_PATH` with default repo path `infra/data/post-phase25-external-restoration/latest.json`. Create `services/control-api/src/routes/internal-post-phase25-external-restoration.ts` with `GET /internal/post-phase25-external-restoration/latest` that returns `{ latest: null }` when the file is missing and the parsed latest payload when it exists. Reuse `read-latest-state.ts` so BOM-prefixed `latest.json` still parses cleanly. Register the route in `server.ts`, add tests, and rebuild the `dist` files the deployed host actually serves.</action>
  <verify>rg -n "POST_PHASE25_EXTERNAL_RESTORATION_STATE_PATH|postPhase25ExternalRestorationStatePath|/internal/post-phase25-external-restoration/latest|latest: null|ubuntuSshReachable|reverseTunnelTaskStatus|externalChatStatus|externally_ready|hold_rollout" services/control-api/src services/control-api/dist services/control-api/test</verify>
  <acceptance_criteria>
    - `services/control-api/src/routes/internal-post-phase25-external-restoration.ts` exists
    - `config.ts` supports `POST_PHASE25_EXTERNAL_RESTORATION_STATE_PATH`
    - `server.ts` registers `/internal/post-phase25-external-restoration/latest`
    - tests prove the route returns `{ latest: null }` when no file exists
    - the route reuses BOM-safe parsing
    - `dist` contains the same route and config contract after build
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Render the latest post-Phase-25 external-restoration result on the internal admin page</name>
  <files>services/control-api/src/routes/internal-admin-page.ts, services/control-api/dist/routes/internal-admin-page.js, services/control-api/test/internal-admin-page.test.ts</files>
  <read_first>services/control-api/src/routes/internal-admin-page.ts, services/control-api/test/internal-admin-page.test.ts, services/control-api/src/routes/internal-post-phase25-external-restoration.ts</read_first>
  <action>Add a dedicated `Latest post-phase25 external restoration` section to `/internal/admin`. Make the page fetch `/internal/post-phase25-external-restoration/latest`, render `No post-phase25 external restoration result captured yet.` when there is no file, and render the latest timestamp, Ubuntu SSH truth, canonical upstream truth, reverse-tunnel task state, tunnel-listener truth, ready-worker count, external `healthz`, `models`, and `chat` status, external model used, compatibility version, and verdict when the file exists. Keep the existing sections intact and rebuild the `dist` admin page runtime.</action>
  <verify>rg -n "Latest post-phase25 external restoration|No post-phase25 external restoration result captured yet.|/internal/post-phase25-external-restoration/latest|Ubuntu SSH|canonical upstream|reverse-tunnel task|tunnel listener|ready worker count|external chat|Compatibility|verdict" services/control-api/src/routes/internal-admin-page.ts services/control-api/dist/routes/internal-admin-page.js services/control-api/test/internal-admin-page.test.ts</verify>
  <acceptance_criteria>
    - the admin page contains `Latest post-phase25 external restoration`
    - the admin page fetches `/internal/post-phase25-external-restoration/latest`
    - the admin page has the exact empty-state text
    - the populated state renders Ubuntu SSH truth, canonical upstream truth, reverse-tunnel task state, tunnel-listener truth, ready-worker count, external `healthz`, `models`, and `chat` status, model used, compatibility version, and verdict
    - the existing sections remain present
  </acceptance_criteria>
</task>

</tasks>

<success_criteria>

- the latest Phase 26 restoration result is visible from the same runtime files the deployed host actually serves
- operators can confirm Ubuntu sync, tunnel retention, and public smoke truth without reconstructing it from raw terminal output

</success_criteria>

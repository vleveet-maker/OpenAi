---
phase: 36-server-bearer-token-and-ubuntu-ssh-listener-recovery-before-isolated-external-chat-revalidation
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - services/control-api/src/config.ts
  - services/control-api/src/server.ts
  - services/control-api/src/routes/internal-post-phase35-server-token-ssh-listener-recovery.ts
  - services/control-api/src/routes/internal-admin-page.ts
  - services/control-api/test/internal-post-phase35-server-token-ssh-listener-recovery.test.ts
  - services/control-api/test/internal-admin-page.test.ts
autonomous: true
requirements: [SBTU-03]
user_setup: []
---

# Plan 36-02

## Goal

Expose the Phase 36 token/SSH/listener recovery truth through a latest route and `/internal/admin`.

## Tasks

<task id="36-02-01">
<name>Add config-backed latest-state wiring.</name>
<objective>Add config-backed latest-state wiring.</objective>
<files>
- services/control-api/src/config.ts
</files>
<action>
Add `postPhase35ServerTokenSshListenerRecoveryStatePath` pointing at `infra/data/post-phase35-server-token-ssh-listener-recovery/latest.json`.
</action>
<acceptance_criteria>
- config exposes the new state path
- default path resolves under `infra/data/post-phase35-server-token-ssh-listener-recovery/latest.json`
</acceptance_criteria>
</task>

<task id="36-02-02">
<name>Add the Phase 36 latest route and admin section.</name>
<objective>Add the Phase 36 latest route and admin section.</objective>
<files>
- services/control-api/src/routes/internal-post-phase35-server-token-ssh-listener-recovery.ts
- services/control-api/src/server.ts
- services/control-api/src/routes/internal-admin-page.ts
</files>
<action>
Add `GET /internal/post-phase35-server-token-ssh-listener-recovery/latest` and render a new admin section titled `Latest post-phase35 server token SSH listener recovery`. The section must show token-source status without secret values, Ubuntu SSH truth, listener truth, external `healthz/models/chat` status, and final verdict.
</action>
<acceptance_criteria>
- route returns `{ latest }` from the file-backed artifact
- admin page includes the Phase 36 section
- admin page never renders token or password values
- admin page makes `token_missing`, `ssh_unreachable`, and `listeners_missing` visually distinct from browser/chat failures
</acceptance_criteria>
</task>

<task id="36-02-03">
<name>Add focused tests for route and admin visibility.</name>
<objective>Add focused tests for route and admin visibility.</objective>
<files>
- services/control-api/test/internal-post-phase35-server-token-ssh-listener-recovery.test.ts
- services/control-api/test/internal-admin-page.test.ts
</files>
<action>
Add focused tests proving the latest route returns the expected artifact, missing artifact behavior stays safe, and `/internal/admin` includes the new Phase 36 section and route reference.
</action>
<acceptance_criteria>
- focused tests pass
- full `control-api` suite passes
- `npm.cmd --prefix services/control-api run build` passes
</acceptance_criteria>
</task>

## Requirements

- `SBTU-03`

## Verification

- `npm.cmd --prefix services/control-api test -- internal-post-phase35-server-token-ssh-listener-recovery.test.ts internal-admin-page.test.ts`
- `npm.cmd --prefix services/control-api test`
- `npm.cmd --prefix services/control-api run build`

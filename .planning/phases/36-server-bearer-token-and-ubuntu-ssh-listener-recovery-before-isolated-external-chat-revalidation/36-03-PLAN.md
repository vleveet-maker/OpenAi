---
phase: 36-server-bearer-token-and-ubuntu-ssh-listener-recovery-before-isolated-external-chat-revalidation
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - .planning/phases/36-server-bearer-token-and-ubuntu-ssh-listener-recovery-before-isolated-external-chat-revalidation/36-03-SUMMARY.md
  - .planning/phases/36-server-bearer-token-and-ubuntu-ssh-listener-recovery-before-isolated-external-chat-revalidation/36-VERIFICATION.md
  - .planning/phases/36-server-bearer-token-and-ubuntu-ssh-listener-recovery-before-isolated-external-chat-revalidation/36-TOKEN-SSH-LISTENER-RECOVERY-SUMMARY.json
  - .planning/phases/36-server-bearer-token-and-ubuntu-ssh-listener-recovery-before-isolated-external-chat-revalidation/36-TOKEN-SSH-LISTENER-RECOVERY-SUMMARY.md
  - infra/data/post-phase35-server-token-ssh-listener-recovery/latest.json
autonomous: true
requirements: [SBTU-01, SBTU-02, SBTU-04]
user_setup:
  - Use execution-time secrets only if needed. Do not write bearer tokens or SSH passwords into repo files or artifacts.
---

# Plan 36-03

## Goal

Run the bounded recovery and rerun the isolated external chat proof only when token, SSH, and listener truth are good enough.

## Tasks

<task id="36-03-01">
<name>Run the Phase 36 recovery wrapper and record preconditions.</name>
<objective>Run the Phase 36 recovery wrapper and record preconditions.</objective>
<action>
Run the Phase 36 wrapper on the current/server path. Record bearer-token source status, Ubuntu SSH reachability, Ubuntu repo path/hash, nginx canonical upstream truth, reverse-tunnel task state, Ubuntu listener truth for `14021..14027` and `14040`, and public `/healthz` truth. If token, SSH, or listeners remain blocked, stop with `hold_rollout` and do not pretend authenticated chat was tested.
</action>
<acceptance_criteria>
- token truth is recorded without storing the token value
- Ubuntu SSH truth is recorded per host/port
- listener truth is recorded from Ubuntu
- public `/healthz` is recorded
- stop reason is exact if preconditions remain red
</acceptance_criteria>
</task>

<task id="36-03-02">
<name>Rerun authenticated external isolated chat revalidation when preconditions are green.</name>
<objective>Rerun authenticated external isolated chat revalidation when preconditions are green.</objective>
<action>
When the wrapper proves a token source plus Ubuntu SSH/listeners, rerun the isolated external chat revalidation against `http://77.66.186.75`. Prefer the already-proven isolated server model and stop on the first real `/v1/chat/completions = 200` with expected `probe-ok`. If the proof is blocked by the Phase 35 `robocopy exit code 11` branch or another exact server-side blocker, record that blocker and end `hold_rollout`.
</action>
<acceptance_criteria>
- external `/v1/models` is tested only with a valid bearer token
- external `/v1/chat/completions` is tested only after token plus listener truth
- final verdict is exactly `externally_ready` or `hold_rollout`
- if `robocopy exit code 11` is the next blocker, it is recorded as server-transfer debt rather than browser architecture debt
</acceptance_criteria>
</task>

<task id="36-03-03">
<name>Close Phase 36 with one honest verdict and next blocker.</name>
<objective>Close Phase 36 with one honest verdict and next blocker.</objective>
<action>
Write `36-03-SUMMARY.md`, `36-VERIFICATION.md`, `36-TOKEN-SSH-LISTENER-RECOVERY-SUMMARY.json`, `36-TOKEN-SSH-LISTENER-RECOVERY-SUMMARY.md`, and update the latest artifact. Confirm the internal route and admin section after local `control-api` restart. Update GSD state with the final verdict and the next recommended step.
</action>
<acceptance_criteria>
- summary and verification files exist
- latest JSON and Markdown artifacts exist
- route and admin section match latest artifact
- preserve-first guarantees are explicitly recorded
- next step is written plainly, including whether to proceed to server transfer copy repair or rollout readiness
</acceptance_criteria>
</task>

## Requirements

- `SBTU-01`
- `SBTU-02`
- `SBTU-04`

## Verification

- Live recovery artifact review
- `GET /internal/post-phase35-server-token-ssh-listener-recovery/latest`
- `/internal/admin` contains `Latest post-phase35 server token SSH listener recovery`
- final external smoke against `http://77.66.186.75`

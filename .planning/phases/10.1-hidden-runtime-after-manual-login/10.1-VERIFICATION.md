---
phase: 10.1-hidden-runtime-after-manual-login
verified: 2026-03-28T18:10:15+03:00
status: complete
score: 3/3 must-haves verified
---

# Phase 10.1: Hidden Runtime After Manual Login Verification Report

Phase goal: split host-native workers into explicit visible-auth and hidden-runtime paths so manual ChatGPT auth stays deliberate, but routine household use no longer opens browser windows on the operator desktop.

Verified: 2026-03-28T18:10:15+03:00
Status: complete

## Goal Achievement

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Host-native workers now have a real visible-auth versus hidden-runtime split, and hidden runtime is the default pool path. | x VERIFIED | `workers/agent/src/browser-launch.ts`, `workers/agent/src/server.ts`, `infra/host-worker/start-host-native-worker.ps1`, `services/host-controller/src/host-controller.mjs`, `services/host-controller/test/host-controller.test.mjs` |
| 2 | Control-api exposes explicit manual-auth transitions, runtime-mode telemetry, and operator UI wording for visible auth and hidden runtime without silent window reopening. | x VERIFIED | `services/control-api/src/routes/internal-worker-actions.ts`, `services/control-api/src/routes/internal-workers.ts`, `services/control-api/src/workers/worker-health-monitor.ts`, `services/control-api/src/routes/internal-admin-page.ts`, `services/control-api/test/internal-worker-actions.test.ts`, `services/control-api/test/internal-admin-page.test.ts` |
| 3 | There is now one canonical internal-only validation path and doc trail for visible login followed by hidden relay, including the architecture-review escalation rule. | x VERIFIED | `infra/host-worker/test-hidden-runtime-transition.ps1`, `docs/host-native-worker.md`, `docs/internal-worker-ops.md` |

Score: 3/3 truths verified

## Automated Checks

| Command | Status |
|---------|--------|
| `cmd /c npm.cmd test --prefix workers/agent` | x PASSED |
| `cmd /c npm.cmd run build --prefix workers/agent` | x PASSED |
| `cmd /c npm.cmd test --prefix services/host-controller` | x PASSED |
| `cmd /c npm.cmd test --prefix services/control-api` | x PASSED |
| `cmd /c npm.cmd run build --prefix services/control-api` | x PASSED |
| PowerShell parser check for `infra/host-worker/test-hidden-runtime-transition.ps1` | x PASSED |

## Live Checks

| Probe | Result |
|-------|--------|
| `powershell -ExecutionPolicy Bypass -File .\\infra\\host-worker\\test-hidden-runtime-transition.ps1 -WorkerId <worker>` | ~ READY - probe created for next rollout-confidence pass |

## Requirements Coverage

| Requirement | Status |
|-------------|--------|
| HIDE-01: Operator can intentionally start one host worker in visible interactive auth mode and then switch that same durable profile back into hidden runtime | x VERIFIED by route coverage and internal admin controls |
| HIDE-02: Pool start and routine household relay use hidden runtime by default and do not open visible browser windows on the operator desktop | x VERIFIED by hidden-runtime default start path plus operator docs and probe gate |
| HIDE-03: Hidden auth loss surfaces manual reauth plus reliability-review status instead of silently reopening windows | x VERIFIED by worker health events, admin copy, and probe escalation string |

## Residual Risk

- This phase is complete as code, tests, operator surface, and validation tooling, but the new hidden-runtime probe still needs to be run on a real logged-in worker as part of Phase 11 rollout confidence.
- `dad` remains the main real-world tail from Phase 10, and the new probe exists specifically to tell whether that remaining issue is now resolved or still points to runtime-architecture weakness.

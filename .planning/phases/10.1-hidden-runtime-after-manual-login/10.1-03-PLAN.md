---
phase: 10.1-hidden-runtime-after-manual-login
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - infra/host-worker/test-host-worker-relay.ps1
  - infra/host-worker/test-hidden-runtime-transition.ps1
  - docs/host-native-worker.md
  - docs/internal-worker-ops.md
autonomous: true
requirements: [HIDE-01, HIDE-02, HIDE-03]
user_setup: []
must_haves:
    truths:
      - "There is a repeatable internal-only proof that a worker can move from visible manual auth to hidden runtime and still relay successfully."
      - "The validation path explicitly fails when hidden runtime falls back into auth loss after manual login."
      - "Operator docs describe the visible-auth then hidden-runtime workflow and the architecture-review rule in exact terms."
    artifacts:
      - path: "infra/host-worker/test-hidden-runtime-transition.ps1"
        provides: "operator and maintainer smoke path for visible-auth to hidden-runtime transition"
        contains: "hidden_runtime_auth_unstable"
      - path: "docs/host-native-worker.md"
        provides: "runtime split and hidden-default pool operator runbook"
        contains: "Complete login -> hidden"
      - path: "docs/internal-worker-ops.md"
        provides: "auth-loss handling and architecture-review guidance"
        contains: "architecture review required"
    key_links:
      - from: "infra/host-worker/test-hidden-runtime-transition.ps1"
        to: "infra/host-worker/test-host-worker-relay.ps1"
        via: "transition probe reuses relay smoke after asserting hidden runtime state"
        pattern: "TimeoutSeconds|WorkerId|hidden_runtime_auth_unstable"
      - from: "docs/host-native-worker.md"
        to: "services/control-api/src/routes/internal-admin-page.ts"
        via: "docs mirror the exact operator buttons and hidden-runtime semantics"
        pattern: "Start visible login|Complete login -> hidden"
---

<objective>
Finish the phase with a repeatable hidden-runtime validation path and operator docs that make the new rules unambiguous before the milestone moves on to rollout smoke confidence.
</objective>

<execution_context>
@../gsd/get-shit-done/workflows/execute-plan.md
@../gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10.1-hidden-runtime-after-manual-login/10.1-CONTEXT.md
@.planning/phases/10.1-hidden-runtime-after-manual-login/10.1-RESEARCH.md
@infra/host-worker/test-host-worker-relay.ps1
@docs/host-native-worker.md
@docs/internal-worker-ops.md
@services/control-api/src/routes/internal-admin-page.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a dedicated visible-auth to hidden-runtime verification script</name>
  <files>infra/host-worker/test-host-worker-relay.ps1, infra/host-worker/test-hidden-runtime-transition.ps1</files>
  <read_first>infra/host-worker/test-host-worker-relay.ps1, infra/host-worker/start-host-native-worker.ps1, services/control-api/src/routes/internal-worker-actions.ts, services/control-api/src/routes/internal-workers.ts</read_first>
  <action>Create `infra/host-worker/test-hidden-runtime-transition.ps1` as the maintainer-facing live probe for this phase. It must accept `-WorkerId` and `-TimeoutSeconds`, verify the worker health payload reports `runtimeMode` exactly `hidden_runtime`, verify the worker CDP port is not listening, then call or reuse `test-host-worker-relay.ps1` to perform a real bootstrap plus relay smoke. If worker health or relay shows `reauth_required` or `bootstrap_auth_required` while runtimeMode is hidden, the script must exit non-zero with an exact error string containing `hidden_runtime_auth_unstable`. Extend `test-host-worker-relay.ps1` only as needed so the transition probe can reuse it cleanly without duplicating session-relay logic.</action>
  <verify>rg -n "hidden_runtime_auth_unstable|runtimeMode|bootstrap_auth_required|reauth_required|TimeoutSeconds|WorkerId" infra/host-worker/test-hidden-runtime-transition.ps1 infra/host-worker/test-host-worker-relay.ps1</verify>
  <acceptance_criteria>
    - infra/host-worker/test-hidden-runtime-transition.ps1 contains "hidden_runtime_auth_unstable"
    - infra/host-worker/test-hidden-runtime-transition.ps1 contains "runtimeMode"
    - infra/host-worker/test-hidden-runtime-transition.ps1 contains "bootstrap_auth_required"
    - infra/host-worker/test-hidden-runtime-transition.ps1 contains "reauth_required"
    - infra/host-worker/test-host-worker-relay.ps1 contains "TimeoutSeconds"
  </acceptance_criteria>
  <done>The phase now has one concrete probe that proves the new hidden-runtime contract instead of relying on ad hoc manual intuition.</done>
</task>

<task type="auto">
  <name>Task 2: Update host-native and internal-ops docs for the new runtime policy</name>
  <files>docs/host-native-worker.md, docs/internal-worker-ops.md</files>
  <read_first>docs/host-native-worker.md, docs/internal-worker-ops.md, .planning/phases/10.1-hidden-runtime-after-manual-login/10.1-CONTEXT.md, services/control-api/src/routes/internal-admin-page.ts</read_first>
  <action>Update `docs/host-native-worker.md` so the operator flow is written exactly as: `Start pool` uses hidden runtime only, `Start visible login` is used only for first login or reauth, and `Complete login -> hidden` returns the worker to routine hidden operation. Update `docs/internal-worker-ops.md` so it states explicitly that minimized windows are no longer the accepted steady-state behavior, that hidden auth loss after manual login should be treated as `architecture review required`, and that the canonical validation path is `test-hidden-runtime-transition.ps1`.</action>
  <verify>rg -n "Start pool|hidden runtime only|Start visible login|Complete login -> hidden|minimized windows|architecture review required|test-hidden-runtime-transition.ps1" docs/host-native-worker.md docs/internal-worker-ops.md</verify>
  <acceptance_criteria>
    - docs/host-native-worker.md contains "Start visible login"
    - docs/host-native-worker.md contains "Complete login -> hidden"
    - docs/host-native-worker.md contains "hidden runtime only"
    - docs/internal-worker-ops.md contains "architecture review required"
    - docs/internal-worker-ops.md contains "test-hidden-runtime-transition.ps1"
  </acceptance_criteria>
  <done>The operator docs now encode the exact runtime contract instead of leaving it as team memory.</done>
</task>

<task type="auto">
  <name>Task 3: Document the hidden-runtime completion gate for the milestone handoff</name>
  <files>docs/host-native-worker.md, docs/internal-worker-ops.md, infra/host-worker/test-hidden-runtime-transition.ps1</files>
  <read_first>docs/host-native-worker.md, docs/internal-worker-ops.md, infra/host-worker/test-hidden-runtime-transition.ps1, .planning/ROADMAP.md</read_first>
  <action>Make the completion gate explicit in the docs and probe output: the phase is not considered live-complete until at least one worker has been manually logged in visibly, switched to hidden runtime, and returned a relay smoke without any visible desktop browser. The docs and script output must also make clear that if this gate fails because hidden runtime loses auth, the correct next step is runtime architecture review rather than more silent mode-toggling.</action>
  <verify>rg -n "not considered live-complete|without any visible desktop browser|runtime architecture review|hidden runtime loses auth" docs/host-native-worker.md docs/internal-worker-ops.md infra/host-worker/test-hidden-runtime-transition.ps1</verify>
  <acceptance_criteria>
    - docs/host-native-worker.md contains "without any visible desktop browser"
    - docs/internal-worker-ops.md contains "runtime architecture review"
    - infra/host-worker/test-hidden-runtime-transition.ps1 contains "hidden runtime loses auth"
    - infra/host-worker/test-hidden-runtime-transition.ps1 contains "live-complete"
  </acceptance_criteria>
  <done>The milestone handoff now has a concrete hidden-runtime truth gate instead of vague confidence language.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] a dedicated transition probe exists and asserts hidden runtime explicitly
- [ ] docs describe the exact visible-auth to hidden flow and completion gate
- [ ] hidden auth instability is called out as architecture review, not just another retry
</verification>

<success_criteria>

- All tasks completed
- there is a repeatable proof for visible auth followed by hidden relay
- operator docs clearly encode the new runtime policy
- hidden auth instability has an explicit escalation rule

</success_criteria>

<output>
After completion, create the matching SUMMARY document in this phase directory.
</output>

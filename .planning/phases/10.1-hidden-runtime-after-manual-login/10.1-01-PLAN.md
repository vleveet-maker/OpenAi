---
phase: 10.1-hidden-runtime-after-manual-login
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workers/agent/src/browser-launch.ts
  - workers/agent/src/server.ts
  - workers/agent/test/worker-runtime-mode.test.ts
  - infra/host-worker/start-host-native-worker.ps1
  - infra/host-worker/start-host-worker-agent.ps1
  - infra/host-worker/stop-host-native-worker.ps1
  - services/host-controller/src/config.mjs
  - services/host-controller/src/host-controller.mjs
  - services/host-controller/src/server.mjs
  - services/host-controller/test/host-controller.test.mjs
  - services/host-controller/test/server.test.mjs
autonomous: true
requirements: [HIDE-01, HIDE-02]
user_setup: []
must_haves:
    truths:
      - "A host worker can run in exactly one of two explicit modes: visible_auth or hidden_runtime."
      - "Hidden runtime launches through the worker-agent itself with headless execution and the same durable profile, not through a minimized desktop browser window."
      - "Pool start requests hidden runtime by default and never opens visible browser windows during routine household use."
    artifacts:
      - path: "workers/agent/src/server.ts"
        provides: "runtime mode, headless, and proxy-aware worker config plus health payloads"
        contains: "runtimeMode"
      - path: "infra/host-worker/start-host-native-worker.ps1"
        provides: "mode-aware host worker bootstrap for visible auth or hidden runtime"
        contains: "RuntimeMode"
      - path: "services/host-controller/src/host-controller.mjs"
        provides: "mode-aware worker and pool start orchestration"
        contains: "hidden_runtime"
    key_links:
      - from: "infra/host-worker/start-host-native-worker.ps1"
        to: "workers/agent/src/server.ts"
        via: "script populates env vars that drive visible_auth versus hidden_runtime behavior"
        pattern: "WORKER_RUNTIME_MODE|WORKER_HEADLESS|WORKER_CDP_ENDPOINT_URL|WORKER_PROXY_SERVER"
      - from: "services/host-controller/src/host-controller.mjs"
        to: "infra/host-worker/start-host-native-worker.ps1"
        via: "host-controller passes runtime mode into per-worker startup"
        pattern: "runtimeMode|startPool|startWorker"
---

<objective>
Split the current host-native launch path into an explicit visible-auth mode and a real hidden-runtime mode before any control-plane or admin changes build on top of it.
</objective>

<execution_context>
@../gsd/get-shit-done/workflows/execute-plan.md
@../gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10.1-hidden-runtime-after-manual-login/10.1-CONTEXT.md
@.planning/phases/10.1-hidden-runtime-after-manual-login/10.1-RESEARCH.md
@infra/host-worker/start-host-native-worker.ps1
@infra/host-worker/start-host-worker-agent.ps1
@infra/host-worker/stop-host-native-worker.ps1
@services/host-controller/src/config.mjs
@services/host-controller/src/host-controller.mjs
@services/host-controller/src/server.mjs
@workers/agent/src/browser-launch.ts
@workers/agent/src/server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add an explicit worker runtime-mode contract and proxy-aware hidden launch path</name>
  <files>workers/agent/src/browser-launch.ts, workers/agent/src/server.ts, workers/agent/test/worker-runtime-mode.test.ts</files>
  <read_first>workers/agent/src/browser-launch.ts, workers/agent/src/server.ts, infra/host-worker/start-host-native-worker.ps1, .planning/phases/10.1-hidden-runtime-after-manual-login/10.1-RESEARCH.md</read_first>
  <action>Extend the worker-agent config and health contract so it exposes exact runtime mode values `visible_auth` and `hidden_runtime`. Add env parsing for `WORKER_RUNTIME_MODE` and `WORKER_PROXY_SERVER`. In `workers/agent/src/browser-launch.ts`, keep CDP attach for `visible_auth`, but for `hidden_runtime` require no `WORKER_CDP_ENDPOINT_URL`, launch the persistent context directly, set `headless: true`, and add the same mixed proxy through a concrete Chromium launch arg `--proxy-server=<WORKER_PROXY_SERVER>` when that env var is present. Update `/health` and `/internal/worker` payloads so they include `runtimeMode`, `headless`, `cdpAttached`, and `proxyServerConfigured`. Create `workers/agent/test/worker-runtime-mode.test.ts` that verifies visible_auth prefers CDP attach and hidden_runtime prefers persistent launch with headless true and proxy propagation.</action>
  <verify>rg -n "WORKER_RUNTIME_MODE|WORKER_PROXY_SERVER|visible_auth|hidden_runtime|proxyServerConfigured|cdpAttached|headless" workers/agent/src/browser-launch.ts workers/agent/src/server.ts workers/agent/test/worker-runtime-mode.test.ts</verify>
  <acceptance_criteria>
    - workers/agent/src/server.ts contains "visible_auth"
    - workers/agent/src/server.ts contains "hidden_runtime"
    - workers/agent/src/server.ts contains "WORKER_PROXY_SERVER"
    - workers/agent/src/browser-launch.ts contains "--proxy-server="
    - workers/agent/src/browser-launch.ts contains "headless: true"
    - workers/agent/test/worker-runtime-mode.test.ts contains "hidden_runtime"
  </acceptance_criteria>
  <done>The worker-agent can now distinguish deliberate visible auth from routine hidden execution and can launch the hidden browser itself with the same proxy path.</done>
</task>

<task type="auto">
  <name>Task 2: Make host scripts and host-controller mode-aware, with hidden runtime as the pool default</name>
  <files>infra/host-worker/start-host-native-worker.ps1, infra/host-worker/start-host-worker-agent.ps1, infra/host-worker/stop-host-native-worker.ps1, services/host-controller/src/config.mjs, services/host-controller/src/host-controller.mjs, services/host-controller/src/server.mjs</files>
  <read_first>infra/host-worker/start-host-native-worker.ps1, infra/host-worker/start-host-worker-agent.ps1, infra/host-worker/stop-host-native-worker.ps1, services/host-controller/src/config.mjs, services/host-controller/src/host-controller.mjs, services/host-controller/src/server.mjs</read_first>
  <action>Refactor the host worker scripts so `start-host-native-worker.ps1` accepts an exact `-RuntimeMode VisibleAuth|HiddenRuntime` parameter. `VisibleAuth` must keep the current branded browser launch plus `WORKER_CDP_ENDPOINT_URL`. `HiddenRuntime` must not start an external browser process, must clear `WORKER_CDP_ENDPOINT_URL`, and must pass `WORKER_RUNTIME_MODE=hidden_runtime`, `WORKER_HEADLESS=true`, and `WORKER_PROXY_SERVER=<mixed-proxy-url>` into `start-host-worker-agent.ps1`. Update `stop-host-native-worker.ps1` so it still shuts down either mode cleanly. In `services/host-controller`, extend worker and pool start contracts so `startPool()` always requests hidden_runtime for every worker, while `startWorker()` accepts a concrete runtimeMode argument and returns it in the result payload. Update `POST /workers/:id/start` and `POST /pool/start` to accept JSON bodies with `runtimeMode` when relevant and to return that field back to the caller.</action>
  <verify>rg -n "RuntimeMode|HiddenRuntime|VisibleAuth|WORKER_RUNTIME_MODE|WORKER_PROXY_SERVER|runtimeMode|hidden_runtime" infra/host-worker/start-host-native-worker.ps1 infra/host-worker/start-host-worker-agent.ps1 infra/host-worker/stop-host-native-worker.ps1 services/host-controller/src/config.mjs services/host-controller/src/host-controller.mjs services/host-controller/src/server.mjs</verify>
  <acceptance_criteria>
    - infra/host-worker/start-host-native-worker.ps1 contains "RuntimeMode"
    - infra/host-worker/start-host-native-worker.ps1 contains "HiddenRuntime"
    - infra/host-worker/start-host-native-worker.ps1 contains "VisibleAuth"
    - infra/host-worker/start-host-worker-agent.ps1 contains "WORKER_RUNTIME_MODE"
    - services/host-controller/src/host-controller.mjs contains "hidden_runtime"
    - services/host-controller/src/server.mjs contains "runtimeMode"
  </acceptance_criteria>
  <done>The host-controller and scripts now have a real runtime split instead of always opening a desktop browser and calling it "host-native".</done>
</task>

<task type="auto">
  <name>Task 3: Add automated coverage for runtime-mode startup and hidden-default pool behavior</name>
  <files>services/host-controller/test/host-controller.test.mjs, services/host-controller/test/server.test.mjs, workers/agent/test/worker-runtime-mode.test.ts</files>
  <read_first>services/host-controller/src/host-controller.mjs, services/host-controller/src/server.mjs, workers/agent/src/browser-launch.ts, workers/agent/src/server.ts, services/host-controller/test/host-controller.test.mjs, services/host-controller/test/server.test.mjs</read_first>
  <action>Extend `services/host-controller/test/host-controller.test.mjs` so it proves `startPool()` requests `hidden_runtime` for each configured worker and direct worker start can request `visible_auth`. Extend `services/host-controller/test/server.test.mjs` so `POST /pool/start` and `POST /workers/:id/start` return the chosen `runtimeMode`. Keep the worker-agent runtime-mode tests verifying that hidden runtime never depends on a CDP endpoint and visible auth never pretends to be hidden.</action>
  <verify>rg -n "hidden_runtime|visible_auth|runtimeMode|POST /pool/start|POST /workers/:id/start" services/host-controller/test/host-controller.test.mjs services/host-controller/test/server.test.mjs workers/agent/test/worker-runtime-mode.test.ts</verify>
  <acceptance_criteria>
    - services/host-controller/test/host-controller.test.mjs contains "hidden_runtime"
    - services/host-controller/test/host-controller.test.mjs contains "visible_auth"
    - services/host-controller/test/server.test.mjs contains "runtimeMode"
    - services/host-controller/test/server.test.mjs contains "POST /pool/start"
    - workers/agent/test/worker-runtime-mode.test.ts contains "cdp"
  </acceptance_criteria>
  <done>The runtime split is regression-tested before the control plane starts building operator flows on top of it.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] hidden runtime launches without a CDP endpoint and with headless true
- [ ] host-controller startPool defaults to hidden runtime
- [ ] visible auth remains available only by explicit mode request
</verification>

<success_criteria>

- All tasks completed
- the worker runtime has an explicit visible vs hidden mode split
- routine pool start no longer opens desktop browser windows by default
- the same durable profile can be reused sequentially across visible auth and hidden runtime

</success_criteria>

<output>
After completion, create the matching SUMMARY document in this phase directory.
</output>

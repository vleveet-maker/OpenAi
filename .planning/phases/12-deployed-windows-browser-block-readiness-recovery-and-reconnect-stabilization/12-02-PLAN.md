---
phase: 12-deployed-windows-browser-block-readiness-recovery-and-reconnect-stabilization
plan: 02
type: execute
wave: 2
depends_on: [12-01-PLAN.md]
files_modified:
  - services/control-api/src/
  - services/control-api/test/
autonomous: true
requirements: [WREC-01, WREC-03]
user_setup: []
---

<objective>
Expose the latest readiness-recovery result in an internal operator surface so recovery truth is not trapped in raw terminal output.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-deployed-windows-browser-block-readiness-recovery-and-reconnect-stabilization/12-RESEARCH.md
@.planning/phases/12-deployed-windows-browser-block-readiness-recovery-and-reconnect-stabilization/12-VALIDATION.md
@services/control-api/src/config.ts
@services/control-api/src/server.ts
@services/control-api/src/routes/internal-admin-page.ts
@services/control-api/src/routes/internal-rollout-smoke.ts
@services/control-api/src/routes/internal-workers.ts
@services/control-api/test/internal-admin-page.test.ts
@services/control-api/test/internal-rollout-smoke.test.ts
@services/control-api/test/internal-workers.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a file-backed latest readiness-recovery route to control-api</name>
  <files>services/control-api/src/config.ts, services/control-api/src/server.ts, services/control-api/src/routes/internal-readiness-recovery.ts, services/control-api/test/</files>
  <read_first>services/control-api/src/config.ts, services/control-api/src/server.ts, services/control-api/src/routes/internal-rollout-smoke.ts, services/control-api/test/internal-rollout-smoke.test.ts</read_first>
  <action>Add a new config field `readinessRecoveryStatePath` sourced from `READINESS_RECOVERY_STATE_PATH` with default repo path `infra/data/readiness-recovery/latest.json`. Create `services/control-api/src/routes/internal-readiness-recovery.ts` with `GET /internal/readiness-recovery/latest` that returns `{ latest: null }` when the file is missing and returns the parsed latest recovery payload when it exists. Register the route behind the internal admin guard in `server.ts`, and add tests that prove both the empty state and the populated state.</action>
  <verify>rg -n "READINESS_RECOVERY_STATE_PATH|/internal/readiness-recovery/latest|latest: null|readinessRecoveryStatePath" services/control-api/src services/control-api/test</verify>
  <acceptance_criteria>
    - `services/control-api/src/routes/internal-readiness-recovery.ts` exists
    - `config.ts` supports `READINESS_RECOVERY_STATE_PATH`
    - `server.ts` registers `/internal/readiness-recovery/latest`
    - tests prove the route returns `{ latest: null }` when no recovery result exists
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Render the latest readiness recovery on the internal admin page</name>
  <files>services/control-api/src/routes/internal-admin-page.ts, services/control-api/test/internal-admin-page.test.ts</files>
  <read_first>services/control-api/src/routes/internal-admin-page.ts, services/control-api/test/internal-admin-page.test.ts, services/control-api/src/routes/internal-readiness-recovery.ts</read_first>
  <action>Add a dedicated `Latest readiness recovery` section to `/internal/admin`. Make the page fetch `/internal/readiness-recovery/latest`, render an empty state `No readiness recovery captured yet.` when there is no file, and render the latest timestamp, total workers, ready workers, `reauth_required`, `reachable_but_unusable`, `disconnected`, recovered worker IDs, and held blocker summaries when the file exists. Keep the existing worker summary and latest rollout-smoke sections intact.</action>
  <verify>rg -n "Latest readiness recovery|No readiness recovery captured yet|/internal/readiness-recovery/latest|reauth_required|reachable_but_unusable|disconnected" services/control-api/src/routes/internal-admin-page.ts services/control-api/test/internal-admin-page.test.ts</verify>
  <acceptance_criteria>
    - the admin page contains the text `Latest readiness recovery`
    - the admin page fetches `/internal/readiness-recovery/latest`
    - the admin page has an explicit empty state `No readiness recovery captured yet.`
    - the existing worker summary and latest rollout-smoke sections remain present
  </acceptance_criteria>
</task>

</tasks>

<success_criteria>

- the latest readiness-recovery result is visible from an internal operator surface
- no operator has to rely on raw terminal output to understand the latest recovery truth

</success_criteria>

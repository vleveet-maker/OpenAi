---
phase: 10.5-alternate-desktop-navigation-rescue-and-auth-renewal
verified: 2026-03-29T06:55:00+03:00
status: partial
score: 2/3 phase truths verified
---

# Phase 10.5: Alternate Desktop Navigation Rescue And Auth Renewal Verification Report

Phase goal: rescue alternate-desktop one more time, wire validation into the control plane, and end with a written unblock-or-stay-blocked decision for Phase 11.

Verified: 2026-03-29T06:55:00+03:00
Status: partial

## Goal Achievement

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | At least one non-auth-blocked alternate-desktop worker can reach usable fresh-chat bootstrap or full `phase11Ready=true`. | x VERIFIED | `wife` reached `Temporary Chat`, selected `GPT-5.4 Thinking`, accepted a real prompt, and returned `smoke-ok` in alternate-desktop without a routine visible desktop browser. |
| 2 | `dad` can be renewed through visible auth and revalidated through the control plane instead of remaining only `bootstrap_auth_required`. | ~ PARTIAL | The control-plane path exists and now reports a clean `auth_required` result on `dad`, but a successful renewed proof was not completed in this phase. |
| 3 | Phase 10.5 ends with worker-by-worker written evidence that either yields a named passing worker or keeps Phase 11 blocked explicitly. | x VERIFIED | This report, `10.5-UNBLOCK-DECISION.md`, updated docs, and updated roadmap/state now name `wife` as the passing worker and preserve the remaining `dad/shared-1` tails explicitly. |

Score: 2/3 truths verified

## Live Proof

### wife

- primary role in bounded proof: navigation-rescue target
- final result: `alternate_desktop_usable`
- phase11Ready: `true`
- proofFailureClass: `null`
- bootstrapFailureCode: `null`
- bootstrapStep: `complete`
- live evidence:
  - orphaned validation session `bd2de47d-4396-4286-879d-d2d7ba91ebdc` on `wife` reached `chatBootstrap.status=ready`, `conversationMode=temporary`, `modelLabel=GPT-5.4 Thinking`, and `canSend=true`
  - a live message `Please reply with exactly: smoke-ok` returned assistant body `smoke-ok`
  - after session end, `/internal/workers` reported `wife.runtimeCapability=usable`, `lastBootstrapUsability=usable`, `lastBootstrapStep=complete`, and `lastRelayFailureCode=null`

### dad

- primary role in bounded proof: auth-renewal target
- final result: `alternate_desktop_reachable_but_unusable`
- phase11Ready: `false`
- proofFailureClass: `auth_required`
- bootstrapFailureCode: `bootstrap_auth_required`
- bootstrapStep: `auth_check`
- live evidence:
  - `POST /internal/workers/dad/validate-runtime` returned `202`
  - validation payload classified the runtime as `auth_required`
  - worker truth now shows `status=reauth_required`, `lastBootstrapFailureCode=bootstrap_auth_required`, `lastBootstrapUsability=auth_required`

### shared-1

- primary role in bounded proof: proof-noise cleanup target
- final result: `alternate_desktop_reachable_but_unusable`
- phase11Ready: `false`
- proofFailureClass: `assignment_timeout`
- bootstrapFailureCode: `null`
- bootstrapStep: `null`
- live evidence:
  - direct host-controller validation returned `assignment_timeout`
  - runtime stayed reachable on alternate desktop, but the proof did not complete because the targeted session never resolved cleanly onto `shared-1`

## Automated Checks

| Command | Status |
|---------|--------|
| `cmd /c npm.cmd test --prefix workers/agent` | x PASSED |
| `cmd /c npm.cmd test --prefix services/host-controller` | x PASSED |
| `cmd /c npm.cmd test --prefix services/control-api` | x PASSED |
| `cmd /c npm.cmd run build --prefix services/control-api` | x PASSED |
| `cmd /c npm.cmd test --prefix services/control-api -- edge-config.test.ts` | x PASSED |

## Requirements Coverage

| Requirement | Status |
|-------------|--------|
| ADNR-01 | x VERIFIED - `wife` now reaches usable alternate-desktop fresh chat and real relay success |
| ADNR-02 | ~ PARTIAL - explicit control-plane renewal exists, but `dad` still needs a successful renewed proof |
| ADNR-03 | x VERIFIED - the phase ends with written worker-by-worker evidence and an explicit Phase 11 decision |

## Residual Risk

- `dad` still needs a successful visible reauth followed by `Validate non-visible runtime`.
- `shared-1` still has assignment-timeout proof noise, so it should not yet be treated as interchangeable with `wife`.
- The current passing worker is `wife`, not the full household pool.

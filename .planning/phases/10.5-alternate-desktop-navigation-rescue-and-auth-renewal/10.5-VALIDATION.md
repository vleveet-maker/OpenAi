---
phase: 10.5
slug: alternate-desktop-navigation-rescue-and-auth-renewal
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-29
---

# Phase 10.5 - Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `node --test` and `vitest` |
| **Config file** | `services/host-controller/package.json`, `services/control-api/package.json`, `workers/agent/package.json` |
| **Quick run command** | `cmd /c npm.cmd test --prefix services/host-controller && cmd /c npm.cmd test --prefix services/control-api && cmd /c npm.cmd test --prefix workers/agent` |
| **Full suite command** | `cmd /c npm.cmd test --prefix services/host-controller && cmd /c npm.cmd test --prefix services/control-api && cmd /c npm.cmd run build --prefix services/control-api && cmd /c npm.cmd test --prefix workers/agent && cmd /c npm.cmd run build --prefix workers/agent` |
| **Estimated runtime** | ~180 seconds |

---

## Sampling Rate

- **After every task commit:** run the smallest affected package test command
- **After every plan wave:** run the full suite command
- **Before `$gsd-verify-work`:** full suite must be green
- **Max feedback latency:** 180 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 10.5-01-* | 01 | 1 | ADNR-01 | unit | `cmd /c npm.cmd test --prefix workers/agent` | partial | pending |
| 10.5-02-* | 02 | 2 | ADNR-02 | unit/integration | `cmd /c npm.cmd test --prefix services/host-controller && cmd /c npm.cmd test --prefix services/control-api` | partial | pending |
| 10.5-03-* | 03 | 3 | ADNR-01, ADNR-02, ADNR-03 | docs + live proof | `powershell -ExecutionPolicy Bypass -File .\\infra\\host-worker\\test-alternate-desktop-runtime.ps1 -WorkerId wife -ReturnJson` | yes | pending |

*Status: pending / green / red / flaky*

---

## Wave 0 Requirements

- [ ] Add host-controller coverage for alternate-desktop validation endpoint and response shape
- [ ] Add control-api coverage for validation proxy route and worker-status update behavior
- [ ] Add worker-agent tests for navigation rescue branches and branch-specific `stepDetail`

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Confirm `dad` can be renewed through visible auth and then validated back in `alternate_desktop` | ADNR-02 | Requires real ChatGPT auth and real Windows desktop behavior | 1. Use `Start visible reauth` for `dad`. 2. Complete login manually. 3. Use `Complete login -> non-visible runtime`. 4. Run the new validate action. 5. Confirm the worker no longer remains only as `bootstrap_auth_required`. |
| Confirm `wife` reaches usable fresh-chat bootstrap in alternate desktop | ADNR-01 | Current failure is live ChatGPT navigation/bootstrap behavior | 1. Run the canonical alternate-desktop probe or new validate action for `wife`. 2. Confirm `bootstrap_navigation_failed` disappears. 3. Verify the proof reaches usable bootstrap or full `phase11Ready=true`. |
| Confirm Phase 11 can resume only from named passing evidence | ADNR-03 | This is a milestone gate, not just a unit assertion | 1. Review final proof results for `wife`, `dad`, and `shared-1`. 2. Verify roadmap/state only unblock when one worker is named as passing. |

---

## Validation Sign-Off

- [ ] All tasks have automated verify or explicit manual gate coverage
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all missing references
- [ ] No watch-mode flags
- [ ] Feedback latency < 180s for automated checks
- [ ] `nyquist_compliant: true` set in frontmatter once plan-specific task rows are finalized

**Approval:** pending

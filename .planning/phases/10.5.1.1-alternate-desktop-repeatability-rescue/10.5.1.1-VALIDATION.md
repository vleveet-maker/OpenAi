---
phase: 10.5.1.1
slug: alternate-desktop-repeatability-rescue
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-29
---

# Phase 10.5.1.1 - Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `node --test` and `vitest` |
| **Config file** | `services/host-controller/package.json`, `services/control-api/package.json`, `workers/agent/package.json` |
| **Quick run command** | `cmd /c npm.cmd test --prefix workers/agent && cmd /c npm.cmd test --prefix services/control-api` |
| **Full suite command** | `cmd /c npm.cmd test --prefix workers/agent && cmd /c npm.cmd run build --prefix workers/agent && cmd /c npm.cmd test --prefix services/control-api && cmd /c npm.cmd run build --prefix services/control-api && cmd /c npm.cmd test --prefix services/host-controller` |
| **Estimated runtime** | ~300 seconds |

---

## Sampling Rate

- **After every task commit:** run the smallest affected package test command
- **After every plan wave:** run the full suite command
- **Before `$gsd-verify-work`:** full suite must be green
- **Max feedback latency:** 300 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 10.5.1.1-01-* | 01 | 1 | ADRR-01 | unit | `cmd /c npm.cmd test --prefix workers/agent` | partial | pending |
| 10.5.1.1-02-* | 02 | 2 | ADNR-02 | route/admin | `cmd /c npm.cmd test --prefix services/control-api` | partial | pending |
| 10.5.1.1-03-* | 03 | 3 | ADRR-01, ADNR-02, RSG-02 | docs + live repeated proof | `powershell -ExecutionPolicy Bypass -File .\\infra\\host-worker\\test-alternate-desktop-runtime.ps1 -WorkerId wife -ReturnJson` | yes | pending |

*Status: pending / green / red / flaky*

---

## Wave 0 Requirements

- [ ] Add worker-agent coverage for bounded navigation rescue branches and branch-level failure detail
- [ ] Add control-api coverage for `manual-auth/complete-and-validate`
- [ ] Keep repeated-proof artifacts tied to fresh rescue runs rather than historical pass counts

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Confirm `wife` or `shared-1` can pass rescue twice in a row | ADRR-01, RSG-02 | Requires real ChatGPT UI, real alternate-desktop runtime, and real relay | 1. Run the rescue proof on `wife`. 2. If it passes once, restart `wife` in alternate desktop and rerun. 3. If `wife` still fails navigation, repeat the same bounded sequence on `shared-1`. 4. Confirm one worker reaches `stable (2/2)` or record precise rescue-branch failure evidence. |
| Confirm `dad` can complete visible auth and immediately re-enter validation | ADNR-02 | Requires real visible login and real non-visible handoff | 1. Start visible reauth for `dad`. 2. Complete manual ChatGPT login. 3. Use the new internal action to complete login and validate. 4. Confirm the returned payload contains both the alternate-desktop restart and the validation result. |
| Confirm a fresh temporary profile changes the outcome only if durable profiles remain suspicious | ADRR-01, RSG-02 | Requires real login and should not destroy existing profiles | 1. Leave current durable profiles untouched. 2. Start one temporary fresh-profile diagnostic branch on the selected worker. 3. Complete manual login there. 4. Return the temporary profile to alternate desktop and validate it. 5. Record whether the fresh profile behaves differently from the durable profile. |
| Confirm Phase 11 only resumes from fresh repeated proof | RSG-02 | Milestone decision artifact, not just code | 1. Review the new verification and unblock decision files. 2. Confirm the worker named in the decision reached `stable (2/2)` during this phase, or that Phase 11 stayed blocked. |

---

## Validation Sign-Off

- [ ] All tasks have automated verify or explicit manual gate coverage
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all missing references
- [ ] No watch-mode flags
- [ ] Feedback latency < 300s for automated checks
- [ ] `nyquist_compliant: true` set in frontmatter once plan-specific task rows are finalized

**Approval:** pending

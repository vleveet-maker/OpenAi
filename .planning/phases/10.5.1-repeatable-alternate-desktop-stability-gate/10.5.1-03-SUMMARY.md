---
phase: 10.5.1-repeatable-alternate-desktop-stability-gate
plan: 03
subsystem: repeated-proof-and-decision
tags: [verification, rollout-gate, alternate-desktop, docs]
provides:
  - repeated proof on the primary worker
  - final gate-backed decision for Phase 11
  - operator docs aligned with provisional vs stable truth
key-files:
  modified:
    - docs/host-native-worker.md
    - docs/internal-worker-ops.md
    - .planning/phases/10.5.1-repeatable-alternate-desktop-stability-gate/10.5.1-VERIFICATION.md
    - .planning/phases/10.5.1-repeatable-alternate-desktop-stability-gate/10.5.1-UNBLOCK-DECISION.md
requirements-completed: []
completed: 2026-03-29
---

# Phase 10.5.1 Plan 03: Summary

The repeated proof was completed, and it kept Phase 11 blocked.

## Accomplishments

- Ran the primary repeatability proof on `wife` twice, with a controlled alternate-desktop restart between passes.
- Recorded current carry-forward evidence for `dad` and `shared-1`.
- Updated operator docs so `Validate non-visible runtime` now clearly means:
  - one pass = at most `provisional`
  - two consecutive passes = `stable`
  - any failure = reset to `unstable`
- Wrote the final decision artifact for this phase.

## Key Live Result

- `wife` did **not** reach `2/2`.
- First repeated proof attempt: `bootstrap_navigation_failed @ navigation`
- Controlled restart
- Second repeated proof attempt: `bootstrap_navigation_failed @ navigation`
- `dad`: `bootstrap_auth_required @ auth_check`
- `shared-1`: `bootstrap_navigation_failed @ navigation`

## Outcome

- Repeatability was **not** proven.
- No worker reached `stabilityGateStatus=stable`.
- `Phase 11` stays blocked.

## Verification

- Live `POST /internal/workers/wife/validate-runtime` twice with restart between passes
- Live `POST /internal/workers/dad/validate-runtime`
- Live `POST /internal/workers/shared-1/validate-runtime`
- `cmd /c npm.cmd test --prefix services/control-api`
- `cmd /c npm.cmd run build --prefix services/control-api`

## Residual Risk

- The project now has repeatable evidence, but that evidence is still negative. Another stabilization follow-up is required before rollout smoke work resumes.

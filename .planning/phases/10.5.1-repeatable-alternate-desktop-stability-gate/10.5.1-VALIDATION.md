---
phase: 10.5.1
slug: repeatable-alternate-desktop-stability-gate
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-29
---

# Phase 10.5.1 - Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `node --test` and `vitest` |
| **Config file** | `services/host-controller/package.json`, `services/control-api/package.json`, `workers/agent/package.json` |
| **Quick run command** | `cmd /c npm.cmd test --prefix services/host-controller && cmd /c npm.cmd test --prefix services/control-api` |
| **Full suite command** | `cmd /c npm.cmd test --prefix services/host-controller && cmd /c npm.cmd test --prefix services/control-api && cmd /c npm.cmd run build --prefix services/control-api && cmd /c npm.cmd test --prefix workers/agent && cmd /c npm.cmd run build --prefix workers/agent` |
| **Estimated runtime** | ~240 seconds |

---

## Sampling Rate

- **After every task commit:** run the smallest affected package test command
- **After every plan wave:** run the full suite command
- **Before `$gsd-verify-work`:** full suite must be green
- **Max feedback latency:** 240 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 10.5.1-01-* | 01 | 1 | RSG-01 | unit/integration | `cmd /c npm.cmd test --prefix services/control-api` | partial | pending |
| 10.5.1-02-* | 02 | 2 | RSG-02, RSG-03 | unit/integration | `cmd /c npm.cmd test --prefix services/control-api && cmd /c npm.cmd test --prefix services/host-controller` | partial | pending |
| 10.5.1-03-* | 03 | 3 | RSG-01, RSG-02, RSG-03 | docs + live proof | `powershell -ExecutionPolicy Bypass -File .\\infra\\host-worker\\test-alternate-desktop-runtime.ps1 -WorkerId wife -ReturnJson` | yes | pending |

*Status: pending / green / red / flaky*

---

## Wave 0 Requirements

- [ ] Add control-api coverage for deterministic worker-pinned validation-session creation or equivalent worker-directed proof path
- [ ] Add worker truth coverage for repeatability counters and reset semantics
- [ ] Add admin/UI coverage for provisional vs stable repeatability state

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Confirm `wife` can pass two consecutive non-visible validations | RSG-02 | Requires real ChatGPT UI, real alternate-desktop runtime, and real relay | 1. Run `Validate non-visible runtime` or the canonical proof for `wife`. 2. Confirm one pass. 3. Restart `wife` in alternate desktop. 4. Run the same validation again. 5. Confirm the gate reaches its required pass count without reset. |
| Confirm gate resets after failure | RSG-02 | Requires live failure or forced invalid state | 1. Produce or observe a failed validation on a worker that previously passed once. 2. Confirm repeatability count/status resets to unstable or zero. |
| Confirm Phase 11 stays blocked unless the repeatability gate is crossed | RSG-03 | Milestone decision artifact, not just code | 1. Review final verification and unblock decision. 2. Confirm roadmap/state only unblock Phase 11 when a named worker is marked repeatably stable. |

---

## Validation Sign-Off

- [ ] All tasks have automated verify or explicit manual gate coverage
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all missing references
- [ ] No watch-mode flags
- [ ] Feedback latency < 240s for automated checks
- [ ] `nyquist_compliant: true` set in frontmatter once plan-specific task rows are finalized

**Approval:** pending

# Phase 10.6.1.2.2.1.1.1 Validation

## Validation Focus

This phase is valid only if it closes the final public-edge ambiguity without endangering the preserved 9-account Windows Server block.

It is invalid if it:

- mass-starts or mass-restarts the 9 workers just to chase HTTPS
- silently changes the public client contract without writing it down
- breaks the already-working public HTTP edge and leaves no rollback record
- claims success from local loopback proof alone without one truly independent outside-client chat result
- opens `4040` or worker-agent ports publicly

## Required Evidence

- one artifact that records the exact Windows-host TLS/public-contract truth
- one preserve-first rollback-safe record if HTTPS reconciliation changes the edge
- one canary-only HTTPS or final-public-contract proof path on `shared-6`
- one fully independent outside-client `POST /v1/chat/completions` result
- one final verification artifact with verdict:
  - `safe_to_promote`
  - `safe_but_hold`
  - `hold_preserve_accounts`

## What Must Be True Before Closeout

- the already-working HTTP edge is either preserved or explicitly rolled back safely
- the project now knows whether `HTTPS` is real, blocked, or intentionally deferred for an exact reason
- `shared-6` remains the only required worker for chat proof unless the final contract no longer needs worker-backed proof
- the independent outside-client proof is recorded against the real public edge, not just loopback or same-host shortcuts
- the public client contract is explicit enough that `Phase 11` can resume or stay blocked on written proof instead of inference

## Recommended Checks

- run a direct Windows-host TLS and binding audit using the current edge tooling
- prove that `http://77.66.186.75/healthz` and `http://77.66.186.75/v1/models` remain healthy before any HTTPS change
- run bounded HTTPS activation or reconciliation through `Caddy`
- prove `https://.../healthz` and `https://.../v1/models` if HTTPS is meant to be real
- run one truly independent outside-client `POST /v1/chat/completions` on `shared-6`
- rollback immediately if the promoted public contract regresses

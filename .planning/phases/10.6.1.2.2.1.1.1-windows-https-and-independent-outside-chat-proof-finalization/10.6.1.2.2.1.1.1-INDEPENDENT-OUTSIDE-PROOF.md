# 10.6.1.2.2.1.1.1 Independent Outside Proof

- Phase: `10.6.1.2.2.1.1.1`
- Date: `2026-03-31`
- Source: preserve-first Windows-agent follow-up plus corroborating local checks
- Status: `blocked_before_chat`

## Short Report

A truly acceptable independent outside chat proof was not completed in this phase.

Simple explanation:

- the public edge was already broken earlier than chat
- `77.66.186.75` responded from Ubuntu `nginx` with `404`
- because `healthz` and `v1/models` already failed on the real public contract, there was no honest reason to claim a valid outside chat proof
- the blocker is now exact and narrow: `public_edge_only`

## Public Contract Under Test

- HTTP base URL: `http://77.66.186.75`
- HTTPS base URL: `https://77.66.186.75`

## Real Public-Edge Results

- `GET /healthz` on the real public path -> `404`
- `GET /v1/models` on the real public path -> `404`
- responder observed: `nginx/1.18.0 (Ubuntu)`

## HTTPS Corroboration

- `https://77.66.186.75` does not yield a usable Windows-side edge
- standard Windows TLS probe classified this as `tls_handshake_failed`
- external TLS checks still pointed at the Ubuntu-side responder, not the Windows `Caddy` edge

## Chat Proof Result

- truthful independent outside `POST /v1/chat/completions`: `not_run`
- reason:
  - the public edge was already invalid earlier on `healthz` and `v1/models`
  - running chat against that broken public path would not have been a valid proof of the Windows edge
- blocker class:
  - `public_edge_only`

## Local Corroboration

- if the request path is forced directly to the Windows host, the local Windows edge still responds correctly
- that means the blocker is upstream public-edge ownership/termination, not preserved browser accounts

## Preserve-First Status

- the 9 accounts remained preserved
- `shared-6` remained the only intended canary
- `shared-6` was left stopped again at the end
- no broad worker changes were made

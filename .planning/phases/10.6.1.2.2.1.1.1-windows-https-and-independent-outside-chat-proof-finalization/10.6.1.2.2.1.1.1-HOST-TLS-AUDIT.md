# 10.6.1.2.2.1.1.1 Host TLS Audit

- Phase: `10.6.1.2.2.1.1.1`
- Date: `2026-03-31`
- Host: `192.168.88.250`
- Status: `captured`

## Short Report

The exact TLS/public-contract truth is now narrower and harsher than the previous phase assumed.

Simple explanation:

- the Windows-side `Caddy` edge is alive locally
- but the real public IP `77.66.186.75` does not currently terminate on that Windows `Caddy`
- instead, public requests land on `nginx/1.18.0 (Ubuntu)` and return `404`
- so the remaining blocker is now clearly upstream public-edge ownership, not browser accounts

## Exact Public-Contract Truth

- public identifier under test: `77.66.186.75`
- intended Windows-side public edge owner: `Caddy` on the deployed Windows Server
- actual public responder for `77.66.186.75` during this phase:
  - `nginx/1.18.0 (Ubuntu)`
  - response class: `404`

## Windows Host Truth

- the Windows edge itself is alive locally
- if traffic is forced directly to the Windows host with the matching raw-IP host-header path, the Windows edge can still serve:
  - `/healthz` -> `200`
  - `/v1/models` -> `200`
- this means the Windows API stack is not the primary blocker for this phase

## HTTPS/TLS Truth

- `https://77.66.186.75` is not a usable Windows-side HTTPS contract right now
- standard Windows TLS probe fails at secure-channel creation
- external TLS observations also point to the same Ubuntu-side responder rather than the Windows `Caddy` edge

## Rollback And Preserve-First Truth

- the 9 preserved browser profiles were left intact
- cookies/local storage were not cleared
- no mass restart of all 9 workers was performed
- `shared-6` remained the canary and was left stopped again at the end

## Final Audit Conclusion

The blocker for this phase is now precisely:

- `public_edge_only`

Meaning:

- public `77.66.186.75` routing/termination does not currently point to the Windows `Caddy` edge that owns the preserved worker/API path
- Windows local API truth is corroborative only unless upstream edge ownership is reconciled

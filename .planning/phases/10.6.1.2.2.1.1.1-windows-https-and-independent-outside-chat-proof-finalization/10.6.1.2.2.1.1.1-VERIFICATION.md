# 10.6.1.2.2.1.1.1 Verification

- Phase: `10.6.1.2.2.1.1.1`
- Date: `2026-03-31`
- Status: `complete`
- Final verdict: `hold_preserve_accounts`

## Short Report

This phase did exactly what it needed to do: it removed the last ambiguity without risking the 9 live accounts.

Simple explanation:

- the Windows local edge is still alive
- but the real public IP does not currently reach that Windows edge
- public `77.66.186.75` is still terminating on Ubuntu `nginx`
- so rollout smoke still cannot resume honestly

The important improvement is that the blocker is now no longer vague.

It is now exact:

- `public_edge_only`

## What Landed

Phase artifacts:

- [10.6.1.2.2.1.1.1-HOST-TLS-AUDIT.md](d:/OpenAi/.planning/phases/10.6.1.2.2.1.1.1-windows-https-and-independent-outside-chat-proof-finalization/10.6.1.2.2.1.1.1-HOST-TLS-AUDIT.md)
- [10.6.1.2.2.1.1.1-HTTPS-PROOF.md](d:/OpenAi/.planning/phases/10.6.1.2.2.1.1.1-windows-https-and-independent-outside-chat-proof-finalization/10.6.1.2.2.1.1.1-HTTPS-PROOF.md)
- [10.6.1.2.2.1.1.1-INDEPENDENT-OUTSIDE-PROOF.md](d:/OpenAi/.planning/phases/10.6.1.2.2.1.1.1-windows-https-and-independent-outside-chat-proof-finalization/10.6.1.2.2.1.1.1-INDEPENDENT-OUTSIDE-PROOF.md)

## Test And Evidence Summary

TLS/public-contract truth:

- the deployed Windows `Caddy` edge is alive locally
- forced direct path to the Windows host still gives:
  - `/healthz` -> `200`
  - `/v1/models` -> `200`
- but the real public IP `77.66.186.75` currently does not terminate there
- public responder observed in this phase:
  - `nginx/1.18.0 (Ubuntu)`
  - `404`

HTTP result:

- truthful public `http://77.66.186.75` is not a working Windows public contract right now
- it returns `404` from Ubuntu `nginx`

HTTPS result:

- truthful public `https://77.66.186.75` is also not a usable Windows-edge contract
- standard Windows TLS probe classified it as:
  - `tls_handshake_failed`
- corroborating checks still pointed at the Ubuntu-side responder

Independent outside chat:

- a valid outside-client `POST /v1/chat/completions` was not accepted as proof in this phase
- reason:
  - the public edge was already wrong earlier on `healthz` and `v1/models`
- blocker class:
  - `public_edge_only`

Preserve-first result:

- 9 accounts remained intact
- no profile deletion/reset
- no cookie/local-storage clearing
- no mass restart of all 9 workers
- `shared-6` left stopped again after canary-safe work

## Requirement Verdict

- `WTLS-01` complete
- `WTLS-02` complete as blocker-classified public-contract truth
- `WTLS-03` complete as blocker-classified outside-proof verdict
- `WTLS-04` complete

## Final Decision

Decision: `hold_preserve_accounts`

Why:

1. The final ambiguity was reduced to one precise blocker.
2. The blocker is not browser accounts, not worker runtime, and not preserve-first canary discipline.
3. The blocker is that the real public IP still reaches the wrong edge owner.
4. The 9 valuable accounts remained protected.

## Impact On Phase 11

`Phase 11` still cannot resume.

It remains blocked until one new narrow follow-up resolves public-edge ownership and split-ingress truth for:

- `77.66.186.75`
- Windows `Caddy`
- the Ubuntu `nginx` responder currently answering instead

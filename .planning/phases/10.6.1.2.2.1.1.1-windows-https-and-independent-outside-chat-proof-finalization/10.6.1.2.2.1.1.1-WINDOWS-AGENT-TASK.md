# Windows Agent Task: Phase 10.6.1.2.2.1.1.1

## Goal

Finish the preserve-first public-edge follow-up on the deployed Windows Server `192.168.88.250` without endangering the 9 already logged-in ChatGPT accounts.

This is a preserve-first task.

The previous phase already proved:

- direct Windows-host backup and audit
- shadow canary on `shared-6`
- promoted HTTP edge through `Caddy`
- public HTTP `GET /healthz` and `GET /v1/models`

This phase is narrower. It must settle:

1. exact HTTPS/TLS/public-contract truth
2. one fully independent outside-client `POST /v1/chat/completions`

## Hard Rules

- Do not delete or reset any browser profile directories.
- Do not clear cookies, local storage, or browser data.
- Do not mass-restart all 9 workers.
- Do not relogin accounts unless an exact blocker proves that is the only remaining safe path.
- Do not expose `4040` or worker-agent ports as public endpoints.
- Do not bypass CAPTCHA or anti-bot checks.
- Do not regress the already-working public HTTP path blindly just to chase HTTPS.
- If a promoted edge change threatens the preserved pool, roll back immediately instead of experimenting live.

## Phase Files To Produce

Write your outputs into these exact files inside the repo checkout on the Windows Server:

- `.planning/phases/10.6.1.2.2.1.1.1-windows-https-and-independent-outside-chat-proof-finalization/10.6.1.2.2.1.1.1-HOST-TLS-AUDIT.md`
- `.planning/phases/10.6.1.2.2.1.1.1-windows-https-and-independent-outside-chat-proof-finalization/10.6.1.2.2.1.1.1-HTTPS-PROOF.md`
- `.planning/phases/10.6.1.2.2.1.1.1-windows-https-and-independent-outside-chat-proof-finalization/10.6.1.2.2.1.1.1-INDEPENDENT-OUTSIDE-PROOF.md`
- `.planning/phases/10.6.1.2.2.1.1.1-windows-https-and-independent-outside-chat-proof-finalization/10.6.1.2.2.1.1.1-VERIFICATION.md`

If any step fails, still write the file and record the exact blocker.

## Read First

- `docs/windows-public-api-block.md`
- `docs/windows-server-agent-task.md`
- `infra/windows-block/backup-public-edge-state.ps1`
- `infra/windows-block/collect-public-edge-audit.ps1`
- `infra/windows-block/start-public-api.ps1`
- `infra/windows-block/probe-public-api.ps1`
- `infra/windows-block/activate-public-edge.ps1`
- `infra/windows-block/rollback-public-edge.ps1`

## Step 1: Exact HTTPS/TLS And Public-Contract Truth

Re-run preserve-first edge audit only as needed:

```powershell
powershell -ExecutionPolicy Bypass -File .\infra\windows-block\backup-public-edge-state.ps1
powershell -ExecutionPolicy Bypass -File .\infra\windows-block\collect-public-edge-audit.ps1
```

Then write `10.6.1.2.2.1.1.1-HOST-TLS-AUDIT.md` with:

- exact `Caddy` service name
- exact active `Caddy` config path
- exact current public identifier under test:
  - raw IP
  - hostname
  - or both
- whether HTTPS is intended to work on that identifier right now
- whether a certificate is present, missing, mismatched, or otherwise unusable
- whether the currently-working public HTTP path must remain the truthful client contract for now
- exact rollback path that protects the current HTTP edge

Important: if the real answer is “HTTPS on raw IP is not the intended valid contract”, write that plainly instead of trying to force a fake green result.

## Step 2: Bounded HTTPS/Public-Edge Proof

Do not widen worker impact beyond canary `shared-6`.

Use the smallest preserve-first change needed to answer the TLS/public-contract question.

If the current promoted HTTP edge is already correct and HTTPS still fails, use the updated `probe-public-api.ps1` to record the exact failure class rather than treating it as a generic broken edge.

Useful probes:

```powershell
powershell -ExecutionPolicy Bypass -File .\infra\windows-block\probe-public-api.ps1 -BaseUrl http://77.66.186.75
powershell -ExecutionPolicy Bypass -File .\infra\windows-block\probe-public-api.ps1 -BaseUrl https://77.66.186.75
```

If a bounded HTTPS reconciliation is safe and necessary, perform it preserve-first. If it risks the working HTTP path or the preserved 9-account pool, do not force it.

Write `10.6.1.2.2.1.1.1-HTTPS-PROOF.md` with:

- exact commands run
- whether HTTP still returns `200` for `healthz` and `v1/models`
- whether HTTPS now works
- if HTTPS fails, exact failure class:
  - `tls_handshake_failed`
  - `http_error`
  - `connection_refused`
  - `timeout`
  - or another exact blocker
- whether rollback was needed
- whether the real public contract after this step is:
  - `https-ready`
  - `http-only-for-now`
  - `hold-preserve-first`

## Step 3: Independent Outside Chat Proof

Keep the remaining 8 workers untouched.

Use only canary worker `shared-6`.

If the final public contract after Step 2 is still HTTP, prove chat over that HTTP contract truthfully. If HTTPS is real, prove chat over HTTPS. Do not fake a contract the edge does not actually support.

Run the final public chat proof with:

- `healthz`
- `v1/models`
- one canary `POST /v1/chat/completions`

Use the current public edge and preserve-first canary discipline.

Write `10.6.1.2.2.1.1.1-INDEPENDENT-OUTSIDE-PROOF.md` with:

- exact public base URL used
- exact status/result for:
  - `/healthz`
  - `/v1/models`
  - `/v1/chat/completions`
- whether `shared-6` was the worker used
- whether the reply was real
- whether the remaining blocker is:
  - `tls_only`
  - `public_edge_only`
  - `chat_path_only`
  - `outside_network_only`
  - or `none`

If a truly independent outside-NAT proof cannot be completed from the Windows host itself, record that explicitly and stop rather than pretending the proof is independent.

## Step 4: Final Verdict

Write `10.6.1.2.2.1.1.1-VERIFICATION.md` and end with exactly one verdict:

- `safe_to_promote`
- `safe_but_hold`
- `hold_preserve_accounts`

Also state explicitly:

- whether `Phase 11` can resume now
- what the truthful public client contract is right now
- whether the already-working HTTP path stayed intact
- whether the 9 accounts remained preserved
- what exact blocker remains if rollout smoke is still blocked

## What To Send Back

Send back a short summary in plain Russian:

- TLS/public-contract truth
- HTTP result
- HTTPS result
- independent outside chat result
- preserved ли 9 аккаунтов
- final verdict

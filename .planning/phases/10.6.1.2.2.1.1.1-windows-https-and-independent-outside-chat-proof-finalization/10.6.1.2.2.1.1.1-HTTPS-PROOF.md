# 10.6.1.2.2.1.1.1 HTTPS Proof

- Phase: `10.6.1.2.2.1.1.1`
- Date: `2026-03-31`
- Status: `complete_with_hold`

## Short Report

HTTPS was not proven for the Windows public edge.

Simple explanation:

- the real public IP does not hit Windows `Caddy`
- public HTTP on `77.66.186.75` currently returns `404` from Ubuntu `nginx`
- public HTTPS also does not produce a usable Windows-side contract
- the Windows local edge itself still works if traffic is forced directly to that host

## Public HTTP Truth

As the real public contract under test:

- `http://77.66.186.75/healthz` -> `404`
- `http://77.66.186.75/v1/models` -> `404`
- responder observed: `nginx/1.18.0 (Ubuntu)`

## Public HTTPS Truth

- `https://77.66.186.75` fails as a usable Windows-side client contract
- standard Windows probe result:
  - `tls_handshake_failed`
- corroborating evidence:
  - forcing TLS checks externally still reaches the same Ubuntu-side responder rather than Windows `Caddy`
  - no usable public HTTPS contract for the preserved Windows edge exists at the end of this phase

## Local Windows Corroboration

If traffic is forced to the Windows host itself:

- `/healthz` -> `200`
- `/v1/models` -> `200`

That means:

- the Windows local API stack still works
- the current blocker is not primarily the Windows worker/API runtime
- the current blocker is who actually owns and serves the public IP path

## Rollback / Preserve-First Outcome

- no broad 9-worker intervention was performed
- no relogins were performed
- no profile reset happened
- no evidence in this phase justified touching the remaining 8 preserved workers

## Verdict For This Plan

- result: `complete_with_hold`
- blocker class: `public_edge_only`
- real public contract after this step:
  - `http-only-for-now` is not even truthful for the Windows edge
  - the public IP still terminates elsewhere

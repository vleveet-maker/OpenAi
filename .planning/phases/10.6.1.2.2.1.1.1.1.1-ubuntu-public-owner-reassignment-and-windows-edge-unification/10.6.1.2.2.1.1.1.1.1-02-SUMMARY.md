# 10.6.1.2.2.1.1.1.1.1 Wave 2 Summary

Коротко простыми словами:

- Ubuntu public owner реально переключён;
- `ascii-art` сохранил свои hostname-сайты, но больше не держит raw IP;
- canary `shared-6` через новый public path прошёл `healthz`, `v1/models` и реальный chat.

# 10.6.1.2.2.1.1.1.1.1 External Proof

- Phase: `10.6.1.2.2.1.1.1.1.1`
- Date: `2026-03-31`
- Status: `passed_with_known_tls_debt`

## Short Report

Independent outside-client proof now passes on the unified owner path.

Simple explanation:

- from the operator machine, public HTTP now works end-to-end
- `healthz`, `v1/models`, and canary chat all return `200`
- public HTTPS also reaches the unified path if the client allows the current self-signed certificate
- the remaining HTTPS issue is now certificate trust, not owner-path routing

## Outside HTTP Proof

### `GET http://77.66.186.75/healthz`

- status: `200`
- headers included:
  - `Server: nginx/1.18.0 (Ubuntu)`
  - `Via: 1.1 Caddy`
- body:

```json
{"ok":true,"service":"windows-public-api","hostControllerReachable":true,"poolStatus":"degraded","allowedWorkerIds":["shared-6"],"defaultWorkerId":"shared-6"}
```

### `GET http://77.66.186.75/v1/models`

- status: `200`
- model list returned successfully

### `POST http://77.66.186.75/v1/chat/completions`

- status: `200`
- canary worker: `shared-6`
- assistant reply:
  - `ubuntu-owner-ok`

## Outside HTTPS Proof

### Truthful trust-path result

From the standard Windows trust path:

- `https://77.66.186.75/healthz` still fails certificate trust
- failure class:
  - `self_signed_or_untrusted_cert`

That is no longer an owner-path blocker.

It is certificate trust debt.

### Functional HTTPS result

With certificate verification intentionally bypassed for proof only:

- `curl -k https://77.66.186.75/healthz` -> `200`
- `curl -k https://77.66.186.75/v1/chat/completions` on `shared-6` -> `200`
- assistant reply:
  - `ubuntu-owner-https-ok`

This proves:

- Ubuntu now owns and serves the public HTTPS path too
- the path reaches the preserved Windows edge correctly
- TLS transport works
- only client trust for the current certificate remains debt

## Final Outside-Proof Classification

- public HTTP contract: `working`
- public HTTPS transport: `working`
- public HTTPS trust: `not_consumer_ready`
- public owner ambiguity: `resolved`

## Preserve-First Outcome

- the 9-account pool remained untouched except for canary use of `shared-6`
- no relogin
- no profile cleanup
- no mass restart

# 10.6.1.2.2.1.1.1.1.1 Canary Proof

- Phase: `10.6.1.2.2.1.1.1.1.1`
- Date: `2026-03-31`
- Canary worker: `shared-6`
- Status: `passed`

## Short Report

The unified Ubuntu-owned public path now passes the canary proof.

Simple explanation:

- `healthz` now lands on the deliberate Ubuntu public owner path
- `v1/models` now lands on the same path
- one real `POST /v1/chat/completions` on `shared-6` returned a normal assistant reply

## Public HTTP Canary Evidence

### `GET http://77.66.186.75/healthz`

- status: `200`
- responder headers included:
  - `Server: nginx/1.18.0 (Ubuntu)`
  - `Via: 1.1 Caddy`
- body:

```json
{"ok":true,"service":"windows-public-api","hostControllerReachable":true,"poolStatus":"degraded","allowedWorkerIds":["shared-6"],"defaultWorkerId":"shared-6"}
```

### `GET http://77.66.186.75/v1/models`

- status: `200`
- responder headers included:
  - `Server: nginx/1.18.0 (Ubuntu)`
  - `Via: 1.1 Caddy`
- body included:
  - `owmcgp-browser`
  - `gpt-5.4-thinking`
  - `gpt-5.4`

### `POST http://77.66.186.75/v1/chat/completions`

- status: `200`
- worker: `shared-6`
- model label: `GPT-5.4 Thinking`
- assistant reply:
  - `ubuntu-owner-ok`

Observed response excerpt:

```json
{
  "model": "owmcgp-browser",
  "actual_model_label": "GPT-5.4 Thinking",
  "worker_id": "shared-6",
  "choices": [
    {
      "message": {
        "content": "ubuntu-owner-ok"
      }
    }
  ]
}
```

## Preserve-First Scope

- only canary `shared-6` was used for the worker-backed proof
- this phase did not widen validation to the other 8 preserved accounts
- raw `4040` and worker-agent ports remained private

## Exact Result

Canary result:

- `shared-6_public_owner_canary_passed`

This closes the old owner-path blocker:

- the public path is no longer failing before chat is reached

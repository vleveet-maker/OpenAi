# Phase 10.6.1.2.2.1.1.1.1.1 Validation

## Validation Focus

This phase is valid only if it unifies the real public owner path preserve-first.

It is invalid if it:

- mass-restarts or mass-revalidates the 9 live workers
- damages the preserved Windows profiles
- breaks unrelated Ubuntu hostnames while trying to fix the API path
- exposes raw `4040`, worker-agent ports, or raw public `:4010`
- claims chat proof before `healthz` and `v1/models` are already on the deliberate owner path

## Required Evidence

- one context-backed strategy artifact naming the primary owner model and rollback-safe baseline
- one Ubuntu-side owner reassignment or deliberate-owner proof artifact
- one canary `shared-6` proof artifact through the unified public owner path
- one final verification artifact with verdict:
  - `safe_to_promote`
  - `safe_but_hold`
  - `hold_preserve_accounts`

## What Must Be True Before Closeout

- public `77.66.186.75` is owned by one deliberate path, not split between Ubuntu and Windows
- unrelated Ubuntu-side sites remain intentionally preserved or explicitly accounted for
- Windows remains a preserved runtime/API edge, not a broad casualty of ingress changes
- canary `shared-6` proof is run only after the public owner path is coherent
- `Phase 11` is unblocked or held on one exact written reason

## Recommended Checks

- prove which Ubuntu vhost currently owns the public IP
- back up the live Ubuntu owner config before any reassignment
- make the deliberate public owner path explicit in repo-backed config/assets
- prove `healthz` and `v1/models` on the unified owner before attempting canary chat
- run one canary `POST /v1/chat/completions` only after the owner path is coherent
- confirm raw `4040` and worker-agent ports remain private after the change

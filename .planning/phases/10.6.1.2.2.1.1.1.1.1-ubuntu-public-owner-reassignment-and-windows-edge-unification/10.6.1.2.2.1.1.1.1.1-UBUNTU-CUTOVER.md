# 10.6.1.2.2.1.1.1.1.1 Ubuntu Cutover

- Phase: `10.6.1.2.2.1.1.1.1.1`
- Date: `2026-03-31`
- Status: `complete`

## Short Report

The preserve-first Ubuntu owner cutover is now live.

Simple explanation:

- Ubuntu stayed the real public owner of `77.66.186.75`
- the conflicting raw-IP ownership was removed from `ascii-art`
- Ubuntu now proxies the API routes to the preserved Windows edge over LAN HTTP
- the local Ubuntu relay path was preserved separately on `127.0.0.1:8080`

## Backups

Rollback-safe backups were captured on the Ubuntu host:

- `/etc/nginx/owmcgp-preserve-first/20260331T102327`
- `/etc/nginx/owmcgp-preserve-first/20260331T102348`

Latest backup used as the canonical rollback point:

- `/etc/nginx/owmcgp-preserve-first/20260331T102348`

Each backup contains:

- `ascii-art.conf.before`
- `owmcgp-remote-relay.conf.before`

## Live Ubuntu Changes

### `ascii-art.conf`

Before:

- `server_name main.ascii-art.ru www.ascii-art.ru 77.66.186.75;`

After:

- `server_name main.ascii-art.ru www.ascii-art.ru;`

Meaning:

- the unrelated hostname site remains intact
- the raw public IP is no longer owned by the `ascii-art` vhost

### `owmcgp-remote-relay.conf`

Now serves three deliberate layers:

1. Public HTTP owner:
   - `80 default_server`
   - proxies `/healthz`, `/api/relay/*`, `/v1/*` to `http://192.168.88.250`
   - forces upstream `Host: 77.66.186.75`

2. Public HTTPS owner:
   - `443 ssl default_server`
   - same API routes proxy to `http://192.168.88.250`
   - TLS terminates on Ubuntu using the existing self-signed cert path

3. Ubuntu-local relay path:
   - `8080`
   - keeps `/healthz` and `/api/relay/*`, `/v1/*` for the local Linux relay on `127.0.0.1:4010`

## Validation On Ubuntu

Config validation:

- `nginx -t` -> success
- `nginx -s reload` -> success

Post-cutover local truth:

- `http://127.0.0.1/healthz` -> `200` JSON from the preserved Windows public API
- `http://127.0.0.1:8080/healthz` -> `200 ok`

This proves:

- Ubuntu now deliberately owns the public API path
- the Linux local relay still survives as a separate internal path

## Preserved Hostname Check

Unrelated hostnames were checked directly on Ubuntu:

- `Host: main.ascii-art.ru` over HTTP -> `200`
- `Host: main.ascii-art.ru` over HTTPS -> `200`

So the `ascii-art` site remained intact while the raw public IP ownership moved away from it.

## Preserve-First Safety Outcome

- no Windows profile cleanup
- no cookie or local-storage clearing
- no mass restart of the 9-worker pool
- no relogin step
- no public exposure of raw `4040`, worker-agent ports, or raw `:4010`

## Result

Cutover result:

- `ubuntu_public_owner_unified`

The remaining public path is no longer split between Ubuntu `nginx` and the wrong Ubuntu vhost.

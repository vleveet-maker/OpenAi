---
phase: 05-guardrails-and-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/docker-compose.yml
  - infra/nginx/default.conf.template
  - services/control-api/src/server.ts
  - services/control-api/src/security/internal-admin-guard.ts
  - services/control-api/src/routes/internal-health.ts
  - services/control-api/test/edge-config.test.ts
  - services/control-api/test/internal-observability.test.ts
autonomous: true
requirements: [SECU-01, OBSV-02]
user_setup: []
must_haves:
    truths:
      - "Public traffic reaches only the approved session app and public APIs through one edge."
      - "Internal admin routes require the injected admin token instead of a LAN bypass."
      - "Health and readiness endpoints expose only safe aggregate signals."
    artifacts:
      - path: "infra/nginx/default.conf.template"
        provides: "Split public and internal edge listeners with token injection"
        contains: "listen 8080;"
      - path: "services/control-api/src/security/internal-admin-guard.ts"
        provides: "Token-first admin guard"
        contains: "allowPrivateNetworks === true"
      - path: "services/control-api/src/routes/internal-health.ts"
        provides: "Safe healthz and readyz routes"
        contains: "/healthz"
    key_links:
      - from: "infra/nginx/default.conf.template"
        to: "services/control-api/src/security/internal-admin-guard.ts"
        via: "internal edge injects x-internal-admin-token for protected routes"
        pattern: "x-internal-admin-token"
---

<objective>
Enforce user and admin boundary at the app and reverse-proxy layers.
</objective>

<success_criteria>

- Public traffic reaches only the session client and public `/api/*`
- Internal routes require the admin token in the default deployment path
- Health and readiness checks expose no worker profile or recovery data
</success_criteria>

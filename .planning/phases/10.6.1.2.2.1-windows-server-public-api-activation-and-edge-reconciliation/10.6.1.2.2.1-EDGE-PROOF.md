# 10.6.1.2.2.1 Edge Proof

- Phase: `10.6.1.2.2.1`
- Date: `2026-03-30`
- Result: `blocked_before_safe_promotion`

## What Was Actually Proven

Outside-client proof from this machine against `http://77.66.186.75`:

- `GET /healthz` -> `308 Permanent Redirect`
- `Location: https://77.66.186.75/healthz`
- `Server: Caddy`

- `GET /v1/models` -> `308 Permanent Redirect`
- `Location: https://77.66.186.75/v1/models`
- `Server: Caddy`

- `POST /v1/chat/completions` -> `308 Permanent Redirect`
- `Location: https://77.66.186.75/v1/chat/completions`
- `Server: Caddy`

Deployed Windows host proof through the Linux pivot:

- `80` open
- `443` open
- `4010` closed
- `4040` open
- `http://192.168.88.250/healthz` -> `308` via `Caddy`
- `http://192.168.88.250/v1/models` -> `308` via `Caddy`
- `https://192.168.88.250/healthz` -> TLS failure

## What Was Not Proven

Not executed during this phase:

- shadow canary through `shared-6` on the deployed Windows Server
- promoted-edge proof through a repaired `Caddy -> 127.0.0.1:4010` path
- rollback execution on the deployed Windows Server

Reason:

- this execution had network-level truth and preserve-first repo changes
- but it did not have a direct Windows-side admin execution path into the deployed `Caddy` config/service layer

## Rollback Status

- rollback was **not needed**, because no promoted-edge change was applied on the deployed Windows Server during this execution
- rollback assets now exist in the repo:
  - `infra/windows-block/backup-public-edge-state.ps1`
  - `infra/windows-block/activate-public-edge.ps1`
  - `infra/windows-block/rollback-public-edge.ps1`

## Canary Reference

The canary sequence remains the intended next safe proof path:

1. start loopback shadow API on `127.0.0.1:4011`
2. allowlist `shared-6`
3. activate shadow `Caddy` edge
4. prove:
   - `healthz`
   - `v1/models`
   - `v1/chat/completions`
5. only then consider promotion to `127.0.0.1:4010`

## Conclusion

This phase did not produce a promotable edge. It produced one exact blocker:

- the public Windows edge still terminates in `Caddy` redirects and broken HTTPS, while the repo now has the right scripts but the deployed host still needs one direct preserve-first edge execution pass.

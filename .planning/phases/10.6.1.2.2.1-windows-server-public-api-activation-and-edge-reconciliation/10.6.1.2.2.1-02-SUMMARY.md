# 10.6.1.2.2.1 Plan 02 Summary

- The repo now has rollbackable Windows edge scripts for `shadow` and `promoted` paths.
- The safe topology is now explicit in code and docs: `Caddy -> 127.0.0.1:4011` for shadow and `Caddy -> 127.0.0.1:4010` for promoted.
- Raw public `:4010` is no longer treated as the intended Windows-side public API contract.
- The canary flow is now explicit and pinned to `shared-6` before any broader promotion.

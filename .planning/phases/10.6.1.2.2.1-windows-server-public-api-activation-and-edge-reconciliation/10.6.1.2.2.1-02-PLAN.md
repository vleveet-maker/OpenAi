---
phase: 10.6.1.2.2.1-windows-server-public-api-activation-and-edge-reconciliation
plan: 02
type: execute
wave: 2
depends_on: [10.6.1.2.2.1-01-SUMMARY.md]
files_modified:
  - infra/windows-block/
  - docs/windows-public-api-block.md
  - docs/windows-server-agent-task.md
  - services/control-api/
autonomous: true
requirements: [WEDGE-02, WEDGE-03]
user_setup:
  - No full nine-worker restart; canary worker access only
---

<objective>
Build a rollbackable shadow-to-promoted Windows-side API edge path that reconciles the current `Caddy` boundary with the local `control-api`.
</objective>

<context>
@.planning/phases/10.6.1.2.2.1-windows-server-public-api-activation-and-edge-reconciliation/10.6.1.2.2.1-CONTEXT.md
@.planning/phases/10.6.1.2.2.1-windows-server-public-api-activation-and-edge-reconciliation/10.6.1.2.2.1-RESEARCH.md
@.planning/phases/10.6.1.2.2.1-windows-server-public-api-activation-and-edge-reconciliation/10.6.1.2.2.1-VALIDATION.md
@docs/windows-public-api-block.md
@docs/windows-server-agent-task.md
@infra/windows-block/start-public-api.ps1
@infra/windows-block/run-public-api.ps1
@infra/windows-block/probe-public-api.ps1
@services/control-api/src/routes/public-openai-compatible.ts
@services/control-api/src/routes/public-remote-relay.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shadow edge activation and rollback scripts</name>
  <files>infra/windows-block, docs/windows-public-api-block.md, docs/windows-server-agent-task.md</files>
  <read_first>infra/windows-block/start-public-api.ps1, infra/windows-block/run-public-api.ps1, docs/windows-public-api-block.md</read_first>
  <action>Create `infra/windows-block/activate-public-edge.ps1` and `infra/windows-block/rollback-public-edge.ps1`. The activation path must support a shadow config that proxies the chosen edge path to `127.0.0.1:4011` and a promoted config that proxies the chosen edge path to `127.0.0.1:4010`. Use the audit-discovered `Caddy` config path, validate config before reload, and restore the backed-up config on rollback.</action>
  <verify>rg -n "activate-public-edge|rollback-public-edge|127.0.0.1:4011|127.0.0.1:4010|reload|rollback" infra/windows-block docs/windows-public-api-block.md docs/windows-server-agent-task.md</verify>
  <acceptance_criteria>
    - the repo contains `activate-public-edge.ps1`
    - the repo contains `rollback-public-edge.ps1`
    - the scripts and docs mention both `127.0.0.1:4011` and `127.0.0.1:4010`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Formalize canary-safe shadow API proof through the edge</name>
  <files>infra/windows-block, docs/windows-public-api-block.md, docs/windows-server-agent-task.md, services/control-api/</files>
  <read_first>infra/windows-block/probe-public-api.ps1, services/control-api/src/routes/public-openai-compatible.ts, docs/windows-public-api-block.md</read_first>
  <action>Extend the Windows-side API and probe flow so a canary worker such as `shared-6` can be proven on the shadow path before promotion. Keep `host-controller` on `127.0.0.1:4040`, keep worker-agent ports private, and document the exact canary sequence: start shadow API on `4011`, allowlist `shared-6`, probe `healthz`, probe `v1/models`, probe `v1/chat/completions`, then stop the worker again.</action>
  <verify>rg -n "shared-6|4011|4040|healthz|v1/models|v1/chat/completions|allowlist" infra/windows-block docs/windows-public-api-block.md docs/windows-server-agent-task.md services/control-api</verify>
  <acceptance_criteria>
    - docs describe a canary flow using `shared-6`
    - docs keep `4040` internal and do not promote worker-agent ports
    - the shadow proof path includes `healthz`, `v1/models`, and `v1/chat/completions`
  </acceptance_criteria>
</task>

</tasks>

<success_criteria>

- the Windows-side API edge has a shadow path and rollback path
- the canary flow is explicit before any promoted public cutover

</success_criteria>

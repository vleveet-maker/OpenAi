---
phase: 18-deployed-windows-disconnected-runtime-remediation-forced-host-edge-repair-and-runtime-parity-resync
plan: 01
type: execute
wave: 1
depends_on: [17-VERIFICATION.md]
files_modified:
  - .planning/phases/18-deployed-windows-disconnected-runtime-remediation-forced-host-edge-repair-and-runtime-parity-resync/
  - docs/windows-browser-block.md
  - docs/windows-public-api-block.md
  - docs/internal-worker-ops.md
  - infra/windows-block/
  - infra/data/disconnected-runtime-remediation/
autonomous: true
requirements: [WREP-01, WREP-02]
user_setup: []
---

<objective>
Sync the latest deployed-host compatibility restores back into the repo, add the canonical disconnected-runtime remediation harness, and document the exact repo-to-host sync contract for the next preserve-first live run.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-deployed-windows-disconnected-runtime-remediation-forced-host-edge-repair-and-runtime-parity-resync/18-RESEARCH.md
@.planning/phases/17-deployed-windows-post-stabilization-degraded-runtime-and-repeated-public-canary-502-investigation/17-VERIFICATION.md
@docs/windows-browser-block.md
@docs/windows-public-api-block.md
@docs/internal-worker-ops.md
@infra/windows-block/probe-public-api.ps1
@infra/windows-block/test-rollout-smoke.ps1
@infra/windows-block/investigate-post-stabilization-runtime-and-public-502.ps1
@infra/windows-block/stabilize-post-remediation-degraded-smoke.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync the exact Phase 17 deployed-host compatibility restores back into the repo and stamp a canonical parity marker</name>
  <files>infra/windows-block/probe-public-api.ps1, infra/windows-block/test-rollout-smoke.ps1, infra/windows-block/investigate-post-stabilization-runtime-and-public-502.ps1, infra/windows-block/stabilize-post-remediation-degraded-smoke.ps1</files>
  <read_first>infra/windows-block/probe-public-api.ps1, infra/windows-block/test-rollout-smoke.ps1, infra/windows-block/investigate-post-stabilization-runtime-and-public-502.ps1, infra/windows-block/stabilize-post-remediation-degraded-smoke.ps1, .planning/phases/17-deployed-windows-post-stabilization-degraded-runtime-and-repeated-public-canary-502-investigation/17-VERIFICATION.md</read_first>
  <action>Update the four runtime-critical Phase 17 wrappers so the repo copy encodes the same compatibility behavior that the deployed host still needed after overlay. In `infra/windows-block/probe-public-api.ps1`, make the forced-Host probe path callable through named PowerShell parameters from wrapper scripts and keep the exact hop vocabulary `loopback_api`, `windows_edge_forced_host`, and `ubuntu_public_owner`. In `infra/windows-block/test-rollout-smoke.ps1`, preserve the current bounded-canary cleanup contract so `shared-6` is stopped again and port `4028` is not left listening after the run, while still writing the JSON, Markdown, and latest outputs. In `infra/windows-block/investigate-post-stabilization-runtime-and-public-502.ps1` and `infra/windows-block/stabilize-post-remediation-degraded-smoke.ps1`, switch the compatibility marker to the exact string `phase18-runtime-remediation-v1`, keep named-parameter calls into `probe-public-api.ps1`, and preserve the current post-run cleanup so future overlays do not require live-host script edits before the Phase 18 run can start.</action>
  <verify>rg -n "phase18-runtime-remediation-v1|loopback_api|windows_edge_forced_host|ubuntu_public_owner|4028|shared-6|probe-public-api" infra/windows-block/probe-public-api.ps1 infra/windows-block/test-rollout-smoke.ps1 infra/windows-block/investigate-post-stabilization-runtime-and-public-502.ps1 infra/windows-block/stabilize-post-remediation-degraded-smoke.ps1</verify>
  <acceptance_criteria>
    - the four runtime-critical wrapper files still exist
    - `infra/windows-block/investigate-post-stabilization-runtime-and-public-502.ps1` contains `phase18-runtime-remediation-v1`
    - `infra/windows-block/stabilize-post-remediation-degraded-smoke.ps1` contains `phase18-runtime-remediation-v1`
    - the probe wrapper still exposes `loopback_api`, `windows_edge_forced_host`, and `ubuntu_public_owner`
    - the smoke wrapper still contains the bounded-canary cleanup path for `shared-6` and `4028`
    - the repo copy no longer relies on manual deployed-host edits to express the latest Phase 17 compatibility behavior
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add the canonical preserve-first disconnected-runtime remediation harness and durable artifact contract</name>
  <files>infra/windows-block/remediate-disconnected-runtime-and-forced-host-edge.ps1, infra/data/disconnected-runtime-remediation/, .planning/phases/18-deployed-windows-disconnected-runtime-remediation-forced-host-edge-repair-and-runtime-parity-resync/</files>
  <read_first>infra/windows-block/probe-public-api.ps1, infra/windows-block/test-rollout-smoke.ps1, infra/windows-block/recover-browser-block-readiness.ps1, infra/windows-block/remediate-disconnected-baseline-and-forced-host-edge.ps1, infra/windows-block/stabilize-post-remediation-degraded-smoke.ps1, .planning/phases/18-deployed-windows-disconnected-runtime-remediation-forced-host-edge-repair-and-runtime-parity-resync/18-RESEARCH.md</read_first>
  <action>Create `infra/windows-block/remediate-disconnected-runtime-and-forced-host-edge.ps1` as the canonical Phase 18 wrapper. Give it concrete defaults `SessionBaseUrl=http://127.0.0.1:8080`, `PublicApiBaseUrl=http://77.66.186.75`, `InternalBaseUrl=http://127.0.0.1:8081`, `HostControllerBaseUrl=http://127.0.0.1:4040`, `WindowsEdgeBaseUrl=http://127.0.0.1`, `PublicHostHeader=77.66.186.75`, `CanaryWorkerId=shared-6`, `RecoveryWaitSeconds=15`, and `EdgeSettleSeconds=10`. Make it capture exact stage snapshots named `before_runtime_remediation`, `after_targeted_reconnect`, `after_loopback_probe`, `after_forced_host_probe`, `after_public_owner_probe`, and `after_canary_stop`. Reuse the existing readiness-recovery, forced-Host/public-owner probe, and bounded-canary helpers instead of inventing a second edge dialect. Write `.planning/phases/18-deployed-windows-disconnected-runtime-remediation-forced-host-edge-repair-and-runtime-parity-resync/18-REMEDIATION-SUMMARY.json`, `.planning/phases/18-deployed-windows-disconnected-runtime-remediation-forced-host-edge-repair-and-runtime-parity-resync/18-REMEDIATION-SUMMARY.md`, and `infra/data/disconnected-runtime-remediation/latest.json`. Make the remediation artifact expose the exact fields `scriptCompatibilityVersion`, `preRemediationReadyCount`, `postRemediationReadyCount`, `dominantRuntimeBlocker`, `firstFailingHop`, `forcedHostStatus`, `publicOwnerStatus`, and `verdict`, where `verdict` is exactly `remediated_ready_for_smoke` or `hold_rollout`.</action>
  <verify>rg -n "before_runtime_remediation|after_targeted_reconnect|after_loopback_probe|after_forced_host_probe|after_public_owner_probe|after_canary_stop|preRemediationReadyCount|postRemediationReadyCount|dominantRuntimeBlocker|firstFailingHop|forcedHostStatus|publicOwnerStatus|remediated_ready_for_smoke|hold_rollout|18-REMEDIATION-SUMMARY|disconnected-runtime-remediation/latest.json" infra/windows-block/remediate-disconnected-runtime-and-forced-host-edge.ps1</verify>
  <acceptance_criteria>
    - `infra/windows-block/remediate-disconnected-runtime-and-forced-host-edge.ps1` exists
    - the wrapper contains the exact stage names `before_runtime_remediation`, `after_targeted_reconnect`, `after_loopback_probe`, `after_forced_host_probe`, `after_public_owner_probe`, and `after_canary_stop`
    - the wrapper writes both Phase 18 artifacts and `infra/data/disconnected-runtime-remediation/latest.json`
    - the wrapper exposes the exact fields `scriptCompatibilityVersion`, `preRemediationReadyCount`, `postRemediationReadyCount`, `dominantRuntimeBlocker`, `firstFailingHop`, `forcedHostStatus`, `publicOwnerStatus`, and `verdict`
    - the remediation verdict is constrained to `remediated_ready_for_smoke` or `hold_rollout`
    - the wrapper keeps the preserve-first bounded-canary contract instead of widening the worker set by default
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Document the exact Phase 18 repo-to-host sync list and live remediation command</name>
  <files>docs/windows-browser-block.md, docs/windows-public-api-block.md, docs/internal-worker-ops.md</files>
  <read_first>docs/windows-browser-block.md, docs/windows-public-api-block.md, docs/internal-worker-ops.md, infra/windows-block/remediate-disconnected-runtime-and-forced-host-edge.ps1, .planning/phases/18-deployed-windows-disconnected-runtime-remediation-forced-host-edge-repair-and-runtime-parity-resync/18-RESEARCH.md</read_first>
  <action>Update `docs/windows-browser-block.md`, `docs/windows-public-api-block.md`, and `docs/internal-worker-ops.md` with one exact Phase 18 repo-to-host sync checklist and one exact live remediation command. The sync list must explicitly include `infra/windows-block/remediate-disconnected-runtime-and-forced-host-edge.ps1`, `infra/windows-block/probe-public-api.ps1`, `infra/windows-block/test-rollout-smoke.ps1`, `infra/windows-block/investigate-post-stabilization-runtime-and-public-502.ps1`, and `infra/windows-block/stabilize-post-remediation-degraded-smoke.ps1`. The live command must use `shared-6`, `RecoveryWaitSeconds=15`, `EdgeSettleSeconds=10`, `PublicHostHeader=77.66.186.75`, and the exact output paths `18-REMEDIATION-SUMMARY.json` plus `18-REMEDIATION-SUMMARY.md`. Repeat the preserve-first ban on profile deletion, cookie clearing, local-storage clearing, blind full-pool restart, and mass relogin.</action>
  <verify>rg -n "remediate-disconnected-runtime-and-forced-host-edge.ps1|probe-public-api.ps1|test-rollout-smoke.ps1|investigate-post-stabilization-runtime-and-public-502.ps1|stabilize-post-remediation-degraded-smoke.ps1|shared-6|RecoveryWaitSeconds|EdgeSettleSeconds|77.66.186.75|18-REMEDIATION-SUMMARY|profile deletion|mass relogin" docs/windows-browser-block.md docs/windows-public-api-block.md docs/internal-worker-ops.md</verify>
  <acceptance_criteria>
    - all three docs mention `remediate-disconnected-runtime-and-forced-host-edge.ps1`
    - all three docs include the exact Phase 18 sync list with the five runtime-critical script paths
    - all three docs restate the exact output artifact names `18-REMEDIATION-SUMMARY.json` and `18-REMEDIATION-SUMMARY.md`
    - all three docs repeat the preserve-first ban on destructive reset behavior
    - the documented command includes `shared-6`, `RecoveryWaitSeconds=15`, `EdgeSettleSeconds=10`, and `PublicHostHeader=77.66.186.75`
  </acceptance_criteria>
</task>

</tasks>

<success_criteria>

- the next remediation archive is parity-clean for the runtime-critical wrapper chain
- one canonical disconnected-runtime remediation harness exists for the deployed Windows browser-block host
- operators have one exact sync list and one exact live remediation command instead of free-form compatibility notes

</success_criteria>

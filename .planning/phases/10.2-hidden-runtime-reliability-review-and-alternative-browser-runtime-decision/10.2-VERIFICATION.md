---
phase: 10.2-hidden-runtime-reliability-review-and-alternative-browser-runtime-decision
verified: 2026-03-28T19:48:00+03:00
status: complete
score: 3/3 must-haves verified
---

# Phase 10.2: Hidden Runtime Reliability Review And Alternative Browser Runtime Decision Verification Report

Phase goal: decide, with live evidence, whether the current hidden runtime can be stabilized for household rollout or whether the milestone must pivot before Phase 11 continues.

Verified: 2026-03-28T19:48:00+03:00
Status: complete

## Goal Achievement

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | The project now has a live-evidence-backed answer on the current host hidden runtime: it is not rollout-safe after manual login on the same durable profile. | x VERIFIED | `infra/host-worker/test-hidden-runtime-transition.ps1`, `infra/data/host-worker-logs/runtime-matrix.jsonl`, `workers/agent/src/chat-bootstrap/temporary-chat-runner.ts`, `workers/agent/src/server.ts` |
| 2 | Pool and worker truth are now stronger and expose capability semantics instead of collapsing rollout decisions to raw liveness. | x VERIFIED | `services/host-controller/src/host-controller.mjs`, `services/control-api/src/workers/host-pool-service.ts`, `services/control-api/src/routes/internal-workers.ts`, live `/internal/workers` response with `runtimeCapability` |
| 3 | Phase 10.2 ended with an explicit runtime decision and a blocked handoff contract for Phase 11. | x VERIFIED | `.planning/phases/10.2-hidden-runtime-reliability-review-and-alternative-browser-runtime-decision/10.2-RUNTIME-DECISION.md`, `docs/internal-worker-ops.md`, `docs/host-native-worker.md`, `.planning/ROADMAP.md`, `.planning/STATE.md` |

Score: 3/3 truths verified

## Automated Checks

| Command | Status |
|---------|--------|
| `cmd /c npm.cmd test --prefix services/host-controller` | x PASSED |
| `cmd /c npm.cmd test --prefix services/control-api` | x PASSED |
| `cmd /c npm.cmd run build --prefix services/control-api` | x PASSED |
| `cmd /c npm.cmd test --prefix workers/agent` | x PASSED |
| `cmd /c npm.cmd run build --prefix workers/agent` | x PASSED |
| `docker compose -f infra/docker-compose.yml config` | x PASSED |
| PowerShell parser checks for host-worker probe scripts | x PASSED |

## Live Checks

| Probe | Result |
|-------|--------|
| `powershell -ExecutionPolicy Bypass -File .\\infra\\host-worker\\test-hidden-runtime-transition.ps1 -WorkerId wife -TimeoutSeconds 180` | x PASSED as evidence gate -> `hidden_runtime_unusable_after_bounded_rescue`, runtime-matrix row written |
| direct host hidden-runtime comparison on durable profiles | x VERIFIED -> visible auth reached normal ChatGPT page while hidden runtime previously reached challenge URLs |
| live Docker/Xvfb candidate bootstrap on `worker-dad` | x VERIFIED -> `bootstrap_challenge_detected`, `runtimeUsability=challenge_blocked`, `pageUrl=https://chatgpt.com/?__cf_chl_rt_tk=...` |
| `GET /internal/workers` after `control-api` restart | x VERIFIED -> live worker payloads expose `runtimeCapability` |

## Requirements Coverage

| Requirement | Status |
|-------------|--------|
| RREV-01: The project has a live-evidence-backed answer on current hidden/headless viability | x VERIFIED - answer is negative and evidence-backed |
| RREV-02: Pool and worker control-plane status reflects real runtime state closely enough for rollout decisions | x VERIFIED - startup truth, capability semantics, and live worker payloads now expose rollout-facing truth |
| RREV-03: If the current hidden runtime is not viable, the milestone chooses and documents a concrete alternative runtime path before rollout confidence continues | x VERIFIED - current host hidden runtime and current `docker_headed_xvfb` candidate were both rejected, and Phase 11 is blocked pending a new runtime-design phase |

## Residual Risk

- `/internal/host-pool` now reflects the real host-controller after Phase 10.2 corrected the host-native override wiring, but future operator runs still need to keep using that override instead of the base compose file alone.
- Phase 11 cannot proceed yet. The current non-visible runtime options reviewed in Phase 10.2 are both rejected for rollout use.
- The next real task is not more selector tuning; it is a new runtime-design phase for a steadier non-visible browser path after manual visible auth.

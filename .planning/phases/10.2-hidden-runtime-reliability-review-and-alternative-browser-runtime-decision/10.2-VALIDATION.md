---
phase: 10.2
slug: hidden-runtime-reliability-review-and-alternative-browser-runtime-decision
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-28
---

# Phase 10.2 - Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `node --test` and `vitest` |
| **Config file** | `services/host-controller/package.json`, `workers/agent/package.json`, `services/control-api/package.json` |
| **Quick run command** | `cmd /c npm.cmd test --prefix services/host-controller && cmd /c npm.cmd test --prefix workers/agent && cmd /c npm.cmd test --prefix services/control-api` |
| **Full suite command** | `cmd /c npm.cmd test --prefix services/host-controller && cmd /c npm.cmd test --prefix workers/agent && cmd /c npm.cmd run build --prefix workers/agent && cmd /c npm.cmd test --prefix services/control-api && cmd /c npm.cmd run build --prefix services/control-api` |
| **Estimated runtime** | ~120 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cmd /c npm.cmd test --prefix services/host-controller` or the smallest affected package test command
- **After every plan wave:** Run the full suite command
- **Before `$gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 120 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 10.2-01-* | 01 | 1 | RREV-02 | unit/integration | `cmd /c npm.cmd test --prefix services/host-controller` | pending plan | pending |
| 10.2-02-* | 02 | 2 | RREV-01 | unit/integration + manual probe | `cmd /c npm.cmd test --prefix workers/agent && powershell -ExecutionPolicy Bypass -File .\\infra\\host-worker\\test-hidden-runtime-transition.ps1 -WorkerId wife` | pending plan | pending |
| 10.2-03-* | 03 | 3 | RREV-03 | docs/decision + targeted tests | `cmd /c npm.cmd test --prefix services/control-api` | pending plan | pending |

*Status: pending / green / red / flaky*

---

## Wave 0 Requirements

- [ ] Existing infrastructure covers all phase requirements.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Confirm hidden runtime lands in challenge flow while visible auth on the same profile reaches normal ChatGPT | RREV-01 | Live ChatGPT anti-bot behavior cannot be simulated reliably in unit tests | 1. Start one worker in `visible_auth`. 2. Confirm normal logged-in ChatGPT page. 3. Return same profile to `hidden_runtime`. 4. Run bootstrap probe and record result/failure URL. |
| Confirm pool/start truth matches actual worker reachability and not just request acceptance | RREV-02 | Requires live host processes and real port/runtime state | 1. Stop pool. 2. Start pool. 3. Compare `host-controller /health`, `control-api /internal/workers`, and `control-api /internal/host-pool`. 4. Record mismatches. |
| Confirm chosen replacement runtime, if needed, preserves non-visible routine use | RREV-03 | Runtime decision depends on operator workflow and live browser behavior | 1. Run the selected replacement runtime path. 2. Verify no visible routine windows on operator desktop. 3. Verify ChatGPT bootstrap or relay usability. |

---

## Validation Sign-Off

- [ ] All tasks have automated verify or explicit manual gate coverage
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all missing references
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s for automated checks
- [ ] `nyquist_compliant: true` set in frontmatter once plan-specific task rows are finalized

**Approval:** pending

---
phase: 37-restore-ubuntu-reverse-ssh-tunnel-listeners-and-complete-external-authenticated-chat-smoke-after-token-models-proof
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - .planning/phases/37-restore-ubuntu-reverse-ssh-tunnel-listeners-and-complete-external-authenticated-chat-smoke-after-token-models-proof/37-REVERSE-TUNNEL-CHAT-SMOKE-SUMMARY.json
  - .planning/phases/37-restore-ubuntu-reverse-ssh-tunnel-listeners-and-complete-external-authenticated-chat-smoke-after-token-models-proof/37-REVERSE-TUNNEL-CHAT-SMOKE-SUMMARY.md
  - .planning/phases/37-restore-ubuntu-reverse-ssh-tunnel-listeners-and-complete-external-authenticated-chat-smoke-after-token-models-proof/37-03-SUMMARY.md
  - .planning/phases/37-restore-ubuntu-reverse-ssh-tunnel-listeners-and-complete-external-authenticated-chat-smoke-after-token-models-proof/37-VERIFICATION.md
  - infra/data/post-phase36-reverse-tunnel-chat-smoke/latest.json
autonomous: true
requirements: [RTUN-01, RTUN-02, RTUN-04]
user_setup:
  - Use execution-time secrets only if needed. Do not write bearer tokens or SSH passwords into repo files, artifacts, or command lines.
---

# Plan 37-03

## Goal

Run the live Phase 37 restore-and-smoke flow and close with one honest verdict.

## Tasks

<task id="37-03-01">
<name>Run implementation verification before live smoke.</name>
<objective>Run implementation verification before live smoke.</objective>
<action>
Run parser checks for the touched PowerShell scripts, Python compile checks for the reverse-tunnel runner, focused `control-api` route/admin tests, the full `control-api` suite, and the `control-api` build. Fix only Phase 37 implementation defects; do not broaden scope into account relogin or profile mutation.
</action>
<acceptance_criteria>
- parser checks pass
- Python compile check passes
- focused route/admin tests pass
- full `control-api` suite passes
- build passes
</acceptance_criteria>
</task>

<task id="37-03-02">
<name>Restore reverse tunnels and verify Ubuntu listeners.</name>
<objective>Restore reverse tunnels and verify Ubuntu listeners.</objective>
<action>
Run the Phase 37 wrapper from the current Windows machine. It must use the known Ubuntu public target `77.66.186.75:2222`, prefer the durable scheduled task when available, and otherwise record any temporary process owner clearly. Verify listeners on Ubuntu with `ss` for `127.0.0.1:14021..14027` plus `127.0.0.1:14040`. If listeners remain missing, stop with `hold_rollout` and the exact `nextBlocker`.
</action>
<acceptance_criteria>
- Windows task/process truth is recorded
- Ubuntu listener truth is recorded directly from Ubuntu
- listener success is not inferred from Windows-side state alone
- if the tunnel owner is temporary instead of durable, the artifact says so plainly
- preserve-first constraints are recorded
</acceptance_criteria>
</task>

<task id="37-03-03">
<name>Run authenticated external smoke only after listeners are green.</name>
<objective>Run authenticated external smoke only after listeners are green.</objective>
<action>
After token source plus Ubuntu listeners are green, run authenticated public smoke against `http://77.66.186.75`: `/healthz`, `/v1/models`, and `/v1/chat/completions` with model `owmcgp-browser`. Prefer the already-proven isolated canary `wife` for the first bounded chat smoke. If `wife` is worker-specific red while the bridge is green, record the exact error and only rotate if the wrapper already supports a bounded preserve-first next-candidate path without opening a broad account-debug loop.
</action>
<acceptance_criteria>
- `/v1/models` is tested with a valid bearer token
- `/v1/chat/completions` is tested only after token plus listener truth are green
- expected successful reply is `probe-ok` or an explicitly equivalent probe marker
- final verdict is exactly `externally_ready` or `hold_rollout`
- any chat failure records exact HTTP status, error code, worker id, and next blocker
</acceptance_criteria>
</task>

<task id="37-03-04">
<name>Close Phase 37 with artifacts and GSD verification.</name>
<objective>Close Phase 37 with artifacts and GSD verification.</objective>
<action>
Write `37-REVERSE-TUNNEL-CHAT-SMOKE-SUMMARY.json`, `37-REVERSE-TUNNEL-CHAT-SMOKE-SUMMARY.md`, `37-03-SUMMARY.md`, `37-VERIFICATION.md`, and update the latest artifact. Restart local `control-api` only on `127.0.0.1:8081` if needed to confirm the latest route and admin section. Run GSD phase-completeness verification and update state with the final verdict and next suggested step.
</action>
<acceptance_criteria>
- all Phase 37 artifacts exist
- latest route and admin section match the latest artifact
- secret redaction is checked
- GSD phase-completeness passes or records only explained warnings
- next step is written plainly for the operator
</acceptance_criteria>
</task>

## Requirements

- `RTUN-01`
- `RTUN-02`
- `RTUN-04`

## Verification

- PowerShell parser checks
- `python -m py_compile infra/windows-block/start-reverse-tunnels.py`
- `npm.cmd --prefix services/control-api test -- internal-post-phase36-reverse-tunnel-chat-smoke.test.ts internal-admin-page.test.ts`
- `npm.cmd --prefix services/control-api test`
- `npm.cmd --prefix services/control-api run build`
- `GET /internal/post-phase36-reverse-tunnel-chat-smoke/latest`
- `/internal/admin` contains `Latest post-phase36 reverse tunnel chat smoke`
- Ubuntu `ss` proves listeners `14021..14027` plus `14040`
- authenticated public smoke against `http://77.66.186.75`

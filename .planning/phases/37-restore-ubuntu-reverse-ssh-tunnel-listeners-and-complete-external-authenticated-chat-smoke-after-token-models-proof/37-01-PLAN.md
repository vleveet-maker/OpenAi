---
phase: 37-restore-ubuntu-reverse-ssh-tunnel-listeners-and-complete-external-authenticated-chat-smoke-after-token-models-proof
plan: 01
type: execute
wave: 1
depends_on: [36-VERIFICATION.md]
files_modified:
  - infra/windows-block/start-reverse-tunnels.py
  - infra/windows-block/start-reverse-tunnels.ps1
  - infra/windows-block/register-browser-block-tasks.ps1
  - infra/windows-block/restore-reverse-tunnels-and-complete-external-chat-smoke.ps1
  - docs/remote-relay-server.md
  - docs/windows-browser-block.md
autonomous: true
requirements: [RTUN-01, RTUN-02]
user_setup:
  - If an SSH password or bearer token is required, provide it only as an execution-time secret or environment variable. Never commit it and never print it.
---

# Plan 37-01

## Goal

Make reverse-tunnel startup reliable enough to restore Ubuntu listeners, while keeping secrets out of tracked files, artifacts, and command lines.

## Tasks

<task id="37-01-01">
<name>Harden the reverse-tunnel runner.</name>
<objective>Harden the reverse-tunnel runner.</objective>
<files>
- infra/windows-block/start-reverse-tunnels.py
- infra/windows-block/start-reverse-tunnels.ps1
- infra/windows-block/register-browser-block-tasks.ps1
</files>
<read_first>
- infra/windows-block/start-reverse-tunnels.py
- infra/windows-block/start-reverse-tunnels.ps1
- infra/windows-block/register-browser-block-tasks.ps1
- infra/windows-block/stop-reverse-tunnels.ps1
- .planning/phases/36-server-bearer-token-and-ubuntu-ssh-listener-recovery-before-isolated-external-chat-revalidation/36-TOKEN-SSH-LISTENER-RECOVERY-SUMMARY.json
</read_first>
<action>
Update the reverse-tunnel runner so password auth can come from an execution-time environment variable such as `OWMCGP_REMOTE_SSH_PASSWORD` instead of requiring a password argument. Keep backward compatibility if practical, but the Phase 37 path must not place the SSH password in command-line arguments, tracked files, or artifacts. Add bounded retry/backoff for intermittent SSH banner/session failures and make the runner clearly report which remote forwards were requested.
</action>
<acceptance_criteria>
- `start-reverse-tunnels.py` can use an environment-provided password without printing or storing it
- SSH connect has bounded retry/backoff and an actionable final failure
- remote forwards include `14021..14027` and `14040` when host-controller forwarding is enabled
- PowerShell launcher avoids putting the SSH password into child process arguments in the Phase 37 path
- parser/compile checks pass
</acceptance_criteria>
</task>

<task id="37-01-02">
<name>Create the canonical Phase 37 restore-and-smoke wrapper.</name>
<objective>Create the canonical Phase 37 restore-and-smoke wrapper.</objective>
<files>
- infra/windows-block/restore-reverse-tunnels-and-complete-external-chat-smoke.ps1
- infra/data/post-phase36-reverse-tunnel-chat-smoke/latest.json
</files>
<read_first>
- infra/windows-block/probe-public-api.ps1
- infra/windows-block/recover-server-token-ssh-listeners-and-external-chat.ps1
- infra/windows-block/start-reverse-tunnels.ps1
- infra/windows-block/stop-reverse-tunnels.ps1
- .planning/phases/36-server-bearer-token-and-ubuntu-ssh-listener-recovery-before-isolated-external-chat-revalidation/36-VERIFICATION.md
</read_first>
<action>
Create a wrapper that records baseline local and Ubuntu state, starts or repairs the reverse tunnels preserve-first, verifies Ubuntu-side listener truth via SSH, resolves only the bearer-token source label and runtime token value in memory, and runs authenticated public smoke only when listeners are green. The wrapper must write JSON and Markdown artifacts plus `infra/data/post-phase36-reverse-tunnel-chat-smoke/latest.json`.
</action>
<acceptance_criteria>
- wrapper exists and passes PowerShell parser checks
- artifact records `scriptCompatibilityVersion=phase37-reverse-tunnel-chat-smoke-v1` or equivalent
- artifact records task/process truth, Ubuntu listener truth, token source label, and public smoke truth
- artifact never records bearer-token values, SSH passwords, cookies, local storage, profile contents, or browser session secrets
- chat smoke is skipped unless token plus Ubuntu listener truth are green
- final verdict is constrained to `externally_ready` or `hold_rollout`
</acceptance_criteria>
</task>

<task id="37-01-03">
<name>Document the tunnel restoration operating contract.</name>
<objective>Document the tunnel restoration operating contract.</objective>
<files>
- docs/remote-relay-server.md
- docs/windows-browser-block.md
</files>
<action>
Document that the active public path is Ubuntu `nginx -> 127.0.0.1:4010`, while external chat depends on Windows reverse SSH tunnels being visible on Ubuntu `127.0.0.1:14021..14027` plus `14040`. Document that file handoff remains GitHub-first, secrets remain execution-time only, and Phase 37 does not touch browser account state.
</action>
<acceptance_criteria>
- docs identify Ubuntu listener truth as the next bridge gate
- docs state that Windows Caddy is not the canonical public edge
- docs state that profiles/cookies/localStorage must not be modified by this phase
- docs state that cross-host file handoff is GitHub-first
</acceptance_criteria>
</task>

## Requirements

- `RTUN-01`
- `RTUN-02`

## Verification

- PowerShell parser checks for touched `.ps1` files
- `python -m py_compile infra/windows-block/start-reverse-tunnels.py`
- Manual secret-redaction review of wrapper outputs

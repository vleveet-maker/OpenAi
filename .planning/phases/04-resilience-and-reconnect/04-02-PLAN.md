---
phase: 04-resilience-and-reconnect
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - apps/session-client/src/session-types.ts
  - apps/session-client/src/session-api.ts
  - apps/session-client/src/use-session-view.ts
  - apps/session-client/src/app.tsx
  - apps/session-client/src/styles.css
  - apps/session-client/src/app.test.tsx
  - apps/session-client/src/session-storage.ts
autonomous: true
requirements: [RELY-01, RELY-03]
user_setup: []
must_haves:
    truths:
      - "The shared-screen app can resume the last active or queued session after the app is reopened."
      - "Temporary poll failures keep the current history visible and surface a reconnecting state instead of a blank error screen."
      - "Retrying or terminal relay failures are shown clearly in the active conversation UI."
    artifacts:
      - path: "apps/session-client/src/use-session-view.ts"
        provides: "Reconnect-safe polling and relay-status view state"
        contains: "connectionState"
      - path: "apps/session-client/src/session-storage.ts"
        provides: "Local storage for the last shared-screen session id"
        contains: "one-hour.activeSessionId"
      - path: "apps/session-client/src/app.tsx"
        provides: "Reconnect, retrying, and failed relay banners in the shared-screen UI"
        contains: "Connection lost. Reconnecting to the current session..."
    key_links:
      - from: "apps/session-client/src/use-session-view.ts"
        to: "services/control-api/src/routes/public-chat.ts"
        via: "the client reads relay retry or failure state from the public conversation snapshot"
        pattern: "relay"
      - from: "apps/session-client/src/app.tsx"
        to: "apps/session-client/src/session-storage.ts"
        via: "the start route and session route coordinate last-session resume"
        pattern: "activeSessionId"
---

<objective>
Add reconnect flow and same-worker session rehydration to the shared-screen app.

Purpose: Let the client recover gracefully from app or network interruption while keeping the existing session and worker binding intact.
Output: Local session resume storage, reconnect-safe polling behavior, retry and failure UI states in the shared-screen app, and automated client tests for reconnect and resume flows.
</objective>

<execution_context>
@../gsd/get-shit-done/workflows/execute-plan.md
@../gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-resilience-and-reconnect/04-CONTEXT.md
@.planning/phases/04-resilience-and-reconnect/04-RESEARCH.md
@.planning/phases/04-resilience-and-reconnect/04-01-PLAN.md
@apps/session-client/src/app.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add local resume storage and reconnect-aware client state</name>
  <files>apps/session-client/src/session-types.ts, apps/session-client/src/session-api.ts, apps/session-client/src/use-session-view.ts, apps/session-client/src/session-storage.ts</files>
  <read_first>apps/session-client/src/session-types.ts, apps/session-client/src/session-api.ts, apps/session-client/src/use-session-view.ts, services/control-api/src/chat/chat-types.ts, services/control-api/src/routes/public-chat.ts</read_first>
  <action>Create apps/session-client/src/session-storage.ts with a constant key exactly one-hour.activeSessionId and helpers getStoredSessionId(), storeSessionId(sessionId), and clearStoredSessionId(). Extend apps/session-client/src/session-types.ts with the RelayStatusSnapshot shape produced by Phase 4 backend work. Update apps/session-client/src/use-session-view.ts so it exposes connectionState exactly connected, reconnecting, or initial_load_failed; keeps the last good snapshot and message history visible when a poll fails after bootstrap; clears storage when a session becomes ended, expired, or cancelled; and stores the session id whenever the current session is queued or active.</action>
  <verify>rg -n "one-hour.activeSessionId|RelayStatusSnapshot|connectionState|reconnecting|initial_load_failed|storeSessionId|clearStoredSessionId" apps/session-client/src/session-types.ts apps/session-client/src/session-api.ts apps/session-client/src/use-session-view.ts apps/session-client/src/session-storage.ts</verify>
  <acceptance_criteria>
    - apps/session-client/src/session-storage.ts contains "one-hour.activeSessionId"
    - apps/session-client/src/use-session-view.ts contains "connectionState"
    - apps/session-client/src/use-session-view.ts contains "reconnecting"
    - apps/session-client/src/session-types.ts contains "RelayStatusSnapshot"
  </acceptance_criteria>
  <done>The client can remember the last non-terminal session and preserve its UI state during transient reconnects.</done>
</task>

<task type="auto">
  <name>Task 2: Render reconnect, retrying, and failed relay states in the shared-screen UI</name>
  <files>apps/session-client/src/app.tsx, apps/session-client/src/styles.css</files>
  <read_first>apps/session-client/src/app.tsx, apps/session-client/src/styles.css, apps/session-client/src/use-session-view.ts, .planning/phases/04-resilience-and-reconnect/04-CONTEXT.md</read_first>
  <action>Update apps/session-client/src/app.tsx so the start route checks getStoredSessionId() and automatically navigates to /session/:sessionId when bootstrap confirms the stored session is still queued or active. In the session route, render a visible banner with the exact copy "Connection lost. Reconnecting to the current session..." whenever connectionState is reconnecting. Render a visible retry banner with the exact copy "Retrying assistant delivery" when conversation.relay.status === \"retrying\" and include the current attemptCount and maxAttempts. Render a visible failure banner with the exact copy "Reply delivery failed" when conversation.relay.status === \"failed\". Keep the current history visible in all three cases, and disable the composer whenever connectionState !== connected or conversation.relay.status === \"retrying\". Update apps/session-client/src/styles.css with status-banner variants for reconnecting, retrying, and failed relay states while preserving the existing layout and typography.</action>
  <verify>rg -n "Connection lost\\. Reconnecting to the current session|Retrying assistant delivery|Reply delivery failed|connectionState !== \"connected\"|relay.status === \"retrying\"" apps/session-client/src/app.tsx apps/session-client/src/styles.css</verify>
  <acceptance_criteria>
    - apps/session-client/src/app.tsx contains "Connection lost. Reconnecting to the current session..."
    - apps/session-client/src/app.tsx contains "Retrying assistant delivery"
    - apps/session-client/src/app.tsx contains "Reply delivery failed"
    - apps/session-client/src/app.tsx contains "connectionState !== \"connected\""
  </acceptance_criteria>
  <done>The shared-screen UI explains reconnect and relay-failure states clearly without dropping the operator out of the current session.</done>
</task>

<task type="auto">
  <name>Task 3: Cover resume and reconnect behavior with client tests</name>
  <files>apps/session-client/src/app.test.tsx</files>
  <read_first>apps/session-client/src/app.tsx, apps/session-client/src/use-session-view.ts, apps/session-client/src/session-storage.ts, apps/session-client/src/app.test.tsx</read_first>
  <action>Extend apps/session-client/src/app.test.tsx with tests that verify: the app resumes the stored active session on reload, the current history stays visible while a poll failure puts the client into reconnecting state, the retry banner renders from conversation.relay.status === retrying, and the stored session id is cleared when bootstrap returns a terminal session. Stub localStorage directly in the test environment instead of creating ad-hoc globals. Use exact test names "resumes the stored active session on app reload", "keeps the current history visible while reconnecting after a poll failure", "shows relay retry status from the conversation snapshot", and "clears stored session id when the session is terminal".</action>
  <verify>rg -n "resumes the stored active session on app reload|keeps the current history visible while reconnecting after a poll failure|shows relay retry status from the conversation snapshot|clears stored session id when the session is terminal" apps/session-client/src/app.test.tsx</verify>
  <acceptance_criteria>
    - apps/session-client/src/app.test.tsx contains "resumes the stored active session on app reload"
    - apps/session-client/src/app.test.tsx contains "keeps the current history visible while reconnecting after a poll failure"
    - apps/session-client/src/app.test.tsx contains "shows relay retry status from the conversation snapshot"
    - apps/session-client/src/app.test.tsx contains "clears stored session id when the session is terminal"
  </acceptance_criteria>
  <done>The reconnect and resume experience is protected by automated client coverage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] the app resumes the last queued or active session from local storage
- [ ] poll failures after bootstrap keep the visible history on screen
- [ ] relay retry and failure states are readable without exposing internal worker controls
</verification>

<success_criteria>

- All tasks completed
- The shared screen can resume the last active or queued session after interruption
- Reconnecting and retrying states are visible and non-destructive
- The same session and worker remain authoritative after client interruption
</success_criteria>

<output>
After completion, create the matching SUMMARY document in this phase directory.
</output>

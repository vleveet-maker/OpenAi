---
phase: 05.1-internal-browser-access-for-manual-chatgpt-login-and-reauthentication
plan: 01
subsystem: infra
tags: [docker, playwright, novnc, xvfb, worker-runtime]
provides:
  - custom worker runtime with Xvfb, fluxbox, x11vnc, and websockify
  - browser access health metadata on worker-agent routes
  - startup path that keeps worker health visible before ChatGPT finishes loading
key-files:
  created:
    - infra/worker-runtime/Dockerfile
    - infra/worker-runtime/start-worker-runtime.sh
    - workers/agent/test/browser-access-runtime.test.ts
  modified:
    - infra/docker-compose.yml
    - workers/agent/src/server.ts
    - workers/agent/src/browser-launch.ts
requirements-completed: [ADMN-04, SECU-03]
completed: 2026-03-28
---

# Phase 05.1 Plan 01: Summary

Docker worker containers now boot with a persistent X display and an internal noVNC viewer, while `worker-agent` exposes `browserAccess` readiness metadata without waiting for ChatGPT to finish loading.

## Performance

- Duration: 1 min
- Started: 2026-03-28T08:08:49+03:00
- Completed: 2026-03-28T08:08:58+03:00
- Tasks: 2
- Files modified: 6

## Accomplishments

- Replaced raw Playwright worker containers with a custom runtime image that includes `fluxbox`, `x11vnc`, `novnc`, and `websockify`.
- Moved worker startup into one runtime entrypoint so browser viewer services and `worker-agent` start together inside Docker.
- Extended `/health` and `/internal/worker` to publish `browserAccess` metadata while browser startup is still in progress.

## Task Commits

1. Task 1: Build a custom worker runtime with a persistent display and browser viewer services - `5736355` (`chore`)
2. Task 2: Extend worker-agent health to publish browser-access metadata and cover it with tests - `937214d` (`feat`)

## Files Created/Modified

- `infra/worker-runtime/Dockerfile` - custom Playwright runtime with X11, VNC, and noVNC packages
- `infra/worker-runtime/start-worker-runtime.sh` - shared container entrypoint that boots Xvfb, fluxbox, x11vnc, websockify, and `worker-agent`
- `infra/docker-compose.yml` - worker services now build from `./worker-runtime` and expose browser access only on the private network
- `workers/agent/src/server.ts` - health routes now include `browserAccess`
- `workers/agent/src/browser-launch.ts` - bounded initial navigation remains explicit and documented
- `workers/agent/test/browser-access-runtime.test.ts` - startup and health contract coverage

## Decisions Made

- Use one runtime entrypoint per worker container instead of inline `xvfb-run`, so the display stack and HTTP viewer stay coupled.
- Derive browser access readiness from environment-backed runtime state instead of blocking health on ChatGPT navigation.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Workers now host an internal browser viewer on the private Docker network.
- Control-api can safely build worker-scoped access sessions and route the viewer through the internal edge.

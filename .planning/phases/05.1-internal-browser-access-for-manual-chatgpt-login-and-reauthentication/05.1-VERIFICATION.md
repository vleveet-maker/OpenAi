---
phase: 05.1-internal-browser-access-for-manual-chatgpt-login-and-reauthentication
verified: 2026-03-28T08:22:31+03:00
status: passed
score: 8/8 must-haves verified
---

# Phase 05.1: Internal Browser Access for Manual ChatGPT Login and Reauthentication Verification Report

Phase goal: give the operator an internal-only way to open a live worker browser for first login and reauthentication without exposing raw browser access publicly.

Verified: 2026-03-28T08:22:31+03:00
Status: passed

## Goal Achievement

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Worker containers run a visible browser viewer stack inside Docker without publishing raw host ports. | x VERIFIED | `infra/worker-runtime/Dockerfile`, `infra/worker-runtime/start-worker-runtime.sh`, and `infra/docker-compose.yml` |
| 2 | Worker-agent health exposes browser access metadata while startup is still in progress. | x VERIFIED | `workers/agent/src/server.ts` and `workers/agent/test/browser-access-runtime.test.ts` |
| 3 | Control-api can create, inspect, cancel, complete, and authorize worker-scoped browser access sessions. | x VERIFIED | `services/control-api/src/routes/internal-browser-access.ts` and `services/control-api/test/internal-browser-access.test.ts` |
| 4 | Reauthentication now uses the same browser access session lifecycle as first login. | x VERIFIED | `services/control-api/src/routes/internal-recovery.ts` and `services/control-api/test/internal-browser-access.test.ts` |
| 5 | The public edge blocks browser viewer routes. | x VERIFIED | `infra/nginx/default.conf.template` plus live request to `http://127.0.0.1:8080/internal/browser/...` returning `404` |
| 6 | The internal edge serves the worker noVNC viewer only after authorization. | x VERIFIED | `infra/nginx/default.conf.template` plus live request to `http://127.0.0.1:8081/internal/browser/...` returning `200` and serving noVNC HTML |
| 7 | Internal admin exposes browser access controls for open, reauth, cancel, and complete actions. | x VERIFIED | `services/control-api/src/routes/internal-admin-page.ts` and `services/control-api/test/internal-admin-page.test.ts` |
| 8 | Completing a browser access session returns the worker to `ready`. | x VERIFIED | live `POST /internal/workers/dad/browser-access/complete` smoke check returned `worker.status.status = ready` |

Score: 8/8 truths verified

## Automated Checks

| Command | Status |
|---------|--------|
| `cmd /c npm.cmd run build` in `workers/agent` | x PASSED |
| `cmd /c npm.cmd test` in `workers/agent` | x PASSED |
| `cmd /c npm.cmd run build` in `services/control-api` | x PASSED |
| `cmd /c npm.cmd test` in `services/control-api` | x PASSED |
| `docker compose -f infra/docker-compose.yml config` | x PASSED |

## Live Smoke Checks

| Check | Result |
|------|--------|
| `GET http://127.0.0.1:8080/` | `200` |
| `GET http://127.0.0.1:8081/internal/admin` | `200` |
| `GET http://127.0.0.1:8080/internal/browser/dad/vnc.html` | `404` |
| `GET http://127.0.0.1:8081/healthz` | `200`, `status = ok` |
| `GET http://127.0.0.1:8081/readyz` | `200` |
| `POST http://127.0.0.1:8081/internal/workers/dad/browser-access/start` | `202`, `browserAccess.status = browser_ready` |
| `GET http://127.0.0.1:8081/internal/browser/...viewerPath...` | `200`, noVNC HTML served |
| `POST http://127.0.0.1:8081/internal/workers/dad/browser-access/complete` | `202`, `worker.status.status = ready` |

## Requirements Coverage

| Requirement | Status |
|-------------|--------|
| ADMN-04: Admin can open an internal-only recovery path to a worker for login or reauthentication | x SATISFIED |
| SECU-03: Worker recovery endpoints and admin controls are reachable only from the trusted internal admin surface | x SATISFIED |

## Human Verification Still Recommended

One final manual household check remains valuable: open a real worker viewer, complete a real ChatGPT login or challenge page inside noVNC, and verify the same persistent profile remains authenticated on the next browser-access session.

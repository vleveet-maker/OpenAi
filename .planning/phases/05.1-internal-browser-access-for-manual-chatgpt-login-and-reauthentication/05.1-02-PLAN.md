---
phase: 05.1-internal-browser-access-for-manual-chatgpt-login-and-reauthentication
plan: 02
type: execute
wave: 2
depends_on: ["05.1-01"]
files_modified:
  - services/control-api/src/routes/internal-browser-access.ts
  - services/control-api/src/routes/internal-recovery.ts
  - services/control-api/src/server.ts
  - infra/nginx/default.conf.template
  - services/control-api/test/internal-browser-access.test.ts
  - services/control-api/test/edge-config.test.ts
autonomous: true
requirements: [ADMN-04, SECU-03]
user_setup: []
must_haves:
    truths:
      - "Browser access is granted only through time-bounded worker-scoped sessions created from internal admin routes."
      - "Internal edge authorizes browser viewer requests before proxying to worker-local noVNC endpoints."
      - "Public edge never exposes browser access routes."
    artifacts:
      - path: "services/control-api/src/routes/internal-browser-access.ts"
        provides: "Browser access session lifecycle and auth endpoint"
        contains: "/internal/workers/:id/browser-access/start"
      - path: "infra/nginx/default.conf.template"
        provides: "Protected browser viewer proxy on the internal edge"
        contains: "auth_request /internal/browser-access/authorize"
      - path: "services/control-api/test/internal-browser-access.test.ts"
        provides: "Automated lifecycle and authorization coverage"
        contains: "browser-access"
    key_links:
      - from: "infra/nginx/default.conf.template"
        to: "services/control-api/src/routes/internal-browser-access.ts"
        via: "Nginx auth_request delegates browser access authorization to control-api"
        pattern: "authorize"
---

<objective>
Add secure internal browser access sessions and gate the worker viewer behind the internal edge.
</objective>

<execution_context>
@../gsd/get-shit-done/workflows/execute-plan.md
@../gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-internal-browser-access-for-manual-chatgpt-login-and-reauthentication/05.1-CONTEXT.md
@.planning/phases/05.1-internal-browser-access-for-manual-chatgpt-login-and-reauthentication/05.1-RESEARCH.md
@services/control-api/src/routes/internal-recovery.ts
@infra/nginx/default.conf.template
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add browser access session routes in control-api and make reauth start delegate to them</name>
  <files>services/control-api/src/routes/internal-browser-access.ts, services/control-api/src/routes/internal-recovery.ts, services/control-api/src/server.ts, services/control-api/test/internal-browser-access.test.ts</files>
  <read_first>services/control-api/src/server.ts, services/control-api/src/routes/internal-recovery.ts, services/control-api/src/routes/internal-workers.ts, services/control-api/src/workers/worker-registry.ts, .planning/phases/05.1-internal-browser-access-for-manual-chatgpt-login-and-reauthentication/05.1-CONTEXT.md</read_first>
  <action>Create `services/control-api/src/routes/internal-browser-access.ts` with an in-memory `InternalBrowserAccessSession` shape containing `sessionId`, `accessToken`, `workerId`, `mode`, `status`, `startedAt`, `expiresAt`, and `viewerPath`. Add these exact routes: `POST /internal/workers/:id/browser-access/start`, `GET /internal/workers/:id/browser-access`, `POST /internal/workers/:id/browser-access/cancel`, `POST /internal/workers/:id/browser-access/complete`, and `GET /internal/browser-access/authorize`. `POST /start` must accept `{ mode: "first_login" | "reauth" }`, generate `crypto.randomUUID()` values for `sessionId` and `accessToken`, enforce a 15-minute TTL, store at most one active session per worker, and return `viewerPath` exactly as `/internal/browser/${workerId}/vnc.html?path=/internal/browser/${workerId}/websockify&autoconnect=true&resize=remote&sessionId=${sessionId}&accessToken=${accessToken}`. `GET /internal/browser-access/authorize` must return `204` only when `workerId`, `sessionId`, and `accessToken` match a non-expired active session, and `403` otherwise. Update `services/control-api/src/routes/internal-recovery.ts` so `POST /internal/workers/:id/reauth/start` delegates to browser access start in `mode: "reauth"` while preserving the current response shape fields `workerId`, `reauth`, and `manualLoginRequired`. Mount the new router in `services/control-api/src/server.ts`. Add `services/control-api/test/internal-browser-access.test.ts` to cover start, authorize success/failure, cancel, complete, expiry, and compatibility behavior for `reauth/start`.</action>
  <verify>rg -n "/internal/workers/:id/browser-access/start|/internal/browser-access/authorize|manualLoginRequired|viewerPath|accessToken|autoconnect=true" services/control-api/src/routes/internal-browser-access.ts services/control-api/src/routes/internal-recovery.ts services/control-api/src/server.ts services/control-api/test/internal-browser-access.test.ts</verify>
  <acceptance_criteria>
    - services/control-api/src/routes/internal-browser-access.ts contains "/internal/workers/:id/browser-access/start"
    - services/control-api/src/routes/internal-browser-access.ts contains "/internal/browser-access/authorize"
    - services/control-api/src/routes/internal-browser-access.ts contains "autoconnect=true"
    - services/control-api/src/routes/internal-recovery.ts contains "manualLoginRequired"
    - services/control-api/test/internal-browser-access.test.ts contains "returns 204 for an active browser access session"
    - services/control-api/test/internal-browser-access.test.ts contains "delegates reauth start to browser access"
  </acceptance_criteria>
  <done>Control-api can create, authorize, cancel, and complete time-bounded operator browser access sessions.</done>
</task>

<task type="auto">
  <name>Task 2: Protect internal browser viewer proxying through the Nginx edge and cover the contract with tests</name>
  <files>infra/nginx/default.conf.template, services/control-api/test/edge-config.test.ts</files>
  <read_first>infra/nginx/default.conf.template, infra/docker-compose.yml, services/control-api/test/edge-config.test.ts, .planning/phases/05.1-internal-browser-access-for-manual-chatgpt-login-and-reauthentication/05.1-RESEARCH.md</read_first>
  <action>In `infra/nginx/default.conf.template`, keep the public server returning `404` for `/internal/browser/`. In the internal server, add `resolver 127.0.0.11 ipv6=off;` and a `map` that resolves `dad`, `wife`, and `shared-1` to `worker-dad:6080`, `worker-wife:6080`, and `worker-shared-1:6080`. Add an internal-only regex location for `/internal/browser/<workerId>/<browserPath>` that sets `proxy_set_header X-Browser-Worker-Id $worker_id`, `proxy_set_header X-Browser-Session-Id $arg_sessionId`, `proxy_set_header X-Browser-Access-Token $arg_accessToken`, performs `auth_request /internal/browser-access/authorize`, forwards websocket upgrade headers, and proxies to `http://$browser_upstream/$browser_path$is_args$args`. Extend `services/control-api/test/edge-config.test.ts` so it asserts the public `404`, the `resolver 127.0.0.11` line, the `auth_request /internal/browser-access/authorize` line, and the worker upstream mapping for `worker-dad:6080`, `worker-wife:6080`, and `worker-shared-1:6080`.</action>
  <verify>rg -n "/internal/browser/|auth_request /internal/browser-access/authorize|resolver 127.0.0.11|worker-dad:6080|worker-wife:6080|worker-shared-1:6080" infra/nginx/default.conf.template services/control-api/test/edge-config.test.ts</verify>
  <acceptance_criteria>
    - infra/nginx/default.conf.template contains "auth_request /internal/browser-access/authorize"
    - infra/nginx/default.conf.template contains "resolver 127.0.0.11"
    - infra/nginx/default.conf.template contains "worker-dad:6080"
    - infra/nginx/default.conf.template contains "worker-wife:6080"
    - infra/nginx/default.conf.template contains "worker-shared-1:6080"
    - services/control-api/test/edge-config.test.ts contains "/internal/browser/"
  </acceptance_criteria>
  <done>The internal edge can proxy browser viewer traffic only for authorized operator browser access sessions and keeps the public edge closed.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Browser access session lifecycle is test-covered
- [ ] Authorization failures return `403`
- [ ] Public edge blocks `/internal/browser/*`
- [ ] Internal edge contains the protected browser viewer proxy contract
</verification>

<success_criteria>

- All tasks completed
- Browser viewer traffic is time-bounded and worker-scoped
- Internal edge authorizes each browser viewer request before proxying
- Public edge cannot reach browser access routes
</success_criteria>

<output>
After completion, create the matching SUMMARY document in this phase directory.
</output>

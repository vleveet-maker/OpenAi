---
phase: 05.1-internal-browser-access-for-manual-chatgpt-login-and-reauthentication
plan: 03
type: execute
wave: 3
depends_on: ["05.1-01", "05.1-02"]
files_modified:
  - services/control-api/src/routes/internal-admin-page.ts
  - docs/internal-worker-ops.md
  - services/control-api/test/internal-admin-page.test.ts
autonomous: true
requirements: [ADMN-04, SECU-03]
user_setup: []
must_haves:
    truths:
      - "Operator can start browser access, open the viewer, and complete or cancel login or reauth from the internal admin surface."
      - "The admin page shows current browser access state per worker without exposing the viewer on the public edge."
      - "Operator docs match the real internal URLs and manual flow."
    artifacts:
      - path: "services/control-api/src/routes/internal-admin-page.ts"
        provides: "Admin UI controls for browser access"
        contains: "Open browser"
      - path: "docs/internal-worker-ops.md"
        provides: "Operator runbook for first login and reauth"
        contains: "browser access"
      - path: "services/control-api/test/internal-admin-page.test.ts"
        provides: "Admin UI control coverage"
        contains: "Open browser"
---

<objective>
Expose browser access controls in the internal admin UI and document the manual login or reauth workflow end to end.
</objective>

<execution_context>
@../gsd/get-shit-done/workflows/execute-plan.md
@../gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-internal-browser-access-for-manual-chatgpt-login-and-reauthentication/05.1-CONTEXT.md
@.planning/phases/05.1-internal-browser-access-for-manual-chatgpt-login-and-reauthentication/05.1-RESEARCH.md
@services/control-api/src/routes/internal-admin-page.ts
@docs/internal-worker-ops.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add browser access controls to the internal admin page</name>
  <files>services/control-api/src/routes/internal-admin-page.ts, services/control-api/test/internal-admin-page.test.ts</files>
  <read_first>services/control-api/src/routes/internal-admin-page.ts, services/control-api/src/routes/internal-workers.ts, services/control-api/src/routes/internal-observability.ts, services/control-api/src/routes/internal-browser-access.ts, .planning/phases/05.1-internal-browser-access-for-manual-chatgpt-login-and-reauthentication/05.1-CONTEXT.md</read_first>
  <action>Update `services/control-api/src/routes/internal-admin-page.ts` so the worker summary section becomes actionable per worker. For each worker card, render the current worker status, current browser access status if present, and buttons labeled exactly `Open browser`, `Start reauth`, `Cancel access`, and `Complete login/reauth` when those actions are valid. Use `fetch()` calls to `POST /internal/workers/:id/browser-access/start`, `POST /internal/workers/:id/browser-access/cancel`, `POST /internal/workers/:id/browser-access/complete`, and `POST /internal/workers/:id/reauth/start`, and open the returned `viewerPath` with `window.open(viewerPath, "_blank", "noopener")`. Keep the existing recent failures and recent lifecycle events sections, and continue polling every 5 seconds so the action state refreshes automatically. Add `services/control-api/test/internal-admin-page.test.ts` that asserts the HTML contains `Open browser`, `Start reauth`, `Complete login/reauth`, `/internal/workers/`, and `/internal/observability/summary`.</action>
  <verify>rg -n "Open browser|Start reauth|Complete login/reauth|browser-access|window.open|/internal/observability/summary" services/control-api/src/routes/internal-admin-page.ts services/control-api/test/internal-admin-page.test.ts</verify>
  <acceptance_criteria>
    - services/control-api/src/routes/internal-admin-page.ts contains "Open browser"
    - services/control-api/src/routes/internal-admin-page.ts contains "Complete login/reauth"
    - services/control-api/src/routes/internal-admin-page.ts contains "window.open"
    - services/control-api/test/internal-admin-page.test.ts contains "Open browser"
    - services/control-api/test/internal-admin-page.test.ts contains "/internal/workers/"
  </acceptance_criteria>
  <done>The operator can trigger and manage browser access directly from the internal admin page.</done>
</task>

<task type="auto">
  <name>Task 2: Update the operator runbook for first login and reauth through the new browser access path</name>
  <files>docs/internal-worker-ops.md</files>
  <read_first>docs/internal-worker-ops.md, services/control-api/src/routes/internal-browser-access.ts, services/control-api/src/routes/internal-admin-page.ts, infra/nginx/default.conf.template</read_first>
  <action>Rewrite `docs/internal-worker-ops.md` so the setup, first login, and reauthentication sections describe the exact browser access workflow: open `/internal/admin`, start browser access for a worker, launch the viewer path under `/internal/browser/<workerId>/vnc.html`, complete ChatGPT login manually, and then press `Complete login/reauth` so the worker returns to `ready`. Add a troubleshooting note that browser access sessions expire after 15 minutes and that `http://<host>:8080/internal/browser/...` must remain unavailable while `http://127.0.0.1:8081/internal/browser/...` is internal-only. Document a final smoke checklist that includes `healthz`, `readyz`, opening the viewer, and confirming the worker returns to `ready` after completion.</action>
  <verify>rg -n "browser access|/internal/admin|/internal/browser/|Complete login/reauth|15 minutes|readyz" docs/internal-worker-ops.md</verify>
  <acceptance_criteria>
    - docs/internal-worker-ops.md contains "browser access"
    - docs/internal-worker-ops.md contains "/internal/admin"
    - docs/internal-worker-ops.md contains "/internal/browser/"
    - docs/internal-worker-ops.md contains "Complete login/reauth"
    - docs/internal-worker-ops.md contains "15 minutes"
    - docs/internal-worker-ops.md contains "readyz"
  </acceptance_criteria>
  <done>The household operator has an accurate runbook for first login and reauthentication using the new internal browser access flow.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Internal admin page exposes browser access actions and still renders observability sections
- [ ] Admin page tests cover the new controls
- [ ] Operator docs match the actual internal endpoints and completion flow
</verification>

<success_criteria>

- All tasks completed
- Operator can start, open, complete, and cancel browser access from internal admin
- Internal browser access workflow is documented end to end
- Manual smoke steps are ready for a real ChatGPT login or challenge flow
</success_criteria>

<output>
After completion, create the matching SUMMARY document in this phase directory.
</output>

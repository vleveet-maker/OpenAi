---
phase: 17-deployed-windows-post-stabilization-degraded-runtime-and-repeated-public-canary-502-investigation
plan: 02
type: execute
wave: 2
depends_on: [17-01-PLAN.md]
files_modified:
  - services/control-api/src/
  - services/control-api/dist/
  - services/control-api/test/
autonomous: true
requirements: [WRTI-02, WRTI-03]
user_setup: []
---

<objective>
Expose the latest post-stabilization runtime investigation result in the real `control-api` runtime so operators can compare the deployed-host artifact against the same file-backed surface they already trust.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-deployed-windows-post-stabilization-degraded-runtime-and-repeated-public-canary-502-investigation/17-RESEARCH.md
@.planning/phases/17-deployed-windows-post-stabilization-degraded-runtime-and-repeated-public-canary-502-investigation/17-VALIDATION.md
@services/control-api/src/config.ts
@services/control-api/src/server.ts
@services/control-api/src/routes/internal-admin-page.ts
@services/control-api/src/routes/read-latest-state.ts
@services/control-api/test/internal-admin-page.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a file-backed latest post-stabilization runtime-investigation route to control-api and regenerate dist runtime files</name>
  <files>services/control-api/src/config.ts, services/control-api/src/server.ts, services/control-api/src/routes/internal-post-stabilization-runtime-investigation.ts, services/control-api/dist/, services/control-api/test/</files>
  <read_first>services/control-api/src/config.ts, services/control-api/src/server.ts, services/control-api/src/routes/read-latest-state.ts, services/control-api/src/routes/internal-post-remediation-degraded-smoke.ts, services/control-api/test/internal-post-remediation-degraded-smoke.test.ts</read_first>
  <action>Add a new config field `postStabilizationRuntimeInvestigationStatePath` sourced from `POST_STABILIZATION_RUNTIME_INVESTIGATION_STATE_PATH` with default repo path `infra/data/post-stabilization-runtime-investigation/latest.json`. Create `services/control-api/src/routes/internal-post-stabilization-runtime-investigation.ts` with `GET /internal/post-stabilization-runtime-investigation/latest` that returns `{ latest: null }` when the file is missing and returns the parsed latest runtime-investigation payload when it exists. Reuse `read-latest-state.ts` so BOM-prefixed `latest.json` still parses cleanly. Register the route behind the internal admin guard in `server.ts`, add tests that prove both the empty state and the populated state, and regenerate the runtime files the deployed host actually starts by running `npm.cmd --prefix services/control-api run build` so `services/control-api/dist/config.js`, `services/control-api/dist/server.js`, and `services/control-api/dist/routes/internal-post-stabilization-runtime-investigation.js` contain the same route/config contract.</action>
  <verify>rg -n "POST_STABILIZATION_RUNTIME_INVESTIGATION_STATE_PATH|postStabilizationRuntimeInvestigationStatePath|/internal/post-stabilization-runtime-investigation/latest|latest: null|preInvestigationReadyCount|postCanaryReadyCount|finalReadyCount|dominantRuntimeBlocker|firstFailingHop|verdict" services/control-api/src services/control-api/dist services/control-api/test</verify>
  <acceptance_criteria>
    - `services/control-api/src/routes/internal-post-stabilization-runtime-investigation.ts` exists
    - `config.ts` supports `POST_STABILIZATION_RUNTIME_INVESTIGATION_STATE_PATH`
    - `server.ts` registers `/internal/post-stabilization-runtime-investigation/latest`
    - tests prove the route returns `{ latest: null }` when no runtime-investigation result exists
    - the route reuses BOM-safe latest-state parsing
    - `services/control-api/dist/config.js`, `services/control-api/dist/server.js`, and `services/control-api/dist/routes/internal-post-stabilization-runtime-investigation.js` contain the new route/config contract after build
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Render the latest runtime investigation result on the internal admin page</name>
  <files>services/control-api/src/routes/internal-admin-page.ts, services/control-api/dist/routes/internal-admin-page.js, services/control-api/test/internal-admin-page.test.ts</files>
  <read_first>services/control-api/src/routes/internal-admin-page.ts, services/control-api/test/internal-admin-page.test.ts, services/control-api/src/routes/internal-post-stabilization-runtime-investigation.ts</read_first>
  <action>Add a dedicated `Latest post-stabilization runtime investigation` section to `/internal/admin`. Make the page fetch `/internal/post-stabilization-runtime-investigation/latest`, render an empty state `No post-stabilization runtime investigation captured yet.` when there is no file, and render the latest timestamp, pre-investigation ready count, post-canary ready count, final ready count, dominant runtime blocker, first failing hop, compatibility version, and final verdict when the file exists. Keep the existing worker summary, latest rollout smoke, latest readiness recovery, latest post-recovery smoke regression, latest zero-ready root cause, latest disconnected baseline remediation, and latest post-remediation degraded smoke stabilization sections intact. Regenerate `services/control-api/dist/routes/internal-admin-page.js` through the build output so the deployed host serves the same admin page from `dist`.</action>
  <verify>rg -n "Latest post-stabilization runtime investigation|No post-stabilization runtime investigation captured yet.|/internal/post-stabilization-runtime-investigation/latest|pre-investigation ready count|post-canary ready count|final ready count|dominant runtime blocker|first failing hop|Compatibility|verdict" services/control-api/src/routes/internal-admin-page.ts services/control-api/dist/routes/internal-admin-page.js services/control-api/test/internal-admin-page.test.ts</verify>
  <acceptance_criteria>
    - the admin page contains the text `Latest post-stabilization runtime investigation`
    - the admin page fetches `/internal/post-stabilization-runtime-investigation/latest`
    - the admin page has an explicit empty state `No post-stabilization runtime investigation captured yet.`
    - the populated state renders pre-investigation ready count, post-canary ready count, final ready count, dominant runtime blocker, first failing hop, compatibility version, and verdict
    - the existing worker summary, latest rollout smoke, latest readiness recovery, latest post-recovery smoke regression, latest zero-ready root cause, latest disconnected baseline remediation, and latest post-remediation degraded smoke stabilization sections remain present
  </acceptance_criteria>
</task>

</tasks>

<success_criteria>

- the latest post-stabilization runtime investigation result is visible from the same runtime files the deployed host actually serves
- no operator has to reconstruct the repeated `502` branch from mixed terminal output and older stabilization notes

</success_criteria>

---
phase: 07-proxy-backed-on-demand-host-worker-pool
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/proxy/install-sing-box.ps1
  - infra/proxy/start-sing-box.ps1
  - infra/proxy/build-sing-box-config.mjs
  - infra/host-worker/start-proxied-host-pool.ps1
  - infra/host-worker/stop-proxied-host-pool.ps1
  - infra/host-worker/start-host-native-worker.ps1
  - infra/docker-compose.host-native.yml
  - docs/host-native-worker.md
autonomous: true
requirements: [HOST-01, HOST-02, HOST-03, PROXY-01, PROXY-02, PROXY-03, LIVE-01, LIVE-02, SECU-04]
user_setup: []
must_haves:
    truths:
      - "Several proxy share links are parsed into a local sing-box mixed proxy config that can prefer a healthy outbound automatically."
      - "The three host-native workers can be started only when needed and stopped again cleanly."
      - "`control-api` keeps seeing the host-native workers through the normal worker contract while the proxied pool is active."
    artifacts:
      - path: "infra/proxy/build-sing-box-config.mjs"
        provides: "runtime sing-box config generation from local share links"
        contains: "type: \"urltest\""
      - path: "infra/host-worker/start-proxied-host-pool.ps1"
        provides: "one-command start path for the proxied host-native household pool"
        contains: "start-sing-box.ps1"
      - path: "infra/host-worker/stop-proxied-host-pool.ps1"
        provides: "one-command stop path for the proxied host-native household pool"
        contains: "stop-host-native-worker.ps1"
      - path: "docs/host-native-worker.md"
        provides: "operator runbook for the verified on-demand proxy-backed path"
        contains: "start-proxied-host-pool.ps1"
---

<objective>
Build and verify a proxy-backed on-demand host-native household worker pool that can replace the always-open browser fallback.
</objective>

<execution_context>
@../gsd/get-shit-done/workflows/execute-plan.md
@../gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-proxy-backed-on-demand-host-worker-pool/07-CONTEXT.md
@infra/host-worker/start-proxied-host-pool.ps1
@infra/proxy/build-sing-box-config.mjs
@docs/host-native-worker.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the local proxy layer and on-demand host-native worker pool</name>
  <files>infra/proxy/install-sing-box.ps1, infra/proxy/start-sing-box.ps1, infra/proxy/build-sing-box-config.mjs, infra/host-worker/start-proxied-host-pool.ps1, infra/host-worker/stop-proxied-host-pool.ps1, infra/host-worker/start-host-native-worker.ps1, infra/docker-compose.host-native.yml, docs/host-native-worker.md</files>
  <read_first>infra/docker-compose.host-native.yml, infra/host-worker/start-host-native-worker.ps1, docs/host-native-worker.md</read_first>
  <action>Implement a local sing-box mixed proxy that is generated from several share links stored in ignored local config, then add one-command start and stop scripts for the three host-native workers so the household pool only launches when needed. Keep the existing `control-api` worker contract intact, verify that the proxied pool reports `ready` workers through the normal admin surfaces, and record the verified operator path in the runbook.</action>
  <verify>rg -n "urltest|start-sing-box.ps1|stop-host-native-worker.ps1|start-proxied-host-pool.ps1|share-links.local.json" infra/proxy infra/host-worker docs/host-native-worker.md</verify>
  <acceptance_criteria>
    - `infra/proxy/build-sing-box-config.mjs` contains `type: "urltest"`
    - `infra/host-worker/start-proxied-host-pool.ps1` starts sing-box and all three workers
    - `infra/host-worker/stop-proxied-host-pool.ps1` stops all three workers and the local proxy process
    - live `GET /internal/workers` reports `dad`, `wife`, and `shared-1` as `ready` while the pool is running
    - at least two different workers complete a live proxied relay smoke through the pool
  </acceptance_criteria>
  <done>The household operator can treat proxied host-native workers as an on-demand pool instead of leaving native browsers open full-time.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `services/host-controller` tests pass
- [ ] `services/control-api` tests pass
- [ ] `services/control-api` build passes
- [ ] Live proxied pool start shows all three workers as `ready`
- [ ] Live proxied pool stop returns the workers to `disconnected`
</verification>

<success_criteria>

- All tasks completed
- The proxy-backed host-native pool can be started and stopped on demand
- The household relay system still works against the proxied pool without changing the existing session model

</success_criteria>

<output>
After completion, create the matching SUMMARY document in this phase directory.
</output>

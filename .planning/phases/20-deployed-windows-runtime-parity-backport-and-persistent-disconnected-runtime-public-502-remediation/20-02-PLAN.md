---
phase: 20-deployed-windows-runtime-parity-backport-and-persistent-disconnected-runtime-public-502-remediation
plan: 02
type: execute
wave: 2
depends_on: [20-01-PLAN.md]
files_modified:
  - services/control-api/src/
  - services/control-api/dist/
  - services/control-api/test/
autonomous: true
requirements: [WPARB-01, WPARB-03]
user_setup: []
---

<objective>
Expose the latest Phase 20 runtime parity backport remediation result in the real `control-api` runtime so operators can compare the deployed-host artifact against the same file-backed surface they already trust.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-deployed-windows-runtime-parity-backport-and-persistent-disconnected-runtime-public-502-remediation/20-RESEARCH.md
@.planning/phases/20-deployed-windows-runtime-parity-backport-and-persistent-disconnected-runtime-public-502-remediation/20-VALIDATION.md
@services/control-api/src/config.ts
@services/control-api/src/server.ts
@services/control-api/src/routes/internal-admin-page.ts
@services/control-api/src/routes/read-latest-state.ts
@services/control-api/test/internal-admin-page.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a file-backed latest runtime parity backport remediation route to control-api and regenerate dist runtime files</name>
  <files>services/control-api/src/config.ts, services/control-api/src/server.ts, services/control-api/src/routes/internal-runtime-parity-backport-remediation.ts, services/control-api/dist/, services/control-api/test/</files>
  <read_first>services/control-api/src/config.ts, services/control-api/src/server.ts, services/control-api/src/routes/read-latest-state.ts, services/control-api/src/routes/internal-persistent-disconnected-runtime-followup.ts, services/control-api/test/internal-persistent-disconnected-runtime-followup.test.ts</read_first>
  <action>Add a new config field `runtimeParityBackportRemediationStatePath` sourced from `RUNTIME_PARITY_BACKPORT_REMEDIATION_STATE_PATH` with default repo path `infra/data/runtime-parity-backport-remediation/latest.json`. Create `services/control-api/src/routes/internal-runtime-parity-backport-remediation.ts` with `GET /internal/runtime-parity-backport-remediation/latest` that returns `{ latest: null }` when the file is missing and returns the parsed latest remediation payload when it exists. Reuse `read-latest-state.ts` so BOM-prefixed `latest.json` still parses cleanly. Register the route behind the internal admin guard in `server.ts`, add tests that prove both the empty state and the populated state, and regenerate the runtime files the deployed host actually starts by running `npm.cmd --prefix services/control-api run build` so `services/control-api/dist/config.js`, `services/control-api/dist/server.js`, and `services/control-api/dist/routes/internal-runtime-parity-backport-remediation.js` contain the same route/config contract.</action>
  <verify>rg -n "RUNTIME_PARITY_BACKPORT_REMEDIATION_STATE_PATH|runtimeParityBackportRemediationStatePath|/internal/runtime-parity-backport-remediation/latest|latest: null|parityBackportFiles|preRemediationReadyCount|postRemediationReadyCount|dominantRuntimeBlocker|firstFailingHop|forcedHostStatus|publicOwnerStatus|verdict" services/control-api/src services/control-api/dist services/control-api/test</verify>
  <acceptance_criteria>
    - `services/control-api/src/routes/internal-runtime-parity-backport-remediation.ts` exists
    - `config.ts` supports `RUNTIME_PARITY_BACKPORT_REMEDIATION_STATE_PATH`
    - `server.ts` registers `/internal/runtime-parity-backport-remediation/latest`
    - tests prove the route returns `{ latest: null }` when no remediation result exists
    - the route reuses BOM-safe latest-state parsing
    - `services/control-api/dist/config.js`, `services/control-api/dist/server.js`, and `services/control-api/dist/routes/internal-runtime-parity-backport-remediation.js` contain the new route/config contract after build
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Render the latest runtime parity backport remediation result on the internal admin page</name>
  <files>services/control-api/src/routes/internal-admin-page.ts, services/control-api/dist/routes/internal-admin-page.js, services/control-api/test/internal-admin-page.test.ts</files>
  <read_first>services/control-api/src/routes/internal-admin-page.ts, services/control-api/test/internal-admin-page.test.ts, services/control-api/src/routes/internal-runtime-parity-backport-remediation.ts</read_first>
  <action>Add a dedicated `Latest runtime parity backport remediation` section to `/internal/admin`. Make the page fetch `/internal/runtime-parity-backport-remediation/latest`, render an empty state `No runtime parity backport remediation captured yet.` when there is no file, and render the latest timestamp, pre-remediation ready count, post-remediation ready count, dominant runtime blocker, first failing hop, forced-host status, public-owner status, compatibility version, parity-backport files, and final verdict when the file exists. Keep the existing worker summary and current latest-result sections intact. Regenerate `services/control-api/dist/routes/internal-admin-page.js` through the build output so the deployed host serves the same admin page from `dist`.</action>
  <verify>rg -n "Latest runtime parity backport remediation|No runtime parity backport remediation captured yet.|/internal/runtime-parity-backport-remediation/latest|pre-remediation ready count|post-remediation ready count|dominant runtime blocker|first failing hop|forced-host status|public-owner status|parity-backport files|Compatibility|verdict" services/control-api/src/routes/internal-admin-page.ts services/control-api/dist/routes/internal-admin-page.js services/control-api/test/internal-admin-page.test.ts</verify>
  <acceptance_criteria>
    - the admin page contains the text `Latest runtime parity backport remediation`
    - the admin page fetches `/internal/runtime-parity-backport-remediation/latest`
    - the admin page has an explicit empty state `No runtime parity backport remediation captured yet.`
    - the populated state renders pre-remediation ready count, post-remediation ready count, dominant runtime blocker, first failing hop, forced-host status, public-owner status, parity-backport files, compatibility version, and verdict
    - the existing worker summary and current latest-result sections remain present
  </acceptance_criteria>
</task>

</tasks>

<success_criteria>

- the latest runtime parity backport remediation result is visible from the same runtime files the deployed host actually serves
- no operator has to reconstruct the parity-clean remediation truth from mixed terminal output and older follow-up notes

</success_criteria>

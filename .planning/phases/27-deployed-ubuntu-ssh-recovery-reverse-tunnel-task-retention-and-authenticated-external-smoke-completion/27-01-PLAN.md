---
phase: 27-deployed-ubuntu-ssh-recovery-reverse-tunnel-task-retention-and-authenticated-external-smoke-completion
plan: 01
type: execute
wave: 1
depends_on: [26-VERIFICATION.md]
files_modified:
  - .planning/phases/27-deployed-ubuntu-ssh-recovery-reverse-tunnel-task-retention-and-authenticated-external-smoke-completion/
  - docs/windows-browser-block.md
  - docs/windows-public-api-block.md
  - docs/remote-relay-server.md
  - docs/internal-worker-ops.md
  - infra/windows-block/
  - infra/data/post-phase26-ubuntu-ssh-recovery/
autonomous: true
requirements: [URTS-01, URTS-02]
user_setup: []
---

<objective>
Add the canonical repo-backed Phase 27 recovery wrapper and GitHub-first deployed-host handoff so Ubuntu SSH recovery, reverse-tunnel retention, and authenticated external smoke can be executed from tracked assets instead of one-off operator memory.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-deployed-ubuntu-sync-recovery-reverse-tunnel-task-retention-and-external-authenticated-smoke-restoration/26-VERIFICATION.md
@.planning/phases/27-deployed-ubuntu-ssh-recovery-reverse-tunnel-task-retention-and-authenticated-external-smoke-completion/27-RESEARCH.md
@docs/windows-browser-block.md
@docs/windows-public-api-block.md
@docs/remote-relay-server.md
@docs/internal-worker-ops.md
@infra/windows-block/recover-ubuntu-sync-and-external-auth-smoke.ps1
@infra/windows-block/start-reverse-tunnels.ps1
@infra/windows-block/register-browser-block-tasks.ps1
@infra/windows-block/probe-public-api.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the canonical Phase 27 wrapper with exact Ubuntu SSH and reverse-tunnel retention truth</name>
  <files>infra/windows-block/recover-ubuntu-ssh-reverse-tunnels-and-authenticated-smoke.ps1, infra/data/post-phase26-ubuntu-ssh-recovery/</files>
  <read_first>infra/windows-block/recover-ubuntu-sync-and-external-auth-smoke.ps1, infra/windows-block/start-reverse-tunnels.ps1, infra/windows-block/register-browser-block-tasks.ps1, .planning/phases/27-deployed-ubuntu-ssh-recovery-reverse-tunnel-task-retention-and-authenticated-external-smoke-completion/27-RESEARCH.md</read_first>
  <action>Create `infra/windows-block/recover-ubuntu-ssh-reverse-tunnels-and-authenticated-smoke.ps1` as the canonical Phase 27 wrapper. It must accept concrete parameters for `-RemoteHost`, `-RemotePort`, `-RemoteUser`, `-SshKeyPath`, `-PublicBaseUrl`, `-BearerToken`, `-BearerTokenEnvVar`, `-InternalBaseUrl`, `-HostControllerBaseUrl`, `-OutputJsonPath`, and `-OutputMarkdownPath`. The wrapper must record exact Ubuntu SSH reachability, classify SSH blocker shape when Ubuntu is still unreachable, record the exact Ubuntu repo path and commit hash when reachable, record `nginx -t` and `nginx -T` truth, record Ubuntu local `127.0.0.1:4010` truth, observe reverse-tunnel task retention across a settle window, record listener truth for `14021..14027` and `14040`, record ready-worker truth, record authenticated external smoke for `/healthz`, `/v1/models`, and `/v1/chat/completions`, and constrain the verdict to exactly `externally_ready` or `hold_rollout`. It must write `27-SSH-RECOVERY-SUMMARY.json`, `27-SSH-RECOVERY-SUMMARY.md`, and `infra/data/post-phase26-ubuntu-ssh-recovery/latest.json`. Stamp the chain with `phase27-ubuntu-ssh-recovery-v1`.</action>
  <verify>rg -n "phase27-ubuntu-ssh-recovery-v1|sshFailureKind|ubuntuRepoPath|ubuntuRepoCommit|reverseTunnelTaskStatus|ubuntuTunnelListenerStatus|authenticatedSmokeTokenSource|externally_ready|hold_rollout|post-phase26-ubuntu-ssh-recovery" infra/windows-block/recover-ubuntu-ssh-reverse-tunnels-and-authenticated-smoke.ps1</verify>
  <acceptance_criteria>
    - `infra/windows-block/recover-ubuntu-ssh-reverse-tunnels-and-authenticated-smoke.ps1` exists
    - the wrapper records exact SSH truth, exact Ubuntu repo path/hash truth, tunnel-retention truth, listener truth, and authenticated smoke truth
    - the wrapper writes both Phase 27 artifacts and `infra/data/post-phase26-ubuntu-ssh-recovery/latest.json`
    - the wrapper verdict is constrained to `externally_ready` or `hold_rollout`
    - the deployment-critical chain contains `phase27-ubuntu-ssh-recovery-v1`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add a GitHub-first deployed-host prompt and narrow docs for the real Phase 27 execution contract</name>
  <files>.planning/phases/27-deployed-ubuntu-ssh-recovery-reverse-tunnel-task-retention-and-authenticated-external-smoke-completion/27-DEPLOYED-HOST-AGENT-TASK.md, docs/windows-browser-block.md, docs/windows-public-api-block.md, docs/remote-relay-server.md, docs/internal-worker-ops.md</files>
  <read_first>.planning/phases/26-deployed-ubuntu-sync-recovery-reverse-tunnel-task-retention-and-external-authenticated-smoke-restoration/26-DEPLOYED-HOST-AGENT-TASK.md, .planning/phases/27-deployed-ubuntu-ssh-recovery-reverse-tunnel-task-retention-and-authenticated-external-smoke-completion/27-RESEARCH.md, docs/windows-browser-block.md, docs/remote-relay-server.md</read_first>
  <action>Create `27-DEPLOYED-HOST-AGENT-TASK.md` in Russian with copy-paste-friendly instructions. It must point only to GitHub branch `windows-browser-block-api-20260331`, tell both Windows and Ubuntu to `git fetch`, `git checkout`, and `git pull`, explicitly require reporting the exact Ubuntu repo path and hash, explicitly require reporting whether the reverse-tunnel task stayed `Running`, and explicitly say that final authenticated smoke may run from whichever host actually has the bearer token while still targeting public `77.66.186.75`. Update the operator docs to match the same truth and stop implying that local-only archives or Windows-only token availability are acceptable defaults.</action>
  <verify>rg -n "windows-browser-block-api-20260331|git fetch origin|git checkout windows-browser-block-api-20260331|git pull --ff-only origin windows-browser-block-api-20260331|Ubuntu repo path|reverse-tunnel task|77.66.186.75|bearer token|GitHub" .planning/phases/27-deployed-ubuntu-ssh-recovery-reverse-tunnel-task-retention-and-authenticated-external-smoke-completion/27-DEPLOYED-HOST-AGENT-TASK.md docs</verify>
  <acceptance_criteria>
    - `27-DEPLOYED-HOST-AGENT-TASK.md` exists
    - the prompt uses GitHub branch `windows-browser-block-api-20260331` as the only source of truth
    - the prompt tells Ubuntu and Windows to report exact repo path/hash truth
    - the prompt makes reverse-tunnel retention and token-host choice explicit
    - the docs match the same GitHub-first execution contract
  </acceptance_criteria>
</task>

</tasks>

<success_criteria>

- operators have one canonical Phase 27 wrapper instead of another ad-hoc SSH plus smoke chain
- deployed-host handoff stays GitHub-first and copy-paste friendly
- the next live run can distinguish SSH failure, tunnel-retention failure, and token-host failure without inventing a new architecture branch

</success_criteria>

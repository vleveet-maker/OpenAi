---
phase: 27-deployed-ubuntu-ssh-recovery-reverse-tunnel-task-retention-and-authenticated-external-smoke-completion
plan: 02
type: execute
wave: 2
depends_on: [27-01-PLAN.md]
files_modified:
  - services/control-api/src/config.ts
  - services/control-api/src/server.ts
  - services/control-api/src/routes/internal-admin-page.ts
  - services/control-api/src/routes/internal-post-phase26-ubuntu-ssh-recovery.ts
  - services/control-api/test/internal-admin-page.test.ts
  - services/control-api/test/internal-post-phase26-ubuntu-ssh-recovery.test.ts
  - services/control-api/dist/
autonomous: true
requirements: [URTS-03]
user_setup: []
---

<objective>
Expose the latest Phase 27 Ubuntu SSH recovery result through `control-api` and `/internal/admin` so operators can verify the exact tracked artifact before claiming external readiness.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-deployed-ubuntu-ssh-recovery-reverse-tunnel-task-retention-and-authenticated-external-smoke-completion/27-RESEARCH.md
@.planning/phases/27-deployed-ubuntu-ssh-recovery-reverse-tunnel-task-retention-and-authenticated-external-smoke-completion/27-01-PLAN.md
@services/control-api/src/config.ts
@services/control-api/src/server.ts
@services/control-api/src/routes/internal-admin-page.ts
@services/control-api/src/routes/read-latest-state.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a file-backed latest Phase 27 Ubuntu SSH recovery route</name>
  <files>services/control-api/src/config.ts, services/control-api/src/server.ts, services/control-api/src/routes/internal-post-phase26-ubuntu-ssh-recovery.ts, services/control-api/test/internal-post-phase26-ubuntu-ssh-recovery.test.ts</files>
  <read_first>services/control-api/src/config.ts, services/control-api/src/server.ts, services/control-api/src/routes/read-latest-state.ts, services/control-api/test/internal-post-phase25-external-restoration.test.ts</read_first>
  <action>Add config support for `POST_PHASE26_UBUNTU_SSH_RECOVERY_STATE_PATH` with default path `infra/data/post-phase26-ubuntu-ssh-recovery/latest.json`. Create `services/control-api/src/routes/internal-post-phase26-ubuntu-ssh-recovery.ts` that serves `GET /internal/post-phase26-ubuntu-ssh-recovery/latest` using the shared latest-state reader and returns `{ latest: null }` when no artifact exists. Wire the route into `server.ts` and add a focused route test that proves both the empty contract and the file-backed happy path.</action>
  <verify>npm.cmd --prefix services/control-api test -- internal-post-phase26-ubuntu-ssh-recovery.test.ts</verify>
  <acceptance_criteria>
    - `GET /internal/post-phase26-ubuntu-ssh-recovery/latest` exists
    - the route reads `infra/data/post-phase26-ubuntu-ssh-recovery/latest.json` through config
    - the route returns `{ latest: null }` when no artifact exists
    - the route returns the parsed latest artifact when the file exists
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add a matching internal-admin section for the latest Phase 27 result</name>
  <files>services/control-api/src/routes/internal-admin-page.ts, services/control-api/test/internal-admin-page.test.ts</files>
  <read_first>services/control-api/src/routes/internal-admin-page.ts, services/control-api/test/internal-admin-page.test.ts, .planning/phases/27-deployed-ubuntu-ssh-recovery-reverse-tunnel-task-retention-and-authenticated-external-smoke-completion/27-RESEARCH.md</read_first>
  <action>Render a new admin section titled `Latest post-phase26 Ubuntu SSH recovery`. The empty state must read exactly `No post-phase26 Ubuntu SSH recovery result captured yet.` The populated state must surface Ubuntu SSH truth, `sshFailureKind`, exact Ubuntu repo path/hash, canonical upstream truth, reverse-tunnel task state, tunnel listener truth, ready-worker counts, external `healthz/models/chat` truth, execution host, token source, compatibility version, and final verdict. Extend admin-page coverage so the new section is exercised and does not regress older sections.</action>
  <verify>npm.cmd --prefix services/control-api test -- internal-admin-page.test.ts</verify>
  <acceptance_criteria>
    - `/internal/admin` renders `Latest post-phase26 Ubuntu SSH recovery`
    - the empty state text matches exactly
    - the populated state surfaces Ubuntu SSH, repo path/hash, tunnel retention, listener truth, external smoke truth, token source, compatibility version, and verdict
    - admin-page tests cover the new section
  </acceptance_criteria>
</task>

</tasks>

<success_criteria>

- operators can confirm the latest Phase 27 artifact from a stable route and from `/internal/admin`
- the operator surface exposes SSH failure, tunnel retention, and authenticated smoke truth separately
- Phase 27 live execution can reference one durable latest-state contract instead of raw terminal output alone

</success_criteria>

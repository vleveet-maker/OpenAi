---
phase: 27-deployed-ubuntu-ssh-recovery-reverse-tunnel-task-retention-and-authenticated-external-smoke-completion
plan: 03
type: execute
wave: 3
depends_on: [27-01-PLAN.md, 27-02-PLAN.md]
files_modified:
  - .planning/phases/27-deployed-ubuntu-ssh-recovery-reverse-tunnel-task-retention-and-authenticated-external-smoke-completion/
  - infra/data/post-phase26-ubuntu-ssh-recovery/
autonomous: false
requirements: [URTS-01, URTS-02, URTS-03, URTS-04]
user_setup:
  - SSH access to Ubuntu `77.66.186.75` as `mi50` on port `2222`
  - Admin access to Windows host `192.168.88.250`
  - A valid bearer token on at least one execution host
---

<objective>
Run one repo-backed live recovery pass across Ubuntu and Windows, prove reverse-tunnel retention plus the operator surface, and finish authenticated external smoke with one explicit verdict `externally_ready` or `hold_rollout`.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-deployed-ubuntu-sync-recovery-reverse-tunnel-task-retention-and-external-authenticated-smoke-restoration/26-VERIFICATION.md
@.planning/phases/27-deployed-ubuntu-ssh-recovery-reverse-tunnel-task-retention-and-authenticated-external-smoke-completion/27-RESEARCH.md
@.planning/phases/27-deployed-ubuntu-ssh-recovery-reverse-tunnel-task-retention-and-authenticated-external-smoke-completion/27-VALIDATION.md
@.planning/phases/27-deployed-ubuntu-ssh-recovery-reverse-tunnel-task-retention-and-authenticated-external-smoke-completion/27-DEPLOYED-HOST-AGENT-TASK.md
@infra/windows-block/recover-ubuntu-ssh-reverse-tunnels-and-authenticated-smoke.ps1
</context>

<tasks>

<task type="manual">
  <name>Task 1: Re-establish repo-backed Ubuntu truth on the live hosts</name>
  <files>.planning/phases/27-deployed-ubuntu-ssh-recovery-reverse-tunnel-task-retention-and-authenticated-external-smoke-completion/27-SSH-RECOVERY-SUMMARY.json, .planning/phases/27-deployed-ubuntu-ssh-recovery-reverse-tunnel-task-retention-and-authenticated-external-smoke-completion/27-SSH-RECOVERY-SUMMARY.md</files>
  <read_first>.planning/phases/27-deployed-ubuntu-ssh-recovery-reverse-tunnel-task-retention-and-authenticated-external-smoke-completion/27-DEPLOYED-HOST-AGENT-TASK.md</read_first>
  <action>On Windows `192.168.88.250` and Ubuntu `77.66.186.75`, sync GitHub branch `windows-browser-block-api-20260331`, record the exact repo path plus commit hash on both hosts, and re-verify the canonical Ubuntu path. When Ubuntu SSH succeeds, capture `pwd`, `git rev-parse --short HEAD`, `sudo nginx -t`, `curl -i http://127.0.0.1:4010/healthz`, and `sudo nginx -T` evidence that `80/443/8080` still proxy to `127.0.0.1:4010`. When Ubuntu SSH still fails, record the exact blocker shape instead of guessing. Do not reopen the retired Windows `Caddy` branch.</action>
  <verify>operator report contains exact Ubuntu repo path, exact Ubuntu commit hash or exact SSH blocker classification, and canonical `nginx -> 127.0.0.1:4010` truth</verify>
  <acceptance_criteria>
    - Windows and Ubuntu both report exact repo path and commit hash, or Ubuntu reports an exact SSH blocker class
    - Ubuntu canonical upstream truth is explicitly confirmed or explicitly blocked
    - no new live-only topology branch is introduced
  </acceptance_criteria>
</task>

<task type="manual">
  <name>Task 2: Run the canonical Phase 27 wrapper, prove the operator surface, and finish authenticated external smoke</name>
  <files>.planning/phases/27-deployed-ubuntu-ssh-recovery-reverse-tunnel-task-retention-and-authenticated-external-smoke-completion/27-VERIFICATION.md, infra/data/post-phase26-ubuntu-ssh-recovery/latest.json</files>
  <read_first>infra/windows-block/recover-ubuntu-ssh-reverse-tunnels-and-authenticated-smoke.ps1, .planning/phases/27-deployed-ubuntu-ssh-recovery-reverse-tunnel-task-retention-and-authenticated-external-smoke-completion/27-VALIDATION.md</read_first>
  <action>Run `recover-ubuntu-ssh-reverse-tunnels-and-authenticated-smoke.ps1` from the live Windows host, confirm `GET /internal/post-phase26-ubuntu-ssh-recovery/latest` or the matching `/internal/admin` section, and finish authenticated smoke against `http://77.66.186.75` from whichever host actually has the bearer token. The wrapper output must explicitly state whether the reverse-tunnel scheduled task stayed `Running`, whether listeners `14021..14027` and `14040` remained present after the settle window, what host provided the token, and whether `/healthz`, `/v1/models`, and `/v1/chat/completions` all passed. Only record `externally_ready` if Ubuntu SSH truth is explicit, no post-pull hotfixes were required, the tunnel task stayed `Running`, the listeners stayed present, the operator surface matched the artifact, and authenticated smoke passed. Otherwise record `hold_rollout`.</action>
  <verify>operator report includes route/admin confirmation, reverse-tunnel task retention truth, listener truth, token host, external smoke results, and final verdict</verify>
  <acceptance_criteria>
    - the wrapper writes the Phase 27 artifact and `infra/data/post-phase26-ubuntu-ssh-recovery/latest.json`
    - `/internal/post-phase26-ubuntu-ssh-recovery/latest` or `/internal/admin` matches the written artifact
    - reverse-tunnel task retention is explicitly proven or explicitly classified as failed
    - authenticated external smoke completes from a token-bearing host or produces an exact blocker
    - final verdict is exactly `externally_ready` or `hold_rollout`
  </acceptance_criteria>
</task>

</tasks>

<success_criteria>

- the live Phase 27 run distinguishes Ubuntu SSH failure, tunnel-retention failure, and authenticated-smoke failure without ambiguity
- the operator surface reflects the exact latest artifact before the final verdict is claimed
- the project ends the phase with one honest external-readiness verdict from tracked, repo-backed assets

</success_criteria>

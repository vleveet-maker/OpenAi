---
phase: 24-deployed-windows-post-phase23-exact-smoke-wrapper-compat-backport-and-persistent-disconnected-runtime-forced-host-public-owner-502-remediation
plan: 02
type: execute
wave: 2
depends_on: [24-01-PLAN.md]
files_modified:
  - services/control-api/src/
  - services/control-api/dist/
  - services/control-api/test/
autonomous: true
requirements: [WSCB-03]
user_setup: []
---

<objective>
Expose the latest Phase 24 exact smoke-wrapper compat remediation result in the real `control-api` runtime so operators can compare the deployed-host artifact against the same file-backed surface they already trust.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-deployed-windows-post-phase23-exact-smoke-wrapper-compat-backport-and-persistent-disconnected-runtime-forced-host-public-owner-502-remediation/24-RESEARCH.md
@.planning/phases/24-deployed-windows-post-phase23-exact-smoke-wrapper-compat-backport-and-persistent-disconnected-runtime-forced-host-public-owner-502-remediation/24-VALIDATION.md
@services/control-api/src/config.ts
@services/control-api/src/server.ts
@services/control-api/src/routes/internal-admin-page.ts
@services/control-api/src/routes/read-latest-state.ts
@services/control-api/test/internal-admin-page.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a file-backed latest post-Phase-23 exact smoke-wrapper compat remediation route to control-api and regenerate dist runtime files</name>
  <files>services/control-api/src/config.ts, services/control-api/src/server.ts, services/control-api/src/routes/internal-post-phase23-exact-smoke-wrapper-compat-remediation.ts, services/control-api/dist/, services/control-api/test/</files>
  <read_first>services/control-api/src/config.ts, services/control-api/src/server.ts, services/control-api/src/routes/read-latest-state.ts, services/control-api/src/routes/internal-post-phase22-smoke-wrapper-parity-remediation.ts, services/control-api/test/internal-post-phase22-smoke-wrapper-parity-remediation.test.ts</read_first>
  <action>Add a new config field `postPhase23ExactSmokeWrapperCompatRemediationStatePath` sourced from `POST_PHASE23_EXACT_SMOKE_WRAPPER_COMPAT_REMEDIATION_STATE_PATH` with default repo path `infra/data/post-phase23-exact-smoke-wrapper-compat-remediation/latest.json`. Create `services/control-api/src/routes/internal-post-phase23-exact-smoke-wrapper-compat-remediation.ts` with `GET /internal/post-phase23-exact-smoke-wrapper-compat-remediation/latest` that returns `{ latest: null }` when the file is missing and returns the parsed latest exact-compat payload when it exists. Reuse `read-latest-state.ts` so BOM-prefixed `latest.json` still parses cleanly. Register the route behind the internal admin guard in `server.ts`, add tests that prove both the empty state and the populated state, and regenerate the runtime files the deployed host actually starts by running `npm.cmd --prefix services/control-api run build` so `services/control-api/dist/config.js`, `services/control-api/dist/server.js`, and `services/control-api/dist/routes/internal-post-phase23-exact-smoke-wrapper-compat-remediation.js` contain the same route/config contract.</action>
  <verify>rg -n "POST_PHASE23_EXACT_SMOKE_WRAPPER_COMPAT_REMEDIATION_STATE_PATH|postPhase23ExactSmokeWrapperCompatRemediationStatePath|/internal/post-phase23-exact-smoke-wrapper-compat-remediation/latest|latest: null|smokeWrapperCompatStatus|preRemediationReadyCount|postRemediationReadyCount|dominantRuntimeBlocker|firstFailingHop|loopbackStatus|forcedHostStatus|publicOwnerStatus|verdict" services/control-api/src services/control-api/dist services/control-api/test</verify>
  <acceptance_criteria>
    - `services/control-api/src/routes/internal-post-phase23-exact-smoke-wrapper-compat-remediation.ts` exists
    - `config.ts` supports `POST_PHASE23_EXACT_SMOKE_WRAPPER_COMPAT_REMEDIATION_STATE_PATH`
    - `server.ts` registers `/internal/post-phase23-exact-smoke-wrapper-compat-remediation/latest`
    - tests prove the route returns `{ latest: null }` when no Phase 24 result exists
    - the route reuses BOM-safe latest-state parsing
    - `services/control-api/dist/config.js`, `services/control-api/dist/server.js`, and `services/control-api/dist/routes/internal-post-phase23-exact-smoke-wrapper-compat-remediation.js` contain the new route/config contract after build
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Render the latest post-Phase-23 exact smoke-wrapper compat remediation result on the internal admin page</name>
  <files>services/control-api/src/routes/internal-admin-page.ts, services/control-api/dist/routes/internal-admin-page.js, services/control-api/test/internal-admin-page.test.ts</files>
  <read_first>services/control-api/src/routes/internal-admin-page.ts, services/control-api/test/internal-admin-page.test.ts, services/control-api/src/routes/internal-post-phase23-exact-smoke-wrapper-compat-remediation.ts</read_first>
  <action>Add a dedicated `Latest post-phase23 exact smoke-wrapper compat remediation` section to `/internal/admin`. Make the page fetch `/internal/post-phase23-exact-smoke-wrapper-compat-remediation/latest`, render an empty state `No post-phase23 exact smoke-wrapper compat remediation captured yet.` when there is no file, and render the latest timestamp, smoke-wrapper compat status, pre-remediation ready count, post-remediation ready count, dominant runtime blocker, first failing hop, loopback status, forced-host status, public-owner status, compatibility version, and final verdict when the file exists. Keep the existing worker summary and current latest-result sections intact. Regenerate `services/control-api/dist/routes/internal-admin-page.js` through the build output so the deployed host serves the same admin page from `dist`.</action>
  <verify>rg -n "Latest post-phase23 exact smoke-wrapper compat remediation|No post-phase23 exact smoke-wrapper compat remediation captured yet.|/internal/post-phase23-exact-smoke-wrapper-compat-remediation/latest|smoke-wrapper compat status|pre-remediation ready count|post-remediation ready count|dominant runtime blocker|first failing hop|loopback status|forced-host status|public-owner status|Compatibility|verdict" services/control-api/src/routes/internal-admin-page.ts services/control-api/dist/routes/internal-admin-page.js services/control-api/test/internal-admin-page.test.ts</verify>
  <acceptance_criteria>
    - the admin page contains the text `Latest post-phase23 exact smoke-wrapper compat remediation`
    - the admin page fetches `/internal/post-phase23-exact-smoke-wrapper-compat-remediation/latest`
    - the admin page has an explicit empty state `No post-phase23 exact smoke-wrapper compat remediation captured yet.`
    - the populated state renders smoke-wrapper compat status, pre-remediation ready count, post-remediation ready count, dominant runtime blocker, first failing hop, loopback status, forced-host status, public-owner status, compatibility version, and verdict
    - the existing worker summary and current latest-result sections remain present
  </acceptance_criteria>
</task>

</tasks>

<success_criteria>

- the latest Phase 24 exact compat remediation result is visible from the same runtime files the deployed host actually serves
- no operator has to reconstruct the Phase 24 exact compat truth from mixed terminal output and stale archive notes

</success_criteria>

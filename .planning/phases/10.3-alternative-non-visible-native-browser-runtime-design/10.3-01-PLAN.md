---
phase: 10.3-alternative-non-visible-native-browser-runtime-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/host-worker/start-host-native-worker.ps1
  - infra/host-worker/start-host-worker-agent.ps1
  - infra/host-worker/stop-host-native-worker.ps1
  - infra/host-worker/start-alternate-desktop-browser.ps1
  - infra/host-worker/test-alternate-desktop-runtime.ps1
  - workers/agent/src/browser-launch.ts
  - workers/agent/src/server.ts
  - workers/agent/test/browser-launch.test.ts
  - workers/agent/test/server.test.ts
autonomous: true
requirements: [NVRT-01]
user_setup:
  - "For the final live proof in Plan 03, a household operator will still log in manually during `visible_auth` if the profile is not already valid."
must_haves:
    truths:
      - "The replacement steady-state runtime is no longer the rejected `hidden_runtime`; it is an explicit alternate-desktop native runtime."
      - "The same durable browser profile can move from visible auth to the replacement non-visible runtime without creating a second Windows identity."
      - "Routine browser windows no longer belong to the operator's main desktop path."
    artifacts:
      - path: "infra/host-worker/start-alternate-desktop-browser.ps1"
        provides: "same-session alternate-desktop browser launcher with durable metadata"
        contains: "alternate_desktop"
      - path: "workers/agent/src/server.ts"
        provides: "worker health telemetry for the new runtime class"
        contains: "host_alternate_desktop"
      - path: "infra/host-worker/test-alternate-desktop-runtime.ps1"
        provides: "canonical probe for the replacement runtime"
        contains: "alternate_desktop"
    key_links:
      - from: "infra/host-worker/start-alternate-desktop-browser.ps1"
        to: "workers/agent/src/browser-launch.ts"
        via: "launcher publishes runtime metadata and CDP endpoint the worker uses"
        pattern: "desktopName|cdpPort|profilePath"
      - from: "infra/host-worker/start-host-native-worker.ps1"
        to: "workers/agent/src/server.ts"
        via: "runtime mode and runtime class are consistent between launcher and worker health"
        pattern: "alternate_desktop|host_alternate_desktop"
---

<objective>
Introduce the replacement same-session alternate-desktop runtime foundation so one worker can leave `visible_auth`, relaunch natively on a non-default Windows desktop, and report truthful runtime telemetry without using the rejected `hidden_runtime` path.
</objective>

<execution_context>
@../gsd/get-shit-done/workflows/execute-plan.md
@../gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10.3-alternative-non-visible-native-browser-runtime-design/10.3-CONTEXT.md
@.planning/phases/10.3-alternative-non-visible-native-browser-runtime-design/10.3-RESEARCH.md
@.planning/phases/10.3-alternative-non-visible-native-browser-runtime-design/10.3-VALIDATION.md
@infra/host-worker/start-host-native-worker.ps1
@infra/host-worker/start-host-worker-agent.ps1
@infra/host-worker/stop-host-native-worker.ps1
@workers/agent/src/browser-launch.ts
@workers/agent/src/server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a same-session alternate-desktop browser launcher and runtime metadata contract</name>
  <files>infra/host-worker/start-host-native-worker.ps1, infra/host-worker/start-alternate-desktop-browser.ps1, infra/host-worker/stop-host-native-worker.ps1</files>
  <read_first>infra/host-worker/start-host-native-worker.ps1, infra/host-worker/stop-host-native-worker.ps1, .planning/phases/10.3-alternative-non-visible-native-browser-runtime-design/10.3-RESEARCH.md</read_first>
  <action>Create `infra/host-worker/start-alternate-desktop-browser.ps1` as the new launcher responsible for the replacement non-visible runtime. It must create or open a named desktop in the current interactive window station, launch the native Edge/Chromium browser onto that desktop using the same durable profile and the worker's fixed CDP port, and write a metadata file under `infra/data/host-worker-state/<workerId>.json` containing the exact fields `workerId`, `runtimeClass`, `runtimeMode`, `desktopName`, `browserPid`, `cdpPort`, `profilePath`, and `launchedAt`. Update `infra/host-worker/start-host-native-worker.ps1` so `RuntimeMode HiddenRuntime` no longer means the old headless path; instead introduce a new explicit runtime mode `AlternateDesktop` and route that mode through the new launcher. Update `infra/host-worker/stop-host-native-worker.ps1` to read the metadata file and stop the right browser process cleanly.</action>
  <verify>rg -n "AlternateDesktop|alternate_desktop|desktopName|host-worker-state|browserPid" infra/host-worker/start-host-native-worker.ps1 infra/host-worker/start-alternate-desktop-browser.ps1 infra/host-worker/stop-host-native-worker.ps1</verify>
  <acceptance_criteria>
    - infra/host-worker/start-alternate-desktop-browser.ps1 contains "alternate_desktop"
    - infra/host-worker/start-alternate-desktop-browser.ps1 contains "desktopName"
    - infra/host-worker/start-host-native-worker.ps1 contains "AlternateDesktop"
    - infra/host-worker/stop-host-native-worker.ps1 contains "host-worker-state"
  </acceptance_criteria>
  <done>The project has a concrete launcher path for a non-default Windows desktop instead of the rejected hidden/headless runtime.</done>
</task>

<task type="auto">
  <name>Task 2: Teach worker-agent health and browser attach logic about the new runtime class</name>
  <files>workers/agent/src/browser-launch.ts, workers/agent/src/server.ts, infra/host-worker/start-host-worker-agent.ps1</files>
  <read_first>workers/agent/src/browser-launch.ts, workers/agent/src/server.ts, infra/host-worker/start-host-worker-agent.ps1, infra/host-worker/start-host-native-worker.ps1</read_first>
  <action>Update the worker agent so the new routine runtime mode is explicit. `workers/agent/src/browser-launch.ts` must support `alternate_desktop` by attaching over the worker's CDP endpoint instead of attempting the rejected headless persistent launch path. `workers/agent/src/server.ts` must report `runtimeMode: "alternate_desktop"` and `runtimeClass: "host_alternate_desktop"` in `/health`, keep `headless: false`, and include any available desktop metadata for diagnostics. Update `infra/host-worker/start-host-worker-agent.ps1` so it passes the new runtime mode and runtime class through environment variables exactly.</action>
  <verify>rg -n "alternate_desktop|host_alternate_desktop|headless|desktop" workers/agent/src/browser-launch.ts workers/agent/src/server.ts infra/host-worker/start-host-worker-agent.ps1</verify>
  <acceptance_criteria>
    - workers/agent/src/browser-launch.ts contains "alternate_desktop"
    - workers/agent/src/server.ts contains "host_alternate_desktop"
    - workers/agent/src/server.ts contains "alternate_desktop"
    - infra/host-worker/start-host-worker-agent.ps1 contains "alternate_desktop"
  </acceptance_criteria>
  <done>Worker health and runtime telemetry now describe the replacement non-visible runtime explicitly.</done>
</task>

<task type="auto">
  <name>Task 3: Add probe and regression tests for alternate-desktop runtime scaffolding</name>
  <files>infra/host-worker/test-alternate-desktop-runtime.ps1, workers/agent/test/browser-launch.test.ts, workers/agent/test/server.test.ts</files>
  <read_first>workers/agent/test/browser-launch.test.ts, workers/agent/test/server.test.ts, infra/host-worker/test-hidden-runtime-transition.ps1, .planning/phases/10.3-alternative-non-visible-native-browser-runtime-design/10.3-VALIDATION.md</read_first>
  <action>Create `infra/host-worker/test-alternate-desktop-runtime.ps1` as the canonical probe for the new runtime. It must read runtime metadata, call the worker health endpoint, and emit an evidence row that distinguishes `alternate_desktop_unreachable`, `alternate_desktop_reachable_but_unusable`, and `alternate_desktop_usable`. Extend worker-agent tests so they cover the new runtime mode and runtime class strings. At least one test must assert the exact string `host_alternate_desktop` and another must assert `alternate_desktop`.</action>
  <verify>rg -n "alternate_desktop|host_alternate_desktop|alternate_desktop_unreachable|alternate_desktop_reachable_but_unusable|alternate_desktop_usable" infra/host-worker/test-alternate-desktop-runtime.ps1 workers/agent/test/browser-launch.test.ts workers/agent/test/server.test.ts</verify>
  <acceptance_criteria>
    - infra/host-worker/test-alternate-desktop-runtime.ps1 contains "alternate_desktop_usable"
    - workers/agent/test/browser-launch.test.ts contains "alternate_desktop"
    - workers/agent/test/server.test.ts contains "host_alternate_desktop"
  </acceptance_criteria>
  <done>The replacement runtime has a canonical probe and regression coverage before control surfaces start relying on it.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] alternate-desktop launch metadata exists and is durable
- [ ] worker `/health` exposes the new runtime class and mode
- [ ] the new runtime has a canonical probe script and tests
</verification>

<success_criteria>

- All tasks completed
- replacement non-visible runtime foundation exists without the old headless launch path
- same durable profile can be targeted by the new runtime contract
- worker telemetry is explicit enough for control-plane integration

</success_criteria>

<output>
After completion, create the matching SUMMARY document in this phase directory.
</output>

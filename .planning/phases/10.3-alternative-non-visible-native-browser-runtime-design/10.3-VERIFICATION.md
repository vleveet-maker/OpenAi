---
phase: 10.3-alternative-non-visible-native-browser-runtime-design
verified: 2026-03-28T21:12:00+03:00
status: complete
score: 3/3 must-haves verified
---

# Phase 10.3: Alternative Non-Visible Native Browser Runtime Design Verification Report

Phase goal: design, integrate, and live-test a replacement non-visible native browser runtime before Phase 11 rollout smoke can continue.

Verified: 2026-03-28T21:12:00+03:00
Status: complete

## Goal Achievement

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | The project now has a concrete replacement non-visible native runtime path based on a same-session alternate Windows desktop. | x VERIFIED | `infra/host-worker/start-alternate-desktop-browser.ps1`, `infra/host-worker/start-host-native-worker.ps1`, `workers/agent/src/server.ts`, `infra/data/host-worker-state/*.json` |
| 2 | Host-controller, control-api, and internal admin can move a worker from visible auth into the replacement runtime and report that runtime truthfully. | x VERIFIED | `services/host-controller/src/host-controller.mjs`, `services/control-api/src/routes/internal-worker-actions.ts`, `services/control-api/src/routes/internal-workers.ts`, `services/control-api/src/routes/internal-admin-page.ts`, live `/internal/workers` and `/internal/host-pool` |
| 3 | Phase 10.3 ended with a live evidence-backed accept/reject decision instead of an indefinite experiment. | x VERIFIED | `infra/host-worker/test-alternate-desktop-runtime.ps1`, `infra/data/host-worker-logs/runtime-matrix.jsonl`, `.planning/phases/10.3-alternative-non-visible-native-browser-runtime-design/10.3-RUNTIME-DECISION.md`, `.planning/ROADMAP.md`, `.planning/STATE.md` |

Score: 3/3 truths verified

## Automated Checks

| Command | Status |
|---------|--------|
| `cmd /c npm.cmd test --prefix workers/agent` | x PASSED |
| `cmd /c npm.cmd run build --prefix workers/agent` | x PASSED |
| `cmd /c npm.cmd test --prefix services/control-api` | x PASSED |
| `cmd /c npm.cmd run build --prefix services/control-api` | x PASSED |
| `cmd /c npm.cmd test --prefix services/host-controller` | x PASSED |

## Live Checks

| Probe | Result |
|-------|--------|
| `GET http://127.0.0.1:4040/health` after host-controller restart | x VERIFIED -> host-controller now exposes `runtimeMode`, `runtimeClass`, and `runtimeDesktopName` |
| `GET http://127.0.0.1:8081/internal/workers` after control-api restart | x VERIFIED -> host workers now surface `alternate_desktop` / `host_alternate_desktop` truth |
| `powershell -ExecutionPolicy Bypass -File .\\infra\\host-worker\\test-alternate-desktop-runtime.ps1 -WorkerId dad -TimeoutSeconds 300` | x VERIFIED -> `bootstrap_auth_required`, `phase11Ready=false`, `runtimeDesktopName=OWMCGPT-dad` |
| `powershell -ExecutionPolicy Bypass -File .\\infra\\host-worker\\test-alternate-desktop-runtime.ps1 -WorkerId wife -TimeoutSeconds 300` | x VERIFIED -> runtime reached alternate desktop, follow-up session snapshot recorded `temporary_confirmation_not_found`, `phase11Ready=false` |
| `powershell -ExecutionPolicy Bypass -File .\\infra\\host-worker\\test-alternate-desktop-runtime.ps1 -WorkerId shared-1 -TimeoutSeconds 300` | x VERIFIED -> isolated proof timed out before a successful assignment, `phase11Ready=false` |

## Requirements Coverage

| Requirement | Status |
|-------------|--------|
| NVRT-01: replacement native runtime can run outside the operator's main desktop while reusing the same durable profile | x VERIFIED - alternate desktop launcher and live state metadata exist |
| NVRT-02: control surfaces can transition a worker into the replacement runtime instead of hidden runtime | x VERIFIED - control-plane defaults and manual-auth completion now target `alternate_desktop` |
| NVRT-03: project records a live evidence-backed accept/reject decision before Phase 11 resumes | x VERIFIED - decision recorded, Phase 11 remains blocked |

## Residual Risk

- Alternate desktop is the strongest non-visible candidate so far, but still not rollout-ready.
- `dad` still needs manual auth repair in the new runtime path.
- `wife` proves the runtime gets deeper into bootstrap than the old hidden-runtime failure, but `Temporary Chat` drift still blocks completion.
- `shared-1` still needs a clean isolated proof before the candidate can be considered stable.
- The next step is a follow-up stabilization phase before Phase 11.

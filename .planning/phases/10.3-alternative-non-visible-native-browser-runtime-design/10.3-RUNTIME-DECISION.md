---
phase: 10.3-alternative-non-visible-native-browser-runtime-design
decision_recorded: 2026-03-28T21:12:00+03:00
selected_runtime_for_phase_11: none
decision: keep_phase_11_blocked_pending_alternate_desktop_followup
---

# Phase 10.3 Runtime Decision

Decision: `keep_phase_11_blocked_pending_alternate_desktop_followup`

## Evidence Reviewed

1. Alternate-desktop control-plane truth
   - live `/internal/workers` now reports all three host workers with:
     - `runtimeMode=alternate_desktop`
     - `runtimeClass=host_alternate_desktop`
     - `headless=false`
     - `cdpAttached=true`
     - worker-specific `runtimeDesktopName`
   - live `/internal/host-pool` now reports the same runtime contract at pool level
2. `dad` bounded proof
   - command: `powershell -ExecutionPolicy Bypass -File .\infra\host-worker\test-alternate-desktop-runtime.ps1 -WorkerId dad -TimeoutSeconds 300`
   - result row: `alternate_desktop_reachable_but_unusable`
   - failure: `bootstrap_auth_required`
   - runtime metadata persisted with `runtimeDesktopName=OWMCGPT-dad`
3. `wife` bounded proof and follow-up session snapshot
   - command: `powershell -ExecutionPolicy Bypass -File .\infra\host-worker\test-alternate-desktop-runtime.ps1 -WorkerId wife -TimeoutSeconds 300`
   - result row: `alternate_desktop_reachable_but_unusable`
   - follow-up session snapshot for `dc1a8736-62a0-444b-8768-81a30df707af` recorded:
     - `chatBootstrap.status=failed`
     - `failureCode=temporary_confirmation_not_found`
4. `shared-1` bounded proof
   - command: `powershell -ExecutionPolicy Bypass -File .\infra\host-worker\test-alternate-desktop-runtime.ps1 -WorkerId shared-1 -TimeoutSeconds 300`
   - result row: `alternate_desktop_reachable_but_unusable`
   - failure surface: isolated proof timed out before successful assignment and relay completion

## Candidate Verdict

Candidate reviewed: `host_alternate_desktop`

What improved:
- The runtime is truly non-visible on the operator's main desktop without returning to the rejected headless path.
- The same durable profile is reused across visible auth and the non-visible runtime.
- Live evidence no longer centers on the old Cloudflare hidden-runtime challenge loop.

What still failed:
- `dad` still needs auth in the alternate-desktop runtime.
- `wife` advanced further, but `Temporary Chat` bootstrap still failed with `temporary_confirmation_not_found`.
- `shared-1` did not complete an isolated end-to-end proof.
- No worker produced `phase11Ready=true`.

Verdict: `candidate_not_yet_selected_for_phase_11`

## Selected Runtime For Phase 11

Selected Runtime For Phase 11: none

Reason:
- Phase 11 only resumes when the replacement runtime passes a bounded live proof.
- Phase 10.3 produced a better candidate direction, but not a passing rollout runtime.

## Explicit Follow-Up

Phase 11 stays blocked.

The next required follow-up is not a return to the rejected hidden runtime. It is a focused stabilization step on the alternate-desktop path:

- recover or re-establish valid auth for at least one alternate-desktop worker without returning to routine visible operation
- harden the current `Temporary Chat` bootstrap path where `wife` reached `temporary_confirmation_not_found`
- rerun the bounded proof until at least one worker records `phase11Ready=true`

## Next Step

Insert an urgent follow-up before Phase 11:

`$gsd-insert-phase 10.4 "Alternate desktop auth and bootstrap stabilization"`

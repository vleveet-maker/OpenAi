---
phase: 10.3
slug: alternative-non-visible-native-browser-runtime-design
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-28
---

# Phase 10.3 - Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `node --test` and `vitest` |
| **Config file** | `services/host-controller/package.json`, `workers/agent/package.json`, `services/control-api/package.json` |
| **Quick run command** | `cmd /c npm.cmd test --prefix services/host-controller && cmd /c npm.cmd test --prefix workers/agent && cmd /c npm.cmd test --prefix services/control-api` |
| **Full suite command** | `cmd /c npm.cmd test --prefix services/host-controller && cmd /c npm.cmd test --prefix workers/agent && cmd /c npm.cmd run build --prefix workers/agent && cmd /c npm.cmd test --prefix services/control-api && cmd /c npm.cmd run build --prefix services/control-api` |
| **Estimated runtime** | ~150 seconds |

---

## Sampling Rate

- **After every task commit:** run the smallest affected package test command
- **After every plan wave:** run the full suite command
- **Before `$gsd-verify-work`:** full suite must be green
- **Max feedback latency:** 150 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 10.3-01-* | 01 | 1 | NVRT-01 | unit + probe scaffolding | `cmd /c npm.cmd test --prefix workers/agent` | partial | pending |
| 10.3-02-* | 02 | 2 | NVRT-02 | unit/integration | `cmd /c npm.cmd test --prefix services/host-controller && cmd /c npm.cmd test --prefix services/control-api` | yes | pending |
| 10.3-03-* | 03 | 3 | NVRT-03 | docs + live probe + targeted tests | `powershell -ExecutionPolicy Bypass -File .\\infra\\host-worker\\test-alternate-desktop-runtime.ps1 -WorkerId wife` | no | pending |

*Status: pending / green / red / flaky*

---

## Wave 0 Requirements

- [ ] Existing infrastructure covers all phase requirements.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Confirm the routine browser no longer appears on the operator's main desktop after `Complete login -> alternate_desktop` | NVRT-01 | Windows desktop visibility is environment-specific and cannot be asserted in unit tests | 1. Start one worker in visible auth. 2. Confirm ChatGPT is logged in. 3. Complete login into `alternate_desktop`. 4. Verify no routine browser window remains on the main desktop while worker health stays reachable. |
| Confirm the same durable profile survives the visible-auth to alternate-desktop handoff | NVRT-01 | Requires live ChatGPT session continuity | 1. Log in visibly. 2. Complete login into `alternate_desktop`. 3. Run fresh-chat bootstrap on the same worker. 4. Record success or failure in the probe artifact. |
| Confirm internal admin no longer routes routine work back to the rejected `hidden_runtime` path | NVRT-02 | Requires end-to-end route and operator flow | 1. Use internal admin to start visible login. 2. Use internal admin to complete login. 3. Inspect `/internal/workers` and `/internal/host-pool`. 4. Verify runtime mode is `alternate_desktop`. |
| Confirm Phase 11 is either explicitly unblocked or still blocked with evidence | NVRT-03 | This is a milestone decision, not only a code assertion | 1. Review the live probe artifact and runtime decision document. 2. Verify roadmap/state next step matches that decision. |

---

## Validation Sign-Off

- [ ] All tasks have automated verify or explicit manual gate coverage
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all missing references
- [ ] No watch-mode flags
- [ ] Feedback latency < 150s for automated checks
- [ ] `nyquist_compliant: true` set in frontmatter once plan-specific task rows are finalized

**Approval:** pending

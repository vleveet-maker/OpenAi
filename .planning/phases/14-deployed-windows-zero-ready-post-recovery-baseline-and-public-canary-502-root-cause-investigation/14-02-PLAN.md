---
phase: 14-deployed-windows-zero-ready-post-recovery-baseline-and-public-canary-502-root-cause-investigation
plan: 02
type: execute
wave: 2
depends_on: [14-01-PLAN.md]
files_modified:
  - services/control-api/src/
  - services/control-api/test/
autonomous: true
requirements: [WROOT-01, WROOT-03]
user_setup: []
---

<objective>
Expose the latest zero-ready root-cause result in an internal operator surface so the remaining rollout blocker is visible without terminal archaeology.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-deployed-windows-zero-ready-post-recovery-baseline-and-public-canary-502-root-cause-investigation/14-RESEARCH.md
@.planning/phases/14-deployed-windows-zero-ready-post-recovery-baseline-and-public-canary-502-root-cause-investigation/14-VALIDATION.md
@services/control-api/src/config.ts
@services/control-api/src/server.ts
@services/control-api/src/routes/internal-admin-page.ts
@services/control-api/src/routes/internal-post-recovery-regression.ts
@services/control-api/test/internal-admin-page.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a file-backed latest zero-ready root-cause route to control-api</name>
  <files>services/control-api/src/config.ts, services/control-api/src/server.ts, services/control-api/src/routes/internal-zero-ready-root-cause.ts, services/control-api/test/</files>
  <read_first>services/control-api/src/config.ts, services/control-api/src/server.ts, services/control-api/src/routes/internal-post-recovery-regression.ts, services/control-api/test/internal-post-recovery-regression.test.ts</read_first>
  <action>Add a new config field `zeroReadyRootCauseStatePath` sourced from `ZERO_READY_ROOT_CAUSE_STATE_PATH` with default repo path `infra/data/zero-ready-root-cause/latest.json`. Create `services/control-api/src/routes/internal-zero-ready-root-cause.ts` with `GET /internal/zero-ready-root-cause/latest` that returns `{ latest: null }` when the file is missing and returns the parsed latest root-cause payload when it exists. Register the route behind the internal admin guard in `server.ts`, and add tests that prove both the empty state and the populated state, including the exact fields `dominantBlockerClass`, `readyCount`, `firstFailingHop`, and `verdict`.</action>
  <verify>rg -n "ZERO_READY_ROOT_CAUSE_STATE_PATH|zeroReadyRootCauseStatePath|/internal/zero-ready-root-cause/latest|latest: null|dominantBlockerClass|firstFailingHop|verdict" services/control-api/src services/control-api/test</verify>
  <acceptance_criteria>
    - `services/control-api/src/routes/internal-zero-ready-root-cause.ts` exists
    - `config.ts` supports `ZERO_READY_ROOT_CAUSE_STATE_PATH`
    - `server.ts` registers `/internal/zero-ready-root-cause/latest`
    - tests prove the route returns `{ latest: null }` when no root-cause result exists
    - tests prove the populated payload includes `dominantBlockerClass`, `readyCount`, `firstFailingHop`, and `verdict`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Render the latest zero-ready root cause on the internal admin page</name>
  <files>services/control-api/src/routes/internal-admin-page.ts, services/control-api/test/internal-admin-page.test.ts</files>
  <read_first>services/control-api/src/routes/internal-admin-page.ts, services/control-api/test/internal-admin-page.test.ts, services/control-api/src/routes/internal-zero-ready-root-cause.ts</read_first>
  <action>Add a dedicated `Latest zero-ready root cause` section to `/internal/admin`. Make the page fetch `/internal/zero-ready-root-cause/latest`, render an empty state `No zero-ready root-cause result captured yet.` when there is no file, and render the latest timestamp, dominant blocker class, ready/disconnected counts, first failing hop, canary hop results, and final verdict when the file exists. Keep the existing worker summary, latest readiness recovery, latest rollout smoke, and latest post-recovery regression sections intact.</action>
  <verify>rg -n "Latest zero-ready root cause|No zero-ready root-cause result captured yet.|/internal/zero-ready-root-cause/latest|dominant blocker class|first failing hop|verdict" services/control-api/src/routes/internal-admin-page.ts services/control-api/test/internal-admin-page.test.ts</verify>
  <acceptance_criteria>
    - the admin page contains the text `Latest zero-ready root cause`
    - the admin page fetches `/internal/zero-ready-root-cause/latest`
    - the admin page has an explicit empty state `No zero-ready root-cause result captured yet.`
    - the populated state renders dominant blocker class, first failing hop, and verdict
    - the existing worker summary, latest readiness recovery, latest rollout-smoke, and latest post-recovery regression sections remain present
  </acceptance_criteria>
</task>

</tasks>

<success_criteria>

- the latest zero-ready root-cause result is visible from an internal operator surface
- no operator has to infer the current blocker from scattered smoke and recovery artifacts

</success_criteria>

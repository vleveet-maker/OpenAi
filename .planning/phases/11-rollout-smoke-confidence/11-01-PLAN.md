---
phase: 11-rollout-smoke-confidence
plan: 01
type: execute
wave: 1
depends_on: [10.6.1.2.2.1.1.1.1.1-VERIFICATION.md]
files_modified:
  - .planning/phases/11-rollout-smoke-confidence/
  - docs/windows-public-api-block.md
  - docs/remote-relay-server.md
  - infra/windows-block/
autonomous: true
requirements: [CONF-01]
user_setup: []
---

<objective>
Create one repeatable rollout-smoke wrapper for the current public-owner topology and document the exact rerun contract.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-rollout-smoke-confidence/11-RESEARCH.md
@.planning/phases/10.6.1.2.2.1.1.1.1.1-ubuntu-public-owner-reassignment-and-windows-edge-unification/10.6.1.2.2.1.1.1.1.1-VERIFICATION.md
@docs/windows-public-api-block.md
@docs/remote-relay-server.md
@infra/windows-block/probe-public-api.ps1
@infra/host-worker/test-local-rollout-smoke.ps1
@infra/host-worker/test-host-worker-relay.ps1
@services/control-api/src/routes/internal-host-pool.ts
@services/control-api/src/routes/internal-workers.ts
@services/control-api/src/routes/internal-observability.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a deployed rollout-smoke wrapper that composes readiness and public canary proof</name>
  <files>infra/windows-block/test-rollout-smoke.ps1, infra/data/rollout-smoke/, .planning/phases/11-rollout-smoke-confidence/</files>
  <read_first>infra/windows-block/probe-public-api.ps1, infra/host-worker/test-local-rollout-smoke.ps1, infra/host-worker/test-host-worker-relay.ps1, services/control-api/src/routes/internal-host-pool.ts, services/control-api/src/routes/internal-workers.ts, services/control-api/src/routes/internal-observability.ts</read_first>
  <action>Create `infra/windows-block/test-rollout-smoke.ps1` as the canonical repeatable Phase 11 wrapper. Give it concrete defaults `PublicBaseUrl=http://77.66.186.75`, `InternalBaseUrl=http://127.0.0.1:8081`, `HostControllerBaseUrl=http://127.0.0.1:4040`, and `CanaryWorkerId=shared-6`. Make it call `GET /internal/host-pool`, `GET /internal/workers`, and `GET /internal/observability/summary`, then run `probe-public-api.ps1` with `-IncludeChatProbe -WorkerId shared-6 -EnsureWorkerStarted -StopWorkerWhenDone`. Make the wrapper write `.planning/phases/11-rollout-smoke-confidence/11-SMOKE-SUMMARY.json`, `.planning/phases/11-rollout-smoke-confidence/11-SMOKE-SUMMARY.md`, and mirror the same machine-readable payload to `infra/data/rollout-smoke/latest.json` when output paths are provided.</action>
  <verify>rg -n "shared-6|internal/host-pool|internal/workers|internal/observability/summary|latest.json|v1/chat/completions" infra/windows-block/test-rollout-smoke.ps1</verify>
  <acceptance_criteria>
    - `infra/windows-block/test-rollout-smoke.ps1` exists
    - the wrapper contains the exact routes `/internal/host-pool`, `/internal/workers`, and `/internal/observability/summary`
    - the wrapper uses `probe-public-api.ps1` with `shared-6` as the default canary
    - the wrapper writes both phase artifacts and `infra/data/rollout-smoke/latest.json`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Document the exact rollout-smoke rerun contract</name>
  <files>docs/windows-public-api-block.md, docs/remote-relay-server.md</files>
  <read_first>docs/windows-public-api-block.md, docs/remote-relay-server.md, infra/windows-block/probe-public-api.ps1, .planning/phases/11-rollout-smoke-confidence/11-RESEARCH.md</read_first>
  <action>Update `docs/windows-public-api-block.md` and `docs/remote-relay-server.md` with one exact Phase 11 rerun command for `infra/windows-block/test-rollout-smoke.ps1`. State that the smoke checks pool readiness, public `healthz`, public `v1/models`, and one canary `v1/chat/completions` on `shared-6`. Also state exactly what this smoke does not prove: it does not widen the public worker set, does not replace certificate-trust hardening, and does not reopen public-owner reconciliation.</action>
  <verify>rg -n "test-rollout-smoke.ps1|shared-6|77.66.186.75|Latest rollout smoke|does not" docs/windows-public-api-block.md docs/remote-relay-server.md</verify>
  <acceptance_criteria>
    - both docs mention `test-rollout-smoke.ps1`
    - both docs mention `shared-6` and `77.66.186.75`
    - the docs explicitly state what the rollout smoke checks
    - the docs explicitly state what remains out of scope for Phase 11
  </acceptance_criteria>
</task>

</tasks>

<success_criteria>

- one repeatable rollout-smoke wrapper exists for the live public-owner topology
- the rerun contract is documented clearly enough that the operator can reuse it after UI drift

</success_criteria>

---
phase: 11-rollout-smoke-confidence
plan: 02
type: execute
wave: 2
depends_on: [11-01-PLAN.md]
files_modified:
  - services/control-api/src/
  - services/control-api/test/
autonomous: true
requirements: [CONF-02]
user_setup: []
---

<objective>
Expose the latest rollout-smoke result in an internal operator surface so the operator does not have to dig through raw logs.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-rollout-smoke-confidence/11-RESEARCH.md
@.planning/phases/11-rollout-smoke-confidence/11-VALIDATION.md
@services/control-api/src/config.ts
@services/control-api/src/server.ts
@services/control-api/src/routes/internal-admin-page.ts
@services/control-api/src/routes/internal-observability.ts
@services/control-api/test/internal-admin-page.test.ts
@services/control-api/test/internal-observability.test.ts
@services/control-api/test/internal-host-pool.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a file-backed latest rollout-smoke route to control-api</name>
  <files>services/control-api/src/config.ts, services/control-api/src/server.ts, services/control-api/src/routes/internal-rollout-smoke.ts, services/control-api/test/</files>
  <read_first>services/control-api/src/config.ts, services/control-api/src/server.ts, services/control-api/src/routes/internal-observability.ts, services/control-api/test/internal-observability.test.ts</read_first>
  <action>Add a new config field `rolloutSmokeStatePath` sourced from `ROLLOUT_SMOKE_STATE_PATH` with default repo path `infra/data/rollout-smoke/latest.json`. Create `services/control-api/src/routes/internal-rollout-smoke.ts` with `GET /internal/rollout-smoke/latest` that returns `{ latest: null }` when the file is missing and returns the parsed latest smoke payload when it exists. Register the route behind the internal admin guard in `server.ts`, and add tests that prove both the empty state and the populated state.</action>
  <verify>rg -n "ROLLOUT_SMOKE_STATE_PATH|/internal/rollout-smoke/latest|latest: null|rolloutSmokeStatePath" services/control-api/src services/control-api/test</verify>
  <acceptance_criteria>
    - `services/control-api/src/routes/internal-rollout-smoke.ts` exists
    - `config.ts` supports `ROLLOUT_SMOKE_STATE_PATH`
    - `server.ts` registers `/internal/rollout-smoke/latest`
    - tests prove the route returns `{ latest: null }` when no smoke result exists
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Render the latest rollout smoke on the internal admin page</name>
  <files>services/control-api/src/routes/internal-admin-page.ts, services/control-api/test/internal-admin-page.test.ts</files>
  <read_first>services/control-api/src/routes/internal-admin-page.ts, services/control-api/test/internal-admin-page.test.ts, services/control-api/src/routes/internal-rollout-smoke.ts</read_first>
  <action>Add a dedicated `Latest rollout smoke` section to `/internal/admin`. Make the page fetch `/internal/rollout-smoke/latest`, render an empty state `No rollout smoke captured yet.` when there is no file, and render the latest timestamp, verdict, public base URL, canary worker `shared-6`, and key readiness/canary outcome fields when the file exists. Keep the existing worker summary, failures, and lifecycle sections intact.</action>
  <verify>rg -n "Latest rollout smoke|No rollout smoke captured yet|/internal/rollout-smoke/latest|shared-6" services/control-api/src/routes/internal-admin-page.ts services/control-api/test/internal-admin-page.test.ts</verify>
  <acceptance_criteria>
    - the admin page contains the text `Latest rollout smoke`
    - the admin page fetches `/internal/rollout-smoke/latest`
    - the admin page has an explicit empty state `No rollout smoke captured yet.`
    - the existing worker summary and lifecycle sections remain present
  </acceptance_criteria>
</task>

</tasks>

<success_criteria>

- the latest rollout-smoke result is visible from an internal operator surface
- no operator has to rely on raw terminal output to understand the latest smoke verdict

</success_criteria>

---
phase: 01-managed-worker-pool-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-managed-worker-pool-foundation/01-01-ARCHITECTURE-SPIKE.md
autonomous: true
requirements: [WORK-01, WORK-02, WORK-03, WORK-04, ADMN-02, SECU-02, SECU-03]
user_setup: []
must_haves:
    truths:
      - "A documented worker-pool topology defines named workers, per-worker containers, and durable profile mounts."
      - "The architecture spike makes an explicit primary runtime recommendation between raw Playwright worker containers and a BlitzBrowser-backed runtime."
      - "The architecture spike defines an internal-only worker status contract including starting, ready, busy, disconnected, and reauth_required."
    artifacts:
      - path: ".planning/phases/01-managed-worker-pool-foundation/01-01-ARCHITECTURE-SPIKE.md"
        provides: "Short architecture decision for worker runtime, persistence, status, and internal admin boundaries"
        min_lines: 40
        contains: "## Recommended Direction"
    key_links:
      - from: ".planning/phases/01-managed-worker-pool-foundation/01-01-ARCHITECTURE-SPIKE.md"
        to: ".planning/phases/01-managed-worker-pool-foundation/01-CONTEXT.md"
        via: "worker status contract mirrors Phase 1 discussion decisions"
        pattern: "reauth_required"
      - from: ".planning/phases/01-managed-worker-pool-foundation/01-01-ARCHITECTURE-SPIKE.md"
        to: ".planning/phases/01-managed-worker-pool-foundation/01-TOOLING-EVALUATION.md"
        via: "runtime choice is grounded in the tooling review"
        pattern: "BlitzBrowser"
---

<objective>
Create a short architecture spike that selects the worker runtime baseline and defines the minimum viable worker-pool contract for implementation.

Purpose: Remove ambiguity before 01-02 and 01-03 so the phase implements against one worker topology, one persistence model, and one internal status contract.
Output: A concise architecture spike document comparing raw Playwright worker containers against a BlitzBrowser-backed runtime, with a recommended direction and concrete worker, storage, and admin boundaries.
</objective>

<execution_context>
@../gsd/get-shit-done/workflows/execute-plan.md
@../gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-managed-worker-pool-foundation/01-CONTEXT.md
@.planning/phases/01-managed-worker-pool-foundation/01-TOOLING-EVALUATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the worker-runtime comparison and decision frame</name>
  <files>.planning/phases/01-managed-worker-pool-foundation/01-01-ARCHITECTURE-SPIKE.md</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/phases/01-managed-worker-pool-foundation/01-CONTEXT.md, .planning/phases/01-managed-worker-pool-foundation/01-TOOLING-EVALUATION.md</read_first>
  <action>Create `.planning/phases/01-managed-worker-pool-foundation/01-01-ARCHITECTURE-SPIKE.md` with sections `## Decision Goal`, `## Candidates Compared`, and `## Recommended Direction`. Compare exactly two primary runtime options: `raw Playwright worker-agent containers` and `BlitzBrowser-backed workers`. State that the recommended baseline is raw Playwright worker-agent containers on a private Docker network, and keep BlitzBrowser as a bounded fallback spike if headful browser operations become too expensive to own directly.</action>
  <verify>rg -n "^## (Decision Goal|Candidates Compared|Recommended Direction)$" .planning/phases/01-managed-worker-pool-foundation/01-01-ARCHITECTURE-SPIKE.md</verify>
  <acceptance_criteria>
    - `.planning/phases/01-managed-worker-pool-foundation/01-01-ARCHITECTURE-SPIKE.md` contains `## Decision Goal`
    - `.planning/phases/01-managed-worker-pool-foundation/01-01-ARCHITECTURE-SPIKE.md` contains `raw Playwright worker-agent containers`
    - `.planning/phases/01-managed-worker-pool-foundation/01-01-ARCHITECTURE-SPIKE.md` contains `BlitzBrowser`
  </acceptance_criteria>
  <done>The spike doc names the two runtime candidates and makes an explicit baseline recommendation.</done>
</task>

<task type="auto">
  <name>Task 2: Define the concrete worker topology, status contract, and boundaries</name>
  <files>.planning/phases/01-managed-worker-pool-foundation/01-01-ARCHITECTURE-SPIKE.md</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/phases/01-managed-worker-pool-foundation/01-CONTEXT.md</read_first>
  <action>Extend the spike document with sections `## Proposed Runtime Topology`, `## Worker Status Contract`, `## Storage and Security Boundaries`, and `## Next Build Slice`. Specify a `control-api` service, one `worker-agent` container per named worker, a private Docker network, and durable profile mounts under `/srv/chatgpt-workers/profiles/<worker-name>`. Define statuses `starting`, `ready`, `busy`, `disconnected`, and `reauth_required`. State that worker browsers and recovery paths are internal-only and that only the control API can talk to worker agents.</action>
  <verify>rg -n "control-api|/srv/chatgpt-workers/profiles/<worker-name>|starting|ready|busy|disconnected|reauth_required|internal-only" .planning/phases/01-managed-worker-pool-foundation/01-01-ARCHITECTURE-SPIKE.md</verify>
  <acceptance_criteria>
    - `.planning/phases/01-managed-worker-pool-foundation/01-01-ARCHITECTURE-SPIKE.md` contains `control-api`
    - `.planning/phases/01-managed-worker-pool-foundation/01-01-ARCHITECTURE-SPIKE.md` contains `/srv/chatgpt-workers/profiles/<worker-name>`
    - `.planning/phases/01-managed-worker-pool-foundation/01-01-ARCHITECTURE-SPIKE.md` contains `reauth_required`
    - `.planning/phases/01-managed-worker-pool-foundation/01-01-ARCHITECTURE-SPIKE.md` contains `internal-only`
  </acceptance_criteria>
  <done>The spike doc defines a concrete worker topology, status contract, storage model, and the next implementation slice.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run plan-structure verification for `.planning/phases/01-managed-worker-pool-foundation/01-01-PLAN.md`
- [ ] Run reference verification for `.planning/phases/01-managed-worker-pool-foundation/01-01-PLAN.md`
- [ ] Run artifact verification for `.planning/phases/01-managed-worker-pool-foundation/01-01-PLAN.md`
- [ ] Run key-link verification for `.planning/phases/01-managed-worker-pool-foundation/01-01-PLAN.md`
</verification>

<success_criteria>

- All tasks completed
- The spike doc selects one primary runtime direction
- The worker topology and status contract are concrete enough to unblock 01-02
- No public-facing browser access is introduced in the documented design
</success_criteria>

<output>
After completion, create the matching SUMMARY document in this phase directory.
</output>

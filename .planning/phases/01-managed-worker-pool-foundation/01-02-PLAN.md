---
phase: 01-managed-worker-pool-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - services/control-api/package.json
  - services/control-api/tsconfig.json
  - services/control-api/src/server.ts
  - services/control-api/src/config.ts
  - services/control-api/src/workers/worker-registry.ts
  - services/control-api/src/workers/worker-status.ts
  - services/control-api/src/routes/internal-workers.ts
  - services/control-api/src/routes/internal-health.ts
  - workers/agent/package.json
  - workers/agent/src/server.ts
  - infra/docker-compose.yml
autonomous: true
requirements: [WORK-01, WORK-02, WORK-03, ADMN-02, SECU-03]
user_setup: []
must_haves:
    truths:
      - "The backend can list all configured named workers through an internal-only API."
      - "Each worker is represented as a dedicated containerized runtime target rather than an anonymous shared browser."
      - "Worker state is normalized to starting, ready, busy, disconnected, or reauth_required before later session routing is built."
    artifacts:
      - path: "services/control-api/src/workers/worker-status.ts"
        provides: "Canonical worker status model and transition helpers"
        contains: "reauth_required"
      - path: "services/control-api/src/workers/worker-registry.ts"
        provides: "Named worker registry and lookup logic"
        contains: "workerId"
      - path: "services/control-api/src/routes/internal-workers.ts"
        provides: "Internal workers status API"
        contains: "/internal/workers"
      - path: "infra/docker-compose.yml"
        provides: "Dedicated control-api and worker-agent service topology"
        contains: "worker-"
    key_links:
      - from: "services/control-api/src/routes/internal-workers.ts"
        to: "services/control-api/src/workers/worker-registry.ts"
        via: "route reads registry entries"
        pattern: "worker-registry|listWorkers|getWorker"
      - from: "services/control-api/src/server.ts"
        to: "services/control-api/src/routes/internal-workers.ts"
        via: "server mounts internal workers route"
        pattern: "internal-workers|internal/workers"
      - from: "infra/docker-compose.yml"
        to: "workers/agent/src/server.ts"
        via: "compose declares worker-agent runtime"
        pattern: "worker-"
---

<objective>
Build the multi-worker manager skeleton and the internal status API on top of the architecture spike baseline.

Purpose: Give the project a real control plane for named workers so later phases can assign sessions and route traffic without redesigning worker identity or status semantics.
Output: A control-api service, canonical worker status model, named worker registry, internal workers endpoints, and a first compose topology with dedicated worker-agent containers.
</objective>

<execution_context>
@../gsd/get-shit-done/workflows/execute-plan.md
@../gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-managed-worker-pool-foundation/01-CONTEXT.md
@.planning/phases/01-managed-worker-pool-foundation/01-01-ARCHITECTURE-SPIKE.md
@.planning/phases/01-managed-worker-pool-foundation/01-TOOLING-EVALUATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the control-api service and worker status model</name>
  <files>services/control-api/package.json, services/control-api/tsconfig.json, services/control-api/src/server.ts, services/control-api/src/config.ts, services/control-api/src/workers/worker-status.ts</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/phases/01-managed-worker-pool-foundation/01-CONTEXT.md, .planning/phases/01-managed-worker-pool-foundation/01-01-ARCHITECTURE-SPIKE.md</read_first>
  <action>Create a Node plus TypeScript control-api service under services/control-api. Define the canonical worker status union exactly as starting, ready, busy, disconnected, and reauth_required in services/control-api/src/workers/worker-status.ts. Create server bootstrap and config loading in services/control-api/src/server.ts and services/control-api/src/config.ts so later routes and worker registry code have one runtime entrypoint.</action>
  <verify>rg -n "starting|ready|busy|disconnected|reauth_required" services/control-api/src/workers/worker-status.ts</verify>
  <acceptance_criteria>
    - services/control-api/src/workers/worker-status.ts contains starting
    - services/control-api/src/workers/worker-status.ts contains reauth_required
    - services/control-api/src/server.ts contains the control-api bootstrap
  </acceptance_criteria>
  <done>The control-api service exists and the worker status model is defined in one canonical module.</done>
</task>

<task type="auto">
  <name>Task 2: Implement named worker registry and internal status routes</name>
  <files>services/control-api/src/workers/worker-registry.ts, services/control-api/src/routes/internal-workers.ts, services/control-api/src/routes/internal-health.ts, services/control-api/src/server.ts</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/01-managed-worker-pool-foundation/01-CONTEXT.md, .planning/phases/01-managed-worker-pool-foundation/01-01-ARCHITECTURE-SPIKE.md</read_first>
  <action>Implement worker-registry.ts so it can load and return stable named worker records with workerId, containerName, profilePath, and status. Implement internal-workers.ts with internal endpoints for GET /internal/workers, GET /internal/workers/:id, and GET /internal/workers/:id/status. Implement internal-health.ts with a lightweight machine-readable health endpoint for the control-api process. Mount the routes from server.ts and keep them internal-only in naming and intent.</action>
  <verify>rg -n "/internal/workers|workerId|profilePath|containerName" services/control-api/src/routes/internal-workers.ts services/control-api/src/workers/worker-registry.ts</verify>
  <acceptance_criteria>
    - services/control-api/src/routes/internal-workers.ts contains /internal/workers
    - services/control-api/src/workers/worker-registry.ts contains workerId
    - services/control-api/src/workers/worker-registry.ts contains profilePath
    - services/control-api/src/server.ts mounts the internal worker routes
  </acceptance_criteria>
  <done>The backend can expose named worker records and worker status through internal routes.</done>
</task>

<task type="auto">
  <name>Task 3: Add dedicated worker-agent service shape and compose topology</name>
  <files>workers/agent/package.json, workers/agent/src/server.ts, infra/docker-compose.yml</files>
  <read_first>.planning/phases/01-managed-worker-pool-foundation/01-01-ARCHITECTURE-SPIKE.md, .planning/phases/01-managed-worker-pool-foundation/01-TOOLING-EVALUATION.md</read_first>
  <action>Create a minimal worker-agent service skeleton under workers/agent with a server entrypoint that can later expose status and control hooks. Define infra/docker-compose.yml with a control-api service and dedicated named worker services such as worker-dad, worker-wife, and worker-shared-1 on a private network. Make each worker service declare a dedicated durable profile mount path patterned after /srv/chatgpt-workers/profiles/<worker-name> so the container topology matches the architecture spike from day one.</action>
  <verify>rg -n "worker-dad|worker-wife|worker-shared-1|/srv/chatgpt-workers/profiles" infra/docker-compose.yml</verify>
  <acceptance_criteria>
    - infra/docker-compose.yml contains worker-dad
    - infra/docker-compose.yml contains worker-wife
    - infra/docker-compose.yml contains /srv/chatgpt-workers/profiles
    - workers/agent/src/server.ts exists as the worker-agent entrypoint
  </acceptance_criteria>
  <done>The repo contains a concrete container topology for named worker-agent runtimes and the control-api service.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run plan-structure verification for the Phase 1 plan file
- [ ] Run reference verification for the Phase 1 plan file
- [ ] Future implementation should pass the control-api build and route wiring checks
</verification>

<success_criteria>

- All tasks completed
- The project has one canonical worker status model
- Named worker identity is established in code and compose topology
- Internal worker status endpoints are concrete enough to support session routing in Phase 2
</success_criteria>

<output>
After completion, create the matching SUMMARY document in this phase directory.
</output>

# Phase 10.6.1.2.1 Context

## Why This Exists

The dedicated server desktop path is temporarily busy, so the project needs one honest local-machine confidence snapshot before household use continues on this PC. This is not a replacement for `Phase 10.6.1.2`; it is a bounded local proof on top of the already-proven `compact visible` runtime baseline.

## Current Truth

- The current proven local runtime baseline is `compact visible`.
- The official local worker set is `dad`, `wife`, `shared-1`, `shared-2`, `shared-3`, `shared-4`, `shared-5`.
- Each worker already has a durable local profile on the operator PC.
- `infra/host-worker/test-host-worker-relay.ps1` is the canonical single-worker proof path.
- The public/internal local stack (`edge`, `control-api`, `host-controller`) is already present and routable on this machine.
- Browser windows should open only when needed and should be closed again after proof.

## Non-Goals

- Do not replace the planned server-hosted runtime migration.
- Do not claim that the operator PC can be turned off after this phase.
- Do not move or rewrite the remote relay topology in this phase.
- Do not add new browser runtime experiments.

## Required Output

This phase must end with:

1. one repeatable local smoke command for all seven workers
2. one explicit local worker matrix artifact
3. one short operator-facing explanation of what the local result does and does not prove

## Key Inputs

- `.planning/PROJECT.md`
- `.planning/ROADMAP.md`
- `.planning/STATE.md`
- `.planning/REQUIREMENTS.md`
- `docs/host-native-worker.md`
- `docs/internal-worker-ops.md`
- `infra/host-worker/test-host-worker-relay.ps1`

---
phase: 21-deployed-windows-disconnected-runtime-and-windows-edge-forced-host-public-owner-502-remediation-after-parity-clean-proof
plan: 02
type: execute
wave: 2
depends_on: [21-01-PLAN.md]
files_modified:
  - services/control-api/src/
  - services/control-api/dist/
  - services/control-api/test/
autonomous: true
requirements: [WPCP-03]
user_setup: []
---

<objective>
Expose the latest Phase 21 post-parity remediation result in the real `control-api` runtime so operators can compare the deployed-host artifact against the same file-backed surface they already trust.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-deployed-windows-disconnected-runtime-and-windows-edge-forced-host-public-owner-502-remediation-after-parity-clean-proof/21-RESEARCH.md
@.planning/phases/21-deployed-windows-disconnected-runtime-and-windows-edge-forced-host-public-owner-502-remediation-after-parity-clean-proof/21-VALIDATION.md
@services/control-api/src/config.ts
@services/control-api/src/server.ts
@services/control-api/src/routes/internal-admin-page.ts
@services/control-api/src/routes/read-latest-state.ts
@services/control-api/test/internal-admin-page.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a file-backed latest post-parity remediation route to control-api and regenerate dist runtime files</name>
  <files>services/control-api/src/config.ts, services/control-api/src/server.ts, services/control-api/src/routes/internal-post-parity-disconnected-runtime-remediation.ts, services/control-api/dist/, services/control-api/test/</files>
  <read_first>services/control-api/src/config.ts, services/control-api/src/server.ts, services/control-api/src/routes/read-latest-state.ts, services/control-api/src/routes/internal-runtime-parity-backport-remediation.ts, services/control-api/test/internal-runtime-parity-backport-remediation.test.ts</read_first>
  <action>Add a new config field `postParityDisconnectedRuntimeRemediationStatePath` sourced from `POST_PARITY_DISCONNECTED_RUNTIME_REMEDIATION_STATE_PATH` with default repo path `infra/data/post-parity-disconnected-runtime-remediation/latest.json`. Create `services/control-api/src/routes/internal-post-parity-disconnected-runtime-remediation.ts` with `GET /internal/post-parity-disconnected-runtime-remediation/latest` that returns `{ latest: null }` when the file is missing and returns the parsed latest remediation payload when it exists. Reuse `read-latest-state.ts` so BOM-prefixed `latest.json` still parses cleanly. Register the route behind the internal admin guard in `server.ts`, add tests that prove both the empty state and the populated state, and regenerate the runtime files the deployed host actually starts by running `npm.cmd --prefix services/control-api run build` so `services/control-api/dist/config.js`, `services/control-api/dist/server.js`, and `services/control-api/dist/routes/internal-post-parity-disconnected-runtime-remediation.js` contain the same route/config contract.</action>
  <verify>rg -n "POST_PARITY_DISCONNECTED_RUNTIME_REMEDIATION_STATE_PATH|postParityDisconnectedRuntimeRemediationStatePath|/internal/post-parity-disconnected-runtime-remediation/latest|latest: null|preRemediationReadyCount|postRemediationReadyCount|dominantRuntimeBlocker|firstFailingHop|forcedHostStatus|publicOwnerStatus|verdict" services/control-api/src services/control-api/dist services/control-api/test</verify>
  <acceptance_criteria>
    - `services/control-api/src/routes/internal-post-parity-disconnected-runtime-remediation.ts` exists
    - `config.ts` supports `POST_PARITY_DISCONNECTED_RUNTIME_REMEDIATION_STATE_PATH`
    - `server.ts` registers `/internal/post-parity-disconnected-runtime-remediation/latest`
    - tests prove the route returns `{ latest: null }` when no remediation result exists
    - the route reuses BOM-safe latest-state parsing
    - `services/control-api/dist/config.js`, `services/control-api/dist/server.js`, and `services/control-api/dist/routes/internal-post-parity-disconnected-runtime-remediation.js` contain the new route/config contract after build
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Render the latest post-parity remediation result on the internal admin page</name>
  <files>services/control-api/src/routes/internal-admin-page.ts, services/control-api/dist/routes/internal-admin-page.js, services/control-api/test/internal-admin-page.test.ts</files>
  <read_first>services/control-api/src/routes/internal-admin-page.ts, services/control-api/test/internal-admin-page.test.ts, services/control-api/src/routes/internal-post-parity-disconnected-runtime-remediation.ts</read_first>
  <action>Add a dedicated `Latest post-parity disconnected runtime remediation` section to `/internal/admin`. Make the page fetch `/internal/post-parity-disconnected-runtime-remediation/latest`, render an empty state `No post-parity disconnected runtime remediation captured yet.` when there is no file, and render the latest timestamp, pre-remediation ready count, post-remediation ready count, dominant runtime blocker, first failing hop, forced-host status, public-owner status, compatibility version, and final verdict when the file exists. Keep the existing worker summary and current latest-result sections intact. Regenerate `services/control-api/dist/routes/internal-admin-page.js` through the build output so the deployed host serves the same admin page from `dist`.</action>
  <verify>rg -n "Latest post-parity disconnected runtime remediation|No post-parity disconnected runtime remediation captured yet.|/internal/post-parity-disconnected-runtime-remediation/latest|pre-remediation ready count|post-remediation ready count|dominant runtime blocker|first failing hop|forced-host status|public-owner status|Compatibility|verdict" services/control-api/src/routes/internal-admin-page.ts services/control-api/dist/routes/internal-admin-page.js services/control-api/test/internal-admin-page.test.ts</verify>
  <acceptance_criteria>
    - the admin page contains the text `Latest post-parity disconnected runtime remediation`
    - the admin page fetches `/internal/post-parity-disconnected-runtime-remediation/latest`
    - the admin page has an explicit empty state `No post-parity disconnected runtime remediation captured yet.`
    - the populated state renders pre-remediation ready count, post-remediation ready count, dominant runtime blocker, first failing hop, forced-host status, public-owner status, compatibility version, and verdict
    - the existing worker summary and current latest-result sections remain present
  </acceptance_criteria>
</task>

</tasks>

<success_criteria>

- the latest post-parity remediation result is visible from the same runtime files the deployed host actually serves
- no operator has to reconstruct the post-parity remediation truth from mixed terminal output and older follow-up notes

</success_criteria>

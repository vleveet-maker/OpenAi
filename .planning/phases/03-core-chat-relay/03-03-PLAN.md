---
phase: 03-core-chat-relay
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - apps/session-client/src/session-types.ts
  - apps/session-client/src/session-api.ts
  - apps/session-client/src/use-session-view.ts
  - apps/session-client/src/app.tsx
  - apps/session-client/src/styles.css
  - apps/session-client/src/app.test.tsx
autonomous: true
requirements: [CHAT-02, CHAT-03, CHAT-04]
user_setup: []
must_haves:
    truths:
      - "The active shared-screen view renders the full current-session conversation history."
      - "Sending a prompt shows a pending assistant state and disables the composer until the reply resolves."
      - "Ended or expired sessions keep history visible but block further sends."
    artifacts:
      - path: "apps/session-client/src/use-session-view.ts"
        provides: "Polling-based session and conversation view state"
        contains: "pendingAssistantMessageId"
      - path: "apps/session-client/src/app.tsx"
        provides: "Shared-screen conversation UI and composer"
        contains: "Assistant is replying"
      - path: "apps/session-client/src/app.test.tsx"
        provides: "Client coverage for send, pending, reply, and ended-history behavior"
        contains: "renders pending assistant state while waiting for a reply"
    key_links:
      - from: "apps/session-client/src/session-api.ts"
        to: "services/control-api/src/routes/public-chat.ts"
        via: "client fetches conversation history and posts prompts through the public chat API"
        pattern: "/api/sessions/${sessionId}/messages"
      - from: "apps/session-client/src/use-session-view.ts"
        to: "apps/session-client/src/app.tsx"
        via: "view hook exposes both session lifecycle state and conversation relay state"
        pattern: "messages|pendingAssistantMessageId|sendMessage"
---

<objective>
Render the real Phase 3 conversation history and pending reply experience in the shared-screen app.

Purpose: Turn the Phase 2 placeholder shell into a usable text chat surface for one active timed session.
Output: Client-side chat types and API helpers, polling view state for history and pending replies, an active-session composer and history UI, and automated client tests for the Phase 3 flow.
</objective>

<execution_context>
@../gsd/get-shit-done/workflows/execute-plan.md
@../gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-chat-relay/03-CONTEXT.md
@.planning/phases/03-core-chat-relay/03-RESEARCH.md
@.planning/phases/03-core-chat-relay/03-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add client-side conversation types and public chat API helpers</name>
  <files>apps/session-client/src/session-types.ts, apps/session-client/src/session-api.ts</files>
  <read_first>apps/session-client/src/session-types.ts, apps/session-client/src/session-api.ts, services/control-api/src/routes/public-chat.ts, services/control-api/src/chat/chat-types.ts</read_first>
  <action>Extend apps/session-client/src/session-types.ts with ChatMessageRole, ChatMessageState, SessionMessageRecord, and SessionConversationSnapshot that match the public chat API created in Phase 3 backend work. Update apps/session-client/src/session-api.ts with getConversation(sessionId) and sendMessage(sessionId, bodyText) helpers that target /api/sessions/:sessionId/messages and preserve the current JSON error-handling style.</action>
  <verify>rg -n "ChatMessageRole|SessionConversationSnapshot|getConversation|sendMessage|/api/sessions/:sessionId/messages" apps/session-client/src/session-types.ts apps/session-client/src/session-api.ts</verify>
  <acceptance_criteria>
    - apps/session-client/src/session-types.ts contains "SessionConversationSnapshot"
    - apps/session-client/src/session-types.ts contains "pending"
    - apps/session-client/src/session-api.ts contains "getConversation"
    - apps/session-client/src/session-api.ts contains "sendMessage"
  </acceptance_criteria>
  <done>The client understands the Phase 3 conversation contract and can call the new public chat routes.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the placeholder shell with active history and pending reply UI</name>
  <files>apps/session-client/src/use-session-view.ts, apps/session-client/src/app.tsx, apps/session-client/src/styles.css</files>
  <read_first>apps/session-client/src/use-session-view.ts, apps/session-client/src/app.tsx, apps/session-client/src/styles.css, .planning/phases/03-core-chat-relay/03-CONTEXT.md</read_first>
  <action>Update apps/session-client/src/use-session-view.ts so bootstrap and the existing 5000 ms poll load both the session snapshot and the conversation snapshot. Expose messages, pendingAssistantMessageId, canSend, sendMessage, and composer error state from the hook. Update apps/session-client/src/app.tsx so the Active state renders the full message history, enables a composer with a 4000-character textarea when canSend is true, clears the draft after a successful send, and shows a visible pending assistant bubble labeled "Assistant is replying..." while the placeholder is pending. Keep Queued and Ended views on the same shell, but disable the composer with explicit copy explaining that sending is blocked until the session is active. Update styles.css to support user and assistant message bubbles, a pending state treatment, and a disabled composer without changing the existing shared-screen layout structure.</action>
  <verify>rg -n "Assistant is replying|canSend|pendingAssistantMessageId|4000|sending is blocked" apps/session-client/src/use-session-view.ts apps/session-client/src/app.tsx apps/session-client/src/styles.css</verify>
  <acceptance_criteria>
    - apps/session-client/src/app.tsx contains "Assistant is replying"
    - apps/session-client/src/app.tsx contains "4000"
    - apps/session-client/src/use-session-view.ts contains "getConversation"
    - apps/session-client/src/use-session-view.ts contains "sendMessage"
  </acceptance_criteria>
  <done>The shared-screen app now exposes a real current-session chat surface with pending reply handling.</done>
</task>

<task type="auto">
  <name>Task 3: Add client coverage for send, pending, reply, and ended-history flows</name>
  <files>apps/session-client/src/app.test.tsx</files>
  <read_first>apps/session-client/src/app.tsx, apps/session-client/src/use-session-view.ts, apps/session-client/src/session-api.ts</read_first>
  <action>Extend apps/session-client/src/app.test.tsx with tests that verify: an active session loads prior history on refresh, sending a message posts to the public chat API and shows a pending assistant bubble, the pending bubble is replaced by final assistant text after polling, the composer stays disabled while a reply is pending, and ended or expired sessions still render conversation history while blocking send actions.</action>
  <verify>rg -n "loads prior conversation history|renders pending assistant state while waiting for a reply|replaces the pending bubble with the assistant reply|blocks send when the session is ended" apps/session-client/src/app.test.tsx</verify>
  <acceptance_criteria>
    - apps/session-client/src/app.test.tsx contains "loads prior conversation history on refresh"
    - apps/session-client/src/app.test.tsx contains "renders pending assistant state while waiting for a reply"
    - apps/session-client/src/app.test.tsx contains "replaces the pending bubble with the assistant reply after polling"
    - apps/session-client/src/app.test.tsx contains "blocks send when the session is ended"
  </acceptance_criteria>
  <done>The visible Phase 3 chat experience is protected by client tests for the main operator flow.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] session-client tests cover history restore, pending reply state, reply completion, and ended-session send blocking
- [ ] the active composer never allows a second send while a reply is pending
</verification>

<success_criteria>

- All tasks completed
- The shared-screen app renders the full current-session conversation history
- The app shows a pending assistant state while waiting for a reply
- Ended or expired sessions keep history visible and block new sends
</success_criteria>

<output>
After completion, create the matching SUMMARY document in this phase directory.
</output>

---
phase: 03-core-chat-relay
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - workers/agent/package.json
  - workers/agent/tsconfig.json
  - workers/agent/vitest.config.ts
  - workers/agent/src/server.ts
  - workers/agent/src/chat-relay/relay-types.ts
  - workers/agent/src/chat-relay/selector-map.ts
  - workers/agent/src/chat-relay/relay-runner.ts
  - workers/agent/test/relay-runner.test.ts
  - services/control-api/src/chat/worker-relay-client.ts
  - services/control-api/src/chat/chat-relay-service.ts
  - services/control-api/test/chat-relay-service.test.ts
autonomous: true
requirements: [CHAT-01, CHAT-02, CHAT-03]
user_setup: []
must_haves:
    truths:
      - "control-api can dispatch a persisted user turn to the assigned worker over an internal relay contract."
      - "worker-agent submits the prompt into the existing persistent ChatGPT browser context and captures one assistant reply back."
      - "Relay completion or failure resolves the pending assistant placeholder instead of leaving the conversation stuck forever."
    artifacts:
      - path: "workers/agent/src/server.ts"
        provides: "Internal worker relay endpoint"
        contains: "/internal/relay/messages"
      - path: "workers/agent/src/chat-relay/relay-runner.ts"
        provides: "Playwright submission and reply capture logic"
        contains: "runRelay"
      - path: "services/control-api/src/chat/worker-relay-client.ts"
        provides: "Fetch-based client for worker relay dispatch"
        contains: "internal/relay/messages"
    key_links:
      - from: "services/control-api/src/chat/chat-relay-service.ts"
        to: "services/control-api/src/chat/worker-relay-client.ts"
        via: "pending assistant placeholders are completed from the worker relay result"
        pattern: "deliver|completeAssistantMessage|failAssistantMessage"
      - from: "workers/agent/src/server.ts"
        to: "workers/agent/src/chat-relay/relay-runner.ts"
        via: "internal route delegates to one relay runner using the persistent browser context"
        pattern: "runRelay"
---

<objective>
Implement the worker-side Playwright relay and connect it to the control-api contract.

Purpose: Turn the Phase 3 send protocol into a real browser-driven message submission and assistant reply capture flow on the assigned worker.
Output: Worker-agent build and test scaffolding, an internal relay endpoint, Playwright relay runner logic, a control-api worker relay client, and completion or failure wiring for pending assistant replies.
</objective>

<execution_context>
@../gsd/get-shit-done/workflows/execute-plan.md
@../gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-chat-relay/03-CONTEXT.md
@.planning/phases/03-core-chat-relay/03-RESEARCH.md
@.planning/phases/03-core-chat-relay/03-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add worker-agent build, test, and relay route scaffolding</name>
  <files>workers/agent/package.json, workers/agent/tsconfig.json, workers/agent/vitest.config.ts, workers/agent/src/server.ts, workers/agent/src/chat-relay/relay-types.ts</files>
  <read_first>workers/agent/package.json, workers/agent/src/server.ts, workers/agent/src/browser-launch.ts, .planning/phases/03-core-chat-relay/03-RESEARCH.md</read_first>
  <action>Add TypeScript build and Vitest scripts to workers/agent/package.json, create workers/agent/tsconfig.json with rootDir src and outDir dist, and create workers/agent/vitest.config.ts for node-based unit tests. Add workers/agent/src/chat-relay/relay-types.ts with WorkerRelayRequest fields sessionId, userMessageId, assistantMessageId, bodyText and WorkerRelayResult fields assistantText, completedAt, pageUrl, failureCode. Update workers/agent/src/server.ts to launch or hold the persistent browser context at startup and expose POST /internal/relay/messages that accepts the relay request body and returns the relay result.</action>
  <verify>rg -n "build|test|WorkerRelayRequest|assistantMessageId|/internal/relay/messages|launchWorkerBrowser" workers/agent/package.json workers/agent/tsconfig.json workers/agent/vitest.config.ts workers/agent/src/server.ts workers/agent/src/chat-relay/relay-types.ts</verify>
  <acceptance_criteria>
    - workers/agent/package.json contains "\"build\""
    - workers/agent/package.json contains "\"test\""
    - workers/agent/src/chat-relay/relay-types.ts contains "assistantMessageId"
    - workers/agent/src/server.ts contains "/internal/relay/messages"
  </acceptance_criteria>
  <done>The worker-agent can be built and tested locally and exposes the internal relay API surface needed by control-api.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Playwright prompt submission and assistant reply capture</name>
  <files>workers/agent/src/chat-relay/selector-map.ts, workers/agent/src/chat-relay/relay-runner.ts, workers/agent/test/relay-runner.test.ts</files>
  <read_first>workers/agent/src/browser-launch.ts, workers/agent/src/server.ts, .planning/phases/03-core-chat-relay/03-CONTEXT.md, .planning/phases/03-core-chat-relay/03-RESEARCH.md</read_first>
  <action>Create workers/agent/src/chat-relay/selector-map.ts with centralized selector candidates named composerSelectors, sendButtonSelectors, assistantTurnSelectors, and generatingIndicators. Seed composerSelectors with page.getByRole("textbox").last() and a fallback locator("[contenteditable='true']").last(); prefer a send button lookup by role name /send/i and keep keyboard Enter as the final submit fallback. Create workers/agent/src/chat-relay/relay-runner.ts with runRelay(context, request, options) that uses one per-worker relay lock, ensures a ChatGPT page exists, submits bodyText, snapshots the last assistant turn before submit, then waits for a newer assistant turn or changed assistant text and treats the reply as complete only after the captured text stays unchanged for a 1500 ms quiescence window. Use a relay timeout of 120000 ms and return failureCode values selector_not_found, submit_failed, reply_timeout, or reply_empty when the happy path does not complete. Add unit tests in workers/agent/test/relay-runner.test.ts with fake page and locator doubles for successful submit and capture, selector fallback use, and timeout failure.</action>
  <verify>rg -n "composerSelectors|sendButtonSelectors|assistantTurnSelectors|runRelay|1500|120000|selector_not_found|reply_timeout" workers/agent/src/chat-relay/selector-map.ts workers/agent/src/chat-relay/relay-runner.ts workers/agent/test/relay-runner.test.ts</verify>
  <acceptance_criteria>
    - workers/agent/src/chat-relay/selector-map.ts contains "composerSelectors"
    - workers/agent/src/chat-relay/relay-runner.ts contains "runRelay"
    - workers/agent/src/chat-relay/relay-runner.ts contains "reply_timeout"
    - workers/agent/test/relay-runner.test.ts contains "returns assistant text after it stabilizes"
  </acceptance_criteria>
  <done>The worker can drive the logged-in ChatGPT browser and convert one prompt into one captured assistant reply with bounded waiting.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the real worker relay client into control-api completion flow</name>
  <files>services/control-api/src/chat/worker-relay-client.ts, services/control-api/src/chat/chat-relay-service.ts, services/control-api/test/chat-relay-service.test.ts</files>
  <read_first>services/control-api/src/chat/chat-relay-service.ts, services/control-api/src/workers/worker-registry.ts, workers/agent/src/chat-relay/relay-types.ts</read_first>
  <action>Create services/control-api/src/chat/worker-relay-client.ts with a fetch-based deliver method that POSTs to ${agentBaseUrl}/internal/relay/messages and returns the parsed WorkerRelayResult. Update services/control-api/src/chat/chat-relay-service.ts to call that client for each accepted send and, when the promise resolves, complete the pending assistant message with assistantText and completedAt. If the worker relay returns failureCode or throws, mark the pending assistant message failed with the failureCode value and keep the stored user message visible. Extend services/control-api/test/chat-relay-service.test.ts to cover successful completion and failure mapping through the real relay-client contract shape.</action>
  <verify>rg -n "internal/relay/messages|deliver|completeAssistantMessage|failAssistantMessage|failureCode" services/control-api/src/chat/worker-relay-client.ts services/control-api/src/chat/chat-relay-service.ts services/control-api/test/chat-relay-service.test.ts</verify>
  <acceptance_criteria>
    - services/control-api/src/chat/worker-relay-client.ts contains "internal/relay/messages"
    - services/control-api/src/chat/chat-relay-service.ts contains "completeAssistantMessage"
    - services/control-api/src/chat/chat-relay-service.ts contains "failAssistantMessage"
    - services/control-api/test/chat-relay-service.test.ts contains "marks the pending assistant message failed when relay delivery fails"
  </acceptance_criteria>
  <done>Pending assistant placeholders now resolve into final assistant text or a structured failure after the worker relay runs.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] worker-agent has build and test commands
- [ ] worker-agent relay runner tests cover success, fallback, and timeout paths
- [ ] control-api resolves pending assistant placeholders on worker success or failure
</verification>

<success_criteria>

- All tasks completed
- A real browser relay path exists from control-api to worker-agent to ChatGPT and back
- Pending assistant placeholders resolve into final reply or failure
- Worker relay logic is unit-tested without depending on live ChatGPT automation
</success_criteria>

<output>
After completion, create the matching SUMMARY document in this phase directory.
</output>

---
phase: 03-core-chat-relay
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/control-api/src/chat/chat-types.ts
  - services/control-api/src/chat/chat-store.ts
  - services/control-api/src/chat/chat-relay-service.ts
  - services/control-api/src/routes/public-chat.ts
  - services/control-api/src/server.ts
  - services/control-api/test/chat-relay-service.test.ts
  - services/control-api/test/public-chat.test.ts
autonomous: true
requirements: [CHAT-01, CHAT-03, CHAT-04]
user_setup: []
must_haves:
    truths:
      - "Active sessions have a durable current-session message history stored in control-api."
      - "Sending a prompt creates one complete user message and one pending assistant placeholder before browser relay starts."
      - "Only active sessions without an in-flight assistant reply can accept a new outbound prompt."
    artifacts:
      - path: "services/control-api/src/chat/chat-store.ts"
        provides: "SQLite-backed session-scoped chat history"
        contains: "CREATE TABLE IF NOT EXISTS session_messages"
      - path: "services/control-api/src/chat/chat-relay-service.ts"
        provides: "Send eligibility checks and pending placeholder orchestration"
        contains: "pendingAssistantMessageId"
      - path: "services/control-api/src/routes/public-chat.ts"
        provides: "Public session-scoped chat API"
        contains: "/api/sessions/:sessionId/messages"
    key_links:
      - from: "services/control-api/src/routes/public-chat.ts"
        to: "services/control-api/src/chat/chat-relay-service.ts"
        via: "public send and list routes delegate to one relay service"
        pattern: "sendMessage|getConversationSnapshot"
      - from: "services/control-api/src/chat/chat-relay-service.ts"
        to: "services/control-api/src/sessions/session-service.ts"
        via: "chat send eligibility stays pinned to the authoritative session lifecycle"
        pattern: "getSessionSnapshot|state === \"active\""
---

<objective>
Define the durable relay protocol between the shared-screen app, control-api, and the assigned worker.

Purpose: Give Phase 3 a canonical message model, send contract, and public history API before Playwright browser automation is added.
Output: Session-scoped chat types and storage, a relay orchestration service with pending placeholder rules, public chat routes, and control-api tests for the new contract.
</objective>

<execution_context>
@../gsd/get-shit-done/workflows/execute-plan.md
@../gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-chat-relay/03-CONTEXT.md
@.planning/phases/03-core-chat-relay/03-RESEARCH.md
@.planning/phases/02-timed-session-access/02-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add durable session-chat types and storage</name>
  <files>services/control-api/src/chat/chat-types.ts, services/control-api/src/chat/chat-store.ts</files>
  <read_first>services/control-api/src/sessions/session-types.ts, services/control-api/src/sessions/session-store.ts, .planning/phases/03-core-chat-relay/03-CONTEXT.md, .planning/phases/03-core-chat-relay/03-RESEARCH.md</read_first>
  <action>Create services/control-api/src/chat/chat-types.ts with ChatMessageRole exactly user and assistant, ChatMessageState exactly pending, complete, and failed, and SessionMessageRecord fields messageId, sessionId, workerId, role, state, body, replyToMessageId, failureCode, createdAt, updatedAt, completedAt. Create services/control-api/src/chat/chat-store.ts with a session_messages table containing message_id, session_id, worker_id, role, state, body, reply_to_message_id, failure_code, created_at, updated_at, completed_at plus indexes on (session_id, created_at, message_id) and (session_id, state, role). Add helpers to insert messages, complete or fail a pending assistant message, list all messages for one session, and locate the pending assistant placeholder for a session.</action>
  <verify>rg -n "ChatMessageRole|pending|complete|failed|CREATE TABLE IF NOT EXISTS session_messages|reply_to_message_id" services/control-api/src/chat/chat-types.ts services/control-api/src/chat/chat-store.ts</verify>
  <acceptance_criteria>
    - services/control-api/src/chat/chat-types.ts contains "ChatMessageRole"
    - services/control-api/src/chat/chat-types.ts contains "pending"
    - services/control-api/src/chat/chat-store.ts contains "CREATE TABLE IF NOT EXISTS session_messages"
    - services/control-api/src/chat/chat-store.ts contains "reply_to_message_id"
  </acceptance_criteria>
  <done>The backend has a durable session-scoped chat history model that can represent user turns and pending assistant replies.</done>
</task>

<task type="auto">
  <name>Task 2: Implement relay orchestration rules and public chat routes</name>
  <files>services/control-api/src/chat/chat-relay-service.ts, services/control-api/src/routes/public-chat.ts, services/control-api/src/server.ts</files>
  <read_first>services/control-api/src/server.ts, services/control-api/src/routes/public-sessions.ts, services/control-api/src/sessions/session-service.ts, services/control-api/src/workers/worker-registry.ts</read_first>
  <action>Create services/control-api/src/chat/chat-relay-service.ts with methods getConversationSnapshot(sessionId), sendMessage(sessionId, bodyText), completeAssistantMessage(assistantMessageId, assistantText), and failAssistantMessage(assistantMessageId, failureCode). Enforce these exact rules in sendMessage: session must exist, session.state must equal active, session.workerId must be non-null, bodyText must be trimmed to 1 through 4000 characters, and the session must have no existing assistant message in pending state. On a valid send, insert one user message in complete state and one assistant message in pending state linked by replyToMessageId, then invoke an injected relay transport with workerId, sessionId, userMessageId, assistantMessageId, and bodyText without blocking the HTTP response. Create services/control-api/src/routes/public-chat.ts with GET /api/sessions/:sessionId/messages and POST /api/sessions/:sessionId/messages, and mount it in services/control-api/src/server.ts. The public conversation response must include sessionId, worker, canSend, pendingAssistantMessageId, and messages.</action>
  <verify>rg -n "getConversationSnapshot|sendMessage|completeAssistantMessage|failAssistantMessage|/api/sessions/:sessionId/messages|pendingAssistantMessageId|canSend" services/control-api/src/chat/chat-relay-service.ts services/control-api/src/routes/public-chat.ts services/control-api/src/server.ts</verify>
  <acceptance_criteria>
    - services/control-api/src/chat/chat-relay-service.ts contains "sendMessage"
    - services/control-api/src/chat/chat-relay-service.ts contains "pendingAssistantMessageId"
    - services/control-api/src/routes/public-chat.ts contains "/api/sessions/:sessionId/messages"
    - services/control-api/src/server.ts contains "createPublicChatRouter"
  </acceptance_criteria>
  <done>The application has a public send-and-history contract that stays pinned to the active session and creates a durable pending reply state.</done>
</task>

<task type="auto">
  <name>Task 3: Cover relay rules and public chat routes with control-api tests</name>
  <files>services/control-api/test/chat-relay-service.test.ts, services/control-api/test/public-chat.test.ts</files>
  <read_first>services/control-api/src/chat/chat-store.ts, services/control-api/src/chat/chat-relay-service.ts, services/control-api/src/routes/public-chat.ts, services/control-api/test/public-sessions.test.ts</read_first>
  <action>Create services/control-api/test/chat-relay-service.test.ts with an injected fake relay transport and tests that verify: a valid active session send creates one complete user message plus one pending assistant placeholder, queued and ended sessions reject sends, a second send while pending returns a conflict, and completing or failing the pending assistant message updates the stored conversation. Create services/control-api/test/public-chat.test.ts with route coverage for POST and GET /api/sessions/:sessionId/messages, invalid body validation, active-only send rules, and history listing that never exposes internal profilePath or recovery URLs.</action>
  <verify>rg -n "pending assistant placeholder|rejects sends outside active sessions|GET /api/sessions/:sessionId/messages|profilePath" services/control-api/test/chat-relay-service.test.ts services/control-api/test/public-chat.test.ts</verify>
  <acceptance_criteria>
    - services/control-api/test/chat-relay-service.test.ts contains "creates a user message and pending assistant placeholder"
    - services/control-api/test/chat-relay-service.test.ts contains "rejects sends outside active sessions"
    - services/control-api/test/public-chat.test.ts contains "lists conversation history for an active session"
    - services/control-api/test/public-chat.test.ts contains "does not expose internal worker fields"
  </acceptance_criteria>
  <done>The Phase 3 relay contract is automated and safe before browser automation is introduced.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] control-api tests cover send eligibility, pending placeholder creation, and conversation listing
- [ ] public chat routes stay session-local and do not expose internal worker fields
</verification>

<success_criteria>

- All tasks completed
- Active sessions can create durable pending reply state and list full current-session history
- Public chat routes exist for send and history
- Control-api relay rules are test-covered
</success_criteria>

<output>
After completion, create the matching SUMMARY document in this phase directory.
</output>

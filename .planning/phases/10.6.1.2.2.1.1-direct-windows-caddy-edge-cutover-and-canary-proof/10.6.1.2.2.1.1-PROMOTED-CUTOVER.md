# 10.6.1.2.2.1.1 Promoted Cutover

- Phase: `10.6.1.2.2.1.1`
- Date: `2026-03-31`
- Status: `promoted success`

## Short Report

The promoted Windows public edge was activated successfully for HTTP, but it remains intentionally narrow and preserve-first.

Simple explanation:

- the public HTTP path is now active through Windows `Caddy`
- the promoted API was still kept on an allowlist for `shared-6`
- this avoided touching the other 8 accounts while proving the public path

## What Passed

- public `http://77.66.186.75` now routes through the Windows `Caddy` edge
- `healthz` works through the promoted HTTP path
- `v1/models` works through the promoted HTTP path

## What Stayed Protected

- `4040` remained private
- worker-agent ports remained private
- the broader 9-worker pool was not mass-started

## Preserve-First Notes

- promoted mode was proven only over HTTP in this phase
- allowlist remained restricted to `shared-6`
- this was deliberate, so the public edge could be proven without broadening blast radius

## Result

Decision: `promoted success`

Important qualifier:

- `HTTPS` is still broken on direct IP handshake
- outside-NAT independent `POST /v1/chat/completions` is still not fully finished

That is why the phase verdict is not `safe_to_promote`.

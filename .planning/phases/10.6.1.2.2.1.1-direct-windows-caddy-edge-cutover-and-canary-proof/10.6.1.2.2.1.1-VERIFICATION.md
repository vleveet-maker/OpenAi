# 10.6.1.2.2.1.1 Verification

- Phase: `10.6.1.2.2.1.1`
- Date: `2026-03-31`
- Status: `complete`
- Final verdict: `safe_but_hold`

## Short Report

This phase did the direct Windows-host execution it was supposed to do, and it did it without damaging the 9 live accounts.

Simple explanation:

- direct Windows-host backup and audit happened
- shadow canary on `shared-6` passed
- promoted cutover passed over HTTP
- public HTTP `healthz` and `v1/models` now return `200`
- the old HTTP `308` redirect is gone
- but HTTPS still fails and the fully independent outside-NAT canary chat proof is still incomplete

So the honest outcome is:

- the edge is in a much better place
- the preserve-first execution worked
- but Phase 11 still should not resume yet

## What Landed

Phase artifacts:

- [10.6.1.2.2.1.1-HOST-EDGE-AUDIT.md](d:/OpenAi/.planning/phases/10.6.1.2.2.1.1-direct-windows-caddy-edge-cutover-and-canary-proof/10.6.1.2.2.1.1-HOST-EDGE-AUDIT.md)
- [10.6.1.2.2.1.1-SHADOW-CANARY-PROOF.md](d:/OpenAi/.planning/phases/10.6.1.2.2.1.1-direct-windows-caddy-edge-cutover-and-canary-proof/10.6.1.2.2.1.1-SHADOW-CANARY-PROOF.md)
- [10.6.1.2.2.1.1-PROMOTED-CUTOVER.md](d:/OpenAi/.planning/phases/10.6.1.2.2.1.1-direct-windows-caddy-edge-cutover-and-canary-proof/10.6.1.2.2.1.1-PROMOTED-CUTOVER.md)
- [10.6.1.2.2.1.1-EXTERNAL-PROOF.md](d:/OpenAi/.planning/phases/10.6.1.2.2.1.1-direct-windows-caddy-edge-cutover-and-canary-proof/10.6.1.2.2.1.1-EXTERNAL-PROOF.md)

## Test And Evidence Summary

Direct host execution:

- preserve-first backup/audit executed on the deployed Windows host
- rollback point captured before shadow/promoted proof
- existing Windows `Caddy` edge reconciled in place

Shadow canary:

- worker: `shared-6`
- `GET /healthz` -> `200`
- `GET /v1/models` -> `200`
- `POST /v1/chat/completions` -> `200`
- `shared-6` stopped again after proof
- remaining 8 workers untouched

Promoted cutover:

- promoted HTTP path active through Windows `Caddy`
- promoted API kept allowlisted to `shared-6`
- `4040` remained private
- worker-agent ports remained private

Outside proof:

- `GET http://77.66.186.75/healthz` -> `200`
- `GET http://77.66.186.75/v1/models` -> `200`
- old HTTP `308` redirect gone
- `https://77.66.186.75` still fails on TLS handshake
- independent outside-NAT `POST /v1/chat/completions` not yet completed in this phase

## Requirement Verdict

- `WCUT-01` complete
- `WCUT-02` complete
- `WCUT-03` complete
- `WCUT-04` complete as a blocker-classified preserve-first verdict

## Final Decision

Decision: `safe_but_hold`

Why:

1. The direct Windows-host execution finally happened.
2. The preserve-first canary path on `shared-6` passed.
3. The promoted HTTP edge is now live and cleaner than before.
4. The 9 logged-in accounts remained intact.
5. HTTPS and the last independent outside chat proof are still not good enough to call the public API fully rollout-ready.

## Impact On Phase 11

`Phase 11` still cannot resume yet.

It remains blocked until one narrow follow-up finishes:

- Windows public HTTPS/TLS reconciliation
- one fully independent outside-NAT `POST /v1/chat/completions` proof

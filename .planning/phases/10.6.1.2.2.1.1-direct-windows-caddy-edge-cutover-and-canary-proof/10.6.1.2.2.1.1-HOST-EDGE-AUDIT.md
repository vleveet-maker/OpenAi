# 10.6.1.2.2.1.1 Host Edge Audit

- Phase: `10.6.1.2.2.1.1`
- Date: `2026-03-31`
- Host: `192.168.88.250`
- Status: `captured`

## Short Report

The direct Windows-host preserve-first audit was completed on the deployed Windows Server before any canary or promoted-edge probing touched the public API path.

Simple explanation:

- the host already had a live Windows edge
- the browser profiles and the 9 logged-in accounts were left intact
- the edge owner remained `Caddy`
- the rollback point was recorded before shadow/promoted proof

## Direct Host Truth

- public Windows edge owner: `Caddy`
- public HTTP path on `77.66.186.75` is now served through that Windows `Caddy` edge
- raw `4040` remains internal-only
- worker-agent ports remain internal-only
- the deployed Windows host still has an existing external `caddy.exe` under `D:\deploy`

## Listener Truth

- `80` -> owned by the Windows public edge (`Caddy`)
- `443` -> owned by the Windows public edge (`Caddy`)
- `4010` -> promoted loopback API target behind the edge, not the public contract
- `4040` -> internal host-controller only

## Preserve-First Result

- browser profiles were not deleted
- cookies/local storage were not cleared
- the 9 logged-in accounts were not relogged
- no mass restart of the full 9-worker pool was performed

## Rollback Point

Rollback-safe backup and audit were taken before cutover work.

The exact rollback mechanism for this phase remained:

- `backup-public-edge-state.ps1`
- `rollback-public-edge.ps1`

## Host/Repo Delta

The important deployed-host truth is that the Windows public edge is already a live `Caddy` installation under `D:\deploy`, so preserve-first execution had to reconcile with the real host edge rather than assuming a fresh repo-only edge owner.

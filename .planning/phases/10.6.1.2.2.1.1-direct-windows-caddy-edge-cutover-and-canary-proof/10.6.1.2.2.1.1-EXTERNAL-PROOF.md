# 10.6.1.2.2.1.1 External Proof

- Phase: `10.6.1.2.2.1.1`
- Date: `2026-03-31`
- Status: `partial`
- Public host: `77.66.186.75`

## Short Report

Outside proof improved meaningfully: HTTP is now live through the Windows `Caddy` edge and the old `308` redirect on plain HTTP is gone. But HTTPS and a fully independent outside-NAT canary chat proof are still not complete.

## Confirmed From Outside

- `GET http://77.66.186.75/healthz` -> `200`
- `GET http://77.66.186.75/v1/models` -> `200`

## Redirect Status

- old plain-HTTP `308` redirect is no longer present on the public HTTP path

## Remaining Outside Blockers

- `https://77.66.186.75` still fails on TLS handshake
- an independent outside-NAT `POST /v1/chat/completions` proof was not completed in this phase

## Preserve-First Interpretation

This is not a browser-account problem.

The remaining issue is now narrow:

- public HTTP edge is active
- public HTTPS/TLS is still broken
- final outside-client chat proof still needs one more preserve-first follow-up

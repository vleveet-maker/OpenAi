---
phase: 06-stabilize-persistent-browser-profile-recovery-and-live-worker-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/worker-runtime/start-worker-runtime.sh
  - infra/docker-compose.yml
  - workers/agent/src/browser-launch.ts
  - workers/agent/test/browser-access-runtime.test.ts
autonomous: true
requirements: [WORK-03, WORK-04, OBSV-02]
user_setup: []
must_haves:
    truths:
      - "Worker runtime startup removes stale Chromium singleton artifacts before Playwright launches the persistent profile."
      - "Worker containers no longer rely on watch-mode startup in the runtime path."
      - "A recreated worker pool returns to full `ready` status without manual profile cleanup."
    artifacts:
      - path: "infra/worker-runtime/start-worker-runtime.sh"
        provides: "shell-level profile lock cleanup and non-watch worker startup"
        contains: "cleanup_profile_locks"
      - path: "infra/worker-runtime/start-worker-runtime.sh"
        provides: "single-process worker launch path"
        contains: "npm run start --prefix workers/agent"
      - path: "workers/agent/test/browser-access-runtime.test.ts"
        provides: "lock cleanup regression coverage"
        contains: "clears stale chromium singleton artifacts"
---

<objective>
Stabilize persistent Chromium profile recovery so rebuilt or restarted workers return to `ready` without manual cleanup.
</objective>

<execution_context>
@../gsd/get-shit-done/workflows/execute-plan.md
@../gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-stabilize-persistent-browser-profile-recovery-and-live-worker-readiness/06-CONTEXT.md
@infra/worker-runtime/start-worker-runtime.sh
@workers/agent/src/browser-launch.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden worker runtime startup against stale Chromium profile locks</name>
  <files>infra/worker-runtime/start-worker-runtime.sh, workers/agent/src/browser-launch.ts, workers/agent/test/browser-access-runtime.test.ts</files>
  <read_first>infra/worker-runtime/start-worker-runtime.sh, workers/agent/src/browser-launch.ts, workers/agent/src/server.ts</read_first>
  <action>Update `infra/worker-runtime/start-worker-runtime.sh` so the runtime deletes stale Chromium singleton artifacts from `WORKER_PROFILE_PATH` before launching Playwright, and start the worker runtime with `npm run start --prefix workers/agent` instead of `npm run dev --prefix workers/agent`. Keep the TypeScript-side singleton cleanup coverage in `workers/agent/src/browser-launch.ts` and `workers/agent/test/browser-access-runtime.test.ts`. Rebuild the worker containers and verify that `readyz` returns all workers to `ready` after a full worker recreate and an additional single-worker restart.</action>
  <verify>rg -n "cleanup_profile_locks|npm run start --prefix workers/agent|SingletonLock|Default/LOCK" infra/worker-runtime/start-worker-runtime.sh workers/agent/src/browser-launch.ts workers/agent/test/browser-access-runtime.test.ts</verify>
  <acceptance_criteria>
    - infra/worker-runtime/start-worker-runtime.sh contains "cleanup_profile_locks"
    - infra/worker-runtime/start-worker-runtime.sh contains "npm run start --prefix workers/agent"
    - workers/agent/test/browser-access-runtime.test.ts contains "SingletonLock"
    - live `GET /readyz` reports `workerStatusCounts.ready = 3` after worker recreate
    - live `GET /readyz` reports `workerStatusCounts.ready = 3` after restarting one worker
  </acceptance_criteria>
  <done>Worker profile recovery is stable enough to close the milestone without a separate manual cleanup runbook.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `workers/agent` build passes
- [ ] `workers/agent` tests pass
- [ ] `docker compose -f infra/docker-compose.yml config` passes
- [ ] Live recreate and single-worker restart both recover the pool to `ready`
</verification>

<success_criteria>

- All tasks completed
- Worker runtime survives persistent profile restarts without manual cleanup
- Health surfaces return a full ready worker pool after restart

</success_criteria>

<output>
After completion, create the matching SUMMARY document in this phase directory.
</output>

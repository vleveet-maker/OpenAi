---
phase: 10.6.1.1-mikrotik-public-ingress-and-nat-hardening
plan: 02
type: execute
wave: 2
depends_on: [10.6.1.1-01-PLAN.md]
files_modified:
  - infra/remote/
  - docs/remote-relay-server.md
  - .planning/phases/10.6.1.1-mikrotik-public-ingress-and-nat-hardening/10.6.1.1-ROUTER-AUDIT.md
autonomous: true
requirements: [MTIK-02, MTIK-03]
user_setup: []
---

<objective>
Define and apply one deliberate MikroTik NAT path to the server nginx edge while hardening router management exposure.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/10.6.1.1-mikrotik-public-ingress-and-nat-hardening/10.6.1.1-CONTEXT.md
@.planning/phases/10.6.1.1-mikrotik-public-ingress-and-nat-hardening/10.6.1.1-ROUTER-AUDIT.md
@docs/remote-relay-server.md
@infra/remote/nginx/owmcgp-remote-relay.conf
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the deliberate RouterOS ingress target</name>
  <files>infra/remote/, docs/remote-relay-server.md</files>
  <read_first>.planning/phases/10.6.1.1-mikrotik-public-ingress-and-nat-hardening/10.6.1.1-ROUTER-AUDIT.md, docs/remote-relay-server.md, infra/remote/nginx/owmcgp-remote-relay.conf</read_first>
  <action>Define the target public ingress path as `public IP tcp/80 -> MikroTik dst-nat -> 192.168.88.2:80 -> nginx -> 127.0.0.1:4010`. If execution discovers that tcp/80 cannot be used safely, record one exact alternate ingress port and the exact reason. Do not use raw `:4010` as the public target.</action>
  <verify>rg -n "192\\.168\\.88\\.2:80|dst-nat|127\\.0\\.0\\.1:4010|public IP" infra/remote docs/remote-relay-server.md</verify>
  <acceptance_criteria>
    - one exact ingress target path is documented
    - the target path ends at nginx, not the raw relay socket
  </acceptance_criteria>
</task>

<task type="manual">
  <name>Task 2: Apply or precisely stage MikroTik NAT and management hardening</name>
  <files>infra/remote/, .planning/phases/10.6.1.1-mikrotik-public-ingress-and-nat-hardening/10.6.1.1-ROUTER-AUDIT.md, docs/remote-relay-server.md</files>
  <read_first>.planning/phases/10.6.1.1-mikrotik-public-ingress-and-nat-hardening/10.6.1.1-ROUTER-AUDIT.md, docs/remote-relay-server.md</read_first>
  <action>Create a backup-first RouterOS apply path that does these exact things when router auth is available: export or backup current config, add or update the dst-nat rule that forwards the chosen public relay port to `192.168.88.2`, and restrict router management services (`www`, `ssh`, `winbox`, or whichever are currently exposed) so they are not left broadly reachable from WAN. If router auth is not available, record the exact blocked step and generate the exact RouterOS commands or runbook the operator/sysadmin must apply manually.</action>
  <verify>the router audit artifact and docs mention either applied NAT/hardening truth or an exact router-auth blocker</verify>
  <acceptance_criteria>
    - NAT intent is explicit with exact target IP and port
    - management hardening intent is explicit
    - the result is either applied truth or one exact blocker, not a vague failure
  </acceptance_criteria>
</task>

</tasks>

<success_criteria>

- there is one deliberate public NAT path for the relay edge
- router management exposure is addressed explicitly instead of being an accidental side effect

</success_criteria>

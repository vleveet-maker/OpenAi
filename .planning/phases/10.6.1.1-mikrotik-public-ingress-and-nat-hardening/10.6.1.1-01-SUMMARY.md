---
phase: 10.6.1.1-mikrotik-public-ingress-and-nat-hardening
plan: 01
subsystem: router-and-server-truth
tags: [mikrotik, audit, nat, ingress, routeros]
provides:
  - explicit router/server topology truth
  - repeatable audit scripts for MikroTik ingress
  - written distinction between server-local and outside-client proof
key-files:
  modified:
    - infra/remote/audit-mikrotik-ingress.ps1
    - infra/remote/apply-mikrotik-public-ingress.ps1
    - infra/remote/mikrotik/README.md
  added:
    - .planning/phases/10.6.1.1-mikrotik-public-ingress-and-nat-hardening/10.6.1.1-ROUTER-AUDIT.md
requirements-completed: [MTIK-01]
completed: 2026-03-29
---

# Phase 10.6.1.1 Plan 01: Summary

Router/server ingress truth is now explicit instead of being inferred from mismatched server and public-IP symptoms.

## Accomplishments

- Added repeatable audit tooling for the Linux host plus MikroTik edge.
- Proved that the relay host is `192.168.88.2` behind MikroTik `192.168.88.1`.
- Confirmed that the public IP `77.66.186.75` is the router WAN edge, not the Linux box directly.
- Recorded the exact difference between:
  - server-local relay truth
  - router/WAN truth
  - outside-client truth

## Verification

- PowerShell parser check for the MikroTik audit/apply/verify scripts
- live `audit-mikrotik-ingress.ps1`
- live `apply-mikrotik-public-ingress.ps1 -WhatIf`

## Residual Note

The passive audit path is weaker than the apply/verify path after hardening, so the final router truth intentionally relies on both:

- audit for topology and public-root truth
- no-op apply for current NAT/service truth

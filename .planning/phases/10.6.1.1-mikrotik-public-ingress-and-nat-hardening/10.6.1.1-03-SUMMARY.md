---
phase: 10.6.1.1-mikrotik-public-ingress-and-nat-hardening
plan: 03
subsystem: public-proof-and-api-edge
tags: [public-ingress, nginx, relay-api, verification]
provides:
  - outside-client public ingress proof
  - final NAT/edge verdict
  - unblock for Phase 11 and EXTAPI-03
key-files:
  modified:
    - infra/remote/verify-public-ingress.ps1
    - docs/remote-relay-server.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
requirements-completed: [MTIK-04, EXTAPI-03]
completed: 2026-03-29
---

# Phase 10.6.1.1 Plan 03: Summary

Outside-client proof now passes. The public IP reaches the intended relay edge, and the relay boundary behaves correctly for callers.

## Accomplishments

- Added a dedicated public-ingress verification script.
- Proved from the operator machine that:
  - `GET /healthz` returns `200`
  - invalid bearer on `GET /api/relay/health` returns `403`
- Rechecked the real remote relay path with a valid token and confirmed public ingress reaches the live relay health endpoint.
- Updated docs so the deliberate consumer path is port `80`, not raw `:4010`.

## Verification

- live `verify-public-ingress.ps1`
- live `verify-remote-relay.ps1`

## Result

- deliberate public ingress now works
- raw `:4010` remains intentionally private
- Phase 11 can now continue on top of the real remote topology

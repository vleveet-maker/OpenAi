---
phase: 10.6.1.1-mikrotik-public-ingress-and-nat-hardening
verified: 2026-03-29T21:25:00+03:00
status: passed
score: 4/4 requirements satisfied
---

# Phase 10.6.1.1 Verification

## Result

`Phase 10.6.1.1` is **passed**.

In plain words:

- the router-side ingress path is no longer a guess
- one deliberate NAT rule now forwards public HTTP traffic to the server relay edge
- router management is hardened back to the LAN
- outside clients now really reach the relay edge on the public IP

## Requirement Coverage

| Requirement | Status | Notes |
|-------------|--------|-------|
| `MTIK-01` | x VERIFIED | router, server, public IP, and current ingress path are explicit |
| `MTIK-02` | x VERIFIED | `owmcgp-public-relay-http` now forwards WAN `tcp/80` to `192.168.88.2:80` |
| `MTIK-03` | x VERIFIED | RouterOS management services are restricted to `192.168.88.0/24` |
| `MTIK-04` | x VERIFIED | outside-client proof now reaches the server `nginx` edge |

## Proof Summary

- operator machine:
  - `GET http://77.66.186.75/healthz` -> `200`
  - `GET http://77.66.186.75/api/relay/health` with invalid bearer -> `403 remote_relay_forbidden`
- remote relay verification:
  - `externalPublicIngressHealth` -> `200` with the live relay health JSON
- server local:
  - `GET http://127.0.0.1/healthz` -> `200`
  - `GET http://127.0.0.1:8080/healthz` -> `200`
  - raw relay boundary still enforces bearer auth

## Important Nuance

The Linux relay host is inside the MikroTik LAN. Because of that, `http://77.66.186.75/...` from the server itself is no longer the authoritative public-ingress test. The correct public proof source is an outside client.

## Follow-On Effect

This phase also closes the last open public-ingress blocker from `Phase 10.6.1`:

- `EXTAPI-03` is now satisfied
- `Phase 11` is unblocked

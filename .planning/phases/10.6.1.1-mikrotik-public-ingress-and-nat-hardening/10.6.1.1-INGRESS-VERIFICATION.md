# Phase 10.6.1.1 Ingress Verification

Verified on `2026-03-29`.

## Outside-Client Proof

From the operator machine:

- `GET http://77.66.186.75/healthz`
  - result: `200`
  - body: `ok`
- `GET http://77.66.186.75/api/relay/health` with invalid bearer
  - result: `403`
  - body: `{"error":"remote_relay_forbidden"}`

From the existing remote relay verification path:

- `externalPublicIngressHealth`
  - result: `200`
  - body contains the relay health JSON with:
    - `service = owmcgp-remote-relay`
    - `mode = remote_relay`
    - `topology = separate_host_runtime_via_reverse_tunnel`
    - `workerCount = 6`

## Server-Local Proof

From the Linux relay host:

- `GET http://127.0.0.1/healthz` -> `200 ok`
- `GET http://127.0.0.1:8080/healthz` -> `200 ok`
- `GET http://127.0.0.1:4010/api/relay/health` without bearer -> `401 remote_relay_unauthorized`

This proves:

- the `nginx` edge is healthy locally
- the raw relay process is healthy locally
- the bearer boundary on the raw relay remains intact

## Raw Port Truth

- `http://77.66.186.75:4010/...` remains intentionally not public
- deliberate public access is now:
  - `http://77.66.186.75/healthz`
  - `http://77.66.186.75/api/relay/*`

## Important Nuance

From the server itself, `http://77.66.186.75/healthz` still returns a RouterOS-style `404`. This is not the public truth gate anymore, because the relay host lives on the LAN side of the MikroTik. Public ingress must be judged from an outside client, not from a LAN-origin self-call.

## Final Verdict

`MTIK-04` is satisfied.

The deliberate public edge now works for outside clients, and the remaining public boundary is intentional:

- port `80` -> public relay edge
- raw `:4010` -> private only

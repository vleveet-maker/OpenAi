---
phase: 10.6-remote-server-deployment-autostart-and-external-relay-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/control-api/src/config.ts
  - services/control-api/src/server.ts
  - infra/remote/deploy-remote-relay.ps1
  - infra/remote/probe-remote-host.sh
  - infra/remote/systemd/owmcgp-remote-relay.service
  - docs/remote-relay-server.md
autonomous: true
requirements: [REMOTE-01, REMOTE-04]
user_setup: []
must_haves:
    truths:
      - "The extracted module is the standalone relay/session/bootstrap orchestration layer, not an implicit full browser-runtime replacement."
      - "The target host audit path and deployment contract are explicit before live SSH work begins."
      - "Remote service mode is intentionally configured, not inferred from the normal shared-screen app mode."
    artifacts:
      - path: "services/control-api/src/config.ts"
        provides: "standalone remote-relay mode env contract"
        contains: "CONTROL_API_MODE"
      - path: "infra/remote/systemd/owmcgp-remote-relay.service"
        provides: "autostart unit skeleton"
        contains: "owmcgp-remote-relay"
      - path: "infra/remote/probe-remote-host.sh"
        provides: "host audit script for SSH target"
        contains: "os-release"
    key_links:
      - from: "services/control-api/src/config.ts"
        to: "services/control-api/src/server.ts"
        via: "remote relay mode alters startup surface and route exposure intentionally"
        pattern: "CONTROL_API_MODE|remote_relay"
      - from: "infra/remote/deploy-remote-relay.ps1"
        to: "infra/remote/systemd/owmcgp-remote-relay.service"
        via: "deploy script installs the exact service contract it documents"
        pattern: "owmcgp-remote-relay"
---

<objective>
Create the standalone remote-relay service shape, host-audit tooling, and concrete deploy/autostart assets for the dedicated server.
</objective>

<execution_context>
@../gsd/get-shit-done/workflows/execute-plan.md
@../gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10.6-remote-server-deployment-autostart-and-external-relay-api/10.6-CONTEXT.md
@.planning/phases/10.6-remote-server-deployment-autostart-and-external-relay-api/10.6-RESEARCH.md
@.planning/phases/10.6-remote-server-deployment-autostart-and-external-relay-api/10.6-VALIDATION.md
@services/control-api/src/config.ts
@services/control-api/src/server.ts
@services/host-controller/src/server.mjs
@docs/host-native-worker.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add standalone remote-relay mode to control-api</name>
  <files>services/control-api/src/config.ts, services/control-api/src/server.ts</files>
  <read_first>services/control-api/src/config.ts, services/control-api/src/server.ts, .planning/phases/10.6-remote-server-deployment-autostart-and-external-relay-api/10.6-RESEARCH.md</read_first>
  <action>Add an explicit `CONTROL_API_MODE` contract with values `full_app` and `remote_relay`, plus any companion env values needed for remote API auth and request timeout. In `remote_relay` mode, keep the relay/session/bootstrap orchestration available but skip shared-screen static serving and internal admin HTML by default. This mode must stay honest about what it does not provide, and it must not silently claim the whole browser runtime has moved.</action>
  <verify>rg -n "CONTROL_API_MODE|remote_relay|REMOTE_RELAY" services/control-api/src/config.ts services/control-api/src/server.ts</verify>
  <acceptance_criteria>
    - services/control-api/src/config.ts contains `CONTROL_API_MODE`
    - services/control-api/src/server.ts contains `remote_relay`
  </acceptance_criteria>
  <done>control-api can now boot intentionally as a standalone remote relay service instead of only as the local full app.</done>
</task>

<task type="auto">
  <name>Task 2: Add deploy and host-audit assets for the dedicated server</name>
  <files>infra/remote/deploy-remote-relay.ps1, infra/remote/probe-remote-host.sh, infra/remote/systemd/owmcgp-remote-relay.service, docs/remote-relay-server.md</files>
  <read_first>.planning/phases/10.6-remote-server-deployment-autostart-and-external-relay-api/10.6-CONTEXT.md, docs/host-native-worker.md</read_first>
  <action>Create a host-audit script that can run over SSH and capture OS, init-system, Node/npm, memory, disk, browser/runtime clues, and listening ports. Add a deploy PowerShell script with defaults for `77.66.186.75`, port `2222`, and user `mi50`, but do not hardcode passwords or tokens. Add a systemd unit file and document the remote service layout in `docs/remote-relay-server.md`.</action>
  <verify>rg -n "77.66.186.75|2222|owmcgp-remote-relay|os-release" infra/remote/deploy-remote-relay.ps1 infra/remote/probe-remote-host.sh infra/remote/systemd/owmcgp-remote-relay.service docs/remote-relay-server.md</verify>
  <acceptance_criteria>
    - infra/remote/deploy-remote-relay.ps1 contains `77.66.186.75`
    - infra/remote/systemd/owmcgp-remote-relay.service contains `owmcgp-remote-relay`
    - infra/remote/probe-remote-host.sh contains `os-release`
  </acceptance_criteria>
  <done>The repo now has a concrete remote host audit and deployment contract instead of a vague server idea.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for standalone mode and deploy-script validity</name>
  <files>services/control-api/src/config.ts, services/control-api/src/server.ts, infra/remote/deploy-remote-relay.ps1</files>
  <read_first>services/control-api/src/config.ts, services/control-api/src/server.ts, infra/remote/deploy-remote-relay.ps1</read_first>
  <action>Add or update tests so standalone `remote_relay` mode is covered explicitly, and keep deploy-script parser validation part of the phase contract. Do not leave the new mode as an untested branch.</action>
  <verify>cmd /c npm.cmd test --prefix services/control-api</verify>
  <acceptance_criteria>
    - control-api tests pass with standalone mode coverage present
    - infra/remote/deploy-remote-relay.ps1 parses cleanly in PowerShell
  </acceptance_criteria>
  <done>The standalone remote-relay branch and deploy script are now regression-checked instead of being one-off scaffolding.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] standalone `remote_relay` mode exists and is explicit
- [ ] host-audit and deploy assets exist with the target SSH defaults
- [ ] deploy script parser check is part of the phase workflow
</verification>

<success_criteria>

- All tasks completed
- the service/deploy contract is explicit before SSH deployment begins
- the phase still tells the truth about topology boundaries

</success_criteria>

<output>
After completion, create the matching SUMMARY document in this phase directory.
</output>

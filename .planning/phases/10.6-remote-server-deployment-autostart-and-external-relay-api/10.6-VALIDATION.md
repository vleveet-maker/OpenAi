---
phase: 10.6
slug: remote-server-deployment-autostart-and-external-relay-api
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-29
---

# Phase 10.6 - Validation Strategy

> Per-phase validation contract for the remote-server extraction work.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `vitest`, `node --test`, PowerShell parser checks, and manual SSH/live smoke |
| **Config file** | `services/control-api/package.json`, `services/host-controller/package.json` |
| **Quick run command** | `cmd /c npm.cmd test --prefix services/control-api && cmd /c npm.cmd test --prefix services/host-controller` |
| **Full suite command** | `cmd /c npm.cmd test --prefix services/control-api && cmd /c npm.cmd run build --prefix services/control-api && cmd /c npm.cmd test --prefix services/host-controller && powershell -Command "[void][scriptblock]::Create((Get-Content -Raw 'infra/remote/deploy-remote-relay.ps1'))"` |
| **Estimated runtime** | ~300 seconds plus manual SSH time |

---

## Sampling Rate

- **After every task commit:** run the smallest affected package test command
- **After every plan wave:** run the full suite command plus any touched script parser checks
- **Before `$gsd-verify-work`:** full suite must be green and remote smoke notes must exist
- **Max feedback latency:** 300 seconds for automated checks

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 10.6-01-* | 01 | 1 | REMOTE-01, REMOTE-04 | control-api + deploy assets | `cmd /c npm.cmd test --prefix services/control-api` | yes | pending |
| 10.6-02-* | 02 | 2 | REMOTE-02, REMOTE-03 | route/auth tests | `cmd /c npm.cmd test --prefix services/control-api` | yes | pending |
| 10.6-03-* | 03 | 3 | REMOTE-01, REMOTE-02, REMOTE-03, REMOTE-04 | manual SSH deploy + remote smoke | manual | no | pending |

*Status: pending / green / red / flaky*

---

## Wave 0 Requirements

- [ ] Add or update control-api tests for standalone remote-relay mode and dedicated relay API routes
- [ ] Keep deploy-script parser validation explicit
- [ ] Define the host-audit artifact path before live SSH work

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Confirm the target host shape before deployment | REMOTE-01, REMOTE-04 | Requires real SSH access and host inspection | 1. SSH into `77.66.186.75:2222` as `mi50`. 2. Record OS, init system, Node/npm availability, memory, disk, and whether local runtime/browser clues exist. 3. Save the result in `10.6-SERVER-AUDIT.md`. |
| Confirm autostart works after install | REMOTE-02 | Requires real service manager on target host | 1. Install the service. 2. Run `systemctl enable --now owmcgp-remote-relay` or the verified equivalent. 3. Confirm active status and readable logs. |
| Confirm remote API returns a real answer or exact topology blocker | REMOTE-03, REMOTE-04 | Requires live remote network path and current runtime availability | 1. Call `GET /api/relay/health`. 2. Call `POST /api/relay/ask`. 3. Record whether a real assistant reply came back or a precise topology blocker was returned. |

---

## Validation Sign-Off

- [ ] All tasks have automated verify or an explicit manual gate
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers missing route and deploy-script assertions
- [ ] No watch-mode flags
- [ ] Feedback latency < 300s for automated checks
- [ ] `nyquist_compliant: true` only after remote smoke evidence exists

**Approval:** pending

---
phase: 10.6-remote-server-deployment-autostart-and-external-relay-api
plan: 03
subsystem: remote-server-deploy
tags: [server, ssh, systemd, reverse-tunnel, relay-api]
provides:
  - real SSH host audit
  - verified server autostart
  - live remote relay proof against the real Linux service
  - explicit separate-host topology verdict
key-files:
  created:
    - .planning/phases/10.6-remote-server-deployment-autostart-and-external-relay-api/10.6-SERVER-AUDIT.md
  modified:
    - infra/remote/deploy-remote-relay.ps1
    - infra/remote/systemd/owmcgp-remote-relay.service
    - docs/remote-relay-server.md
requirements-progressed: [REMOTE-01, REMOTE-02, REMOTE-03, REMOTE-04]
completed: 2026-03-29
---

# Phase 10.6 Plan 03: Summary

**Linux remote relay service on `77.66.186.75` with real systemd autostart and live relay proof through a reverse-tunneled compact-visible `wife` worker**

## Accomplishments

- Audited the real SSH host and recorded its actual OS, runtime, memory, disk, and network facts.
- Installed a working `Node 22.17.0` runtime on the server after discovering that apt only provided `node 12`.
- Deployed `owmcgp-remote-relay` under `systemd` and verified `enabled + active`.
- Proved the remote API against the real service:
  - remote health
  - fresh dialog ask
  - continuing dialog ask
  - explicit end-dialog
- Recorded the final topology truth: the relay service is on the Linux server, while the browser runtime remains on the Windows host and is currently reached through reverse SSH tunnels.

## Verification

- real SSH host audit on `77.66.186.75:2222`
- `systemctl is-enabled owmcgp-remote-relay`
- `systemctl is-active owmcgp-remote-relay`
- `journalctl -u owmcgp-remote-relay -n 50 --no-pager`
- `GET /api/relay/health` on the real server
- `POST /api/relay/ask` fresh dialog returning `remote-relay-ok`
- `POST /api/relay/ask` continuing dialog returning `second-ok`
- `POST /api/relay/dialogs/:dialogId/end`

## Residual Note

- Raw public ingress on `http://77.66.186.75:4010` is still not a clean public caller path. The current proven path is server-local or SSH-forwarded access to the Linux service, plus reverse-tunneled reachability to the Windows compact-visible workers.

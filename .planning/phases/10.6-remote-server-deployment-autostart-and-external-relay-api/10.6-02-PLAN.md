---
phase: 10.6-remote-server-deployment-autostart-and-external-relay-api
plan: 02
type: execute
wave: 2
depends_on: [10.6-01-PLAN.md]
files_modified:
  - services/control-api/src/config.ts
  - services/control-api/src/server.ts
  - services/control-api/src/remote/remote-relay-service.ts
  - services/control-api/src/routes/public-remote-relay.ts
  - services/control-api/test/public-remote-relay.test.ts
  - docs/remote-relay-server.md
autonomous: true
requirements: [REMOTE-02, REMOTE-03]
user_setup: []
must_haves:
    truths:
      - "The dedicated remote API is authenticated and separate from the shared-screen session routes."
      - "Remote callers can ask for a fresh dialog or continue an existing one without seeing internal admin or worker-control surfaces."
      - "The standalone mode and API docs use the same env contract."
    artifacts:
      - path: "services/control-api/src/routes/public-remote-relay.ts"
        provides: "remote relay HTTP facade"
        contains: "/api/relay/ask"
      - path: "services/control-api/src/remote/remote-relay-service.ts"
        provides: "fresh/continue/end dialog orchestration contract"
        contains: "assistantReplyText"
      - path: "services/control-api/test/public-remote-relay.test.ts"
        provides: "auth and route coverage for remote API"
        contains: "Authorization"
    key_links:
      - from: "services/control-api/src/routes/public-remote-relay.ts"
        to: "services/control-api/src/remote/remote-relay-service.ts"
        via: "route layer maps fresh and continuing dialog requests to orchestration layer"
        pattern: "dialogId|newDialog|assistantReplyText"
      - from: "services/control-api/src/config.ts"
        to: "docs/remote-relay-server.md"
        via: "same env contract is documented for operators"
        pattern: "REMOTE_RELAY_API_TOKEN|REMOTE_RELAY_REQUEST_TIMEOUT_MS"
---

<objective>
Expose a dedicated authenticated remote relay API on top of the standalone service mode.
</objective>

<execution_context>
@../gsd/get-shit-done/workflows/execute-plan.md
@../gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10.6-remote-server-deployment-autostart-and-external-relay-api/10.6-CONTEXT.md
@.planning/phases/10.6-remote-server-deployment-autostart-and-external-relay-api/10.6-RESEARCH.md
@.planning/phases/10.6-remote-server-deployment-autostart-and-external-relay-api/10.6-VALIDATION.md
@services/control-api/src/server.ts
@services/control-api/src/routes/public-sessions.ts
@services/control-api/src/routes/public-chat.ts
@services/control-api/src/chat/chat-relay-service.ts
@services/control-api/src/sessions/session-service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a dedicated remote relay service and routes</name>
  <files>services/control-api/src/remote/remote-relay-service.ts, services/control-api/src/routes/public-remote-relay.ts, services/control-api/src/server.ts</files>
  <read_first>services/control-api/src/server.ts, services/control-api/src/routes/public-sessions.ts, services/control-api/src/routes/public-chat.ts, services/control-api/src/chat/chat-relay-service.ts, services/control-api/src/sessions/session-service.ts</read_first>
  <action>Create a thin dedicated remote relay service that can: start a fresh dialog, continue an existing dialog, and end a dialog. Expose it through `GET /api/relay/health`, `POST /api/relay/ask`, and `POST /api/relay/dialogs/:dialogId/end`. Use an auth guard based on `Authorization: Bearer <REMOTE_RELAY_API_TOKEN>`. Keep the response contract explicit with `dialogId`, `sessionId`, `workerId`, `conversationMode`, `modelLabel`, `assistantReplyText`, and `relayResult`.</action>
  <verify>rg -n "/api/relay/health|/api/relay/ask|assistantReplyText|dialogId|Authorization" services/control-api/src/routes/public-remote-relay.ts services/control-api/src/remote/remote-relay-service.ts services/control-api/src/server.ts</verify>
  <acceptance_criteria>
    - services/control-api/src/routes/public-remote-relay.ts contains `/api/relay/ask`
    - services/control-api/src/remote/remote-relay-service.ts contains `assistantReplyText`
  </acceptance_criteria>
  <done>The remote caller now has one dedicated API instead of needing the shared-screen route shape.</done>
</task>

<task type="auto">
  <name>Task 2: Align standalone mode and docs to the remote API contract</name>
  <files>services/control-api/src/config.ts, services/control-api/src/server.ts, docs/remote-relay-server.md</files>
  <read_first>services/control-api/src/config.ts, services/control-api/src/server.ts, docs/remote-relay-server.md</read_first>
  <action>Make sure standalone `remote_relay` mode and the new relay routes use the same env contract. Document `REMOTE_RELAY_API_TOKEN`, timeout config, bind address or port assumptions, and the exact request/response shape in `docs/remote-relay-server.md`.</action>
  <verify>rg -n "REMOTE_RELAY_API_TOKEN|REMOTE_RELAY_REQUEST_TIMEOUT_MS|/api/relay/ask|assistantReplyText" services/control-api/src/config.ts services/control-api/src/server.ts docs/remote-relay-server.md</verify>
  <acceptance_criteria>
    - docs/remote-relay-server.md contains `REMOTE_RELAY_API_TOKEN`
    - docs/remote-relay-server.md contains `/api/relay/ask`
  </acceptance_criteria>
  <done>The runtime contract and the operator docs now describe the same remote service behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add route coverage for remote auth and dialog lifecycle</name>
  <files>services/control-api/test/public-remote-relay.test.ts</files>
  <read_first>services/control-api/test, services/control-api/src/routes/public-remote-relay.ts, services/control-api/src/remote/remote-relay-service.ts</read_first>
  <action>Add tests that cover: auth required, health success, fresh-dialog ask, continue-dialog ask, and end-dialog. Keep the tests focused on the dedicated remote facade rather than re-testing the whole session-client surface.</action>
  <verify>cmd /c npm.cmd test --prefix services/control-api</verify>
  <acceptance_criteria>
    - services/control-api/test/public-remote-relay.test.ts exists
    - control-api tests pass with remote relay route coverage
  </acceptance_criteria>
  <done>The remote relay API is now regression-covered and not just a one-off deploy-only feature.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] remote relay routes exist and require bearer auth
- [ ] fresh, continue, and end-dialog flows are all covered
- [ ] docs describe the same API contract the code exposes
</verification>

<success_criteria>

- All tasks completed
- remote callers have one documented authenticated API
- standalone mode and route docs stay aligned

</success_criteria>

<output>
After completion, create the matching SUMMARY document in this phase directory.
</output>

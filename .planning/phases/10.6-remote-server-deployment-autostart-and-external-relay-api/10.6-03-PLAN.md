---
phase: 10.6-remote-server-deployment-autostart-and-external-relay-api
plan: 03
type: execute
wave: 3
depends_on: [10.6-01-PLAN.md, 10.6-02-PLAN.md]
files_modified:
  - infra/remote/deploy-remote-relay.ps1
  - docs/remote-relay-server.md
  - .planning/phases/10.6-remote-server-deployment-autostart-and-external-relay-api/10.6-SERVER-AUDIT.md
autonomous: true
requirements: [REMOTE-01, REMOTE-02, REMOTE-03, REMOTE-04]
user_setup: []
must_haves:
    truths:
      - "The target host was audited over SSH before the final deployment claim."
      - "Autostart and remote API were verified from the real server, not only from local tests."
      - "The phase ends with an explicit topology verdict, even if that verdict is a blocker."
    artifacts:
      - path: ".planning/phases/10.6-remote-server-deployment-autostart-and-external-relay-api/10.6-SERVER-AUDIT.md"
        provides: "real host audit result"
        contains: "77.66.186.75"
      - path: "docs/remote-relay-server.md"
        provides: "operator deployment and smoke steps"
        contains: "owmcgp-remote-relay"
    key_links:
      - from: "infra/remote/deploy-remote-relay.ps1"
        to: ".planning/phases/10.6-remote-server-deployment-autostart-and-external-relay-api/10.6-SERVER-AUDIT.md"
        via: "deploy script targets the same audited host"
        pattern: "77.66.186.75|2222|mi50"
      - from: ".planning/phases/10.6-remote-server-deployment-autostart-and-external-relay-api/10.6-SERVER-AUDIT.md"
        to: "docs/remote-relay-server.md"
        via: "topology verdict and operational instructions agree"
        pattern: "topology|systemd|remote api"
---

<objective>
Deploy the remote relay to the SSH server, verify autostart and remote API behavior, and record the final topology verdict honestly.
</objective>

<execution_context>
@../gsd/get-shit-done/workflows/execute-plan.md
@../gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10.6-remote-server-deployment-autostart-and-external-relay-api/10.6-CONTEXT.md
@.planning/phases/10.6-remote-server-deployment-autostart-and-external-relay-api/10.6-RESEARCH.md
@.planning/phases/10.6-remote-server-deployment-autostart-and-external-relay-api/10.6-VALIDATION.md
@infra/remote/deploy-remote-relay.ps1
@docs/remote-relay-server.md
</context>

<tasks>

<task type="manual">
  <name>Task 1: Audit the target host over SSH and record the result</name>
  <files>.planning/phases/10.6-remote-server-deployment-autostart-and-external-relay-api/10.6-SERVER-AUDIT.md, infra/remote/probe-remote-host.sh</files>
  <read_first>infra/remote/probe-remote-host.sh, .planning/phases/10.6-remote-server-deployment-autostart-and-external-relay-api/10.6-CONTEXT.md</read_first>
  <action>Connect to `ssh -p 2222 mi50@77.66.186.75`, run the host-audit script, and write `10.6-SERVER-AUDIT.md` with OS, init system, Node/npm, memory, disk, browser/runtime clues, and any topology-relevant findings. Do not write the password into repo state.</action>
  <verify>10.6-SERVER-AUDIT.md exists and contains the audited host plus a topology section</verify>
  <acceptance_criteria>
    - `10.6-SERVER-AUDIT.md` exists
    - it contains `77.66.186.75`
    - it contains an explicit `Topology Verdict` section
  </acceptance_criteria>
  <done>The target host shape is now evidence-backed instead of assumed.</done>
</task>

<task type="manual">
  <name>Task 2: Deploy the service and verify autostart</name>
  <files>infra/remote/deploy-remote-relay.ps1, docs/remote-relay-server.md</files>
  <read_first>infra/remote/deploy-remote-relay.ps1, infra/remote/systemd/owmcgp-remote-relay.service, docs/remote-relay-server.md</read_first>
  <action>Use the deploy script to copy the service bundle to `/opt/owmcgp-remote-relay`, install the env file at `/etc/owmcgp/remote-relay.env`, install the service unit, and verify autostart with `systemctl enable --now owmcgp-remote-relay`, `systemctl is-active`, `systemctl status`, and `journalctl -u owmcgp-remote-relay` or the audited equivalent if the host differs.</action>
  <verify>remote host reports the service active after install</verify>
  <acceptance_criteria>
    - the remote service is enabled
    - the remote service is active
    - the deployment steps are reflected in docs/remote-relay-server.md
  </acceptance_criteria>
  <done>The remote relay now starts automatically instead of depending on a manual SSH session.</done>
</task>

<task type="manual">
  <name>Task 3: Run remote smoke and write the final topology verdict</name>
  <files>.planning/phases/10.6-remote-server-deployment-autostart-and-external-relay-api/10.6-SERVER-AUDIT.md, docs/remote-relay-server.md</files>
  <read_first>.planning/phases/10.6-remote-server-deployment-autostart-and-external-relay-api/10.6-SERVER-AUDIT.md, docs/remote-relay-server.md</read_first>
  <action>Call `GET /api/relay/health` and `POST /api/relay/ask` against the real remote server. Record whether a real assistant reply came back, or whether the service exposed a precise topology blocker. Update `10.6-SERVER-AUDIT.md` and docs with the final verdict: same-host runtime, separate-host runtime, or blocked-by-incompatibility.</action>
  <verify>remote API smoke results and topology verdict are written down</verify>
  <acceptance_criteria>
    - remote health was called
    - remote ask was called
    - the final topology verdict is explicit, not implied
  </acceptance_criteria>
  <done>The phase now ends with an honest remote-service result instead of a vague “deployed somewhere” claim.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] SSH host audit exists
- [ ] autostart is verified on the real server
- [ ] remote API smoke ran and the topology verdict is explicit
</verification>

<success_criteria>

- All tasks completed
- remote deployment, autostart, and API status are real rather than assumed
- the final topology verdict gives the next phase a trustworthy boundary

</success_criteria>

<output>
After completion, create the matching SUMMARY document in this phase directory.
</output>

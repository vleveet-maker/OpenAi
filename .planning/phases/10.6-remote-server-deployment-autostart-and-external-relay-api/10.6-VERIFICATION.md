# Phase 10.6 Verification

## Result

`Phase 10.6` is **complete**.

The relay/session/bootstrap module now runs as a real standalone service on the dedicated SSH server with verified autostart, and the remote API has been exercised against the real Linux host.

## What Was Verified

### Host Audit

- connected to `77.66.186.75` over `ssh -p 2222`
- confirmed `Ubuntu 22.04.5 LTS`
- confirmed `systemd 249`
- confirmed adequate memory and disk
- confirmed Node/npm were missing initially and documented the final install path

### Autostart

- installed `owmcgp-remote-relay.service`
- `systemctl is-enabled owmcgp-remote-relay` returned `enabled`
- `systemctl is-active owmcgp-remote-relay` returned `active`
- `journalctl` showed `control-api` listening on `0.0.0.0:4010`

### Remote API

- `GET /api/relay/health` returned healthy JSON on the real server
- fresh `POST /api/relay/ask` returned a real assistant reply
- continuing `POST /api/relay/ask` on the same dialog returned a second real assistant reply
- `POST /api/relay/dialogs/:dialogId/end` ended the dialog explicitly

### Proven Working Topology

- remote Linux service on `77.66.186.75`
- local Windows `compact visible` worker runtime
- reverse SSH tunnel from Windows worker host to server-local forwarded port
- real successful dialog through worker `wife`

## Final Verdict

`REMOTE-01`: satisfied  
`REMOTE-02`: satisfied  
`REMOTE-03`: satisfied  
`REMOTE-04`: satisfied

## Important Truth

The service is genuinely remote and working, but the proven browser-runtime topology is still `separate-host`, not `same-host Linux browser runtime`.

Also, raw public ingress on `:4010` is still not the clean public consumer path. The proven caller path is server-local or explicitly forwarded access.

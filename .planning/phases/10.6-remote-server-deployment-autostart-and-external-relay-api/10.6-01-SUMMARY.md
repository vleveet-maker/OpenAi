---
phase: 10.6-remote-server-deployment-autostart-and-external-relay-api
plan: 01
subsystem: remote-relay-foundation
tags: [control-api, remote-relay, deployment, systemd]
provides:
  - standalone control-api remote relay mode
  - remote deploy and host-audit asset skeleton
  - documented Linux/systemd deployment contract
key-files:
  modified:
    - services/control-api/src/config.ts
    - services/control-api/src/server.ts
    - infra/remote/deploy-remote-relay.ps1
    - infra/remote/probe-remote-host.sh
    - infra/remote/systemd/owmcgp-remote-relay.service
    - docs/remote-relay-server.md
requirements-progressed: [REMOTE-01, REMOTE-04]
completed: 2026-03-29
---

# Phase 10.6 Plan 01: Summary

The relay/session/bootstrap layer can now boot intentionally as a standalone `remote_relay` service, and the repo has a concrete Linux/systemd deployment skeleton for the target server.

## Accomplishments

- Added `CONTROL_API_MODE=remote_relay` with companion remote-relay env settings.
- Made `control-api` skip shared-screen and internal admin surfaces in remote-relay mode.
- Added deployment assets for the dedicated server:
  - `infra/remote/deploy-remote-relay.ps1`
  - `infra/remote/probe-remote-host.sh`
  - `infra/remote/systemd/owmcgp-remote-relay.service`
- Wrote `docs/remote-relay-server.md` so the service layout and env contract are explicit before SSH deployment.

## Verification

- `cmd /c npm.cmd test --prefix services/control-api`
- `cmd /c npm.cmd run build --prefix services/control-api`
- `powershell -Command "[void][scriptblock]::Create((Get-Content -Raw 'infra/remote/deploy-remote-relay.ps1'))"`

## Residual Note

- `probe-remote-host.sh` could not be parser-checked locally because this Windows host currently has no working non-WSL `bash -n` path. The script is present and ready for the real Linux host audit in Wave 3.

---
phase: 09-internal-host-pool-orchestration
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - services/control-api/src/routes/internal-admin-page.ts
  - services/control-api/test/internal-admin-page.test.ts
  - docs/host-native-worker.md
  - docs/internal-worker-ops.md
autonomous: true
requirements: [ORCH-01, ORCH-02, ORCH-03]
user_setup: []
must_haves:
    truths:
      - "The internal admin page shows pool-level Start and Stop controls in addition to worker details."
      - "The operator can see lifecycle states starting, stopping, ready, degraded, and failed without reading raw logs."
      - "Docs explain that degraded means partial success with no auto-rollback or auto-retry in this phase."
    artifacts:
      - path: "services/control-api/src/routes/internal-admin-page.ts"
        provides: "pool lifecycle panel and control wiring"
        contains: "Start pool"
      - path: "services/control-api/test/internal-admin-page.test.ts"
        provides: "admin page coverage for host-pool controls"
        contains: "/internal/host-pool"
      - path: "docs/internal-worker-ops.md"
        provides: "operator runbook for in-app pool lifecycle control"
        contains: "Start pool"
---

<objective>
Expose the new host-pool lifecycle inside the existing internal admin page and operator docs.

Purpose: Make the verified script path understandable and one-click from the internal admin surface.
Output: Pool control panel, lifecycle copy, admin-page tests, and updated runbooks.
</objective>

<execution_context>
@../gsd/get-shit-done/workflows/execute-plan.md
@../gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-internal-host-pool-orchestration/09-CONTEXT.md
@.planning/phases/09-internal-host-pool-orchestration/09-RESEARCH.md
@services/control-api/src/routes/internal-admin-page.ts
@services/control-api/src/routes/internal-host-pool.ts
@services/control-api/src/workers/host-pool-service.ts
@docs/host-native-worker.md
@docs/internal-worker-ops.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a pool lifecycle panel and controls to the internal admin page</name>
  <files>services/control-api/src/routes/internal-admin-page.ts</files>
  <read_first>services/control-api/src/routes/internal-admin-page.ts, services/control-api/src/routes/internal-host-pool.ts, services/control-api/src/workers/host-pool-service.ts</read_first>
  <action>Update services/control-api/src/routes/internal-admin-page.ts so the page renders a dedicated host-pool panel above worker cards. The panel must show the current status badge, proxy listening state, last action, last error text when present, and Start pool plus Stop pool buttons. The client-side script must poll GET /internal/host-pool every 5 seconds, disable buttons while status is starting or stopping, and render explicit copy for statuses idle, starting, ready, degraded, stopping, and failed. Keep the existing worker cards and browser-access actions intact.</action>
  <verify>rg -n "Start pool|Stop pool|/internal/host-pool|proxy listening|degraded|failed|starting|stopping" services/control-api/src/routes/internal-admin-page.ts</verify>
  <acceptance_criteria>
    - services/control-api/src/routes/internal-admin-page.ts contains "Start pool"
    - services/control-api/src/routes/internal-admin-page.ts contains "Stop pool"
    - services/control-api/src/routes/internal-admin-page.ts contains "/internal/host-pool"
    - services/control-api/src/routes/internal-admin-page.ts contains "degraded"
    - services/control-api/src/routes/internal-admin-page.ts contains "failed"
  </acceptance_criteria>
  <done>The operator can trigger pool lifecycle and understand current state without leaving the admin page or inferring status from worker cards alone.</done>
</task>

<task type="auto">
  <name>Task 2: Add admin-page tests for host-pool controls and lifecycle copy</name>
  <files>services/control-api/test/internal-admin-page.test.ts</files>
  <read_first>services/control-api/src/routes/internal-admin-page.ts, services/control-api/test/internal-admin-page.test.ts, services/control-api/test/internal-host-pool.test.ts</read_first>
  <action>Extend services/control-api/test/internal-admin-page.test.ts so GET /internal/admin verifies the rendered HTML contains Start pool, Stop pool, /internal/host-pool, and lifecycle copy for degraded or failed host-pool reporting. Keep the existing assertions for browser access controls and observability endpoints so the new panel does not regress the older admin functions.</action>
  <verify>rg -n "Start pool|Stop pool|/internal/host-pool|degraded|failed|Open browser|/internal/observability/summary" services/control-api/test/internal-admin-page.test.ts</verify>
  <acceptance_criteria>
    - services/control-api/test/internal-admin-page.test.ts contains "Start pool"
    - services/control-api/test/internal-admin-page.test.ts contains "Stop pool"
    - services/control-api/test/internal-admin-page.test.ts contains "/internal/host-pool"
    - services/control-api/test/internal-admin-page.test.ts contains "Open browser"
    - services/control-api/test/internal-admin-page.test.ts contains "/internal/observability/summary"
  </acceptance_criteria>
  <done>The admin page contract is covered, so the new pool panel cannot silently disappear or lose its polling hook.</done>
</task>

<task type="auto">
  <name>Task 3: Update operator docs for in-app pool lifecycle and degraded-state meaning</name>
  <files>docs/host-native-worker.md, docs/internal-worker-ops.md</files>
  <read_first>docs/host-native-worker.md, docs/internal-worker-ops.md, .planning/phases/09-internal-host-pool-orchestration/09-CONTEXT.md</read_first>
  <action>Update docs/host-native-worker.md and docs/internal-worker-ops.md so the verified operator flow says the proxied host-native pool can be started and stopped from `http://127.0.0.1:8081/internal/admin`. Document the exact pool statuses idle, starting, ready, degraded, stopping, and failed. State explicitly that degraded means partial success, that this phase does not auto-rollback or auto-retry, and that PowerShell scripts remain fallback tools rather than the primary operator path.</action>
  <verify>rg -n "internal/admin|Start pool|Stop pool|idle|starting|ready|degraded|stopping|failed|auto-rollback|auto-retry" docs/host-native-worker.md docs/internal-worker-ops.md</verify>
  <acceptance_criteria>
    - docs/host-native-worker.md contains "internal/admin"
    - docs/host-native-worker.md contains "degraded"
    - docs/internal-worker-ops.md contains "Start pool"
    - docs/internal-worker-ops.md contains "Stop pool"
    - docs/internal-worker-ops.md contains "auto-rollback"
  </acceptance_criteria>
  <done>The operator playbook now matches the in-app lifecycle path and explains what to do when a pool action only partially succeeds.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] internal admin page shows pool controls and lifecycle meaning
- [ ] admin-page tests cover new host-pool strings and existing worker actions
- [ ] docs explain degraded semantics and fallback scripts clearly
</verification>

<success_criteria>

- All tasks completed
- internal admin can start and stop the host-native pool visually
- lifecycle meaning is explicit enough that the operator does not need raw logs
- docs match the new in-app control path

</success_criteria>

<output>
After completion, create the matching SUMMARY document in this phase directory.
</output>

---
phase: 09-internal-host-pool-orchestration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/host-controller/src/config.mjs
  - services/host-controller/src/host-controller.mjs
  - services/host-controller/src/server.mjs
  - services/host-controller/test/host-controller.test.mjs
  - services/host-controller/test/server.test.mjs
autonomous: true
requirements: [ORCH-01, ORCH-02, ORCH-03]
user_setup: []
must_haves:
    truths:
      - "The host-controller exposes a symmetric pool stop path instead of relying only on PowerShell wrapper scripts."
      - "The host-controller can report proxy and worker reachability clearly enough for control-api to distinguish ready, idle, degraded, and failed states."
      - "Stopping the pool also stops the mixed proxy runtime instead of leaving the proxy process behind."
    artifacts:
      - path: "services/host-controller/src/host-controller.mjs"
        provides: "pool lifecycle orchestration and observed pool status"
        contains: "pool_stop_requested"
      - path: "services/host-controller/src/server.mjs"
        provides: "HTTP routes for pool lifecycle and health snapshots"
        contains: "/pool/stop"
      - path: "services/host-controller/test/host-controller.test.mjs"
        provides: "automated lifecycle coverage for start, stop, and degraded states"
        contains: "pool_stop_requested"
    key_links:
      - from: "services/host-controller/src/server.mjs"
        to: "services/host-controller/src/host-controller.mjs"
        via: "POST /pool/start and POST /pool/stop delegate to controller lifecycle methods"
        pattern: "controller.startPool|controller.stopPool"
---

<objective>
Give the host-controller a complete, testable lifecycle contract for the proxied household pool before control-api builds on top of it.

Purpose: Replace script-only pool stop behavior with a symmetric API and richer health data.
Output: Observed pool status, proxy shutdown support, `POST /pool/stop`, and host-controller tests.
</objective>

<execution_context>
@../gsd/get-shit-done/workflows/execute-plan.md
@../gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-internal-host-pool-orchestration/09-CONTEXT.md
@.planning/phases/09-internal-host-pool-orchestration/09-RESEARCH.md
@services/host-controller/src/host-controller.mjs
@services/host-controller/src/server.mjs
@infra/host-worker/start-proxied-host-pool.ps1
@infra/host-worker/stop-proxied-host-pool.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add testable host-controller lifecycle primitives and proxy shutdown support</name>
  <files>services/host-controller/src/config.mjs, services/host-controller/src/host-controller.mjs</files>
  <read_first>services/host-controller/src/config.mjs, services/host-controller/src/host-controller.mjs, infra/host-worker/start-proxied-host-pool.ps1, infra/host-worker/stop-proxied-host-pool.ps1</read_first>
  <action>Refactor HostController so it can report observed pool state and stop the full pool, not just individual workers. Add helper methods getProxyRuntimeStatus(), getObservedPoolStatus(), stopProxyRuntime(), and stopPool(). getObservedPoolStatus() must return exactly idle, ready, or degraded based on proxy listening and per-worker agent and browser reachability. stopPool() must stop workers in configured order, wait 500 milliseconds between workers, then stop the mixed proxy process bound to HOST_PROXY_MIXED_PORT, and return an object with action set to "pool_stop_requested" and a workers array of per-worker stop results.</action>
  <verify>rg -n "getProxyRuntimeStatus|getObservedPoolStatus|stopProxyRuntime|stopPool|pool_stop_requested|proxyMixedPort" services/host-controller/src/config.mjs services/host-controller/src/host-controller.mjs</verify>
  <acceptance_criteria>
    - services/host-controller/src/host-controller.mjs contains "getObservedPoolStatus"
    - services/host-controller/src/host-controller.mjs contains "stopPool"
    - services/host-controller/src/host-controller.mjs contains "pool_stop_requested"
    - services/host-controller/src/host-controller.mjs contains "stopProxyRuntime"
    - services/host-controller/src/config.mjs contains "proxyMixedPort"
  </acceptance_criteria>
  <done>The controller can now fully own both start and stop lifecycle of the proxied pool instead of depending on an external wrapper for shutdown.</done>
</task>

<task type="auto">
  <name>Task 2: Expose symmetric pool lifecycle and richer health payloads over HTTP</name>
  <files>services/host-controller/src/server.mjs, services/host-controller/src/host-controller.mjs</files>
  <read_first>services/host-controller/src/server.mjs, services/host-controller/src/host-controller.mjs</read_first>
  <action>Update services/host-controller/src/server.mjs so GET /health returns JSON with service, status, proxyListening, proxyServerUrl, poolStatus, and workers; GET /workers returns proxyListening, proxyServerUrl, poolStatus, and workers; POST /pool/start returns 202 with action "pool_start_requested", proxyServerUrl, poolStatus, and workers; and add POST /pool/stop returning 202 with action "pool_stop_requested", proxyServerUrl, poolStatus, and workers. Export a createHostControllerServer helper or equivalent injectable route factory so these routes can be tested without booting the real process entrypoint.</action>
  <verify>rg -n "/pool/start|/pool/stop|proxyListening|proxyServerUrl|poolStatus|createHostControllerServer" services/host-controller/src/server.mjs services/host-controller/src/host-controller.mjs</verify>
  <acceptance_criteria>
    - services/host-controller/src/server.mjs contains "/pool/stop"
    - services/host-controller/src/server.mjs contains "proxyListening"
    - services/host-controller/src/server.mjs contains "poolStatus"
    - services/host-controller/src/server.mjs contains "createHostControllerServer"
    - services/host-controller/src/server.mjs contains "pool_start_requested"
  </acceptance_criteria>
  <done>The control plane now has a stable HTTP contract for both starting and stopping the pool and for understanding current proxy plus worker reachability.</done>
</task>

<task type="auto">
  <name>Task 3: Cover pool stop, observed status, and HTTP routes with node tests</name>
  <files>services/host-controller/test/host-controller.test.mjs, services/host-controller/test/server.test.mjs</files>
  <read_first>services/host-controller/src/host-controller.mjs, services/host-controller/src/server.mjs, services/host-controller/test/proxy-links.test.mjs, services/host-controller/test/proxy-config-script.test.mjs</read_first>
  <action>Create services/host-controller/test/host-controller.test.mjs with injected doubles that verify: observed pool status becomes ready when proxy plus all workers are reachable, degraded when only part of the pool is reachable, and stopPool() returns action "pool_stop_requested" and stops proxy teardown only after worker shutdown attempts. Create services/host-controller/test/server.test.mjs that exercises GET /health, GET /workers, POST /pool/start, and POST /pool/stop against the exported server factory and verifies the payload fields proxyListening, poolStatus, proxyServerUrl, and workers are present.</action>
  <verify>rg -n "pool_stop_requested|proxyListening|poolStatus|GET /health|POST /pool/stop|degraded" services/host-controller/test/host-controller.test.mjs services/host-controller/test/server.test.mjs</verify>
  <acceptance_criteria>
    - services/host-controller/test/host-controller.test.mjs contains "pool_stop_requested"
    - services/host-controller/test/host-controller.test.mjs contains "degraded"
    - services/host-controller/test/server.test.mjs contains "POST /pool/stop"
    - services/host-controller/test/server.test.mjs contains "proxyListening"
    - services/host-controller/test/server.test.mjs contains "poolStatus"
  </acceptance_criteria>
  <done>The host-controller lifecycle contract is regression-tested before control-api starts depending on it.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `services/host-controller` tests cover observed pool status and pool stop
- [ ] `POST /pool/stop` exists and returns a structured lifecycle payload
- [ ] health payloads expose proxy and worker reachability explicitly
</verification>

<success_criteria>

- All tasks completed
- host-controller has symmetric pool start and stop routes
- health payloads expose enough detail for layered lifecycle reporting
- shutdown no longer depends on script-only proxy teardown

</success_criteria>

<output>
After completion, create the matching SUMMARY document in this phase directory.
</output>

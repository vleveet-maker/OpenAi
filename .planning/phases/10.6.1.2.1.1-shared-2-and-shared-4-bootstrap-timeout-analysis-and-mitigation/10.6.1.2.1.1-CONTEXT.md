# Phase 10.6.1.2.1.1 Context

## Why This Exists

The local seven-worker smoke snapshot completed with `5/7` usable, but `shared-2` and `shared-4` both failed twice at the same branch: `worker_chat_bootstrap_timeout`.

This phase exists so the project stops treating that repeated branch as vague flakiness. It must capture real surface evidence, implement one bounded mitigation if possible, and leave explicit tests and documentation either way.

## Current Truth

- The current proven local baseline remains `compact visible`.
- `shared-2` and `shared-4` both start, attach, and reach the worker bootstrap call.
- The timeout is raised by `FetchWorkerChatBootstrapClient` after 20 seconds waiting on `/internal/chat/bootstrap`.
- The single-worker proof path is `infra/host-worker/test-host-worker-relay.ps1`.
- The latest seven-worker matrix is recorded in `10.6.1.2.1-WORKER-MATRIX.json` and `.md`.

## Non-Goals

- Do not replace the pending server-hosted migration.
- Do not redesign the whole bootstrap architecture.
- Do not hide the failure behind retries without preserving branch-level evidence.

## Required Output

This phase must end with:

1. one explicit explanation of what `shared-2` and `shared-4` are actually stuck on
2. one bounded mitigation in code or explicit manual-recovery branch if no safe code fix exists
3. tests and final worker verdict artifacts

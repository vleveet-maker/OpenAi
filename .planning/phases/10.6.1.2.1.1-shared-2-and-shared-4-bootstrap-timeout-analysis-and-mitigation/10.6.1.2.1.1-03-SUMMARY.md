# 10.6.1.2.1.1-03 Summary

Wave 3 reran the live proof against the two previously failing workers.

Results:

- `shared-2` -> `relay_complete`, reply `smoke-ok`
- `shared-4` -> `relay_complete`, reply `smoke-ok`

Final verdict:

- both workers are recovered
- the local compact-visible snapshot is now `7/7 usable`
- windows were closed again after proof, so no operator-desktop clutter remains

# 10.6.1.2.1.1 Verification

- Phase: `10.6.1.2.1.1`
- Date: 2026-03-30
- Status: `complete`

## Short Report

The repeated local bootstrap timeout on `shared-2` and `shared-4` is mitigated.

Simple explanation:

- first blocker was a memory dialog on top of ChatGPT
- second blocker was a `Temporary Chat` entry that existed only through an aria-label path
- after both were covered, both workers passed the same live compact-visible proof as the rest of the pool

## Root Cause

Initial repeated failure:

- `worker_chat_bootstrap_timeout`

Observed browser surface:

- live ChatGPT home page
- blocking memory modal with `Не сейчас`
- after dismissing it, a visible `aria-label="Включить временный чат"` button

## Mitigation

Implemented:

1. bounded dismissal of the memory dialog before fresh-chat bootstrap
2. `aria-label` fallback selectors for `Temporary Chat`
3. regression tests for both branches

## Test Evidence

- `cmd /c npm.cmd test` in `workers/agent` -> `49/49` passed
- `cmd /c npm.cmd run build` in `workers/agent` -> passed

## Live Proof

- `shared-2` -> `relay_complete`, `conversationMode=temporary`, `modelLabel=GPT-5.4 Thinking`, reply `smoke-ok`
- `shared-4` -> `relay_complete`, `conversationMode=temporary`, `modelLabel=GPT-5.4 Thinking`, reply `smoke-ok`

## Final Verdict

- `BTMIT-01` complete
- `BTMIT-02` complete
- `BTMIT-03` complete

The local seven-worker compact-visible snapshot is restored to `7/7 usable`.

---
phase: 25-deployed-windows-post-phase24-live-fix-smoke-wrapper-backport-and-persistent-disconnected-runtime-forced-host-public-owner-503-remediation
plan: 02
type: execute
wave: 2
depends_on: [25-01-PLAN.md]
files_modified:
  - services/control-api/src/
  - services/control-api/dist/
  - services/control-api/test/
autonomous: true
requirements: [WLFB-03]
user_setup: []
---

<objective>
Expose the latest Phase 25 external-readiness result in the real `control-api` runtime so operators can confirm tunnel and upstream truth before making a final external API claim.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-deployed-windows-post-phase24-live-fix-smoke-wrapper-backport-and-persistent-disconnected-runtime-forced-host-public-owner-503-remediation/25-RESEARCH.md
@.planning/phases/25-deployed-windows-post-phase24-live-fix-smoke-wrapper-backport-and-persistent-disconnected-runtime-forced-host-public-owner-503-remediation/25-VALIDATION.md
@services/control-api/src/config.ts
@services/control-api/src/server.ts
@services/control-api/src/routes/internal-admin-page.ts
@services/control-api/src/routes/read-latest-state.ts
@services/control-api/test/internal-admin-page.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a file-backed latest post-Phase-24 external-readiness route and regenerate the served runtime files</name>
  <files>services/control-api/src/config.ts, services/control-api/src/server.ts, services/control-api/src/routes/internal-post-phase24-external-api-readiness.ts, services/control-api/dist/, services/control-api/test/</files>
  <read_first>services/control-api/src/config.ts, services/control-api/src/server.ts, services/control-api/src/routes/read-latest-state.ts, services/control-api/src/routes/internal-post-phase23-exact-smoke-wrapper-compat-remediation.ts</read_first>
  <action>Add a new config field `postPhase24ExternalApiReadinessStatePath` sourced from `POST_PHASE24_EXTERNAL_API_READINESS_STATE_PATH` with default repo path `infra/data/post-phase24-external-api-readiness/latest.json`. Create `services/control-api/src/routes/internal-post-phase24-external-api-readiness.ts` with `GET /internal/post-phase24-external-api-readiness/latest` that returns `{ latest: null }` when the file is missing and the parsed latest payload when it exists. Reuse `read-latest-state.ts` so BOM-prefixed `latest.json` still parses cleanly. Register the route in `server.ts`, add tests, and rebuild the `dist` files the deployed host actually serves.</action>
  <verify>rg -n "POST_PHASE24_EXTERNAL_API_READINESS_STATE_PATH|postPhase24ExternalApiReadinessStatePath|/internal/post-phase24-external-api-readiness/latest|latest: null|canonicalPublicUpstream|reverseTunnelTaskStatus|ubuntuTunnelListenerStatus|readyWorkerCount|verdict" services/control-api/src services/control-api/dist services/control-api/test</verify>
  <acceptance_criteria>
    - `services/control-api/src/routes/internal-post-phase24-external-api-readiness.ts` exists
    - `config.ts` supports `POST_PHASE24_EXTERNAL_API_READINESS_STATE_PATH`
    - `server.ts` registers `/internal/post-phase24-external-api-readiness/latest`
    - tests prove the route returns `{ latest: null }` when no file exists
    - the route reuses BOM-safe parsing
    - `dist` contains the same route/config contract after build
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Render the latest post-Phase-24 external-readiness result on the internal admin page</name>
  <files>services/control-api/src/routes/internal-admin-page.ts, services/control-api/dist/routes/internal-admin-page.js, services/control-api/test/internal-admin-page.test.ts</files>
  <read_first>services/control-api/src/routes/internal-admin-page.ts, services/control-api/test/internal-admin-page.test.ts, services/control-api/src/routes/internal-post-phase24-external-api-readiness.ts</read_first>
  <action>Add a dedicated `Latest post-phase24 external API readiness` section to `/internal/admin`. Make the page fetch `/internal/post-phase24-external-api-readiness/latest`, render `No post-phase24 external API readiness result captured yet.` when there is no file, and render the latest timestamp, canonical upstream, reverse-tunnel task state, tunnel listener state, ready-worker count, compatibility version, and verdict when the file exists. Keep the existing sections intact and rebuild the `dist` admin page runtime.</action>
  <verify>rg -n "Latest post-phase24 external API readiness|No post-phase24 external API readiness result captured yet.|/internal/post-phase24-external-api-readiness/latest|canonical upstream|reverse-tunnel task|tunnel listener|ready worker count|Compatibility|verdict" services/control-api/src/routes/internal-admin-page.ts services/control-api/dist/routes/internal-admin-page.js services/control-api/test/internal-admin-page.test.ts</verify>
  <acceptance_criteria>
    - the admin page contains `Latest post-phase24 external API readiness`
    - the admin page fetches `/internal/post-phase24-external-api-readiness/latest`
    - the admin page has the exact empty-state text
    - the populated state renders canonical upstream, reverse-tunnel task state, tunnel listener state, ready worker count, compatibility version, and verdict
    - the existing sections remain present
  </acceptance_criteria>
</task>

</tasks>

<success_criteria>

- the latest Phase 25 external-readiness result is visible from the same runtime files the deployed host actually serves
- operators can confirm tunnel and upstream truth without reconstructing it from raw terminal output

</success_criteria>

---
phase: 16-deployed-windows-runtime-parity-sync-and-post-remediation-degraded-smoke-stabilization
plan: 01
type: execute
wave: 1
depends_on: [15-VERIFICATION.md]
files_modified:
  - .planning/phases/16-deployed-windows-runtime-parity-sync-and-post-remediation-degraded-smoke-stabilization/
  - docs/windows-browser-block.md
  - docs/windows-public-api-block.md
  - docs/internal-worker-ops.md
  - infra/windows-block/
  - infra/data/post-remediation-degraded-smoke/
autonomous: true
requirements: [WPAR-01, WPAR-02]
user_setup: []
---

<objective>
Sync the live PowerShell hotfix behavior back into the repo, stamp one explicit compatibility version, and add the canonical post-remediation degraded-smoke stabilization harness plus exact repo-to-host sync docs.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-deployed-windows-runtime-parity-sync-and-post-remediation-degraded-smoke-stabilization/16-RESEARCH.md
@.planning/phases/15-deployed-windows-disconnected-baseline-and-forced-host-edge-remediation/15-VERIFICATION.md
@docs/windows-browser-block.md
@docs/windows-public-api-block.md
@docs/internal-worker-ops.md
@infra/windows-block/probe-public-api.ps1
@infra/windows-block/test-rollout-smoke.ps1
@infra/windows-block/remediate-disconnected-baseline-and-forced-host-edge.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync the live PowerShell hotfix behavior back into the repo and stamp one exact compatibility version</name>
  <files>infra/windows-block/probe-public-api.ps1, infra/windows-block/test-rollout-smoke.ps1, infra/windows-block/remediate-disconnected-baseline-and-forced-host-edge.ps1</files>
  <read_first>infra/windows-block/probe-public-api.ps1, infra/windows-block/test-rollout-smoke.ps1, infra/windows-block/remediate-disconnected-baseline-and-forced-host-edge.ps1, .planning/phases/15-deployed-windows-disconnected-baseline-and-forced-host-edge-remediation/15-VERIFICATION.md</read_first>
  <action>Back-port the deployed-host compatibility fixes into the repo copies of `probe-public-api.ps1`, `test-rollout-smoke.ps1`, and `remediate-disconnected-baseline-and-forced-host-edge.ps1` so the next archive no longer depends on manual server-only edits. Keep the exact named parameter surface on `probe-public-api.ps1`: `-BaseUrl`, `-ApiToken`, `-SettingsPath`, `-WorkerId`, `-HostHeader`, `-HopName`, `-HostControllerBaseUrl`, `-HostControllerToken`, `-EnsureWorkerStarted`, `-StopWorkerWhenDone`, and `-IncludeChatProbe`. Ensure `test-rollout-smoke.ps1` exposes `PostCanaryDelaySeconds`, `PostSmokeDelaySeconds`, `KeepCanaryRunningUntilFinalSnapshot`, and writes `scriptCompatibilityVersion` into the JSON and Markdown outputs. Add the exact compatibility marker `phase16-runtime-parity-sync-v1` to the Phase 16-facing wrappers so repo/runtime evidence can be compared without another free-form drift note.</action>
  <verify>rg -n "BaseUrl|ApiToken|SettingsPath|WorkerId|HostHeader|HopName|HostControllerBaseUrl|HostControllerToken|EnsureWorkerStarted|StopWorkerWhenDone|IncludeChatProbe|PostCanaryDelaySeconds|PostSmokeDelaySeconds|KeepCanaryRunningUntilFinalSnapshot|scriptCompatibilityVersion|phase16-runtime-parity-sync-v1" infra/windows-block/probe-public-api.ps1 infra/windows-block/test-rollout-smoke.ps1 infra/windows-block/remediate-disconnected-baseline-and-forced-host-edge.ps1</verify>
  <acceptance_criteria>
    - `probe-public-api.ps1` contains the exact named parameter set `BaseUrl`, `ApiToken`, `SettingsPath`, `WorkerId`, `HostHeader`, `HopName`, `HostControllerBaseUrl`, `HostControllerToken`, `EnsureWorkerStarted`, `StopWorkerWhenDone`, and `IncludeChatProbe`
    - `test-rollout-smoke.ps1` contains `PostCanaryDelaySeconds`, `PostSmokeDelaySeconds`, and `KeepCanaryRunningUntilFinalSnapshot`
    - `test-rollout-smoke.ps1` writes `scriptCompatibilityVersion`
    - the exact compatibility marker `phase16-runtime-parity-sync-v1` exists in the Phase 16-facing wrapper path
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add the canonical degraded-smoke stabilization harness and document the exact Phase 16 sync contract</name>
  <files>infra/windows-block/stabilize-post-remediation-degraded-smoke.ps1, infra/data/post-remediation-degraded-smoke/, docs/windows-browser-block.md, docs/windows-public-api-block.md, docs/internal-worker-ops.md</files>
  <read_first>infra/windows-block/remediate-disconnected-baseline-and-forced-host-edge.ps1, infra/windows-block/test-rollout-smoke.ps1, docs/windows-browser-block.md, docs/windows-public-api-block.md, docs/internal-worker-ops.md, .planning/phases/16-deployed-windows-runtime-parity-sync-and-post-remediation-degraded-smoke-stabilization/16-RESEARCH.md</read_first>
  <action>Create `infra/windows-block/stabilize-post-remediation-degraded-smoke.ps1` as the canonical Phase 16 wrapper. Give it exact defaults `SessionBaseUrl=http://127.0.0.1:8080`, `PublicApiBaseUrl=http://77.66.186.75`, `InternalBaseUrl=http://127.0.0.1:8081`, `HostControllerBaseUrl=http://127.0.0.1:4040`, `WindowsEdgeBaseUrl=http://127.0.0.1`, `PublicHostHeader=77.66.186.75`, `CanaryWorkerId=shared-6`, `RecoveryWaitSeconds=15`, `PostCanaryDelaySeconds=10`, and `PostSmokeDelaySeconds=15`. Make it capture the exact stage names `before_stabilization`, `after_remediation`, `after_smoke_canary`, `after_smoke_settle`, and `after_cleanup`, reuse the existing remediation and smoke helpers, and write `.planning/phases/16-deployed-windows-runtime-parity-sync-and-post-remediation-degraded-smoke-stabilization/16-STABILIZATION-SUMMARY.json`, `.planning/phases/16-deployed-windows-runtime-parity-sync-and-post-remediation-degraded-smoke-stabilization/16-STABILIZATION-SUMMARY.md`, and `infra/data/post-remediation-degraded-smoke/latest.json`. Make the stabilization artifact expose the exact fields `scriptCompatibilityVersion`, `preSmokeReadyCount`, `finalSmokeReadyCount`, `finalPoolStatus`, `publicCanaryPassed`, and `verdict`, where `verdict` is exactly `stabilized_through_smoke` or `hold_rollout`. Update `docs/windows-browser-block.md`, `docs/windows-public-api-block.md`, and `docs/internal-worker-ops.md` with the exact Phase 16 sync list and one exact live command. The sync list must explicitly include `infra/windows-block/probe-public-api.ps1`, `infra/windows-block/test-rollout-smoke.ps1`, `infra/windows-block/remediate-disconnected-baseline-and-forced-host-edge.ps1`, `infra/windows-block/stabilize-post-remediation-degraded-smoke.ps1`, `services/control-api/src/config.ts`, `services/control-api/src/server.ts`, `services/control-api/src/routes/internal-admin-page.ts`, `services/control-api/dist/config.js`, `services/control-api/dist/server.js`, and `services/control-api/dist/routes/internal-admin-page.js`.</action>
  <verify>rg -n "before_stabilization|after_remediation|after_smoke_canary|after_smoke_settle|after_cleanup|preSmokeReadyCount|finalSmokeReadyCount|finalPoolStatus|publicCanaryPassed|stabilized_through_smoke|hold_rollout|16-STABILIZATION-SUMMARY|post-remediation-degraded-smoke/latest.json|services/control-api/src/config.ts|services/control-api/dist/server.js" infra/windows-block/stabilize-post-remediation-degraded-smoke.ps1 docs/windows-browser-block.md docs/windows-public-api-block.md docs/internal-worker-ops.md</verify>
  <acceptance_criteria>
    - `infra/windows-block/stabilize-post-remediation-degraded-smoke.ps1` exists
    - the wrapper contains the exact stage names `before_stabilization`, `after_remediation`, `after_smoke_canary`, `after_smoke_settle`, and `after_cleanup`
    - the wrapper writes both Phase 16 artifacts and `infra/data/post-remediation-degraded-smoke/latest.json`
    - the wrapper exposes the exact fields `scriptCompatibilityVersion`, `preSmokeReadyCount`, `finalSmokeReadyCount`, `finalPoolStatus`, `publicCanaryPassed`, and `verdict`
    - all three docs include the exact Phase 16 sync list and the exact live command for `shared-6`
  </acceptance_criteria>
</task>

</tasks>

<success_criteria>

- the repo now has one explicit parity-synced PowerShell path for the live Windows host
- the degraded-smoke stabilization run can now be repeated from one exact sync list and one exact command instead of operator memory

</success_criteria>

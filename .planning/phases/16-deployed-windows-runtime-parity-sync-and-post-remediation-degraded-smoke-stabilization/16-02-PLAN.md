---
phase: 16-deployed-windows-runtime-parity-sync-and-post-remediation-degraded-smoke-stabilization
plan: 02
type: execute
wave: 2
depends_on: [16-01-PLAN.md]
files_modified:
  - services/control-api/src/
  - services/control-api/dist/
  - services/control-api/test/
autonomous: true
requirements: [WPAR-01, WPAR-03]
user_setup: []
---

<objective>
Expose the latest post-remediation degraded-smoke stabilization result in the real `control-api` runtime so operators can trust the same parity-synced archive they deploy.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-deployed-windows-runtime-parity-sync-and-post-remediation-degraded-smoke-stabilization/16-RESEARCH.md
@.planning/phases/16-deployed-windows-runtime-parity-sync-and-post-remediation-degraded-smoke-stabilization/16-VALIDATION.md
@services/control-api/src/config.ts
@services/control-api/src/server.ts
@services/control-api/src/routes/internal-admin-page.ts
@services/control-api/src/routes/internal-disconnected-baseline-remediation.ts
@services/control-api/test/internal-admin-page.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a file-backed latest post-remediation degraded-smoke route to control-api and regenerate dist runtime files</name>
  <files>services/control-api/src/config.ts, services/control-api/src/server.ts, services/control-api/src/routes/internal-post-remediation-degraded-smoke.ts, services/control-api/dist/, services/control-api/test/</files>
  <read_first>services/control-api/src/config.ts, services/control-api/src/server.ts, services/control-api/src/routes/internal-disconnected-baseline-remediation.ts, services/control-api/test/internal-disconnected-baseline-remediation.test.ts</read_first>
  <action>Add a new config field `postRemediationDegradedSmokeStatePath` sourced from `POST_REMEDIATION_DEGRADED_SMOKE_STATE_PATH` with default repo path `infra/data/post-remediation-degraded-smoke/latest.json`. Create `services/control-api/src/routes/internal-post-remediation-degraded-smoke.ts` with `GET /internal/post-remediation-degraded-smoke/latest` that returns `{ latest: null }` when the file is missing and returns the parsed latest stabilization payload when it exists. Register the route behind the internal admin guard in `server.ts`, add tests that prove both the empty state and the populated state, and regenerate the runtime files the deployed host actually starts by running `npm.cmd --prefix services/control-api run build` so `services/control-api/dist/config.js`, `services/control-api/dist/server.js`, and `services/control-api/dist/routes/internal-post-remediation-degraded-smoke.js` contain the same route/config contract.</action>
  <verify>rg -n "POST_REMEDIATION_DEGRADED_SMOKE_STATE_PATH|postRemediationDegradedSmokeStatePath|/internal/post-remediation-degraded-smoke/latest|latest: null|preSmokeReadyCount|finalSmokeReadyCount|finalPoolStatus|publicCanaryPassed|verdict" services/control-api/src services/control-api/dist services/control-api/test</verify>
  <acceptance_criteria>
    - `services/control-api/src/routes/internal-post-remediation-degraded-smoke.ts` exists
    - `config.ts` supports `POST_REMEDIATION_DEGRADED_SMOKE_STATE_PATH`
    - `server.ts` registers `/internal/post-remediation-degraded-smoke/latest`
    - tests prove the route returns `{ latest: null }` when no stabilization result exists
    - `services/control-api/dist/config.js`, `services/control-api/dist/server.js`, and `services/control-api/dist/routes/internal-post-remediation-degraded-smoke.js` contain the new route/config contract after build
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Render the latest stabilization result on the internal admin page</name>
  <files>services/control-api/src/routes/internal-admin-page.ts, services/control-api/dist/routes/internal-admin-page.js, services/control-api/test/internal-admin-page.test.ts</files>
  <read_first>services/control-api/src/routes/internal-admin-page.ts, services/control-api/test/internal-admin-page.test.ts, services/control-api/src/routes/internal-post-remediation-degraded-smoke.ts</read_first>
  <action>Add a dedicated `Latest post-remediation degraded smoke stabilization` section to `/internal/admin`. Make the page fetch `/internal/post-remediation-degraded-smoke/latest`, render an empty state `No post-remediation degraded smoke stabilization captured yet.` when there is no file, and render the latest timestamp, pre-smoke ready count, final smoke ready count, final pool status, public canary result, compatibility version, and final verdict when the file exists. Keep the existing worker summary, latest rollout smoke, latest readiness recovery, latest post-recovery smoke regression, latest zero-ready root cause, and latest disconnected baseline remediation sections intact. Regenerate `services/control-api/dist/routes/internal-admin-page.js` through the build output so the deployed host serves the same admin page from `dist`.</action>
  <verify>rg -n "Latest post-remediation degraded smoke stabilization|No post-remediation degraded smoke stabilization captured yet.|/internal/post-remediation-degraded-smoke/latest|pre-smoke ready count|final smoke ready count|final pool status|public canary|Compatibility|verdict" services/control-api/src/routes/internal-admin-page.ts services/control-api/dist/routes/internal-admin-page.js services/control-api/test/internal-admin-page.test.ts</verify>
  <acceptance_criteria>
    - the admin page contains the text `Latest post-remediation degraded smoke stabilization`
    - the admin page fetches `/internal/post-remediation-degraded-smoke/latest`
    - the admin page has an explicit empty state `No post-remediation degraded smoke stabilization captured yet.`
    - the populated state renders pre-smoke ready count, final smoke ready count, final pool status, public canary result, compatibility version, and verdict
    - the existing worker summary, latest rollout smoke, latest readiness recovery, latest post-recovery smoke regression, latest zero-ready root cause, and latest disconnected baseline remediation sections remain present
  </acceptance_criteria>
</task>

</tasks>

<success_criteria>

- the latest post-remediation degraded-smoke stabilization result is visible from the same runtime files the deployed host actually serves
- no operator has to reconstruct the current stabilization truth from mixed terminal output and older remediation notes

</success_criteria>

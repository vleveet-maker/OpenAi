---
phase: 02-timed-session-access
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - apps/session-client/package.json
  - apps/session-client/tsconfig.json
  - apps/session-client/vite.config.ts
  - apps/session-client/index.html
  - apps/session-client/src/app.tsx
  - apps/session-client/src/main.tsx
  - apps/session-client/src/session-api.ts
  - apps/session-client/src/session-types.ts
  - apps/session-client/src/use-session-view.ts
  - apps/session-client/src/styles.css
  - apps/session-client/src/app.test.tsx
  - apps/session-client/src/test/setup.ts
  - .gitignore
  - services/control-api/src/server.ts
  - infra/docker-compose.yml
autonomous: true
requirements: [SESS-02, SESS-03, SESS-04]
user_setup: []
must_haves:
    truths:
      - "The shared-screen application has Start, Queued, Active, and Ended states."
      - "The active screen polls the public API every 5 seconds and renders a local countdown from endsAt."
      - "The client exposes a disabled Phase 3 chat shell rather than real message sending."
    artifacts:
      - path: "apps/session-client/src/app.tsx"
        provides: "Shared-screen routed session UI"
        contains: "Start Timed Session"
      - path: "apps/session-client/src/use-session-view.ts"
        provides: "Polling and countdown state management"
        contains: "5000"
      - path: "apps/session-client/src/app.test.tsx"
        provides: "Client coverage for create, queue, resume, countdown, and end flows"
        contains: "Chat relay arrives in Phase 3"
    key_links:
      - from: "apps/session-client/src/session-api.ts"
        to: "services/control-api/src/routes/public-sessions.ts"
        via: "client fetches the public session API contract"
        pattern: "/api/sessions|/api/session-bootstrap"
      - from: "services/control-api/src/server.ts"
        to: "apps/session-client/dist"
        via: "control-api can serve the built SPA in production"
        pattern: "sessionClientDistPath|express.static"
---

<objective>
Build the shared-screen React client and wire it into local development and production serving.

Purpose: Give Phase 2 a real user-facing surface for start, queue, active countdown, manual end, and Phase 3 placeholder chat shell.
Output: A Vite/React SPA, polling-based session view state, UI tests, dev proxying, and static serving from control-api for built assets.
</objective>

<execution_context>
@../gsd/get-shit-done/workflows/execute-plan.md
@../gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-timed-session-access/02-CONTEXT.md
@.planning/phases/02-timed-session-access/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the separate Vite and React shared-screen app</name>
  <files>apps/session-client/package.json, apps/session-client/tsconfig.json, apps/session-client/vite.config.ts, apps/session-client/index.html, apps/session-client/src/main.tsx, apps/session-client/src/session-types.ts, apps/session-client/src/session-api.ts</files>
  <read_first>.planning/phases/02-timed-session-access/02-RESEARCH.md, services/control-api/src/routes/public-sessions.ts</read_first>
  <action>Create apps/session-client as a separate Vite plus React application with React Router, plain fetch calls, and Vitest. Define client-side SessionSnapshot types that match the public API and add API helpers for create, fetch, cancel, end, and bootstrap session routes. Configure Vite to proxy /api to http://localhost:4010 in development.</action>
  <verify>rg -n "react-router-dom|/api/sessions|/api/session-bootstrap|proxy" apps/session-client/package.json apps/session-client/vite.config.ts apps/session-client/src/session-api.ts</verify>
  <acceptance_criteria>
    - apps/session-client/package.json contains react-router-dom
    - apps/session-client/vite.config.ts contains "/api"
    - apps/session-client/src/session-api.ts contains /api/session-bootstrap
  </acceptance_criteria>
  <done>The repo contains a standalone shared-screen client app that knows the public session API contract.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Start, Queued, Active, and Ended session states</name>
  <files>apps/session-client/src/app.tsx, apps/session-client/src/use-session-view.ts, apps/session-client/src/styles.css, services/control-api/src/server.ts, infra/docker-compose.yml, .gitignore</files>
  <read_first>apps/session-client/src/session-api.ts, services/control-api/src/server.ts, .planning/phases/02-timed-session-access/02-CONTEXT.md</read_first>
  <action>Create app.tsx with routes / and /session/:sessionId. The Start screen must collect one requestedForLabel value and redirect to the session route after creation. The Queued state must show queuePosition and a cancel button. The Active state must poll every 5 seconds, render remaining time from endsAt, show assigned worker label, and expose an End Session Now button. The Ended state must keep the shell visible, freeze the timer at 00:00, and show the end reason. Add a disabled composer labeled Chat relay arrives in Phase 3. Update services/control-api/src/server.ts to serve the built dist folder if present. Add a session-client dev service to infra/docker-compose.yml and ignore dist, node_modules, infra/data, and sqlite runtime files in .gitignore.</action>
  <verify>rg -n "Start Timed Session|Queue position|End Session Now|Chat relay arrives in Phase 3|express.static|session-client|\\.sqlite" apps/session-client/src/app.tsx apps/session-client/src/use-session-view.ts services/control-api/src/server.ts infra/docker-compose.yml .gitignore</verify>
  <acceptance_criteria>
    - apps/session-client/src/app.tsx contains "Start Timed Session"
    - apps/session-client/src/app.tsx contains "Chat relay arrives in Phase 3"
    - apps/session-client/src/use-session-view.ts contains "5000"
    - services/control-api/src/server.ts contains express.static
  </acceptance_criteria>
  <done>The shared-screen app shows the full Phase 2 lifecycle and the backend can serve it in production.</done>
</task>

<task type="auto">
  <name>Task 3: Cover shared-screen lifecycle flows with client tests</name>
  <files>apps/session-client/src/app.test.tsx, apps/session-client/src/test/setup.ts</files>
  <read_first>apps/session-client/src/app.tsx, apps/session-client/src/use-session-view.ts</read_first>
  <action>Create client tests for create-and-resume flow, queued-to-active polling transition, countdown rendering, manual end, refresh restore by sessionId, and the disabled Phase 3 placeholder shell. Configure test setup with Testing Library jest-dom matchers.</action>
  <verify>rg -n "queued session to active|countdown|phase 3 placeholder shell|create a session from the start screen" apps/session-client/src/app.test.tsx</verify>
  <acceptance_criteria>
    - apps/session-client/src/app.test.tsx contains "creates a session from the start screen and resumes it"
    - apps/session-client/src/app.test.tsx contains "promotes a queued session to active after polling"
    - apps/session-client/src/app.test.tsx contains "renders a live countdown from endsAt"
    - apps/session-client/src/app.test.tsx contains "restores a session by id and shows the phase 3 placeholder shell"
  </acceptance_criteria>
  <done>The shared-screen client has automated coverage for the main Phase 2 user journeys.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] session-client builds successfully
- [ ] session-client tests cover the four visible UI states and countdown behavior
</verification>

<success_criteria>

- All tasks completed
- The shared-screen client supports the full Phase 2 timed-session lifecycle
- The client polls the API and renders a local countdown
- The Phase 3 placeholder chat shell is visible but disabled
</success_criteria>

<output>
After completion, create the matching SUMMARY document in this phase directory.
</output>

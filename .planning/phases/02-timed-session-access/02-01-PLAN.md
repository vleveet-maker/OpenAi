---
phase: 02-timed-session-access
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/control-api/package.json
  - services/control-api/src/config.ts
  - services/control-api/src/workers/worker-registry.ts
  - services/control-api/src/sessions/session-types.ts
  - services/control-api/src/sessions/session-store.ts
  - services/control-api/src/sessions/session-service.ts
  - infra/docker-compose.yml
autonomous: true
requirements: [SESS-01, SESS-02]
user_setup: []
must_haves:
    truths:
      - "A session can be created durably, queued, activated on one worker, and rehydrated after control-api restart."
      - "Queued sessions use one global FIFO order and active sessions stay pinned to their assigned worker."
      - "The least-recently-assigned ready worker is selected when multiple workers are available."
    artifacts:
      - path: "services/control-api/src/sessions/session-store.ts"
        provides: "SQLite-backed durable session storage"
        contains: "CREATE TABLE IF NOT EXISTS sessions"
      - path: "services/control-api/src/sessions/session-service.ts"
        provides: "Queueing, assignment, timer, and rehydrate orchestration"
        contains: "reconcileQueue"
      - path: "infra/docker-compose.yml"
        provides: "Durable mount for control-api session database"
        contains: "/srv/chatgpt-workers/control-api/session-routing.sqlite"
    key_links:
      - from: "services/control-api/src/sessions/session-service.ts"
        to: "services/control-api/src/workers/worker-registry.ts"
        via: "session service marks workers busy or ready through the registry"
        pattern: "assignedSessionId|assignedUserLabel|listReadyWorkers"
      - from: "services/control-api/src/config.ts"
        to: "infra/docker-compose.yml"
        via: "database path and sweep config line up with compose environment"
        pattern: "SESSION_DB_PATH|SESSION_SWEEP_INTERVAL_MS"
---

<objective>
Build the durable timed-session domain on top of the Phase 1 worker pool.

Purpose: Create the persistent queue, activation rules, timer ownership, and restart rehydrate behavior that make Phase 2 possible before public routes or UI are added.
Output: SQLite-backed session storage, a session service with FIFO assignment logic, worker-registry support for assignment metadata, and Docker wiring for persistent session data.
</objective>

<execution_context>
@../gsd/get-shit-done/workflows/execute-plan.md
@../gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-timed-session-access/02-CONTEXT.md
@.planning/phases/02-timed-session-access/02-RESEARCH.md
@.planning/phases/01-managed-worker-pool-foundation/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add durable session types, store, and config surface</name>
  <files>services/control-api/package.json, services/control-api/src/config.ts, services/control-api/src/sessions/session-types.ts, services/control-api/src/sessions/session-store.ts</files>
  <read_first>services/control-api/package.json, services/control-api/src/config.ts, .planning/phases/02-timed-session-access/02-CONTEXT.md, .planning/phases/02-timed-session-access/02-RESEARCH.md</read_first>
  <action>Add better-sqlite3 plus test tooling to services/control-api/package.json. Extend config.ts with SESSION_DB_PATH, SESSION_DURATION_MINUTES, SESSION_SWEEP_INTERVAL_MS, and SESSION_CLIENT_DIST_PATH. Create services/control-api/src/sessions/session-types.ts with SessionState exactly queued, active, ended, expired, cancelled and SessionEndReason exactly manual_end, timer_expired, cancelled, worker_unavailable. Create services/control-api/src/sessions/session-store.ts with one sessions table and helpers for insert, update, queued lookup, active lookup, queue position, and last assigned timestamps.</action>
  <verify>rg -n "better-sqlite3|SESSION_DB_PATH|queued|active|cancelled|CREATE TABLE IF NOT EXISTS sessions" services/control-api/package.json services/control-api/src/config.ts services/control-api/src/sessions/session-types.ts services/control-api/src/sessions/session-store.ts</verify>
  <acceptance_criteria>
    - services/control-api/src/sessions/session-types.ts contains "manual_end"
    - services/control-api/src/sessions/session-store.ts contains "CREATE TABLE IF NOT EXISTS sessions"
    - services/control-api/src/config.ts contains "SESSION_DB_PATH"
  </acceptance_criteria>
  <done>The control-api has a durable session model and persistent SQLite storage contract.</done>
</task>

<task type="auto">
  <name>Task 2: Implement queue, assignment, timer, and rehydrate orchestration</name>
  <files>services/control-api/src/workers/worker-registry.ts, services/control-api/src/sessions/session-service.ts</files>
  <read_first>services/control-api/src/workers/worker-registry.ts, .planning/phases/01-managed-worker-pool-foundation/01-VERIFICATION.md, .planning/phases/02-timed-session-access/02-CONTEXT.md</read_first>
  <action>Replace assignedUserId in worker-registry.ts with assignedUserLabel and add lastAssignedAt support plus listReadyWorkers. Create services/control-api/src/sessions/session-service.ts that creates sessions, assigns the least-recently-assigned ready worker, keeps one global FIFO queue, rehydrates non-terminal sessions at startup, expires sessions after 60 minutes, ends active sessions with worker_unavailable if a worker becomes disconnected or reauth_required, and runs a background sweep every 5 seconds.</action>
  <verify>rg -n "assignedUserLabel|lastAssignedAt|listReadyWorkers|reconcileQueue|bootstrap|runSweep|worker_unavailable" services/control-api/src/workers/worker-registry.ts services/control-api/src/sessions/session-service.ts</verify>
  <acceptance_criteria>
    - services/control-api/src/workers/worker-registry.ts contains assignedUserLabel
    - services/control-api/src/sessions/session-service.ts contains reconcileQueue
    - services/control-api/src/sessions/session-service.ts contains runSweep
    - services/control-api/src/sessions/session-service.ts contains worker_unavailable
  </acceptance_criteria>
  <done>The backend can queue, assign, rehydrate, expire, and release timed sessions while keeping workers pinned.</done>
</task>

<task type="auto">
  <name>Task 3: Wire durable session storage into local infrastructure</name>
  <files>infra/docker-compose.yml</files>
  <read_first>infra/docker-compose.yml, services/control-api/src/config.ts, .planning/phases/02-timed-session-access/02-CONTEXT.md</read_first>
  <action>Update infra/docker-compose.yml so control-api receives SESSION_DB_PATH=/srv/chatgpt-workers/control-api/session-routing.sqlite, SESSION_DURATION_MINUTES=60, and SESSION_SWEEP_INTERVAL_MS=5000. Mount ./data/control-api into /srv/chatgpt-workers/control-api so queued and active sessions survive control-api restarts.</action>
  <verify>rg -n "SESSION_DB_PATH|SESSION_DURATION_MINUTES|SESSION_SWEEP_INTERVAL_MS|/srv/chatgpt-workers/control-api" infra/docker-compose.yml</verify>
  <acceptance_criteria>
    - infra/docker-compose.yml contains SESSION_DB_PATH
    - infra/docker-compose.yml contains /srv/chatgpt-workers/control-api
    - infra/docker-compose.yml contains SESSION_SWEEP_INTERVAL_MS
  </acceptance_criteria>
  <done>The session database path and timer configuration are durable and reproducible in the local runtime.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Future implementation should pass control-api build and session-service tests
- [ ] Queue promotion and timer expiry must be grep- and test-verifiable
</verification>

<success_criteria>

- All tasks completed
- A durable session queue exists
- Active sessions stay pinned to one worker
- Restart rehydrate and expiry behavior are implemented in the backend
</success_criteria>

<output>
After completion, create the matching SUMMARY document in this phase directory.
</output>

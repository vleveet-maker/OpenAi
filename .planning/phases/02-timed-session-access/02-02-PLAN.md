---
phase: 02-timed-session-access
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - services/control-api/src/routes/public-sessions.ts
  - services/control-api/src/routes/internal-recovery.ts
  - services/control-api/src/routes/internal-worker-actions.ts
  - services/control-api/src/server.ts
  - services/control-api/test/public-sessions.test.ts
  - services/control-api/vitest.config.ts
autonomous: true
requirements: [SESS-01, SESS-03, SESS-04]
user_setup: []
must_haves:
    truths:
      - "The backend exposes public session routes for create, read, cancel, end, and bootstrap."
      - "Session routes never expose internal worker profile paths or recovery details."
      - "Worker recovery and restart paths terminate active sessions safely when the worker becomes unavailable."
    artifacts:
      - path: "services/control-api/src/routes/public-sessions.ts"
        provides: "Public timed-session lifecycle API"
        contains: "/api/sessions"
      - path: "services/control-api/src/server.ts"
        provides: "Mounted public routes and session service runtime"
        contains: "createPublicSessionsRouter"
      - path: "services/control-api/test/public-sessions.test.ts"
        provides: "Route coverage for session lifecycle and visibility boundaries"
        contains: "creates sessions and hides internal worker data"
    key_links:
      - from: "services/control-api/src/server.ts"
        to: "services/control-api/src/routes/public-sessions.ts"
        via: "server mounts public session routes"
        pattern: "createPublicSessionsRouter|/api/sessions"
      - from: "services/control-api/src/routes/internal-recovery.ts"
        to: "services/control-api/src/sessions/session-service.ts"
        via: "reauth flow ends or resumes affected active sessions"
        pattern: "handleWorkerStatusChange|handleWorkerReady"
---

<objective>
Expose the timed session lifecycle through a public API and enforce session release around worker-state changes.

Purpose: Make the queue and timer contract reachable from the app while keeping internal admin and recovery boundaries intact.
Output: Public session routes, mounted runtime integration, internal worker-action hooks that cooperate with timed sessions, and route-level tests.
</objective>

<execution_context>
@../gsd/get-shit-done/workflows/execute-plan.md
@../gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-timed-session-access/02-CONTEXT.md
@.planning/phases/02-timed-session-access/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add public session lifecycle routes</name>
  <files>services/control-api/src/routes/public-sessions.ts, services/control-api/src/server.ts</files>
  <read_first>services/control-api/src/server.ts, services/control-api/src/sessions/session-service.ts, .planning/phases/02-timed-session-access/02-CONTEXT.md</read_first>
  <action>Create services/control-api/src/routes/public-sessions.ts with POST /api/sessions, GET /api/sessions/:sessionId, POST /api/sessions/:sessionId/cancel, POST /api/sessions/:sessionId/end, and GET /api/session-bootstrap/:sessionId. Validate requestedForLabel as a trimmed string from 1 to 64 characters. Mount the router from services/control-api/src/server.ts and expose queuePosition plus a safe worker summary with only workerId, displayName, and status.</action>
  <verify>rg -n "/api/sessions|requestedForLabel|queuePosition|session-bootstrap" services/control-api/src/routes/public-sessions.ts services/control-api/src/server.ts</verify>
  <acceptance_criteria>
    - services/control-api/src/routes/public-sessions.ts contains POST /api/sessions
    - services/control-api/src/routes/public-sessions.ts contains /api/session-bootstrap
    - services/control-api/src/server.ts contains createPublicSessionsRouter
  </acceptance_criteria>
  <done>The application can create, fetch, cancel, end, and resume timed sessions through public API routes.</done>
</task>

<task type="auto">
  <name>Task 2: Make internal worker controls cooperate with active sessions</name>
  <files>services/control-api/src/routes/internal-recovery.ts, services/control-api/src/routes/internal-worker-actions.ts, services/control-api/src/server.ts</files>
  <read_first>services/control-api/src/routes/internal-recovery.ts, services/control-api/src/routes/internal-worker-actions.ts, services/control-api/src/sessions/session-service.ts</read_first>
  <action>Inject sessionService into internal-recovery.ts and internal-worker-actions.ts. When reauth starts or a worker restart is requested, terminate the current active session with worker_unavailable. When a worker is manually marked ready or reauth completes, trigger queue reconciliation. Prevent mark-ready from overriding a worker that still has an active session.</action>
  <verify>rg -n "sessionService|worker_unavailable|handleWorkerReady|hasActiveSessionForWorker" services/control-api/src/routes/internal-recovery.ts services/control-api/src/routes/internal-worker-actions.ts</verify>
  <acceptance_criteria>
    - services/control-api/src/routes/internal-recovery.ts contains handleWorkerStatusChange
    - services/control-api/src/routes/internal-recovery.ts contains handleWorkerReady
    - services/control-api/src/routes/internal-worker-actions.ts contains worker_unavailable
    - services/control-api/src/routes/internal-worker-actions.ts contains hasActiveSessionForWorker
  </acceptance_criteria>
  <done>Internal worker operations no longer leave timed sessions in an inconsistent state.</done>
</task>

<task type="auto">
  <name>Task 3: Cover public API and visibility boundaries with tests</name>
  <files>services/control-api/test/public-sessions.test.ts, services/control-api/vitest.config.ts</files>
  <read_first>services/control-api/src/routes/public-sessions.ts, services/control-api/src/server.ts, services/control-api/src/sessions/session-service.ts</read_first>
  <action>Create Vitest plus Supertest coverage that verifies session creation, queued promotion after release, invalid state transitions on cancel and end, and that worker summaries returned from public routes never expose profilePath or recovery URLs.</action>
  <verify>rg -n "queued position|hides internal worker data|invalid transitions" services/control-api/test/public-sessions.test.ts</verify>
  <acceptance_criteria>
    - services/control-api/test/public-sessions.test.ts contains "creates sessions and hides internal worker data"
    - services/control-api/test/public-sessions.test.ts contains "returns queued position and promotes after release"
    - services/control-api/test/public-sessions.test.ts contains "rejects invalid transitions"
  </acceptance_criteria>
  <done>The public API contract and its internal-surface boundary are test-verified.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] control-api tests cover both session-service behavior and public route behavior
- [ ] No public route returns profilePath or recovery URLs
</verification>

<success_criteria>

- All tasks completed
- Public session routes are mounted and safe
- Internal worker actions cooperate with active sessions
- Route-level tests pass
</success_criteria>

<output>
After completion, create the matching SUMMARY document in this phase directory.
</output>
